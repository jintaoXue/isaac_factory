# 实验设计：四维瓶颈与扰动注入

> 目标：在短单负载（**10** 件水管、并行上限 **5**、真实路径导航）上，按 **物料 / 工人 / 物流 / 工位** 四维之一注入扰动，使瓶颈轨迹可训练。  
> 单次运行只开一维（`--disturbance_dim`），强度分三档 **1.0 / 2.0 / 3.0**（`--disturbance_intensity`）。  
> 对照实现：`env_asset_cfg/cfg_disturbance.py`、`src/disturbance.py`（2026-09-04：现行 L2 下限 **10 min / 600 步**，I=1.0 **2 条、各约 10–15 min**；物流每局至少冻一台 AGV。订单 10、WIP 5。占用正例与过程热窗对齐。machine I=1.0 起关工作台+环焊 ws1；human 5→3。`material` 组焊前藏法兰或弯头，不藏管坯。已落地 77 局生产包仍是改下限之前的旧采样）。

相关文档：

- [`01.瓶颈数据采集规范.md`](./01.瓶颈数据采集规范.md) — 原始日志表与 `disturbance_log`
- [`00.运行方式.md`](./00.运行方式.md) — 采集命令
- [`05.龙门架死锁&WIP槽位修复.md`](./05.龙门架死锁&WIP槽位修复.md) — 看门狗误杀、龙门互锁、空闲龙门挡 `carry_to_robot`、producing 槽位误删
- [`ARCHITECTURE_CN.md`](./ARCHITECTURE_CN.md) — 环境与 task/subtask

---

## 0. 怎么跑（CLI 选维度）

**每次运行用参数决定注入哪一维**；默认 `none` = 不注入（正常生产）。

```bash
conda activate env_isaaclab
python train.py \
  --task HRTPaHC-v1 \
  --algo rule_based \
  --num_envs 1 \
  --seed 42 \
  --device cuda:0 \
  --headless \
  --disturbance_dim machine \
  --disturbance_intensity 2.0 \
  --max_episodes 3 --max_sim_episodes 3
```

`rule_based` 必须同时设 `--max_sim_episodes`，否则采集不会按 episode 数停。


| 参数 | 取值 | 说明 |
|------|------|------|
| `--disturbance_dim` | `none` / `material` / `human` / `logistics` / `machine` | **主扰动维度**（单次只开一维） |
| `--disturbance_intensity` | `1.0` / `2.0` / `3.0`（也可取中间值，按就近档） | 同时放大 L0 数量、L1 噪声、L2 条数与时长 |
| `--disturbance_human_count` | int，可选 | 仅 `human`：覆盖工人数 |
| `--disturbance_agv_count` | int，可选 | 仅 `logistics`：覆盖 AGV 数 |
| `--disturbance_gantry_count` | int，可选 | 仅 `logistics`：覆盖龙门架数 |


对照示例：

```bash
--disturbance_dim none
--disturbance_dim material --disturbance_intensity 1.0
--disturbance_dim human --disturbance_intensity 2.0
--disturbance_dim logistics --disturbance_intensity 3.0
--disturbance_dim machine --disturbance_intensity 2.0
```

日志：`episode_config.csv` 含 `disturbance_dim` / `disturbance_intensity` / `disturbance_applied`；事件写入 `disturbance_log.csv`。

---



## 1. 瓶颈定义（本项目操作化）

**瓶颈**：在任意观察窗口内，**限制整条产线吞吐（throughput）的资源或工序**。

本环境中，瓶颈由下列四类因素产生；四者**相互关联、非独立**：


| 维度   | 英文标签        | 典型可调参数            |
| ---- | ----------- | ----------------- |
| 物料   | `material`  | 短缺、损坏/不良          |
| 工人   | `human`     | 数量、效率、熟练度、疲劳      |
| 物流运输 | `logistics` | AGV 数量、龙门架数量、初始位置 |
| 工位   | `machine`   | 是否故障、是否顺利完成加工     |


> **关联性示例**：物料严重短缺时，产线整体闲置，机器故障或工人疲劳即使发生，也很难表现为「系统级吞吐瓶颈」——缺料会**掩盖**其它维度的瓶颈信号。因此实验设计必须控制「主扰动维度」，并记录其它维度是否被压制。

---



## 2. 总体建议：怎么往确定环境里注入扰动



### 2.1 原则（推荐）

1. **先有负载，再注扰动**
  订单过少（如默认 5）时系统几乎不堵，扰动效果弱。建议在 G1 级负载（订单 ≥10–15，并行上限仍可保持 5）上再做扰动实验。
2. **参数围绕标称值随机浮动，而不是写死另一组常数**
  每道工序的操作时间、成功率等，设为「标称值 ± 噪声」：
   这样同一配置跑多次仍有多样性，又不会偏离「正常生产」太远。
3. **单因子优先，再做组合**
  每个 run **主注入一个维度**（由 `--disturbance_dim` 指定），便于对照「不同环境扰动下冒出的过程瓶颈」。`disturbance_log` 是环境条件，不是瓶颈标签。
4. **分层实现**

  | 层级           | 含义               | 本实现                                                 |
  | ------------ | ---------------- | --------------------------------------------------- |
  | **L0 配置级**   | episode 开始前改 cfg | 人/AGV/龙门架数量；`machine` 关掉双工位 ws1（`apply_disturbance_to_cfgs`） |
  | **L1 采样级**   | 加工/人工时间噪声、成功率、刀具磨损；`material` 开局限时藏库 | 时间/成功率读 `RuntimeDisturbanceCfg`；物料开局波写入该局 L2 队列第一条（`wave=l1`） |
  | **L2 运行时事件** | 中途故障 / 离岗 / 龙门不可用 / 缺料 / 焊后质检 | `DisturbanceInjector`（每 episode 重采样；质检不占故障槽位） |


---



## 3. 三档强度实际改了哪些配置

标称（`--disturbance_dim none`）：工人 5、AGV **4**、龙门 **4** 台 `move_speed=0.5`、加工成功率 1.0、缺料比例 0、无 L2。  
未点名的维度保持标称；**只有 `--disturbance_dim` 对应的那一维**改下表中的项。

分层：

| 层 | 何时生效 | 是否随 episode 变 |
|----|----------|-------------------|
| **L0** | `apply_disturbance_to_cfgs`，进程启动一次 | 否（整次 `train.py` 固定） |
| **L1** | 每次加工/人工计时采样；`material` 开局波在该局 `reset` 时抽件数/时长 | 加工噪声数值固定、样本随机；物料开局波 **每局重抽** |
| **L2** | 每个 episode `reset` 时按 `(seed, env_id, episode_id)` 抽日程 | **是**（目标、时刻、条数、时长都重抽） |

### 3.1 四维共用的 L2 规则

日程**不再写死** 800/4500/9000/13000。每个 episode 独立抽样。短单（10 根 / WIP 5）一局大约 1.5–3 小时，L2 改成 **更少、略短**，仍要撑过多个 60s 窗，且低于 `stall_timeout_steps=5000`。

| intensity | 每局事件条数 | 单次时长上限（步） | 时长抽样 |
|-----------|--------------|--------------------|----------|
| **1.0** | **2** | ~900 | `clip(U(0.7,1.3)×基准×I, 600, 上限)` |
| **2.0** | **2–3** | ~1300 | 同上 |
| **3.0** | **3–4** | ~1400 | 同上 |

`material` 的 L2 **条数**另走 §3.5（I=1.0 → 1–2），时长上限同档。中间值按就近档：`<1.5` 走 1.0，`<2.5` 走 2.0，否则走 3.0。

其它共用规则：

- 起点约 400–8000 逻辑步，事件互不重叠（间隔 ≥**360**）；单次时长下限 **600**（10 min，对齐 `hot_min_windows=8`）
- 相邻两次避免同一目标
- 注入器一次只激活一条；目标当前不能下手（机台/人/龙门/AGV 非空闲，或库里没有该物料）则等待；超过 `max(2×duration, 500)` 则跳过
- 计划队列写入该 episode 的 `disturbance_log` 配置行和 `episode_config.disturbance_applied`（`machine` 另含 `qc_holds`）

各维 L2 **基准时长**与**目标池**：

| 维度 | 基准（步） | 目标池 | 下手时做什么 |
|------|------------|--------|----------------|
| `machine` | **700** | 切管 / 坡口 / 焊机器人 / 旋转焊 / 工作台 | 空闲工位置 `invalid`（日志 `STOP`）；另有并行 `quality_hold` 窗口 |
| `human` | **800** | `human_0` … `human_{n-1}`（n 为当前工人数） | 空闲工人 `working_disturbance_absent` |
| `logistics` | **650** | 当前 `gantry_*` **和** `agv_*`（只冻一台，其余继续） | 空闲龙门 `invalid` / 空闲 AGV `working_disturbance_absent` |
| `material` | **800** | `product_00_flange` / `product_00_elbow`（**不藏管坯**） | 把**仍在库位**的该 SKU 藏到地下，结束再放回；另有开局 L1 波（见 §3.5） |

### 3.2 `machine`（工位）

L0 关掉双工位机台的 ws1（不再恢复）。L1 含刀具磨损（加工时间随 `time_step` 变长）。L2 除故障外另有质检扣留窗口（不占故障槽位，可与 DOWN 重叠）。

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 关闭工位 `disabled_workstations` | 全开 | **工作台 ws1 + 旋转焊 ws1** | 同 1.0 | 同 1.0 |
| L1 加工时间噪声 `animation_time_noise_std` | 0 | **5** | **10** | **15** |
| L1 加工成功率 `machine_success_rate`（失败则返工） | 1.00 | **0.88** | **0.76** | **0.64** |
| L1 刀具磨损 `tool_wear_per_1k_steps` | 0 | **15**（t=10k 时约 +150 步） | **30** | **45** |
| L2 故障条数 | 0 | **2** | **2–3** | **3–4** |
| L2 故障时长（基准 700） | — | 约 **600–900**，上限 900 | 上限 1300 | 上限 1400 |
| L2 质检窗口数 `quality_hold` | 0 | 1–2 | 2–3 | 3–4 |
| L2 质检额外扣留 `hold_steps` | — | 约 40–90 | 约 70–140 | 约 105–190 |

公式：`noise_std = 5I`；`success = max(0.55, 1−0.12I)`；`wear = 15I`（加工目标 = 标称 + wear×t/1000）。  
质检：窗口内若 `arc_welding_root` / `MIG_welding_surface` / `batch_spot_welding` 刚要完工，再等 `hold_steps` 才放行；机台保持 `PROCESSING`，不是 `STOP`。日志 `disturbance_type=quality_hold`。

### 3.3 `human`（工人）

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 工人数 `NormalHuman`（可用 `--disturbance_human_count` 覆盖） | 5 | **3** | **2** | **1** |
| L1 人工时间倍率 `human_time_scale`（全员均值） | 1.00 | **1.55** | **2.10** | **2.65** |
| L1 工人能力不均 `human_skill_scales` | 全 1 | **阶梯 [0.8, 1.4]** | **[0.7, 1.6]** | **[0.6, 1.8]** |
| L1 人工噪声 `human_subtask_noise_std` | 2 | **10** | **18** | **26** |
| L1 背景加工噪声 `animation_time_noise_std`（弱于 machine 主维） | 0 | **2** | **4** | **6** |
| L1 背景成功率 `machine_success_rate` | 1.00 | **0.95** | **0.90** | **0.85** |
| L1 背景刀具磨损 `tool_wear_per_1k_steps` | 0 | **6** | **12** | **18** |
| L2 条数 | 0 | **2** | **2–3** | **3–4** |
| L2 时长（基准 800） | — | 约 **600–900**，上限 900 | 上限 1300 | 上限 1400 |

公式：`人数 = max(1, round(5−(1+I)))`；`scale = 1+0.55I`；`noise = 2+10I`；背景 `process_std = 2I`，`success = max(0.82, 1−0.05I)`，`wear = 6I`。

### 3.4 `logistics`（物流）

龙门标称 `move_speed=0.5`。速度改为 `0.5 / gantry_time_scale`。

四分区厂（4 龙门）**不能减龙门号**：g2 管坡口、g0/g1 管焊与工作台。运力缺口走 AGV 减半 + 降速 + L2 冻一台。

| 档 | 真实对应 | L0 资源（4 龙门厂） | L1 / L2 |
| --- | --- | --- | --- |
| **I=1.0** | 小车不足、限速、偶发堵车 | **4 龙门 + 2 AGV** | 降速；L2 冻**一台**龙门或一台 AGV |
| **I=2.0** | 更慢、只剩一台小车 | **4 龙门 + 1 AGV** | 更慢；L2 更长 |
| **I=3.0** | 同 2.0，事件更多 | **4 龙门 + 1 AGV** | 再慢；事件更多 |


| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 AGV 数（可用 `--disturbance_agv_count` 覆盖） | 4 | **2** | **1** | **1** |
| L0 龙门数（可用 `--disturbance_gantry_count` 覆盖） | 4 | **4**（分区全留） | **4** | **4** |
| L1 时间倍率 `gantry_time_scale` | 1.00 | **1.65** | **2.30** | **2.95** |
| L1 龙门 `move_speed` | 0.50 | **≈0.303** | **≈0.217** | **≈0.169** |
| L1 速度噪声 `move_speed_noise_std` | 0 | **≈0.15** | **≈0.20** | **≈0.22** |
| L1 背景加工噪声 `animation_time_noise_std`（弱于 machine 主维） | 0 | **2** | **4** | **6** |
| L1 背景成功率 `machine_success_rate` | 1.00 | **0.95** | **0.90** | **0.85** |
| L1 背景刀具磨损 `tool_wear_per_1k_steps` | 0 | **6** | **12** | **18** |
| L2 条数 | 0 | **2** | **2–3** | **3–4** |
| L2 时长（基准 650） | — | 约 **455–845**，上限 900 | 上限 1300 | 上限 1400 |
| L2 目标 | — | 当前龙门 **或** AGV（相邻两次避开同一目标；只冻一台） | 同左（龙门仍 4 台，AGV 1） | 同左 |

公式：龙门数 = 标称台数（四分区不减）；AGV 数 `I<2 → n/2` 否则 1；`time_scale = 1+0.65I`。不要传 `--disturbance_agv_count 4`，否则没有运力缺口。改默认后必须**重启**采集进程。

### 3.5 `material`（物料）

**不再永久缺件。** reset 时 **10 件管坯 / 法兰 / 弯头入库**（USD 仍注册 18 件资产，只用前 10）。缺料只做**限时把闲置库件藏到地下，到期放回**。**不改加工成功率**。

对应真实产线：**管子已经到组焊台，发现法兰或弯头没送到 / 来料不良**。仿真里把该 SKU 从库位藏起。`logistic_for_batch_spot_welding` 只运管；组焊因缺法兰/弯头开不了工，工作台停在 `materialReadyFor_batch_spot_welding`（TPM：WAITING）。两工位占满后坡口卸不下来（上游堵），焊机吃不到半成品（下游饿）。

旧实现（已废弃）：L0 跳过放料会整局卡死；2026-08-19 混合藏 5–9 件几乎不拉时钟；2026-08-21 午藏**管坯**只会让产线空转，Stage-C 打不出 `material_shortage`。

现行：L1 在首件接近组焊时藏法兰 **或** 弯头（留 3 件让组焊还能爬，避免 WIP 堆满触发 stall）；L2 藏另一种，且必须落在典型 makespan 之内。不藏管坯。

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L1 SKU | — | 法兰 **或** 弯头（每局抽一种） | 同左 | 同左 |
| L1 开始步 | — | **400–1000** | 同左 | 同左 |
| L1 藏件数 | 0 | **6–7**（留 **3**） | **7–8**（留 2） | **7–8**（留 2） |
| L1 时长 | — | **1400–2200** | **2000–2700** | **2600–3200** |
| L1 加工成功率 | 1.00 | **1.00** | **1.00** | **1.00** |
| L2 条数 | 0 | 1–2 | 2–3 | 3–4 |
| L2 目标 | — | L1 的**另一种**套件 | 同左 | 两种都可 |
| L2 每波 `max_units` | — | **4–6** | **7** | **7** |
| L2 时长 | — | 约 560–900，上限 900 | 上限 1300 | 上限 1400 |
| L2 采样窗 | — | L1 结束之后到 **8000**（与其它维同一上限，避免排到订单结束后） | 同左 | 同左 |

**L1**：`wave=l1`，`start ~ U(400,1000)`。只藏仍在库位的该 SKU。管已在工作台时组焊开不了工，工位保持 `materialReadyFor_` 直到放回。

**L2**：L1 结束 + 间隔之后藏另一种套件，带 `max_units`。

**日志**：`disturbance_type=material_shortage`。`shortage_flag=1` 在坡口已完成或管已送到组焊台且法兰/弯头仍是 `disappear` 时记下（无 ongoing task 也记），聚合打到工作台 ws0 和 ws1。

### 3.6 实现入口

- `env_asset_cfg/cfg_disturbance.py` — CLI → `RuntimeDisturbanceCfg`、L0 改注册数量、L2 `sample_l2_schedule`（含 material 开局波）
- `src/disturbance.py` — L2 Injector（机台/人/龙门/物料限时藏库）+ 时间/成功率采样
- `train.py` — `--disturbance_*`，`gym.make` 前 `apply_disturbance_to_cfgs()`

---



## 4. 维度关联与实验控制


| 主扰动维度     | 建议同时保持    | 避免同时强开          | 原因            |
| --------- | --------- | --------------- | ------------- |
| material  | 人/车/机标称   | 严重 machine DOWN | 缺料会掩盖故障信号     |
| human     | 物料充足、物流标称 | 极端缺料            | 否则人闲置，效率扰动看不见 |
| logistics | 物料充足、机无故障 | 订单过低            | 无负载则争抢不明显     |
| machine   | 物料充足      | 极端缺人+缺车         | 故障传播路径被其它饥饿切断 |


每个 run 标签：`primary_dimension = --disturbance_dim`（已写入 `episode_config`）。

---



## 5. 推荐实验路线



### Phase 0 — 正常生产基线

```bash
--disturbance_dim none --seed 42
```



### Phase 1 — 单维扰动扫描（固定负载与 seed 组）

```text
dim ∈ {material, human, logistics, machine}
intensity ∈ {1.0, 2.0, 3.0}
seed ∈ {42, 43, 44}
每档建议 ≥20 episode（`--max_episodes` 与 `--max_sim_episodes` 同值）
```



### Phase 2 — 资源覆盖细扫（可选）

在 `human` / `logistics` 下用 `--disturbance_human_count` 等做离散水平对照。

---



## 6. 评价指标


| 层级  | 看什么                                                            |
| --- | -------------------------------------------------------------- |
| 系统  | 完工时间、吞吐、总 WIP                                                  |
| 节点  | `active_pct`、`starved_time`、`blocked_time`、队列长度                |
| 事件  | `bottleneck_event`（过程热窗：拐点 / 排队 / 缺料饥饿 / 到货延误 / 上游堵死）是否随 `disturbance_dim` 换工位、换时长 |
| 诊断  | cause 头学的是过程原因（阻塞/饥饿/排队）；扰动 dim 是条件，不是 cause 真值 |
| 关联性 | 强 `material` 下 machine 类过程拐点占比应下降                                |


---



## 7. 与旧版文档的关系

观测与打标流程仍用 `[01.瓶颈数据采集规范.md](./01.瓶颈数据采集规范.md)` + `[03.离线特征聚合&打标签.md](./03.离线特征聚合&打标签.md)`。  
负载底座是短单：**10** 件水管、`parallel_producing_limit=5`。  
龙门长途搬运、互锁、空闲龙门挡 `carry_to_robot`、以及扰动后 WIP 槽位见 [`05.龙门架死锁&WIP槽位修复.md`](./05.龙门架死锁&WIP槽位修复.md)。  
**本文主轴**：用运行参数选择四维之一注入扰动。L2 是环境 X，不是瓶颈标签；要让过程事件够长、够分维，靠拉长 L2 + 占用正例（见 03），不要把 `disturbance_type` 抄进 `root_cause_reason`。

---

## 8. 还可以注入什么（按训练价值排序；2/4/5 与工人能力不均已接入）

人和物流现在已有**弱背景**加工波动/不良率，主信号仍是减人、变慢、离岗 / 少车、龙门故障。下面这些能补现在四维盖不到的瓶颈形态；建议仍 **一次只开一种当主因**，避免标签打架。

| 优先级 | 扰动 | 层 | 状态 | 说明 |
|--------|------|----|------|------|
| 高 | **库位缩容** | L0 | 未做 | 降低 Black/YellowStorage capacity |
| 高 | **工位只开一半** | L0 | **已做（machine）** | I=1.0 起关工作台 ws1 **和** 旋转焊 ws1 |
| 高 | **全员短休息 / 换班** | L2 | 未做 | 所有空闲工人同时 absent |
| 中 | **刀具磨损** | L1 | **已做** | machine：`wear=15I`；human/logistics 背景 `6I` |
| 中 | **焊后质检扣留** | L2 | **已做（machine）** | 与故障队列并行；`quality_hold` |
| 中 | **订单脉冲放行** | L0 | 未做 | 限制 not_started 释放节奏 |
| 中 | **油漆/焊丝耗材断档** | L2 | 未做 | 对末道工序藏库 |
| 低 | **工人能力不均** | L1 | **已做（human）** | `human_0` 最快 → `human_{n-1}` 最慢；有效倍率 = 全局 scale × skill |
| 低 | **返工回前序** | L1 | 未做 | 失败后退回坡口/点焊 |
| 低 | **全局闪断** | L2 | 未做 | 全部 idle 工位同时 STOP |

不建议现阶段做的：改导航地图制造堵路（和真实路径耦合深）、一次混开两维高强度 L2（`root_cause` 无法唯一）。

---

*文档版本：v0.9 | L2 更少更长（I=1.0：2–3 条、10–30 min）+ 物料藏法兰/弯头 | 2026-08-21*