# 实验设计：四维瓶颈与扰动注入

> 目标：在 G1 负载（18 件水管、并行上限 5、真实路径导航）上，按 **物料 / 工人 / 物流 / 工位** 四维之一注入扰动，使瓶颈轨迹可训练。  
> 单次运行只开一维（`--disturbance_dim`），强度分三档 **1.0 / 2.0 / 3.0**（`--disturbance_intensity`）。  
> 对照实现：`env_asset_cfg/cfg_disturbance.py`、`src/disturbance.py`（2026-08-14：`material` 改为限时藏库、到期放回，件数/时长每局抽区间，不再永久跳过放料；L2 每 episode 重采样；`machine` 含工位减半、刀具磨损、焊后质检；`human` 含能力不均）。

相关文档：

- [`01.瓶颈数据采集规范.md`](./01.瓶颈数据采集规范.md) — 原始日志表与 `disturbance_log`
- [`00.运行方式.md`](./00.运行方式.md) — 采集命令
- [`05.仿真卡死与WIP槽位修复.md`](./05.仿真卡死与WIP槽位修复.md) — 看门狗误杀、龙门互锁、扰动后 producing 槽位误删
- [`ARCHITECTURE_CN.md`](./ARCHITECTURE_CN.md) — 环境与 task/subtask

---

## 0. 怎么跑（CLI 选维度）

**每次运行用参数决定注入哪一维**；默认 `none` = 不注入（正常生产）。

```bash
conda activate env_isaaclab
python train.py \
  --task HRTPaHC-v1 \
  --algo rule_based \
  --num_envs 1 \
  --seed 42 \
  --device cuda:0 \
  --headless \
  --disturbance_dim machine \
  --disturbance_intensity 2.0 \
  --max_episodes 3 --max_sim_episodes 3
```

`rule_based` 必须同时设 `--max_sim_episodes`，否则采集不会按 episode 数停。


| 参数 | 取值 | 说明 |
|------|------|------|
| `--disturbance_dim` | `none` / `material` / `human` / `logistics` / `machine` | **主扰动维度**（单次只开一维） |
| `--disturbance_intensity` | `1.0` / `2.0` / `3.0`（也可取中间值，按就近档） | 同时放大 L0 数量、L1 噪声、L2 条数与时长 |
| `--disturbance_human_count` | int，可选 | 仅 `human`：覆盖工人数 |
| `--disturbance_agv_count` | int，可选 | 仅 `logistics`：覆盖 AGV 数 |
| `--disturbance_gantry_count` | int，可选 | 仅 `logistics`：覆盖龙门架数 |


对照示例：

```bash
--disturbance_dim none
--disturbance_dim material --disturbance_intensity 1.0
--disturbance_dim human --disturbance_intensity 2.0
--disturbance_dim logistics --disturbance_intensity 3.0
--disturbance_dim machine --disturbance_intensity 2.0
```

日志：`episode_config.csv` 含 `disturbance_dim` / `disturbance_intensity` / `disturbance_applied`；事件写入 `disturbance_log.csv`。

---



## 1. 瓶颈定义（本项目操作化）

**瓶颈**：在任意观察窗口内，**限制整条产线吞吐（throughput）的资源或工序**。

本环境中，瓶颈由下列四类因素产生；四者**相互关联、非独立**：


| 维度   | 英文标签        | 典型可调参数            |
| ---- | ----------- | ----------------- |
| 物料   | `material`  | 短缺、损坏/不良          |
| 工人   | `human`     | 数量、效率、熟练度、疲劳      |
| 物流运输 | `logistics` | AGV 数量、龙门架数量、初始位置 |
| 工位   | `machine`   | 是否故障、是否顺利完成加工     |


> **关联性示例**：物料严重短缺时，产线整体闲置，机器故障或工人疲劳即使发生，也很难表现为「系统级吞吐瓶颈」——缺料会**掩盖**其它维度的瓶颈信号。因此实验设计必须控制「主扰动维度」，并记录其它维度是否被压制。

---



## 2. 总体建议：怎么往确定环境里注入扰动



### 2.1 原则（推荐）

1. **先有负载，再注扰动**
  订单过少（如默认 5）时系统几乎不堵，扰动效果弱。建议在 G1 级负载（订单 ≥10–15，并行上限仍可保持 5）上再做扰动实验。
2. **参数围绕标称值随机浮动，而不是写死另一组常数**
  每道工序的操作时间、成功率等，设为「标称值 ± 噪声」：
   这样同一配置跑多次仍有多样性，又不会偏离「正常生产」太远。
3. **单因子优先，再做组合**
  每个 run **主注入一个维度**（由 `--disturbance_dim` 指定），便于用 `disturbance_log` 做有监督根因标签。
4. **分层实现**

  | 层级           | 含义               | 本实现                                                 |
  | ------------ | ---------------- | --------------------------------------------------- |
  | **L0 配置级**   | episode 开始前改 cfg | 人/AGV/龙门架数量；`machine` 关掉双工位 ws1（`apply_disturbance_to_cfgs`） |
  | **L1 采样级**   | 加工/人工时间噪声、成功率、刀具磨损；`material` 开局限时藏库 | 时间/成功率读 `RuntimeDisturbanceCfg`；物料开局波写入该局 L2 队列第一条（`wave=l1`） |
  | **L2 运行时事件** | 中途故障 / 离岗 / 龙门不可用 / 缺料 / 焊后质检 | `DisturbanceInjector`（每 episode 重采样；质检不占故障槽位） |


---



## 3. 三档强度实际改了哪些配置

标称（`--disturbance_dim none`）：工人 5、AGV 2、龙门 2 台 `move_speed=0.5`、加工成功率 1.0、缺料比例 0、无 L2。  
未点名的维度保持标称；**只有 `--disturbance_dim` 对应的那一维**改下表中的项。

分层：

| 层 | 何时生效 | 是否随 episode 变 |
|----|----------|-------------------|
| **L0** | `apply_disturbance_to_cfgs`，进程启动一次 | 否（整次 `train.py` 固定） |
| **L1** | 每次加工/人工计时采样；`material` 开局波在该局 `reset` 时抽件数/时长 | 加工噪声数值固定、样本随机；物料开局波 **每局重抽** |
| **L2** | 每个 episode `reset` 时按 `(seed, env_id, episode_id)` 抽日程 | **是**（目标、时刻、条数、时长都重抽） |

### 3.1 四维共用的 L2 规则

日程**不再写死** 800/4500/9000/13000。每个 episode 独立抽样：

| intensity | 每局事件条数 | 单次时长上限（步） | 时长抽样 |
|-----------|--------------|--------------------|----------|
| **1.0** | 2–4 | 300 | `clip(U(0.5,1.5)×基准×I, 80, 上限)` |
| **2.0** | 4–8 | 520 | 同上 |
| **3.0** | 7–12 | 720 | 同上 |

中间值按就近档：`<1.5` 走 1.0，`<2.5` 走 2.0，否则走 3.0。

其它共用规则：

- 起点约 600–14500 逻辑步，事件互不重叠（间隔 ≥350）
- 相邻两次避免同一目标
- 注入器一次只激活一条；目标当前不能下手（机台/人/龙门非空闲，或库里没有该物料）则等待；超过 `max(2×duration, 500)` 则跳过
- 计划队列写入该 episode 的 `disturbance_log` 配置行和 `episode_config.disturbance_applied`（`machine` 另含 `qc_holds`）

各维 L2 **基准时长**与**目标池**：

| 维度 | 基准（步） | 目标池 | 下手时做什么 |
|------|------------|--------|----------------|
| `machine` | 120 | 切管 / 坡口 / 焊机器人 / 旋转焊 / 工作台 | 空闲工位置 `invalid`（日志 `STOP`）；另有并行 `quality_hold` 窗口 |
| `human` | 150 | `human_0` … `human_{n-1}`（n 为当前工人数） | 空闲工人 `working_disturbance_absent` |
| `logistics` | 100 | 当前启用的 `gantry_*` | 空闲龙门置 `invalid` |
| `material` | 150 | `product_00_flange` / `elbow` / `pipe_raw` | 把**仍在库位**的该 SKU 藏到地下，结束再放回；另有开局 L1 波 `target=kit`（三种混抽，见 §3.5） |

### 3.2 `machine`（工位）

L0 关掉双工位机台的 ws1（不再恢复）。L1 含刀具磨损（加工时间随 `time_step` 变长）。L2 除故障外另有质检扣留窗口（不占故障槽位，可与 DOWN 重叠）。

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 关闭工位 `disabled_workstations` | 全开 | **工作台 ws1** | **工作台 ws1 + 旋转焊 ws1** | 同 2.0 |
| L1 加工时间噪声 `animation_time_noise_std` | 0 | **5** | **10** | **15** |
| L1 加工成功率 `machine_success_rate`（失败则返工） | 1.00 | **0.88** | **0.76** | **0.64** |
| L1 刀具磨损 `tool_wear_per_1k_steps` | 0 | **15**（t=10k 时约 +150 步） | **30** | **45** |
| L2 故障条数 | 0 | 2–4 | 4–8 | 7–12 |
| L2 故障时长（基准 120） | — | 约 80–180，上限 300 | 约 120–360，上限 520 | 约 180–540，上限 720 |
| L2 质检窗口数 `quality_hold` | 0 | 1–2 | 2–3 | 3–4 |
| L2 质检额外扣留 `hold_steps` | — | 约 40–90 | 约 70–140 | 约 105–190 |

公式：`noise_std = 5I`；`success = max(0.55, 1−0.12I)`；`wear = 15I`（加工目标 = 标称 + wear×t/1000）。  
质检：窗口内若 `arc_welding_root` / `MIG_welding_surface` / `batch_spot_welding` 刚要完工，再等 `hold_steps` 才放行；机台保持 `PROCESSING`，不是 `STOP`。日志 `disturbance_type=quality_hold`。

### 3.3 `human`（工人）

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 工人数 `NormalHuman`（可用 `--disturbance_human_count` 覆盖） | 5 | **4** | **3** | **2** |
| L1 人工时间倍率 `human_time_scale`（全员均值） | 1.00 | **1.25** | **1.50** | **1.75** |
| L1 工人能力不均 `human_skill_scales` | 全 1 | **阶梯 [0.8, 1.4]** | **[0.7, 1.6]** | **[0.6, 1.8]** |
| L1 人工噪声 `human_subtask_noise_std` | 2 | **10** | **18** | **26** |
| L1 背景加工噪声 `animation_time_noise_std`（弱于 machine 主维） | 0 | **2** | **4** | **6** |
| L1 背景成功率 `machine_success_rate` | 1.00 | **0.95** | **0.90** | **0.85** |
| L1 背景刀具磨损 `tool_wear_per_1k_steps` | 0 | **6** | **12** | **18** |
| L2 条数 | 0 | 2–4 | 4–8 | 7–12 |
| L2 时长（基准 150） | — | 约 80–225，上限 300 | 约 150–450，上限 520 | 约 225–675，上限 720 |

公式：`人数 = max(1, round(5−I))`；`scale = 1+0.25I`；`noise = 2+8I`；背景 `process_std = 2I`，`success = max(0.82, 1−0.05I)`，`wear = 6I`。  
能力不均：`skill_i` 按工号均匀铺在 `[0.9−0.1I, 1.2+0.2I]`（`human_0` 最快，`human_{n-1}` 最慢）；实际耗时倍率 = `human_time_scale × skill_i`。I=1.0、4 人时有效倍率约 **1.00 / 1.25 / 1.50 / 1.75**。

### 3.4 `logistics`（物流）

龙门标称 `move_speed=0.5`。速度改为 `0.5 / gantry_time_scale`。

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L0 AGV 数（可用 `--disturbance_agv_count` 覆盖） | 2 | **1** | **1** | **1** |
| L0 龙门数（可用 `--disturbance_gantry_count` 覆盖） | 2 | **2** | **1** | **1** |
| L1 时间倍率 `gantry_time_scale` | 1.00 | **1.30** | **1.60** | **1.90** |
| L1 龙门 `move_speed` | 0.50 | **≈0.385** | **0.313** | **≈0.263** |
| L1 速度噪声 `move_speed_noise_std` | 0 | **≈0.15** | **≈0.22** | **≈0.26** |
| L1 背景加工噪声 `animation_time_noise_std`（弱于 machine 主维） | 0 | **2** | **4** | **6** |
| L1 背景成功率 `machine_success_rate` | 1.00 | **0.95** | **0.90** | **0.85** |
| L1 背景刀具磨损 `tool_wear_per_1k_steps` | 0 | **6** | **12** | **18** |
| L2 条数 | 0 | 2–4 | 4–8 | 7–12 |
| L2 时长（基准 100） | — | 约 80–150，上限 300 | 约 100–300，上限 520 | 约 150–450，上限 720 |

公式：`AGV = max(1, round(2−I))`（1.0 起已是 1，2.0/3.0 差在龙门数、速度和 L2）；龙门数：`I<1.75` 为 2，否则 1；`time_scale = 1+0.3I`；背景加工同 human：`process_std = 2I`，`success = max(0.82, 1−0.05I)`，`wear = 6I`。

### 3.5 `material`（物料）

**不再永久缺件。** reset 时 **18 件管坯 / 法兰 / 弯头全部入库**（`should_skip_material_placement` 恒为 `False`）。缺料只做**限时把闲置库件藏到地下，到期放回**，保证每一局 18 件订单都能齐套完工。强度只改「藏几件、藏多久」，不改「永远少几件」。

旧实现（已废弃）：L0 `material_shortage_frac` 在 reset 时跳过法兰/弯头放置，管坯不跳 → 组焊永远等不到法兰/弯头，并行 5 槽占满后整局卡死。该字段仍写入 `disturbance_applied`，但**不再用于放料**。

| 配置项 | 标称 | I=1.0 | I=2.0 | I=3.0 |
|--------|------|-------|-------|-------|
| L1 开局藏件数（管坯+法兰+弯头合计，每局在区间内抽） | 0 | **2–5** | **4–10** | **6–12** |
| L1 开局藏库时长（逻辑步，每局在区间内抽） | — | **300–700** | **600–1400** | **900–2000** |
| L1 加工成功率（不良返工） | 1.00 | **0.90** | **0.80** | **0.70** |
| L2 条数 | 0 | 2–4 | 4–8 | 7–12 |
| L2 时长（基准 150） | — | 约 80–225，上限 300 | 约 150–450，上限 520 | 约 225–675，上限 720 |

**L1 开局波（写入该局 L2 队列第一条）**

- `sample_l2_schedule` 在 `dim=material` 时先抽一条 `wave=l1`、`target=kit`、`start≈0–80`。
- 件数 `n ~ U(2I, min(12, 5I))`，时长 `dur ~ U(300I, min(2000, 700I))`；上限 12 件 / 2000 步，低于看门狗 `stall_timeout_steps=5000`。
- 注入器从闲置库位的 **管坯 `product_00_pipe_raw` / 法兰 `product_00_flange` / 弯头 `product_00_elbow`** 里随机抽 `n` 件：释放库位、`storage_name=disappear`、z 降到 −100。到期按件放回空闲库位。
- 后续 L2 的抽样起点推到 `L1 结束 + 间隔`，与开局波不重叠。

**L2 中途波**

- 目标池仍是上述三种 SKU 之一（相邻两次避免同一目标）。
- 下手时把**仍在库位**的该 SKU **整类**藏起（无 `max_units`）；已在加工 / 物流 / 工位上的件不动。结束放回。

**为什么会拖慢完工、又做不完不了**

- 缺管坯：切管物流开不了工，产线头部空等。
- 缺法兰/弯头：坡口之后组焊等料（`WAITING material_ready`），并行上限 5 槽占满，后面的单进不来。
- 强度越高，藏得越多、越久，排队和 makespan 越长；到期放回后继续做完 18 件。

日志：`disturbance_type=material_shortage`。开局波 `target_resource_id=material_kit`，`event_schedule` 里带当局抽到的 `max_units` / `duration` / `wave=l1`。中途波 id 为对应 SKU（如 `material_product_00_flange`）。

### 3.6 实现入口

- `env_asset_cfg/cfg_disturbance.py` — CLI → `RuntimeDisturbanceCfg`、L0 改注册数量、L2 `sample_l2_schedule`（含 material 开局波）
- `src/disturbance.py` — L2 Injector（机台/人/龙门/物料限时藏库）+ 时间/成功率采样
- `train.py` — `--disturbance_*`，`gym.make` 前 `apply_disturbance_to_cfgs()`

---



## 4. 维度关联与实验控制


| 主扰动维度     | 建议同时保持    | 避免同时强开          | 原因            |
| --------- | --------- | --------------- | ------------- |
| material  | 人/车/机标称   | 严重 machine DOWN | 缺料会掩盖故障信号     |
| human     | 物料充足、物流标称 | 极端缺料            | 否则人闲置，效率扰动看不见 |
| logistics | 物料充足、机无故障 | 订单过低            | 无负载则争抢不明显     |
| machine   | 物料充足      | 极端缺人+缺车         | 故障传播路径被其它饥饿切断 |


每个 run 标签：`primary_dimension = --disturbance_dim`（已写入 `episode_config`）。

---



## 5. 推荐实验路线



### Phase 0 — 正常生产基线

```bash
--disturbance_dim none --seed 42
```



### Phase 1 — 单维扰动扫描（固定负载与 seed 组）

```text
dim ∈ {material, human, logistics, machine}
intensity ∈ {1.0, 2.0, 3.0}
seed ∈ {42, 43, 44}
每档建议 ≥20 episode（`--max_episodes` 与 `--max_sim_episodes` 同值）
```



### Phase 2 — 资源覆盖细扫（可选）

在 `human` / `logistics` 下用 `--disturbance_human_count` 等做离散水平对照。

---



## 6. 评价指标


| 层级  | 看什么                                                            |
| --- | -------------------------------------------------------------- |
| 系统  | 完工时间、吞吐、总 WIP                                                  |
| 节点  | `active_pct`、`starved_time`、`blocked_time`、队列长度                |
| 事件  | `bottleneck_event` 是否随 `disturbance_dim` 变化                    |
| 诊断  | `disturbance_log` 为真值时的 `root_cause` / `primary_dimension` 准确率 |
| 关联性 | 强 `material` 下 machine 类瓶颈占比应下降                                |


---



## 7. 与旧版文档的关系

观测与打标流程仍用 `[01.瓶颈数据采集规范.md](./01.瓶颈数据采集规范.md)` + `[02.离线瓶颈特征与标签流水线.md](./02.离线瓶颈特征与标签流水线.md)`。  
负载底座已是 G1：18 件水管、`parallel_producing_limit=5`。  
龙门长途搬运、互锁、以及扰动后 WIP 槽位见 [`05.仿真卡死与WIP槽位修复.md`](./05.仿真卡死与WIP槽位修复.md)。  
**本文主轴**：用运行参数选择四维之一注入扰动。

---

## 8. 还可以注入什么（按训练价值排序；2/4/5 与工人能力不均已接入）

人和物流现在已有**弱背景**加工波动/不良率，主信号仍是减人、变慢、离岗 / 少车、龙门故障。下面这些能补现在四维盖不到的瓶颈形态；建议仍 **一次只开一种当主因**，避免标签打架。

| 优先级 | 扰动 | 层 | 状态 | 说明 |
|--------|------|----|------|------|
| 高 | **库位缩容** | L0 | 未做 | 降低 Black/YellowStorage capacity |
| 高 | **工位只开一半** | L0 | **已做（machine）** | I=1.0 关工作台 ws1；I≥2.0 再关旋转焊 ws1 |
| 高 | **全员短休息 / 换班** | L2 | 未做 | 所有空闲工人同时 absent |
| 中 | **刀具磨损** | L1 | **已做** | machine：`wear=15I`；human/logistics 背景 `6I` |
| 中 | **焊后质检扣留** | L2 | **已做（machine）** | 与故障队列并行；`quality_hold` |
| 中 | **订单脉冲放行** | L0 | 未做 | 限制 not_started 释放节奏 |
| 中 | **油漆/焊丝耗材断档** | L2 | 未做 | 对末道工序藏库 |
| 低 | **工人能力不均** | L1 | **已做（human）** | `human_0` 最快 → `human_{n-1}` 最慢；有效倍率 = 全局 scale × skill |
| 低 | **返工回前序** | L1 | 未做 | 失败后退回坡口/点焊 |
| 低 | **全局闪断** | L2 | 未做 | 全部 idle 工位同时 STOP |

不建议现阶段做的：改导航地图制造堵路（和真实路径耦合深）、一次混开两维高强度 L2（`root_cause` 无法唯一）。

---

*文档版本：v0.8 | 物料限时藏库（管坯+法兰+弯头，件数/时长每局抽区间） | 2026-08-14*