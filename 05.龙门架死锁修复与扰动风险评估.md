# 龙门架死锁修复与扰动风险评估

> 日期：2026-08-01  
> 相关数据：`bottleneck_dataset/2026-07-30_13-08-23_seed42`、`2026-07-30_23-08-34_seed42`  
> 现象：`--disturbance_dim machine` 时 `episode_00` 可完成，`episode_01` 在约 t=3641 后产线逻辑永久卡住（仿真步仍在走）。

---

## 1. 问题结论（修改前）

**不是 Isaac / GPU 挂死，也不是 machine L2 DOWN 直接把机台钉死。**

- L2 切管机故障窗口（ep1：1231–1351）早已恢复。
- 死锁点：两台 active 龙门架在 `safe_x_gap` 下互相挡路，`step_time` 不再前进；人/AGV 长期 `waiting_for_gantry_or_partner`。
- machine 扰动的 L1（加工噪声 + `success_rate≈0.92`）改变时序、抬高并发，把该隐患稳定触发；**扰动策略本身可保留，应修龙门架逻辑**。

典型卡死组合（ep1）：

| 资源 | 状态 |
|------|------|
| `gantry_0` | job5 `batch_spot_welding` / `go_to_processing_machine`，之后无事件 |
| `gantry_1` | job7 `logistic_for_pipe_cutting` / `carry_to_robot`，走不动 |
| human / AGV | `STARVED` + `waiting_for_gantry_or_partner` |

---

## 2. 修改前的逻辑

### 2.1 运动推进（`GantryGroupAnimation.step_next_pose`）

每步对每台 gantry：

1. 按优先级（yield 优先，再按任务 `task_start_time_step`）排序。
2. 计算下一插值位姿的世界坐标 `proposed_x`。
3. 若与任一其他 gantry 的 `committed_x` 距离 `< safe_x_gap`（默认 4.0）→ **blocked**。
4. blocked 时：**保持当前位置，不增加 `step_time`**。

```text
修改前：
  blocked == True  → 永远停住（无超时、无放宽间距、无强制推进）
```

当两台 gantry 目标路径在 X 上交叉/贴近时，可能形成：

- A 要前进会被 B 挡；
- B 要前进会被 A 挡；
- 双方 `step_time` 都不涨 → **永久死锁**。

### 2.2 让行（`num07_gantry_group._update_yield`）

低优先级 gantry 进入走廊冲突时：

1. 计算 `yield_x`，调用 `set_yield_target_pose`（旁路挪动）。
2. `_yielding[i] = True`。
3. `_subtask_go_to_target` 中若 `_yielding`：**直接 return，不设置 `target_joints_position`**。

```text
修改前：
  一旦长期处于 yield，且与对方互相挡死
  → 任务目标设不上 / 或 yield 动画也走不到
  → 无超时解除 yield
```

### 2.3 Episode 层

`rule_based` 为 `while True`；订单完成后 `reset_env`。  
一局逻辑死锁后：buffer 快照仍写，但 `job_trace` 停更 → **后续 episode 全部无法推进**。

---

## 3. 修改后的逻辑

涉及文件：

| 文件 | 改动 |
|------|------|
| `src/utils.py` | `step_next_pose` 增加 blocked 计数与分级解锁 |
| `src/machine.py` | yield 超时强制解除；传 `block_timeout`；reset 清计数 |
| `hc_single_env_base.py` | episode 停滞看门狗（兜底 reset） |

### 3.1 运动推进超时解锁（核心）

`GantryGroupAnimation` 为每台 gantry 维护 `blocked_steps`：

| 条件 | 行为 |
|------|------|
| 未 blocked | `blocked_steps = 0`，正常推进 |
| blocked，且 `< block_timeout`（默认 **400**） | 与修改前相同：停住 |
| blocked，且 `≥ 400` | 用 **放宽间距** `safe_x_gap * 0.25` 再判一次；能过则推进 |
| 仍 blocked，且 `≥ 800`（`2 * block_timeout`） | **强制推进一步**（短暂允许间距违规），并打印 `[GantryUnlock] force advance ...` |

意图：优先保持安全间距；僵持过久则先松约束，再硬推，避免整局冻死。

### 3.2 Yield 超时解锁

`num07_gantry_group`：

- 新增 `_yield_steps`、`YIELD_TIMEOUT_STEPS = 400`。
- 每步开始 `_resolve_yield_timeouts`：yield 累计 ≥ 400 → `_force_clear_yield`（清 yield，若已有任务目标则重新 `set_target_pose`）。
- `_subtask_go_to_target`：仅在 **当前仍 yielding** 时跳过设目标；超时清 yield 后可正常设目标。
- 完成判定：若当前动画仍是 yield sidestep（`is_yield_move`），不把任务子步标为完成，避免“让行走完 = 任务走完”的误判。

### 3.3 Episode 看门狗（兜底）

`HcSingleEnvBase`：

- 用 `_progress_signature()`（成品列表 + ongoing 子任务索引/finished 标志 + 各机 state）检测是否有进展。
- 连续 **`stall_timeout_steps = 5000`** 无变化 → 写 `disturbance_log`（`deadlock_reset`），`reset_env()` 开下一局，打印 `[DeadlockWatchdog] ...`。

说明：正常“等龙门架”在历史数据里多为百秒级（&lt;5000）；真正死锁曾空转 5 万+ 步。5000 作为兜底，避免误杀正常排队，同时保证多 episode run 不会整次卡死。

---

## 4. 修改前后对比（流程）

```text
修改前（会死锁）:
  双 gantry 贴近
    → safe_x_gap 双方 blocked
    → step_time 永不增加
    → yield 可能永久卡住且不设任务目标
    → 人/AGV 永久 wait
    → episode 永不结束，后续局无法开始

修改后:
  双 gantry 贴近
    → 短时仍遵守 safe_x_gap（正常避让）
    → 连续 blocked ≥400：放宽间距再试
    → 仍 blocked ≥800：强制推进一步
    → yield ≥400：强制结束让行并恢复任务目标
    → 若仍全局无进展 ≥5000：看门狗 reset 本局
```

---

## 5. 其他三类扰动会不会触发同类死锁？

共同前提：**双龙门架 `safe_x_gap` + 无超时** 是根因。  
凡是让“两台 gantry 长时间同时搬运、路径在 X 上纠缠”的扰动，都可能触发；修 gantry 后风险显著下降。

### 5.1 `human`

| 机制 | 影响 |
|------|------|
| L0：减少工人数 | 吞吐下降，排队变长 |
| L1：人操作变慢/噪声变大 | 时序抖动，高并发窗口变多 |
| L2：`human_0` 中途离岗 | 少一人，物流更依赖剩余工人+龙门架 |

**会死锁吗？**  
**会（修改前）**：不直接改 gantry 状态，但改变并发时序，与 machine 类似，可踩中双 gantry 互锁。  
**修改后**：靠超时解锁 + 看门狗兜底，不应再永久卡死。

### 5.2 `logistics`

| 机制 | 影响 |
|------|------|
| L0：减 AGV / 减 active 龙门架数量 | 运力下降 |
| L1：龙门架动画更慢、噪声更大 | 单次搬运更长 |
| L2：把一台空闲 gantry 标 `invalid`（默认 `gantry_0`） | **只剩一台**可接任务 |

**会死锁吗？**

- **双机互锁**：L2 生效且只剩 1 台 active 时，**几乎不会**再出现“两台互相挡死”（没有第二台对撞）。
- **仍可能的卡死**：单 gantry + 人/AGV 的 `wait` 同步若有其它 bug；或 L0 仍保留 2 台 gantry 但路径纠缠（与 machine 相同）。
- **看门狗**：运力极低时合法等待可能变长；当前 5000 步阈值对“极慢但有进展”仍安全（有进展会刷新 signature）。若将来把并行订单再加大，可再调高阈值。

**结论**：logistics 反而常**降低**双 gantry 死锁概率（少一台）；修改前更常见的是长时间饥饿，不是经典双机互锁。修改后更稳。

### 5.3 `material`

| 机制 | 影响 |
|------|------|
| L0/L1：`material_shortage_frac` 跳过部分法兰/弯头 | 部分产品缺料、推进变慢 |
| L1：`machine_success_rate` 下降 | 加工返工、时序抖动 |
| L2：无中途事件（`event_start_step=-1`） | — |

**会死锁吗？**  
**会（修改前）**：与 machine 类似，通过时序/WIP 变化触发双 gantry 互锁；也可能因缺料导致部分 job 长期无法完成（那是缺料语义，不是 gantry 互锁）。  
**修改后**：gantry 互锁可解开；真正缺料的产品仍可能完不成订单——这属于扰动设计，不是死锁 bug。

### 5.4 对照表

| 扰动维度 | 是否易触发双 gantry 死锁（改前） | 改后 |
|----------|--------------------------------|------|
| machine | 高（L1 噪声+返工抬并发） | 超时解锁 + 看门狗 |
| human | 中高（少人+变慢） | 同上 |
| logistics | 低～中（L2 减一台时双机死锁↓；两台仍在时仍可能） | 同上 |
| material | 中（返工/缺料改时序） | 互锁可解；缺料导致完不成是预期 |

---

## 6. 验证建议

```bash
conda activate env_isaaclab
cd ~/work/isaac_factory
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --device cuda:0 --headless \
  --disturbance_dim machine --disturbance_intensity 1.0 \
  --max_episodes 3
```

关注：

1. ep1 是否还能在 ~3641 永久停住；若出现 `[GantryUnlock]`，说明超时解锁生效且应继续推进。
2. `job_trace` 是否持续增长；三局是否都能结束或至少被看门狗翻篇。
3. 可选再扫：`--disturbance_dim human|logistics|material` 各跑 `--max_episodes 2`。

---

## 7. 参数一览（可调）

| 常量 | 位置 | 默认 | 含义 |
|------|------|------|------|
| `BLOCK_TIMEOUT_STEPS` | `machine.py` / `num07_gantry_group` | 400 | 开始放宽 `safe_x_gap` |
| `2 * BLOCK_TIMEOUT` | `utils.py` | 800 | 强制推进一步 |
| `relaxed_gap_scale` | `step_next_pose` | 0.25 | 放宽后间距比例 |
| `YIELD_TIMEOUT_STEPS` | `machine.py` | 400 | 强制结束 yield |
| `stall_timeout_steps` | `hc_single_env_base.py` | 5000 | episode 看门狗 |

---

## 8. 未改动的部分

- **扰动参数**（machine L1/L2）保持原设计，未削弱。
- 工作台两工位共用 `gantry_parking_areas_ids=[47]` 仍可能增加走廊重叠；属可选优化，本次未改地图点。
- 人-AGV 路径避让（`route.py`）未改；当前复现死锁主因在龙门架 X 轴 `safe_x_gap`。
