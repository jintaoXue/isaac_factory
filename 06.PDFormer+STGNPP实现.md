# 瓶颈预测模型：当前实现

> 日期：2026-08-16（剩余工单占用头 + 现训 `evt` / `lchf07o9`）  
> 代码：`source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer/factory_bn/`  
> 数据：`output/bottleneck_dataset/<run>/derived/` → `PDFormer/raw_data/<标签>/`  
> 采集 / 聚合 / 导出 / 训练命令：`[00.运行方式.md](./00.运行方式.md)`  
> 仿真扰动：`[02.瓶颈维度&环境扰动.md](./02.瓶颈维度&环境扰动.md)`  
> 借鉴：  
>
> 1. [PDFormer](https://ojs.aaai.org/index.php/AAAI/article/view/25556)（[开源](https://github.com/BUAABIGSCity/PDFormer)）— 稠密「分数/流量」预报
> 2. [STGNPP](https://ojs.aaai.org/index.php/AAAI/article/view/26669) — 稀疏「下一次事件何时、多久」

---

## 0. 总结

两件不同的事：


| 问题                     | 类比              | 模型路径               | 输出                                      |
| ---------------------- | --------------- | ------------------ | --------------------------------------- |
| 从当下到**剩余工单收工**每个工位堵不堵？ | 整段剩余天气，不是只报下一小时 | **PDFormer 剩余占用头** | `(窗 × 工位)` 分数 / 0/1，连通块 = 开始、持续、工位（A.1） |
| 下一次**明显瓶颈**几点开始、持续多久？  | 预报下一场暴雨         | **STGNPP**         | 下一事件的间隔时间 τ、时长 d（删失到剩余工单收工）             |


两件事不能混成一个头：前者要**每个剩余窗口都有数**（稠密），后者事件很少（稀疏）。所以做成 **BNPDFormer = 共享时空编码器 + 两条输出路径**。

```text
过去 12 分钟各资源状态 X  +  当前剩余工单数
        │
        ▼
   时空编码器（PDFormer 骨干）
        │
        ├──────────────► 路径① 剩余占用：直到 jobs_remaining=0 的每个窗每个节点
        │
        └──────────────► 路径② 事件预报：这段剩余时间里下一次瓶颈何时来、堵多久
```

**当前注意**：两条路径分开监督。占用头的 y 是「从当下到剩余工单清零」每窗每节点的分数 / 0/1（近 60 窗加权）；STGNPP 事件只有工艺链**拐点** ∪ **L2**。改规则后必须重跑 `bn_agg`。现训 `evt` 权重已是剩余占用头，见 §4.4。

### 0.1 模型输出（对齐 [`04.期望输出.md`](./04.期望输出.md) A / B）

推理入口：`python -m factory_bn.infer` 或 `BNPredictor.predict_x`。`remain_to_jobs_done=true`（`FactoryBN.json` 默认）时，一次前向给出：


| 输出 | 张量 / 字段 | 对应 04 | 现训能不能用 |
| --- | --- | --- | --- |
| 剩余占用分数 | `score_pred` `(K, N)`，K=`remain_len_pred` 截断，最长 512 | A.1 强度 | 尺度是剩余视界，MAE≈8.6，不能跟旧「下一窗 0.13」比 |
| 剩余占用 0/1 | `hot_prob` ≥ 0.5 → `(K, N)` | A.1 格子 | test hot_f1=0.165，P=0.10 / R=0.50，误报多 |
| 开始 / 持续 / 工位 | `occupancy_to_events`：每段 `start_s, duration_s, resource_id` | **A.1 主交付** | 格式对；格子不准则事件也不准 |
| 还要多久收工 | `remain_len_pred`（60s 窗个数） | A.1 地平线长度 | test MAE 14.8 窗 |
| 当前剩余工单 | `jobs_remaining`（读 `job_kpi`，不是网络估的） | 条件 | 输入 |
| 近 180s 会不会有事件 | `will_prob` | 辅头，**不是** A.1 | test will_f1=0.588 |
| 事件落在谁 | `mark_prob` | 辅头 | 事件稀 |
| 原因 10 类 | `cause_pred` / `cause_name` | **A.3** | acc=1.0 虚高，不可信 |
| 下一事件 τ / 时长 | STGNPP | 稀疏事件 | NLL 仍贴夹紧值 |
| setting × 工位热力图 | 无此头 | **A.2** | 多 run 聚合自己画 |
| 订单/单件/工序完工 | 无此头 | **B.1–B.3** | 未做 |


A.1 事件怎么来：`hot_prob[k, n] ≥ 0.5` 在时间上连成一段 → `start_s = 下一窗起点 + k×60`，`duration_s = 段长×60`，`resource_id` = 该列工位。

没有 `remain_to_jobs_done` 的旧 ckpt 仍只出下一窗 `score_pred` + 180s `will`。现在磁盘上的 `evt/BNPDFormer_best.pt` **已经是剩余占用头**。

## 1. 工厂问题 ↔ 交通论文


| 交通             | 工厂                                        |
| -------------- | ----------------------------------------- |
| 路段节点           | `resource_id`（机台 / 龙门 / 人 / AGV / buffer） |
| 流量、速度          | 队列、等待、利用率、BLOCKED/STARVED、物流延迟…           |
| 未来几步流量         | 剩余工单收工前各窗 `bottleneck_score_s` / 占用 0/1 |
| 下一次拥堵 (时刻, 时长) | 下一次 `bottleneck_event` 的开始与持续             |


离线标签（见 `[03.离线特征聚合&打标签.md](./03.离线特征聚合&打标签.md)`）：

- `window_feature_table.csv`：资源 × 时间窗特征 + score  
- `bottleneck_label.csv`：会不会出事件、落在谁身上、多久开始…  
- `bottleneck_event.csv`：合并后的稀疏事件序列

---



## 2. 数据怎么变成模型输入（先于网络）

**没有单独的 state Embedding，也没有按边类型做 HeteroGNN。**  
异质表里的离散状态 → 统一成 `norm_state` 字符串 → 按时间窗聚合成连续数值特征 → 拼上资源类型 one-hot → 进模型用 `Linear(F→64)` 编码。

```text
仿真 raw state（machine / human / … 各说各的）
        │  src/bottleneck_data.py :: map_*_state
        ▼
resource_event_log.to_state = IDLE / PROCESSING / BLOCKED / …
        │  bn_agg :: build_timelines + 窗口求交
        ▼
window_feature_table 一行 = FEATURE_COLS 运营连续特征
        │  export_dataset.py :: FEATURE_COLS + type_onehot
        ▼
X[t, n, :] ∈ R^F     F = |FEATURE_COLS| + 5（当前 21 + 5 = 26）
        │  dataset.py z-score
        ▼
DataEmbedding: Linear(F→64) + 时间 PE + LapPE
```

默认时间设定：窗长 60s；看过去 **12** 窗（约 12 分钟）→ 预报 **直到剩余工单清零** 的占用（最长 512 窗）。近 180s `will` 只是辅头。  
特征列与打分细节见 `[03.离线特征聚合&打标签.md](./03.离线特征聚合&打标签.md)`。

### 2.1 异质 raw state → 统一 `norm_state`

文件：`src/bottleneck_data.py`。按资源类型选不同映射表，落到同一套状态词表；写入 `resource_event_log` 的是 `to_state = norm_state`，不是原始字符串。

机台 / 龙门架：

```text
free                 → IDLE
invalid              → STOP
working_*            → PROCESSING
waiting_*            → BLOCKED
materialReadyFor_*   → WAITING
```

人 / 机器人（还看 subtask）：

```text
free                 → IDLE
working + wait       → STARVED
working + walk/操作   → PROCESSING
```



### 2.2 离散状态 → 窗口统计（不是 one-hot）

文件：`tools/bn_agg/`。

1. `build_timelines`：事件 `to_state` 展开成区间 `[t_i, t_{i+1}) → state`
2. 窗口内与状态集合求交时长（`constants.py`）：

```text
ACTIVE_STATES  = {PROCESSING}           → active_pct_s, current_active_duration_s
BLOCKED_STATES = {BLOCKED}              → blocked_time_s
STARVED_STATES = {STARVED, WAITING}     → starved_time_s
STOP_STATES    = {STOP}                 → stop_time_s, unavailable_pct_s
```

1. 其它异质表同步聚到**同一行**：`job_trace`（队列 / 等待）、`buffer_event_log`（占用）、`route_transport_task`（物流延迟）、`material_inventory_log`（缺料）、`disturbance_log`（L2 → `disturbance_active_s`）等。

输出：`window_feature_table.csv`，每资源 × 每窗口一行。  
**此时状态已经不是类别 id，而是 float 占比 / 时长。**

### 2.3 拼成模型输入向量 X

文件：`factory_bn/export_dataset.py` + `graph.py`。当前 `FEATURE_COLS`（21 维，以代码为准）：

```text
queue_length_s, avg_waiting_time_s, occupancy_ratio_s, queue_growth_rate_s,
active_pct_s, current_active_duration_s, blocked_time_s, starved_time_s,
stop_time_s, unavailable_pct_s, inter_departure_var_s,
upstream_blocked_ratio_s, downstream_starved_ratio_s,
route_delay_s, inbound_wait_s, material_shortage_propagation_s,
affiliated_buffer_occ_s, tb_minus_ts_s, disturbance_active_s,
is_turning_point, is_momentary_bn
```

资源类型 5 维 one-hot：`machine` / `gantry` / `human` / `transport_robot` / `buffer`。

```text
feat = [row[c] for c in FEATURE_COLS] + type_onehot(resource_type)
# → features[t, n, :]  shape = (T, N, 21+5)
```

- 异质资源共用**同一套运营特征 schema**（没有的表就填 0，如 machine 的 occupancy 常为 0）
- 类型差异靠 **5 维 one-hot** 告诉模型「这是机台还是人」
- **不含** `bottleneck_score_s`（标签，防泄漏）；score 只当监督



### 2.4 模型里怎么 embed

文件：`factory_bn/backbone.py` + `model.py`。BNPDFormer 关掉了日内 / 星期 Embedding（工厂没有真实日历）。

```text
x = Linear(F → embed_dim=64)(X)   # TokenEmbedding，所有节点共用一套权重
x += 时间位置编码
x += LaplacianPE(adj_mx)           # 工艺 / 物流图结构位置
```

之后进 PDFormer 的 Temporal / Geo / Sem 注意力（见 §3）。  
**没有** `nn.Embedding(num_states, …)` 对 IDLE / PROCESSING 做查表。

容易误解的点：


| 可能以为的                   | 当前实际做法                           |
| ----------------------- | -------------------------------- |
| 每张表 / 每类资源各自一套 encoder  | 先映射 + 聚合到统一 float 向量             |
| 离散 state 用 Embedding    | 变成占比 / 时长再 Linear                |
| HeteroGNN（按边类型 message） | 单一 `adj_mx` + 类型 one-hot；边类型未分开传 |


图怎么建（`graph.py`）：工艺链上下游 + buffer 亲和 + 物流资源连机台 + 同类弱连。  
这张图提供：地理邻域（Geo）、Laplacian 位置编码；语义邻域用行为相关度近似（短 episode 没用 DTW）。

---



## 3. 模型内部：共享编码 + 双路径



### 3.1 整体

```text
X (B, T_in=12, N≈34, F=|FEATURE_COLS|+5)
        │
        ▼
DataEmbedding
  Linear(F→64) + 时间位置编码 + 图 Laplacian PE
        │
        ▼
STEncoderBlock × L（PDFormer）
  · Temporal：每个节点看自己的历史
  · Geographic：工艺/物流上邻近的节点 + 「延迟形态」pattern
  · Semantic：行为相似的资源（即使图上不相邻）
        │
        ├─► remain_len + score_pred / hot_prob (B, K≤512, N)   # 路径① A.1 占用
        │
        ├─► will / mark / cause_head        # 辅助：近 180s 会不会堵、落在谁、原因（A.3）
        │
        └─► 历史事件 → Inquirer → Continuous GRU
              → 周期门控强度 λ → 下一事件 τ / duration   # 路径②（删失到剩余工单收工）
```

训练总损失大致是：

```text
总损失 ≈ w_score × 近段分数回归
       + w_hot   × 近段占用 0/1（pos_weight=32）
       + w_remain_len × 剩余窗数
       + w_will  × 近 180s 是否会发生（辅助）
       + w_event × 点过程 NLL（+ 时长）
       + w_cause × 原因分类（有标签的窗）
```

占用 / 分数只在前 `near_remain_windows=60` 窗上算，并乘 `exp(-k / remain_loss_tau)`。`remain_len` 仍看全地平线。

存盘看 **`hot_f1`**（近段占用 F1），不是总 `val_loss`，也不是 `score_mae`。事件 NLL 若贴在 -\log(10^{-6})，按 loss 选 ckpt 会选错头。`w_event` 默认 0.1；will / hot 看 precision / recall / F1 / AP，不要只看 acc。

STGNPP 的事件地平线在剩余占用头打开时是 **剩余工单收工时间**（右删失）；`will` 辅头仍用 180s。周期门控有 `intensity_gate_floor=0.1`，避免 λ 被乘没。

形状直觉（18 物料、60s 窗）：


| 符号           | 含义        | 典型                  |
| ------------ | --------- | ------------------- |
| N            | 资源数       | ~34                 |
| T_in / K     | 历史窗 / 剩余占用窗 | 12 / ≤512（`remain_len_pred` 截断） |
| F            | 输入维       | 当前 26（21 运营 + 5 类型） |
| D            | 隐向量维      | 64                  |




### 3.2 路径① 在干什么（PDFormer）

用注意力在「时间 × 资源图」上读历史，预报**从当下到剩余工单收工**每个资源每个剩余窗的拥堵分数 / 0/1。PDFormer 管的是连续、每个资源每个时间窗都有的瓶颈强度；DFT 是为了对齐工厂里常见的**延迟传播**。


| 借鉴点          | 白话                   | 工厂里怎么用                           |
| ------------ | -------------------- | -------------------------------- |
| 动态空间注意力      | 谁影响谁会随时间变，不是死图       | 拥堵、缺料会沿产线传播                      |
| Geo mask（短程） | 只让跳数不太远的邻居互相看        | 工艺链、缓冲、物流上真正连着的资源                |
| Sem mask（长程） | 行为像的节点也能互相看          | 工作模式相似的机台，即使物理上不挨着               |
| **DFT 延迟感知** | 看邻居时带上「过去几步长什么样」     | 上游积压往往晚几步才传到下游                   |
| 时间注意力 + 剩余占用头 | 每个节点看自己的时间序列，再输出剩余各窗 | 历史 12 窗 + 剩余工单数 → `(K, N)` 占用 |


三个注意力头各管一件事：时间（自己过去堵过吗）、地理（上下游/物流邻居）、语义（行为很像的资源）。

**延迟（DFT）**：上游「积压/高利用率」的短时波形，过一会儿才传到下游。实现上用 k-means 得到一批短时 pattern，匹配后**加到地理注意力的 Key**里——让模型算「该看谁」时带上邻居刚发生过的形态。

（相对原文：语义距离用相关代替 DTW；pattern 用 k-means 代替 k-Shape——仿真 episode 短，够用。）

### 3.3 路径② 在干什么（STGNPP）

分数是每分钟都有的；**真正的大堵车很少**。路径②专门预测「下一次」何时开始、持续多久。


| 借鉴点                      | 白话                            | 工厂里怎么用                                                       |
| ------------------------ | ----------------------------- | ------------------------------------------------------------ |
| Spatio-temporal Inquirer | 在稠密隐状态里，按「事件发生过的位置×时间」把上下文抠出来 | 历史瓶颈在哪些资源、哪些窗，就把对应编码拿出来                                      |
| Continuous GRU           | 事件之间慢慢变，事件时刻猛地跳一下             | 平时缓慢积压；故障/离岗瞬间状态突变                                           |
| 周期门控强度 λ                 | 强度函数带一点「时段」调制                 | 用 episode 进度当相位（订单推进到哪一阶段）                                   |
| **点过程 NLL + 时长误差**       | 真正按「事件时间」训练，不是只问会不会发生         | 主目标：horizon 内的 `next_tau` / `next_dur`；更远或没有则 \Lambda(H) 生存项 |


工厂没有真实「星期几」，周期用 episode 内相位近似（订单推进到哪一阶段）。

窗口级 `will_bottleneck`（未来 H 内有没有事件）是**粗标签辅助**；事件多了应以 NLL 为主。

**A3 原因头**：同一套图级池化特征上再接 10 类 `cause_head`。标签来自 Stage-C 的 `root_cause_reason`（本窗与 L2 重叠则用扰动类型，否则热窗上的规则启发式）。无标签窗（空字符串）id=-1，不进 CE。训练用逆频率类别权重；验证打印 `cause_acc` 和多数类基线 `cause_maj`。旧 `episodes.npz` 没有 `{ep}_cause` 时该头无监督、准确率无意义，需重跑 `export_dataset`。

### 3.4 实现 vs 原文（对照表）


| 模块      | 原文                       | 当前代码                                        |
| ------- | ------------------------ | ------------------------------------------- |
| 稠密骨干    | PDFormer STAttn + DFT    | 已接（相关 / k-means 简化）                         |
| 事件上下文   | Spatio-temporal Inquirer | `stgnpp.SpatioTemporalInquirer`             |
| 事件动力学   | Continuous GRU           | `stgnpp.ContinuousGRU`                      |
| 强度 + 训练 | 周期门控 λ + NLL             | `PeriodicGatedIntensity` + NLL；保留 will/mark |
| 推理下一时刻  | 对密度二分取中位数                | 仍多直接读 τ；二分可后补                               |
| 数据量     | 交通数十万事件                  | 工厂事件应稀疏（约束/L2）；峰值当事件会变成「谁在加工」               |


---



## 4. 指标、扰动对照、超参

聚合 / 导出 / 训练命令（含 `--data_dir`、`--save_dir`、wandb）一律看 `[00.运行方式.md](./00.运行方式.md)`。默认只用 **60s** 窗：合并后的事件主要落在 60s；30s 上 `will` 常为 0。

### 4.1 指标怎么读


| 指标                                                       | 含义             | 注意                                           |
| -------------------------------------------------------- | -------------- | -------------------------------------------- |
| `hot_f1` / `hot_precision` / `hot_recall`                | 近段占用 0/1      | **存盘主指标**（`ckpt_metric=hot_f1`）              |
| `remain_len_mae`                                         | 剩余窗数误差         | 全地平线；不参与存盘                                   |
| `score_mae`                                              | 剩余各窗分数 MAE     | 与旧「下一窗 0.13」不可比                              |
| `loss_score` / `loss_hot`                                | 近段分数 / 占用损失    | 应随训练下降                                       |
| `will_f1` / `will_precision` / `will_recall` / `will_ap` | 未来 180s 内是否有事件 | 正例稀（~7%）时 **不要看 will_acc**                   |
| `will_acc`                                               | 同一任务的准确率       | 仅作对照                                         |
| `loss_event` / `nll` / `nll_arrival` / `nll_surv`        | 点过程：到达 + 生存    | 健康时应明显低于 13.8（夹紧值 -\log(10^{-6})）            |
| `dur_mae`                                                | 事件持续时长误差       | 只在剩余地平线内正例上算                                 |
| `cause_acc`                                              | 原因头分类准确率       | 不参与存盘；必须对照 `cause_maj`；现训 1.0 不可信            |


推理主要看：`hot_prob` → A.1 连通事件、`remain_len_pred`、`score_pred`、`will_prob`、`cause_pred`。命令：`python -m factory_bn.infer`（见 `[00.运行方式.md](./00.运行方式.md)`、`[08.边采边聚合与在线推理.md](./08.边采边聚合与在线推理.md)`）；仿真循环里用 `BNPredictor.predict_x`。

### 4.2 和仿真扰动怎么对上

扰动在仿真侧注入（见 `[02.瓶颈维度&环境扰动.md](./02.瓶颈维度&环境扰动.md)`），一次 run 通常只开一个维度：`material` / `human` / `logistics` / `machine`（或 `none` 对照）；`intensity` 控制强弱。目的是让产线**真的出现可标注的瓶颈事件与分数抬升**，否则两条路径都学不到东西。

事件很少的对照 run 上，分数头还能学，事件头指标不可信。扰动够强、事件条数上来之后，`will` 才不会塌成「永远猜没事」。

### 4.3 默认超参（`FactoryBN.json`）


| 参数                                           | 默认                        | 含义                          |
| -------------------------------------------- | ------------------------- | --------------------------- |
| `input_window`                               | 12                        | 12 分钟历史                     |
| `remain_to_jobs_done`                        | true                      | A.1：预报到剩余工单清零               |
| `max_remain_windows`                         | 512                       | 占用图最长窗数（约 8.5h）             |
| `w_hot` / `w_remain_len`                     | 1.0 / 0.3                 | 占用 0/1 与剩余窗数                |
| `hot_pos_weight`                             | 32                        | 热窗极稀，正例加权                   |
| `near_remain_windows` / `remain_loss_tau`    | 60 / 20                   | 占用/分数只监督近 60 窗，指数衰减         |
| `ckpt_metric`                                | `hot_f1`                  | 近段占用 F1 存盘                  |
| `output_window`                              | 1                         | 仅旧头使用                       |
| `use_stgnpp`                                 | true                      | 打开事件路径                      |
| `will_pos_weight`                            | 8                         | will 正样本权重（原 20 过猛）         |
| `w_score` / `w_will` / `w_event` / `w_cause` | 1.0 / 0.5 / **0.1** / 0.4 | 事件头未离开夹紧前不要把 `w_event` 调回 1 |
| `intensity_gate_floor`                       | 0.1                       | 周期门控下限，避免 λ→0               |


权重写到 `libcity/cache/model_cache/<标签>/BNPDFormer_best.pt`（由训练命令 `--save_dir` 指定）。

### 4.4 现训 `evt`（2026-08-16）

数据：`old_machine2.0` + `old_logistics2.0` + `new_machine1.0`。配方：`hot_pos_weight=32`、`near_remain_windows=60`、`ckpt_metric=hot_f1`。WandB [`lchf07o9`](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/lchf07o9)（相对第一版 remaining [`9wat79fs`](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/9wat79fs)）。


| | 这轮 test（ep47） | 上一轮 remaining（ep27） |
| --- | --- | --- |
| hot_f1 | **0.165** | 0.065 |
| hot P / R | 0.10 / **0.50** | 0.45 / 0.035 |
| will_f1 | **0.588** | 0.445 |
| remain_len MAE | 14.8 窗 | **8.5 窗** |
| nll_arrival | 13.82 | 13.82 |
| cause_acc | 1.0（maj 0.48） | 1.0（maj 0.48） |


占位会亮了，但精度 0.10，**还实现不了可用的 A.1**。原因头虚高，**实现不了可用的 A.3**。A.2 仍靠实验聚合。

---



## 5. 代码地图


| 文件                       | 干什么                                                    |
| ------------------------ | ------------------------------------------------------ |
| `src/bottleneck_data.py` | 异质 raw state → 统一 `norm_state`                         |
| `tools/bn_agg/`          | 窗口聚合、score、事件、KPI                                      |
| `backbone.py`            | Embedding + ST 注意力块                                    |
| `stgnpp.py`              | Inquirer / ContGRU / λ + NLL                           |
| `graph.py`               | 工厂邻接、hop、语义距离、类型 one-hot                               |
| `export_dataset.py`      | CSV → npz（`FEATURE_COLS` + `cause` + `jobs_remaining`） |
| `remain.py`              | 剩余工单序列、占用 pad、A.1 连通事件                                 |
| `causes.py`              | A3 十类 `root_cause_reason` 编码                           |
| `dataset.py`             | 滑窗样本 + **按 episode 划分** train/val/test（按 run 分层）     |
| `model.py`               | BNPDFormer 双路径拼装                                       |
| `train.py`               | 训练（可选 wandb）                                           |
| `configs/FactoryBN.json` | 超参                                                     |
| `BORROWING.md`           | 两篇论文各借了什么                                              |


入口：`python -m factory_bn.train`（不要走原版交通 `run_model.py`）。

---



## 6. 取舍与下一步

**已做的简化（合理）**

- 相关距离代替 DTW；k-means 代替 k-Shape  
- 周期用 episode 相位，无日历日/周  
- 事件少时保留 will/mark 辅助损失

**数据现实**

- 事件头只能证明「管线通了」；要效果必须多采扰动/高负载 episode

**建议优先级**

1. 把 A.1 占位 **precision** 拉上去（现训 R=0.50、P=0.10）
2. 事件序列采够后再看 NLL 是否离开夹紧值
3. A.2 用多 setting 整单聚合热力图，不必改头
4. A.3 先对照 `cause_maj` / 混淆矩阵，标签不够就先不当产品功能

---



## 7. WandB 实时监控

对齐仓库根目录 `train.py` 与 `source/algo/hierarchical/hc_factory/*.py` 的用法：默认关闭；打开后每个 epoch 把损失和指标推到 Weights & Biases。未启用时**不 import wandb**，`bn_pdformer` 里没装这个包也能训。

### 实现

入口：`factory_bn/train.py`。开关与 RL 侧一致：


| 配置 / CLI                              | 默认                                   | 作用                          |
| ------------------------------------- | ------------------------------------ | --------------------------- |
| `wandb_activate` / `--wandb_activate` | `false`                              | 打开后才 `wandb.init`           |
| `wandb_project` / `--wandb_project`   | `FactoryBN_PDFormer`                 | 项目名（和仿真的 `HcFactory` 分开）    |
| `wandb_name` / `--wandb_name`         | 空 → `BNPDFormer_YYYY-mm-dd_HH-MM-SS` | 本次 run 显示名                  |
| `wandb_entity` / `--wandb_entity`     | 空                                    | 团队/用户；空则用 `wandb login` 的账号 |


`wandb.init(..., resume="allow")`，并把超参 + 数据规模（节点数、train/val/test 条数、原因标签数等）写入 `wandb.config`。每个 epoch **一次** `wandb.log`，横轴是 `epoch`：


| 前缀        | 内容                                                                                                                  |
| --------- | ------------------------------------------------------------------------------------------------------------------- |
| `Train/*` | `loss`、`loss_score`、`loss_will`、`loss_cause`、`loss_event`、`nll`、`nll_arrival`、`nll_surv`、`epoch_sec`                |
| `Val/*`   | 同上 + `hot_f1` / `hot_precision` / `hot_recall`、`remain_len_mae`、`score_mae`、`will_*`、`cause_acc`、`best_hot_f1` |
| `Test/*`  | 训练结束、加载 `BNPDFormer_best.pt`（**hot_f1** 最优）后的测试指标，记在最后一个训练 epoch 上 |


验证集 `hot_f1` 创新高时更新 `wandb.summary` 的 `best_hot_f1` / `best_epoch`；结束时写入 `test_*`（含 `test_hot_f1` / `test_will_f1` / `test_remain_len_mae`），并 `wandb.save` 权重与 `last_metrics.json`。异常退出也会 `wandb.finish()`。

JSON 默认在 `factory_bn/configs/FactoryBN.json`（`wandb_activate: false`）。CLI 覆盖 JSON。

### 运行

1. 在 `bn_pdformer` 里装客户端并登录（只需一次）：

```bash
conda activate bn_pdformer
pip install wandb
wandb login
```

浏览器打开提示的链接，把 API key 贴回终端。无图形界面时也可：

```bash
export WANDB_API_KEY=<你的key>
```

1. 正常训练命令加上 `--wandb_activate`：

```bash
conda activate bn_pdformer
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/<标签> \
  --save_dir libcity/cache/model_cache/<标签> \
  --max_epoch 50 \
  --device cuda \
  --wandb_activate \
  --wandb_project FactoryBN_PDFormer \
  --wandb_name "<标签>_ep50"
```

启动后终端会打印：

```text
[wandb] project=FactoryBN_PDFormer name=merged_ep50 url=https://wandb.ai/<user>/FactoryBN_PDFormer/runs/<id>
```

打开该 URL 即可看实时曲线。tmux 里同样加这些参数即可（见 `[07.tmux运行方式.md](./07.tmux运行方式.md)`）。

1. 离线（机器暂时上不了 wandb 云）：

```bash
export WANDB_MODE=offline
python -m factory_bn.train ... --wandb_activate
# 有网后再：wandb sync wandb/offline-run-...
```

JSON 里把 `"wandb_activate": true` 也可，不必每次写 CLI；默认仍是关闭，避免误传实验。