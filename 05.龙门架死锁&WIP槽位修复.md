# 仿真卡死与 WIP 槽位修复

> 两类「18 件订单做不完、看门狗整局掐掉」的原因：  
> 1）真实路径 + 慢龙门后，看门狗把还在走路当成死锁；双龙门间距互锁空转。  
> 2）注入扰动后同类型多件并行，完工时删错 `producing` 槽位，规则策略再也选不到还在库里的旧件。  
> 不要改回 `go_to_*` 固定步数，也不要把龙门标称速度改回更快的值。  
> 现行短单：10 件 / WIP 5。L2 单次最长约 **900 步**（I=1.0 约 **10–15 min**，下限 600），**仍低于** `stall_timeout_steps=5000`。下文「18 件做不完」是 2026-08 旧采集的排障记录。导出训练时丢掉带 `deadlock_reset` / 未完工 **10/10** 的局。

相关：`[00.运行方式.md](./00.运行方式.md)`、`[02.瓶颈维度&环境扰动.md](./02.瓶颈维度&环境扰动.md)`。

---

## 1. 现象（看门狗误杀）

2026-08-13 若干 run（`21-15-23` machine、`21-16-22` human、`21-19-25` logistics、`21-20-52` none、`21-40-15` material）都没有做完 18 件。日志里出现：

```text
[DeadlockWatchdog] env_0 stall=5000 at t=...; forcing episode reset
```

`disturbance_log` 会多一行 `disturbance_type=deadlock_reset`。  
`dim=none` **也会死**，所以主因不是扰动，是看门狗把「还在走路」当成「卡死」。  
`logistics` I=2.0（1 台龙门、更慢）往往第一趟搬运就超时。

物料维当时还有第二原因：reset 永久不放法兰/弯头，5 个 WIP 卡在工作台 `WAITING material_ready`。那部分已改成限时藏库，见 `[02](./02.瓶颈维度&环境扰动.md)`。

这些未完成局**不能当整单数据**用。改代码后必须**重启**正在跑的 tmux 采集会话，旧进程不会热加载。

---



## 2. 看门狗误杀长途搬运



### 2.1 原因

`hc_single_env_base.py` 里 `stall_timeout_steps=5000`：连续 5000 逻辑步「进度指纹」不变就强制 `reset`。

旧指纹只看：

- `progress.finished`
- 进行中任务的 subtask 下标 / 完成标志
- 机台 `state`

真实路径下，`move_to_goal_area` / `carry_to_robot` 以及人、AGV 走路经常 **超过 5000 步还不切 subtask**。位移一直在变，但指纹不变 → 误判死锁。

### 2.2 修复

指纹 `_progress_signature()` 增加运动项：


| 来源                                          | 写入指纹的量             |
| ------------------------------------------- | ------------------ |
| 龙门 `GantryGroupAnimation.distance_traveled` | 每台已走路程（保留 2 位）     |
| 人 / AGV `rigid_prims` 位置                    | 当前区、目标区、xy（保留 2 位） |


只要龙门还在走、人或 AGV 还在挪，计数器就会清零。产线真正静止（完工列表、工序、机台、位移全不变）满 5000 步，仍会 `deadlock_reset`。

实现：`hc_single_env_base.py` → `_progress_signature` / `_maybe_watchdog_reset`。

---



## 3. 双龙门 `safe_x_gap` 互锁



### 3.1 原因

两台龙门共用轨道，步进时要求世界坐标 x 间距 ≥ `safe_x_gap`（配置为 4.0）。低优先级台要让路（yield）。若双方都进不了下一步、yield 又不结束，会在原地空转——此时位移也不变，看门狗同样会开。

`GantryGroupAnimation.step_next_pose` 里本来就有 `block_timeout` 参数，但未接入；`blocked_steps` 也没有在 `initialize()` 里清零。

### 3.2 修复

`src/utils.py` `GantryGroupAnimation`：

- `initialize()` 里 `blocked_steps = [0] * num_gantrys`
- 已被间距挡住时累计 `blocked_steps`
- **400 步**（`BLOCK_TIMEOUT_STEPS`）：间隙放到 `0.25 × safe_x_gap` 再试
- **800 步**：强制前进一步，打印 `[GantryUnlock] force advance gantry_`*
- 已经比安全间距更近时，**允许往远离对方的方向走**（避免「已经贴上就永远不能动」）

`src/machine.py` `num07_gantry_group`：

- `BLOCK_TIMEOUT_STEPS = 400`，传给 `step_next_pose`
- `YIELD_TIMEOUT_STEPS = 400`：让路超时则 `_force_clear_yield`，打印 `[GantryUnlock] clear yield gantry_*`，改回任务目标

这是运行时解锁，不是改导航图，也不是把 `move_speed` 调快。

---



## 4. 扰动后删错 producing 槽位（选不到旧件）

> 2026-08-14。表象仍是看门狗 `deadlock_reset`，根因是调度队列和产品选择对不齐。



### 4.1 现象

`machine` I=1.0 采集（例：`2026-08-14_14-07-25_seed42` episode 0）**17/18 件做完**，剩 job 5 的管坯停在 `BlackStorage_03`，法兰/弯头也还在地上。最后一根完工后再静 5000 步，看门狗掐局。

Job 5 时间线：


| 逻辑秒   | 事件                                         |
| ----- | ------------------------------------------ |
| 3465  | 开工（运去切割）                                   |
| 3992  | 切割结束                                       |
| 4482  | 开槽结束，管坯 enqueue 进 `BlackStorage_03`        |
| 之后    | **再也没有** `logistic_for_batch_spot_welding` |
| 31500 | `deadlock_reset`                           |


同库后来的 job 4、7、9…16 都被取走过；job 5 从 4482 一直坐到局末。

L2 那几次短故障和质检扣留都在 job 5 开工前结束，**不是**它卡死的原因（当时 L2 只有几分钟；现行 L2 更长，但仍低于 stall 5000）。真正改变流量的是 **L0：关掉工作台** `ws1`，组焊/油漆只剩 `ws0`。开槽结束时工作台忙则溢到 `BlackStorage_03`，空则直接进工位（job 0/6/8）。Job 5 溢库后，后开工的 job 6 先做完。

无扰动基线很少出现这种「后开工先完工」，所以这个 bug 平时不容易露出来。

### 4.2 原因

规则策略（Agent B）只给 `progress["producing"]` / `producing_indexs` 里还在的槽位派下一道任务（FIFO：先低槽位，staging 槽最后）。完工删槽时旧代码按**产品类型**找第一项：

```python
i = producing_list.index(product_type)
del producing_list[i]
del producing_indexs[i]
```

18 件都是 `ProductWaterPipe`，`index(类型)` **永远删列表第一项**，不是刚完工的那根。

无扰动时大致按开工顺序完工，碰巧能删对。这局队列大约是 `[5, 6, 7, …]`，job 6 先完工 → **把还在库里等物流的 job 5 踢出 producing**。之后 `product_selection` 再也选不到它，件还在场景里，调度当它不存在。

所以：**扰动没有“强到做不完订单”**；它让完工顺序反超，把同类型多 WIP 共用 producing 列表的 bug 暴露出来。

### 4.3 修复

`src/task_progress_manager.py` → `step_task_records`：按刚完工的 `product_index` 对齐槽位。

```python
i = producing_indexs.index(product_index)
del producing_list[i]
del producing_indexs[i]
```

后完工的件不会再把还在库里的旧件踢出选择队列。已采完的截断局不会变，要验证需重新采集。

---



## 5. 空闲龙门挡在 `carry_to_robot` 走廊上


> 2026-08-16。旧默认 `logistics` I=1.0（2 台龙门 + 1 台 AGV）每局看门狗掐掉，最好 9/18。不是 producing 误删。2026-08-17 起默认改为 I=1.0 保 2+2、I=2.0 为 1 龙门+2 AGV，见 `[02](./02.瓶颈维度&环境扰动.md)` §3.4。


### 5.1 现象

`2026-08-16_03-27-33_seed42` 16 局全部 `deadlock_reset`。卡住时固定是：

- 工作龙门：`carry_to_robot`（去 AGV 停靠点交料）
- 人：`control_gantry`
- 唯一 AGV：`wait` / `waiting_for_gantry_or_partner`
- 另一台龙门：`IDLE`（L2 短冻之后也一样）

L2 当时只冻空闲龙门 80–100 步，且多半发生在卡死之后，不是主因（现行 L2 约 10–30 min，仍低于 stall 5000）。I=2.0/3.0 只留 1 台龙门，不会出现这种双车互锁。现行 I=1.0 **不减龙门**（4 台全留）。

### 5.2 原因

`carry_to_robot` 去的是 **AGV 停靠 x**（如黑库 00 的点 38），不是龙门自己的停车点 39。两台龙门共轨，家在 x=36.7 / 16.7，`safe_x_gap=4`。空闲台停在家或上次落点时，经常挡在工作台去 AGV 点的走廊上。

旧 yield 救不了：工作台还没写上 `target_area_xy` 时走廊被当成一个点，空闲台不会让；让路 400 步超时后又把空闲台送回路中间。位移不再变 → 看门狗 5000 步掐局。

### 5.3 修复

`src/machine.py` `num07_gantry_group`：

- 没任务的龙门每步 `_park_idle_gantries`：默认回自家 x
- 若家落在某台工作龙门的走廊里（含已定的 `target_area_xy` / 关节目标），改停到走廊外侧 `±2×safe_x_gap`
- `_forbidden_corridor` 把任务目标算进走廊，不必等动画起步
- 空闲台不再走 yield 超时（避免被送回路中间）
- L2 把 0/1 标成 `invalid` 时仍让开，只是不接新任务

不改 `go_to_*=None`、标称 `move_speed=0.5`、并行上限 5。日志里若改停点会打 `[GantryPark] gantry_* home=… -> … (clear working corridor)`。

已采的截断局不能当整单。改完必须**重启**正在跑的采集进程。

### 5.4 两台都在干活时让路超时仍会死锁

> 2026-08-17。`2026-08-17_17-29-14_seed42` 当时 I=1.0 还是 **2 龙门 + 2 AGV**（现行四分区厂 I=1.0 是 **4 龙门 + 2 AGV**），ep0 仍 `deadlock_reset`（t=42835）。不是资源数问题。

卡住时两台都有任务：

- `gantry_0`：`carry_to_robot`（去 AGV 停靠 x）
- `gantry_1`：`go_to_processing_machine`（路径穿过同一段轨）
- 对应的人 `control_gantry`，AGV `wait`

空闲停车救不了：两台都不是 idle。旧逻辑让路 400 步后超时，把低优先级台送回任务目标，再次冲进 `carry_to_robot` 走廊，然后原地互锁（subtask 不再变，位移也停，看门狗 5000 步掐局）。L2 还没开始（计划 3927 步）就已经卡死。

`2026-08-17_18-45-02_seed42` 带上「让开后就地停住」之后更快卡死：龙门 0 家在 x≈36.7，AGV 交接点（map 38）在 x≈32.4，差约 4.3，刚好卡在 `safe_x_gap=4` 边上。就地停住等于堵在交接点门口，两台动画都 done，看门狗 t=11785。

修复（仍在 `src/machine.py`）：

- 共轨**同一时刻只让一台 travel**（`go_to_*` / `carry_to_*` / `move_to_*`）；另一台去远端避让点，不要停在家门口
- 正在 `wait` 装卸的台不让走，避免把交接点上的车拉开
- 让开后等到对方走廊不再挡住自己再恢复；不再 400 步 timeout 冲回去
- 对面是停着的车时，不要 `force advance` 顶过去

`2026-08-17_19-11-09_seed42` 好一点（ep0 做完 3 件），仍在 t=5413 后冻住、15414 看门狗。同样是 `gantry_1=carry_to_robot`、`gantry_0=go_to_processing_machine`。家 x≈36.7 相对交接点 32.4 用 1×gap=4 会被当成「已让开」；避让点还跟着走廊每步重算，动画一直被重置。

补丁（按「一台干活、过不去就等」重写，不再绕路/顶牛）：

- 共轨**同时只派一台龙门**（另一台 `invalid`/故障不算占用）
- 干完先 `waiting_park` 回到轨道两端避让点，到点才变 `free`，另一台才能接活
- 过不去就等，不再 `force advance`
- L2 只冻空闲台（在避让点上），另一台继续；挡住了就等到恢复

改完必须重启。`17-29-14`、`18-45-02`、`19-11-09`、`19-35-26` 都不能当整单。