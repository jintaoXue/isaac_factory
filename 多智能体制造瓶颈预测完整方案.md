# 多智能体调度仿真驱动的动态瓶颈预测完整方案

> 版本：代码对齐修订版  
> 方向：Isaac Sim + 多智能体调度仿真 + 多模态观测 + 动态瓶颈预测  
> 核心模型路线：**PDFormer-inspired Dynamic Graph Transformer + STGNPP-style Event Head**  

---

## 0. 方案一句话概括

本方案面向智能制造系统中的动态瓶颈预测问题，基于当前 `isaac_factory` 代码中已经实现的 HC Factory Isaac Sim / Isaac Lab 仿真环境，利用四层多智能体调度链路驱动水喉产品生产过程。当前代码中的四个 agent 不是“订单、机器、运输、人员”四个平行控制器，而是按 `Agent A → B → C → D` 串行决策：产品排序、产品选择、工序任务规划、人机/机器人分配。机器、吊车、AGV 路径、物料和 buffer 的约束由 `AlgoMultiAgentMasker`、`TaskManager` 以及各资源 manager 落地。系统在多产品并行、资源竞争、物流/加工任务切换、人员/机器人/吊车共享以及可选扰动作用下产生 queue bottleneck、process bottleneck、transport bottleneck、human bottleneck、buffer bottleneck 和 material bottleneck。随后，方案从仿真过程中收集资源状态、工件流转、buffer 占用、运输任务、物料库存和视觉观测数据，并在离线阶段补齐 agent 动作派生表、窗口聚合表和瓶颈标签表。最后，基于 **延迟感知动态图 Transformer** 预测未来瓶颈事件的发生概率、位置、类型、开始时间、持续时间和严重程度。

整体流程：

```text
Isaac Sim 工厂仿真环境
        ↓
四层 agent 串行决策驱动生产系统运行
        ↓
多 agent 决策 + 资源竞争 + 固定订单/可扩展随机订单 + 可选扰动
        ↓
自然产生动态瓶颈场景
        ↓
收集已实现仿真日志、task/action 派生信息、多模态视觉观测
        ↓
窗口聚合为 Queue / Process / Transport / Human / System 指标
        ↓
生成 bottleneck_score 和 bottleneck_event 标签
        ↓
构建动态异质生产图序列
        ↓
PDFormer-inspired Dynamic Graph Transformer
        ↓
STGNPP-style Event Head
        ↓
预测未来瓶颈：是否发生、位置、类型、开始时间、持续时间、严重程度
```

### 0.1 与当前代码基础的对齐边界

本文档中凡是涉及当前工程已有代码的描述，以 `dev_xwt` 分支的实现为准：

| 模块 | 当前代码事实 | 文档处理方式 |
|---|---|---|
| 仿真环境 | `HRTPaHC-v1`，入口为 `HcVectorEnv` / `HcSingleEnv` | 作为第一版数据生成基础 |
| 产品工艺 | 当前主要产品为 `ProductWaterPipe`，默认订单量 5 | 第一版先围绕水喉工艺路线建模 |
| 多智能体 | A 产品排序、B 产品选择、C 工序任务规划、D 人机/机器人分配 | 不再写成 order/machine/transport/human 四个平行 agent |
| 调度约束 | 由 `AlgoMultiAgentMasker` 生成 action mask | 标签和特征可从 mask、action、task_record 派生 |
| 仿真推进 | `TaskManager` 生成 `task_record`，human/robot/machine/material/route manager 推进 subtask | task 是调度粒度，subtask 是仿真粒度 |
| 已实现采集 | `episode_config.csv`、`resource_event_log.jsonl`、`job_trace.csv`、`buffer_event_log.csv`、`route_transport_task.csv`、`material_inventory_log.csv`、`disturbance_log.csv` 表头 | 作为第一版原始数据源 |
| 视觉采集 | `perception.py` 采集 `meta.jsonl` 和相机图片，目标是 human subtask done | 作为可选多模态增强，不写成已存在通用 `vision_state_log.csv` |
| 未实现部分 | agent action log、窗口聚合、瓶颈标签、图模型训练 | 标记为后续离线处理或待实现模块 |

---

## 1. 研究背景与问题定义

### 1.1 研究问题

制造系统中的瓶颈是限制整个系统吞吐能力的关键资源或关键环节。传统瓶颈识别通常基于历史统计，例如哪台机器利用率最高、哪个 buffer 队列最长、哪个工位等待时间最长。但在动态制造系统中，瓶颈具有以下特征：

1. **动态性**：瓶颈会随着订单流、机器状态、AGV 调度和人员状态变化而迁移。
2. **传播性**：某个资源的异常不一定立刻形成瓶颈，而是会经过 buffer、上游 blocked、下游 starved 等过程逐步传播。
3. **多源性**：瓶颈可能来自机器、人员、AGV、buffer、物料，也可能来自调度策略本身。
4. **隐式依赖性**：生产拓扑中的显式连接无法完全描述真实瓶颈传播，agent 调度行为会产生新的隐式依赖关系。
5. **事件性**：瓶颈不是一个单独的瞬时状态，而是具有开始时间、结束时间、持续时间、位置、类型和影响范围的事件。

因此，本研究不只是做“当前瓶颈识别”，而是做：

```text
给定过去一段时间的多智能体制造系统运行状态，预测未来一段时间内是否会发生瓶颈，
并预测瓶颈发生的位置、类型、开始时间、持续时间和严重程度。
```

### 1.2 形式化定义

给定过去 `T` 秒内的生产系统图序列：

```text
G_{t-T}, G_{t-T+1}, ..., G_t
```

其中每个图 `G_t = (V_t, E_t, X_t, A_t)` 包含：

- `V_t`：资源节点，例如 machine、human、AGV、buffer、material、route；
- `E_t`：边，例如工艺流程边、运输路径边、物料供应边、agent 动作边；
- `X_t`：节点、边和全局特征；
- `A_t`：动态邻接关系，包括物理拓扑、运输拓扑、物料关系、agent 动作关系和模型学习出的隐式关系。

预测目标为未来 horizon `H` 内的瓶颈事件：

```text
Y_{t:t+H} = {
  will_bottleneck,
  bottleneck_node,
  bottleneck_type,
  time_to_start,
  duration,
  severity_score
}
```

也就是说，模型输入是过去的多源生产状态，输出是未来瓶颈事件。

---

## 2. 文献支撑与模型路线选择

### 2.1 制造瓶颈预测直接支撑

#### 2.1.1 BSTAN：生产系统瓶颈预测的 GNN 方法

**Predicting future production system bottlenecks with a graph neural network approach** 提出了 BSTAN，即 Bottleneck Spatial-Temporal Attention Network。该方法将制造系统中的 station、buffer 和 material flow 表示为图结构，并使用 graph attention network 与 GRU 建模 station 之间的时空关系，用于预测未来 bottleneck location。

它对本方案的支撑意义：

```text
制造系统可以建模为图；
station / buffer / material flow 可以作为图节点和边；
blockage / starvation 是瓶颈预测的重要中间指标；
GAT + GRU 可以作为制造瓶颈预测 baseline。
```

但 BSTAN 的局限是：

```text
没有显式考虑 multi-agent 调度动作；
没有考虑多模态视觉状态；
图结构相对偏静态；
预测目标更偏 bottleneck location，而不是完整 bottleneck event。
```

因此，本方案将 BSTAN 作为制造领域问题定义和 baseline 支撑，而不是最终主模型。

---

#### 2.1.2 Digital Twin-based bottleneck prediction

**Digital Twin-based bottleneck prediction for improved production control** 强调利用数字孪生仿真预测制造系统未来行为，并基于预测状态进行瓶颈分析，从而辅助 production control。

它对本方案的支撑意义：

```text
Isaac Sim / digital twin 可以作为未来生产状态生成与分析平台；
瓶颈预测可以服务于后续 order release、sequencing 和调度优化；
仿真环境不仅用于数据生成，也可以作为瓶颈缓解策略验证平台。
```

因此，本方案中的 Isaac Sim 多智能体仿真环境可以视为一种面向瓶颈预测的数据生成型数字孪生。

---

### 2.2 AI 图模型支撑

#### 2.2.1 PDFormer：传播延迟感知动态图 Transformer

**PDFormer: Propagation Delay-Aware Dynamic Long-Range Transformer for Traffic Flow Prediction** 是 AAAI 2023 的时空图预测论文。它解决了传统 STGNN 的三个问题：

```text
1. 静态图难以刻画动态空间依赖；
2. 局部图卷积难以捕捉长距离依赖；
3. 传统模型忽略状态传播的时间延迟。
```

这些问题和制造瓶颈预测高度相似。制造系统中的瓶颈也不是瞬间传播，而是存在明显时间延迟：

```text
机器处理变慢
    ↓ 若干秒后
前置 buffer 开始堆积
    ↓ 若干秒后
上游 machine 被 blocked
    ↓ 若干秒后
下游 machine 被 starved
    ↓ 若干秒后
系统 throughput 下降
```

因此，本方案以 PDFormer 为主干思想，设计制造场景下的：

```text
Delay-aware Dynamic Spatio-Temporal Graph Transformer
```

用于学习制造资源之间的动态依赖和瓶颈传播延迟。

---

#### 2.2.2 STGNPP：事件级时空图神经点过程

**Spatio-Temporal Graph Neural Point Process for Traffic Congestion Event Prediction** 是 AAAI 2023 的事件预测论文。它指出传统 STGNN 多用于预测 dense variables，例如 flow、speed、demand，但难以预测稀疏分布在连续时间轴上的 congestion events。因此，它提出 STGNPP，同时预测拥堵事件的发生时间和持续时间。

这与制造瓶颈非常相似。瓶颈不是普通连续数值，而是事件：

```text
哪个节点发生瓶颈？
什么时候开始？
持续多久？
属于什么类型？
严重程度如何？
```

因此，本方案借鉴 STGNPP 的事件预测思想，设计简化版 event head：

```text
will_bottleneck head
bottleneck_location head
bottleneck_type head
time_to_start head
duration head
severity head
```

第一版不直接实现完整 neural point process，而是先采用多任务分类 + 回归头。后续可以升级为真正的 continuous-time point process。

---

### 2.3 最终模型路线

综合制造领域瓶颈预测论文和 AI 时空图模型论文，本方案最终采用：

```text
PDFormer-inspired Dynamic Graph Transformer
        +
STGNPP-style Bottleneck Event Head
```

完整命名可以是：

```text
Prior-guided Delay-aware Dynamic Spatio-Temporal Graph Transformer
for Multi-agent Manufacturing Bottleneck Event Prediction
```

中文名称：

```text
面向多智能体制造系统的先验引导延迟感知动态图 Transformer 瓶颈事件预测模型
```

---

## 3. 多智能体调度仿真数据生成机制

### 3.1 总体思路

本方案的数据生成不再以“人为注入扰动”为唯一机制，而是以当前代码中已经实现的四层调度 agent 驱动的动态生产仿真为核心。扰动仍然可以存在，但第一版代码基础里，主要可依赖的自然瓶颈来源是多产品并行、工序前置物流、human / robot / gantry / machine 共享资源、mask 限制以及 storage 容量约束。

```text
旧思路：
人为扰动 → 产生瓶颈 → 收集数据 → 训练预测模型

新思路：
四层 agent 串行调度生产系统 → 资源竞争与物流/加工耦合自然产生瓶颈 → 收集数据 → 离线聚合与标注 → 训练预测模型
```

### 3.2 四个 agent 定义

当前工程中的多智能体链路在 `source/algo/multiagent/hc_factory/` 下，规则版本为 `rule_based.py`，MARL 版本为 `MARL.py` 与 `marl_rl_agents.py`。四个 agent 是层级式串行决策，而不是并列的资源控制器。

| 当前 Agent | 代码类 | 动作含义 | 主要依赖 | 可能影响的瓶颈 |
|---|---|---|---|
| Agent A：Product Sequencing | `ProductSequencingAgent` / `RLProductSequencingAgent` | 从未开工产品类型中选择下一个排产产品类型 | `progress.not_started`、`progress.next_product`、`agent_A_product_sequencer` mask | 决定产品释放顺序；当前默认只有 `ProductWaterPipe`，影响较弱，扩展多产品后影响产品组合瓶颈 |
| Agent B：Product Selection | `ProductSelectionAgent` / `RLProductSelectionAgent` | 在正在生产的批次和 `next_product` 中选择一个焦点产品批次 | `progress.producing`、`progress.producing_indexs`、`ongoing_task_records`、`agent_B_product_selector` mask | 影响并行生产中哪个工件优先推进，可能导致部分工件长期等待 |
| Agent C：Process Task Planning | `ProcessTaskPlanningAgent` / `RLProcessTaskPlanningAgent` | 为选中产品选择下一道 `task`，例如 `logistic_for_pipe_cutting` 或 `pipe_cutting` | human / robot / machine / material task mask 的交集 | 直接决定物流任务与加工任务推进节奏，是 process / transport / material bottleneck 的关键入口 |
| Agent D：Human-Robot Allocation | `HumanRobotMachineAllocationAgent` / `RLHumanRobotAllocatorAgent` | 为非 `none` task 分配 human，并在物流 task 中分配 robot | human / robot `self_availability_mask` | 影响人力、AGV/robot 资源竞争，可能导致 human bottleneck 或 transport bottleneck |

机器和工位不是独立 agent 直接选择的对象。当前代码中，机器/工位由 `TaskManager.update_new_task_record()` 根据 task 类型、目标机器状态和空闲工位确定；吊车由 `_find_free_gantry()` 选择；路线由 `RouteManagerVectorEnv` 根据 area id 生成。

### 3.3 多 agent 状态、动作与奖励

#### 3.3.1 状态 State

当前 rule-based agent 直接读取完整 `env_state_action_dict` 中的 mask 并选取第一个或最后一个合法动作；MARL 版本通过 `MARLObsEncoder` 将状态压缩为固定维向量。第一版预测方案可从 `env_state_action_dict` 和 collector 日志中恢复下列状态：

```text
queue_length
buffer_occupancy
machine_status
machine_status
robot_position
robot_task_status
gantry_status
route_target
human_status
human_workload
material_inventory
current_WIP
throughput_rolling
```

对齐当前 MARL 代码，四层 agent 的 observation 重点如下：

- Agent A：`progress` 汇总、human / robot 空闲数量、产品排序 mask；
- Agent B：`progress` 汇总、Agent A 动作、产品选择 mask；
- Agent C：`progress` 汇总、Agent B 动作、任务规划 mask、human/machine task mask；
- Agent D：`progress` 汇总、Agent C 动作、human / robot self availability mask。

#### 3.3.2 动作 Action

| 当前 Agent | 动作空间 |
|---|---|
| Agent A | one-hot 产品类型选择，目前主要是 `ProductWaterPipe` |
| Agent B | one-hot 产品批次选择，长度为 `parallel_producing_limit + 1` |
| Agent C | one-hot task 选择，对应 `CfgProcessTaskGalleryInAll`：`none`、物流 task、加工 task |
| Agent D | 字典动作：`human` one-hot + `robot` one-hot；当 Agent C 选择 `none` 时二者全 0 |

当前水喉工艺中的 task 编号为：

```text
0 none
1 logistic_for_pipe_cutting
2 pipe_cutting
3 logistic_for_pipe_grooving
4 pipe_grooving
5 logistic_for_batch_spot_welding
6 batch_spot_welding
7 logistic_for_arc_welding_root
8 arc_welding_root
9 logistic_for_MIG_welding_surface
10 MIG_welding_surface
11 logistic_for_paint_rust_proof
12 paint_rust_proof
```

#### 3.3.3 奖励 Reward

如果使用当前 `marl` 算法，代码中已经实现的团队 reward 是进度型奖励：

```text
reward = 10.0 * (next_finished_count - prev_finished_count) - 0.01
```

这是第一版 MARL 闭环的真实 reward。后续若要显式引导减少瓶颈，可在研究扩展中升级为：

```text
reward =
    + α * throughput
    - β * total_waiting_time
    - γ * total_WIP
    - δ * blocked_time
    - ε * starved_time
    - η * robot_gantry_transport_delay
    - λ * bottleneck_duration
```

第一版不一定必须训练复杂 RL。建议先用 rule-based / heuristic agent 跑通闭环，后续再逐步替换为 RL / MARL agent。

当前代码已经具备 rule-based 和 masked DQN 风格 MARL 的基本入口，但瓶颈预测模型本身尚未实现。建议第一版先使用 rule-based / MARL 产生数据，再离线做窗口聚合、标签生成和图模型训练。

---

## 4. 瓶颈生成逻辑

### 4.1 产品释放与并行生产导致的瓶颈

当前代码没有独立的连续随机订单流；默认订单来自 `CfgProductOrder = {"ProductWaterPipe": 5}`，并由 Agent A/B 将未开工产品逐步加入 `producing`。因此第一版中“订单瓶颈”更准确地表达为产品释放和并行生产上限带来的 WIP 压力：

```text
not_started → next_product → producing
producing 数量接近 single_env_parallel_producing_limit
多个产品批次同时竞争 human / robot / gantry / machine
```

则会导致：

```text
ongoing_task_records 增加
human / robot self_availability_mask 长期为 0
关键 machine 长期 working 或 materialReadyFor
storage occupancy 上升或频繁 enqueue/dequeue
```

对应瓶颈类型：

```text
queue bottleneck
buffer bottleneck
process bottleneck
```

后续若加入随机订单到达或多产品 mix，可以再扩展为真正的 order release bottleneck。

### 4.2 工序任务规划导致的瓶颈

当前 Agent C 负责选择下一道 task。水喉产品每道加工前通常有物流 task，完整顺序为：

```text
logistic_for_pipe_cutting → pipe_cutting
logistic_for_pipe_grooving → pipe_grooving
logistic_for_batch_spot_welding → batch_spot_welding
logistic_for_arc_welding_root → arc_welding_root
logistic_for_MIG_welding_surface → MIG_welding_surface
logistic_for_paint_rust_proof → paint_rust_proof
```

则会导致：

```text
某些产品批次一直未被 Agent B/C 选中
某些 task 因 material / machine / human / robot mask 不满足而长期不可选
某台机器长时间 working 或 materialReadyFor
下游加工 task 因前置物流未完成而 starved
```

对应瓶颈类型：

```text
machine bottleneck
process bottleneck
task_planning_delay
```

### 4.3 物流与吊车/AGV 共享导致的瓶颈

当前代码中没有独立 `transport_agent`，物流由 Agent C 选择 logistic task，由 Agent D 分配 robot，并由 `TaskManager` 选择空闲 gantry。物流 subtask 由 human、gantry、robot 共同推进：

```text
human: go_to_material / control_gantry / material_on_goal_area
gantry: go_to_material / carry_to_robot / carry_to_goal_area
robot: go_to_material / carry_to_goal_area
```

则会导致：

```text
route_transport_task.csv 中 requested → in_progress → completed 时间变长
delay_reason = finding_free_gantry
gantry resource 长期 PROCESSING
robot resource 长期 PROCESSING 或长期无空闲
目标 machine 长时间等待物料
```

对应瓶颈类型：

```text
transport bottleneck
gantry bottleneck
robot_allocation_delay
```

### 4.4 人员分配与人工 subtask 导致的瓶颈

当前 Agent D 分配 human，human manager 根据 task_record 的 human 列 subtask 推进人员移动和操作。若 human 资源不足、长期占用或人工 subtask 时间较长：

```text
human self_availability_mask 长期为 0
human resource 长期 PROCESSING
subtask_name 长期停留在 control_machine / control_gantry / material_on_gantry
关键 task 无法被 Agent C 合法选择
```

则会导致：

```text
manual_station waiting_time 增加
human_operation_time 增加
downstream starved
upstream blocked
```

对应瓶颈类型：

```text
human process bottleneck
human transport bottleneck
human_allocation_delay
manual_subtask_delay
```

### 4.5 物料与 storage 导致的瓶颈

当前 `ProductWaterPipe` 包含 `product_00_pipe`、`product_00_flange`、`product_00_elbow`、`product_00_semi`、`product_00_maded` 等 submaterial。`MaterialBatch.step()` 根据 subtask 阶段把物料放在 storage、machine、gantry、robot 或隐藏位置。

若 storage 满、目标工位不可用、半成品未及时转运或 required material 不满足，会表现为：

```text
material_inventory_log.csv 中 storage_location 变化异常
shortage_flag = 1
buffer_event_log.csv 中 occupancy_ratio 长期高
material task_availability_mask 无法开放下一道 task
```

对应瓶颈类型：

```text
material bottleneck
buffer bottleneck
downstream_starvation
```

### 4.6 扰动的作用

扰动在新框架中不是唯一数据生成来源，而是用于增强场景多样性：

| 扰动类型 | 影响对象 | 作用 |
|---|---|---|
| 机器故障 | machine | 测试 task mask、machine state 和下游 starvation 的变化 |
| 人员疲劳 | human | 测试 human subtask 持续时间变长时的瓶颈传播 |
| AGV / robot 延迟 | robot / route | 测试 logistic task 延迟 |
| gantry 不可用 | num07_gantry_group | 测试 `finding_free_gantry` 延迟与 transport bottleneck |
| 物料短缺 | material | 测试 material mask 与 storage_location 的变化 |
| buffer 容量变化 | storage | 测试有限缓存下的阻塞传播 |
| 产品释放压力 | progress / producing | 测试高 WIP 下瓶颈形成过程 |

最终数据生成逻辑：

```text
四层 agent 调度 + resource manager 约束是主要瓶颈来源；
扰动注入是辅助场景增强和 root cause 标注方式；
第一版不应假设代码已经存在独立 order/machine/transport/human agent。
```

---

## 5. 资源对象与瓶颈维度定义

### 5.1 资源类型

| 资源类型 | 包含对象 | 作用 | 典型瓶颈表现 |
|---|---|---|---|
| `machine` | 切管机、压槽机、焊接设备、涂漆工位等 | 对工件进行加工 | 加工慢、故障、setup 久、active 时间高 |
| `human` | 工人、人工搬运、人工点焊、人工涂漆 | 人工操作或辅助搬运 | 人工操作慢、等待人工、人搬运慢 |
| `transport_robot` | AGV、AMR、运输小车 | 在工位和 buffer 间搬运 | 小车不足、路径拥堵、运输延迟 |
| `buffer` | 原材料区、半成品区、成品区 | 存放 WIP、原料、成品 | 半成品区满、成品区满、等待时间长 |
| `material` | 钢管、法兰、弯头、油漆、焊丝 | 被加工或消耗的物料 | 缺料、补料延迟、供应不足 |
| `route` | AGV 路径、运输通道、工位间连接 | 承载运输流 | 路径拥堵、运输时间增加、等待搬运 |

### 5.2 瓶颈维度

#### Queue State 瓶颈

关注：

```text
哪里排队最多？
哪里等待时间最长？
哪里 WIP 堆积最严重？
```

典型指标：

```text
queue_length
avg_waiting_time
max_waiting_time
queue_growth_rate
occupancy_ratio
full_duration_ratio
```

#### Process State 瓶颈

关注：

```text
哪个工位最忙？
哪个工位 active 时间最长？
哪个工位一直处于加工、故障、维修或换型？
哪个工位造成上下游 blocked 或 starved？
```

典型指标：

```text
active_pct
current_active_duration
blocked_time
starved_time
processing_time
setup_time
down_time
repair_time
inter_departure_var
```

#### Transport State 瓶颈

关注：

```text
哪条路径运输延迟最长？
哪辆 AGV 等待或停止比例最高？
哪个工位等待运输时间最长？
```

典型指标：

```text
transport_waiting_time
route_delay
travel_time
agv_stop_ratio
route_occupancy
num_transport_tasks
```

#### Human State 瓶颈

关注：

```text
哪个人工工位操作变慢？
哪个人员长期空闲或长期忙碌？
是否存在疲劳、姿态异常或操作延迟？
```

典型指标：

```text
human_motion_speed
hand_velocity
idle_duration
slow_operation_prob
operation_time
human_assignment_count
human_reallocation_count
```

#### System State 瓶颈

关注：

```text
某个节点是否导致上游 blocked？
某个节点是否导致下游 starved？
系统 throughput 是否下降？
瓶颈是否发生迁移？
```

典型指标：

```text
upstream_blocked_ratio
downstream_starved_ratio
total_WIP
throughput_rolling
throughput_loss
affected_nodes
num_blocked_nodes
num_starved_nodes
```

---

## 6. 状态机设计

本章需要严格区分“当前代码已经输出的归一化状态”和“后续为了更细瓶颈归因可扩展的状态”。第一版标签生成应优先使用当前 collector 已经输出的状态，避免依赖尚未实现的故障、维修、充电、换型等字段。

### 6.1 当前代码中的 Machine 状态

`BottleneckDataCollector.map_machine_state()` 会把 machine 原始状态归一化为下表：

| 原始状态 | 归一化状态 | reason | 含义 |
|---|---|---|---|
| `free` | `IDLE` | `None` | 当前工位空闲 |
| `working_<task>` | `PROCESSING` | `task=<task>` | 当前工位正在执行物流或加工相关 task |
| `materialReadyFor_<task>` | `WAITING` | `material_ready` | 物料已在工位，等待对应加工 task 被选择 |
| `waiting_<task>` | `BLOCKED` | `downstream_not_ready` | 当前代码用于表示下游/下一工位未就绪造成的等待 |
| `invalid` | `STOP` | `invalid_workstation` | 无效或不可用工位 |

当前代码没有原生输出 `SETUP`、`TOOL_CHANGE`、`DOWN`、`REPAIR`。这些可以作为后续扰动和维修模块加入后的扩展状态。

### 6.2 当前代码中的 Human / Robot / Gantry 状态

human、robot 和 gantry 的归一化状态由 `map_human_robot_state()` 根据原始资源状态和当前 subtask 得到：

| 原始状态 / subtask | 归一化状态 | reason | 含义 |
|---|---|---|---|
| `free` | `IDLE` | `None` | 当前资源空闲 |
| `working_<task>` 且 subtask 为移动类 | `PROCESSING` | `subtask=walk` | human/robot 正在去取料、去目标区或去加工机器 |
| `working_<task>` 且 subtask 为操作/搬运类 | `PROCESSING` | `subtask=<subtask>` | human、robot 或 gantry 正在执行对应 subtask |
| `working_<task>` 且 subtask 为 `wait` | `STARVED` | `waiting_for_gantry_or_partner` | 等待伙伴资源，例如等待 gantry、machine 或 robot |

这里的 `STARVED` 是 collector 对等待伙伴的归一化表达，不代表代码中资源 manager 一定有一个独立 `starved` 原始状态。

### 6.3 当前代码中的 Buffer / Storage 状态

buffer 在代码中主要对应 `storage`，由 `Storage.step()` 根据 `num_material` 和 `capacity` 维护：

| 原始状态 | 触发条件 | 可派生状态 |
|---|---|---|
| `empty` | `num_material == 0` | `EMPTY` |
| `partial` | `0 < num_material < capacity` | `NORMAL` 或根据阈值派生 `NEAR_FULL` |
| `full` | `num_material == capacity` | `FULL` |

`OVERFLOW_RISK`、`BLOCKING_UPSTREAM` 不是当前原始日志直接给出的状态，可由窗口聚合时根据 occupancy growth、machine `BLOCKED` 和工艺拓扑派生。

### 6.4 当前代码中的 Material 状态

material 的位置和可用性主要来自 `material_inventory_log.csv`：

| 字段 | 含义 |
|---|---|
| `storage_location` | 当前 submaterial 位于 storage、machine、gantry、robot 或 `disappear` |
| `reserved_quantity` | 当前 material batch 是否被 ongoing task 占用 |
| `consume_quantity` / `replenish_quantity` | 位置变化派生出的消耗/补充事件 |
| `shortage_flag` | 当前 collector 中对部分缺料场景的启发式标记 |
| `finished_task` | 该产品批次已完成到哪一道 task |

后续如果加入真实库存系统，可扩展 `AVAILABLE`、`LOW_STOCK`、`SHORTAGE`、`REPLENISHING`、`RESERVED`、`DELAYED` 等状态。

---

## 7. 数据采集设计

### 7.1 原始日志表

当前 `CfgBottleneckData["collector_version"] = "v0.2"` 已经实现以下仿真原始日志。第一版应优先围绕这些表做窗口聚合和瓶颈标签。

| 表名 | 当前状态 | 作用 |
|---|---|---|
| `run_manifest.json` | 已实现 | 记录 run_id、collector_version、logic_dt 和输出表清单 |
| `episode_config.csv` | 已实现 | 记录 seed、algo、num_envs、sim_dt、logic_dt、产品订单、加工时间、subtask 时间、buffer 容量、人/机器人/吊车配置、并行生产上限等 |
| `disturbance_log.csv` | 已实现表头 | 记录环境扰动；当前默认写表头，具体扰动注入仍需后续模块 |
| `resource_event_log.jsonl` | 已实现 | 记录 machine、gantry、human、robot 的资源状态变化，包括 raw_state、norm_state、task、subtask、reason |
| `job_trace.csv` | 已实现 | 记录产品 task 的 queue_enter、queue_leave、process_start、process_end、departure、stage_complete 等事件 |
| `buffer_event_log.csv` | 已实现 | 记录 storage/buffer 占用、容量、occupancy_ratio、enqueue/dequeue/snapshot |
| `route_transport_task.csv` | 已实现 | 记录物流 task 的 requested、in_progress、delayed、completed，以及 from_node、to_node、chosen_gantry_index、human_key、robot_key |
| `material_inventory_log.csv` | 已实现 | 记录 submaterial 的 storage_location、reserved、consume/replenish、shortage_flag、finished_task |

以下表是方案需要的离线派生表或后续待实现表，不应写成当前代码已直接输出：

| 表名 | 当前状态 | 生成方式 |
|---|---|---|
| `agent_action_log.csv` | 待实现或离线派生 | 可从 `job_trace.csv` 的 `job_selected`、`task_record` 信息、`human_key`、`robot_key` 和 resource event 反推 Agent B/C/D 的有效动作；若要记录 Agent A/B/C/D 原始 one-hot action，需要新增 logger |
| `agent_policy_config.csv` | 部分可由 `episode_config.csv` 替代 | 当前 `episode_config.csv` 已记录 `algo` 和 `agent_yaml_path`；如需更细策略版本，可后续新增 |
| `vision_state_log.csv` | 当前没有通用表 | 当前 perception 采集为 `output/perception_dataset/env_xx_episode_xxxxxx/meta.jsonl` + `cameras/step_xxxxxx/*.jpg`，主要标签是 human subtask 和 done |
| `window_feature_table.csv` | 待离线生成 | 由上面的 raw logs 按 window_size / stride 聚合 |
| `bottleneck_label.csv` | 待离线生成 | 由 bottleneck_score 和 future horizon 规则生成 |
| `bottleneck_event.csv` | 待离线生成 | 由连续窗口合并得到事件级标签 |

### 7.2 当前 raw logs 与字段重点

| 表 | 关键字段 | 可支持的指标 |
|---|---|---|
| `resource_event_log.jsonl` | `resource_id`、`resource_type`、`from_state`、`to_state`、`raw_to_state`、`task`、`subtask_name`、`reason`、`logic_time_s` | active_pct、current_active_duration、waiting/starved duration、resource state transition |
| `job_trace.csv` | `job_id`、`task`、`task_sequence_index`、`event`、`queue_enter_time_step`、`queue_leave_time_step`、`process_start_time_step`、`process_end_time_step`、`departure_time_step` | queue waiting time、process time、departure rate、stage completion |
| `buffer_event_log.csv` | `buffer_id`、`occupancy`、`capacity`、`occupancy_ratio`、`state`、`event` | queue_length proxy、buffer occupancy、full duration、enqueue/dequeue |
| `route_transport_task.csv` | `task_id`、`carrier_id`、`from_node`、`to_node`、`request_time_step`、`pickup_time_step`、`transport_end_time_step`、`delay_reason`、`status` | transport waiting time、travel time、gantry delay、transport completion |
| `material_inventory_log.csv` | `material_id`、`job_id`、`storage_location`、`reserved_quantity`、`shortage_flag`、`finished_task`、`event` | material shortage、material relocation、WIP location、inventory proxy |

### 7.3 Agent action 的代码对齐定义

如果后续新增 `agent_action_log.csv`，字段应按当前 A/B/C/D action 设计，而不是 order/machine/transport/human：

| 字段 | 含义 |
|---|---|
| `episode_id` / `env_id` / `time_step` / `logic_time_s` | 决策发生位置和时间 |
| `agent_id` | `agent_A_product_sequencer`、`agent_B_product_selector`、`agent_C_process_task_planner`、`agent_D_human_robot_allocator` |
| `action_name` | `product_sequencing`、`product_selection`、`process_task_planning`、`human_robot_allocation` |
| `action_index` | one-hot 动作选中的 index；Agent D 可分别记录 human_index / robot_index |
| `action_semantic` | 对 index 的语义解码，例如 `pipe_cutting`、`human_0`、`robot_1` |
| `mask_sum` | 当前合法动作数量 |
| `valid_action_flag` | 当前动作是否满足 mask |
| `selected_product_index` | Agent B/C/D 关联的产品批次 |
| `selected_task` | Agent C/D 关联的 task |
| `selected_human` / `selected_robot` | Agent D 分配的资源 |

### 7.4 Perception 数据与视觉增强

当前视觉模块不是通用目标检测日志，而是 human subtask perception 数据集。采集入口为 `PerceptionManager`，当 `CfgPerception["enabled"] = True`、`mode = "collect"` 且运行时开启 camera 时，输出：

```text
output/perception_dataset/
  env_00_episode_000000/
    meta.jsonl
    cameras/
      step_000000/
        <camera>.jpg
```

`meta.jsonl` 中每行是一个 working human 的样本，包含：

| 字段 | 含义 |
|---|---|
| `time_step`、`env_id`、`human_key`、`human_index` | 样本定位 |
| `product_index`、`task`、`subtask_index`、`subtask_name`、`subtask_done` | human 当前 task/subtask 标签 |
| `area_id` | human 当前区域 |
| `camera_paths` | 同一步相机图像路径 |
| `agent_signal` | 对应 task_record 的 subtask index、num_subtasks、ongoing row、finished 等结构化信号 |

因此第一版瓶颈预测中，视觉增强更合理的接入方式是：用 perception 数据或模型输出补充 human 节点的 `subtask_name`、`subtask_done`、`area_id`、人工操作持续时间等特征，而不是假设已有 bbox、pose keypoints、hand velocity 等通用视觉字段。

---

## 8. 窗口聚合指标

### 8.1 时间概念

| 概念 | 例子 | 含义 |
|---|---|---|
| `simulation_duration` | 由 episode length 或 `max_steps_per_episode` 决定 | 一次 episode 仿真运行多久 |
| `logic_dt` | collector 写入 `run_manifest.json` | 一个 env logic step 对应的逻辑时间 |
| `decision_interval` | 当前近似等于每个 env logic step | agent 多久做一次决策；如后续降频决策需单独记录 |
| `window_size` | 10s / 30s / 60s | 聚合特征的时间窗口 |
| `stride` | 5s / 10s | 窗口滑动步长 |
| `input_length T` | 60s / 120s | 模型看过去多久 |
| `prediction_horizon H` | 60s / 120s | 模型预测未来多久 |

推荐第一版：

```text
window_size = 30s 或 300 logic steps
stride = 5s 或 50 logic steps
input_length T = 120s
prediction_horizon H = 120s
```

具体秒数应以 `logic_time_s` 为准，而不是直接使用 Isaac Sim 物理 `sim_dt`。当前 collector 同时记录 `sim_dt` 和 `logic_dt`，窗口聚合建议统一使用 `logic_time_s`。

### 8.2 Queue State 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `queue_length_s` | 节点 s 前等待加工或运输的工件数量 | `buffer_event_log.csv`、`job_trace.csv` |
| `avg_waiting_time_s` | 工件进入节点 s 前的平均等待时间 | `job_trace.csv` |
| `max_waiting_time_s` | 当前窗口内最长等待时间 | `job_trace.csv` |
| `occupancy_ratio_s` | buffer / storage 占用比例 | `buffer_event_log.csv` |
| `queue_growth_rate_s` | 队列增长速度 | `buffer_event_log.csv` |
| `full_duration_ratio_s` | buffer 满状态持续时间比例 | `buffer_event_log.csv` |

### 8.3 Process State 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `active_pct_s` | 节点 s 在窗口内处于 active 状态的比例 | `resource_event_log.jsonl` |
| `active_time_s` | active 状态累计时间 | `resource_event_log.jsonl` |
| `current_active_duration_s` | 截至窗口结束时连续 active 时长 | `resource_event_log.jsonl` |
| `blocked_time_s` | `to_state=BLOCKED` 的持续时间 | `resource_event_log.jsonl` |
| `starved_time_s` | `to_state=STARVED` 的持续时间，当前主要来自 human/robot/gantry 等待伙伴 | `resource_event_log.jsonl` |
| `processing_time_s` | 实际加工时间 | `resource_event_log.jsonl`、`job_trace.csv` |
| `setup_time_s` | setup / 换型时间 | 后续扩展，目前无直接原始状态 |
| `down_time_s` | 故障停机时间 | 后续扰动模块 + `disturbance_log.csv` |
| `repair_time_s` | 维修时间 | 后续维修模块 |
| `inter_departure_var_s` | 出站间隔方差 | `job_trace.csv` |

### 8.4 Transport State 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `transport_waiting_time_s` | 等待 AGV 或人工搬运的时间 | `route_transport_task.csv` |
| `route_delay_s` | 实际运输时间相比预期运输时间的延迟 | `route_transport_task.csv` |
| `travel_time_s` | 实际行驶时间 | `route_transport_task.csv` |
| `agv_stop_ratio_s` | AGV/robot 停止或等待时间比例 | 后续可由 robot position 序列或 perception 扩展得到，当前不直接输出 |
| `route_occupancy_s` | 路径占用程度 | 后续可由 route log 或 robot position 派生，当前不直接输出 |
| `num_transport_tasks_s` | 当前窗口运输任务数量 | `route_transport_task.csv` |

### 8.5 Human State 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `human_motion_speed_s` | 人员移动速度 | 后续可由 human position 序列或视觉模型派生，当前不直接输出 |
| `hand_velocity_s` | 手部操作速度 | 后续视觉扩展，当前不直接输出 |
| `idle_duration_s` | 人员空闲或无操作时间 | `resource_event_log.jsonl` |
| `slow_operation_prob_s` | 操作变慢概率 | 后续 perception / fatigue 模块扩展 |
| `human_operation_time_s` | 人工操作时间 | `job_trace.csv`、`resource_event_log.jsonl` |
| `human_assignment_count_s` | 当前窗口人员被分配任务次数 | 可由 `job_trace.csv` 的 task selection 与 `resource_event_log.jsonl` 关联派生；新增 `agent_action_log.csv` 后更直接 |
| `human_reallocation_count_s` | 人员切换任务次数 | 可由 resource event 派生 |

### 8.6 Agent Action 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `num_product_selected_s` | 当前窗口新产品批次进入生产数量 | `job_trace.csv` 中首个 `job_selected` / `stage_complete` 派生 |
| `task_selection_count_s` | 当前窗口 Agent C 选择各 task 的次数 | 当前可从 `job_trace.csv` 的 `job_selected` 派生；新增 `agent_action_log.csv` 后直接记录 |
| `machine_assignment_count_s` | 当前 machine/workstation 被 task 占用次数 | `job_trace.csv`、`resource_event_log.jsonl` |
| `task_assignment_imbalance_s` | task / machine 分配不均衡程度 | `job_trace.csv`、resource event 派生 |
| `robot_assignment_count_s` | robot 被分配物流任务次数 | `route_transport_task.csv`、`resource_event_log.jsonl` |
| `gantry_delay_s` | 等待吊车的延迟 | `route_transport_task.csv` 中 `delay_reason=finding_free_gantry` |
| `human_assignment_count_s` | 人员被分配任务次数 | `resource_event_log.jsonl` 或后续 `agent_action_log.csv` |
| `invalid_action_count_s` | 无效调度动作次数 | 当前 action mask 会约束动作；若要记录无效动作需新增 `agent_action_log.csv` |
| `agent_reward_s` | 当前窗口 agent reward | 当前 MARL reward 可离线按 finished count 公式重算；如需原始 reward 需新增 logger |

第一版最小集合：

```text
num_product_selected_s
task_selection_count_s
machine_assignment_count_s
task_assignment_imbalance_s
robot_assignment_count_s
gantry_delay_s
human_assignment_count_s
```

### 8.7 System State 指标

| 指标 | 含义 | 来源 |
|---|---|---|
| `upstream_blocked_ratio_s` | 节点 s 上游资源被阻塞比例 | 上游节点状态日志 |
| `downstream_starved_ratio_s` | 节点 s 下游资源饥饿比例 | 下游节点状态日志 |
| `affected_nodes_s` | 被当前瓶颈影响的节点数 | 图拓扑 + 窗口特征 |
| `throughput_rolling` | 滚动吞吐率 | `job_trace.csv` |
| `throughput_loss` | 相比 baseline 的吞吐下降比例 | baseline episode 对比 |
| `total_WIP` | 系统总在制品数量 | `buffer_event_log.csv` + `job_trace.csv` |
| `system_utilization_mean` | 系统平均利用率 | 所有 machine 节点聚合 |
| `num_blocked_nodes` | 当前 blocked 节点数量 | `resource_event_log.jsonl` |
| `num_starved_nodes` | 当前 starved 节点数量 | `resource_event_log.jsonl` |

---

## 9. 瓶颈标签生成

### 9.1 窗口级 bottleneck_score

第一版可以先设计统一 score：

```text
bottleneck_score_s(t) =
    0.20 * norm(queue_length_s)
  + 0.15 * norm(avg_waiting_time_s)
  + 0.20 * active_pct_s
  + 0.10 * norm(current_active_duration_s)
  + 0.10 * upstream_blocked_ratio_s
  + 0.10 * downstream_starved_ratio_s
  + 0.10 * norm(transport_waiting_time_s)
  + 0.05 * material_shortage_flag_s
```

含义：

- queue 和 waiting time 反映堆积；
- active_pct 和 current_active_duration 反映资源长期忙碌；
- upstream_blocked 和 downstream_starved 反映传播影响；
- transport_waiting_time 反映运输瓶颈；
- material_shortage_flag 反映物料瓶颈。

### 9.2 不同资源类型 score

#### Machine bottleneck score

```text
machine_bottleneck_score_s(t) =
    0.25 * norm(queue_length_s)
  + 0.20 * norm(avg_waiting_time_s)
  + 0.25 * active_pct_s
  + 0.10 * norm(current_active_duration_s)
  + 0.10 * upstream_blocked_ratio_s
  + 0.10 * downstream_starved_ratio_s
```

#### Transport bottleneck score

```text
transport_bottleneck_score_s(t) =
    0.25 * norm(transport_waiting_time_s)
  + 0.20 * norm(route_delay_s)
  + 0.20 * agv_stop_ratio_s
  + 0.15 * norm(num_transport_tasks_s)
  + 0.10 * downstream_starved_ratio_s
  + 0.10 * upstream_blocked_ratio_s
```

#### Human bottleneck score

```text
human_bottleneck_score_s(t) =
    0.25 * norm(avg_waiting_time_s)
  + 0.20 * active_pct_s
  + 0.15 * norm(current_active_duration_s)
  + 0.15 * norm(human_operation_time_s)
  + 0.10 * downstream_starved_ratio_s
  + 0.10 * upstream_blocked_ratio_s
  + 0.05 * human_slow_operation_flag_s
```

### 9.3 窗口级标签

每个窗口选择 score 最高且超过阈值的节点：

```text
bottleneck_node_t = argmax_s bottleneck_score_s(t)
```

如果：

```text
bottleneck_score_s(t) > threshold
```

则该窗口标记为：

```text
will_bottleneck_current = 1
current_bottleneck_node = bottleneck_node_t
current_bottleneck_type = resource_type(bottleneck_node_t)
```

### 9.4 连续窗口合并成 bottleneck_event

如果连续多个窗口满足：

```text
score > threshold
and bottleneck_node 相同或高度相关
and duration > min_duration
```

则合并为事件：

```text
bottleneck_event = {
  event_id,
  episode_id,
  node,
  resource_type,
  start_time,
  end_time,
  duration,
  bottleneck_score_mean,
  bottleneck_score_max,
  affected_nodes,
  root_cause_reason,
  severity_score
}
```

示例：

```text
E001:
  node = S5
  type = machine
  start_time = 100s
  end_time = 140s
  duration = 40s
  root_cause_reason = machine_assignment_imbalance
```

### 9.5 预测标签生成

对于每个输入窗口结束时间 `t`，预测未来 `H` 秒：

```text
[t, t + H]
```

如果未来 horizon 内存在 bottleneck_event，则生成：

| 标签 | 含义 |
|---|---|
| `will_bottleneck` | 未来 horizon 内是否存在瓶颈事件 |
| `future_bottleneck_object_id` | 未来第一个或最主要瓶颈事件的节点 |
| `future_bottleneck_type` | machine / human / transport / buffer / material / route |
| `time_to_start` | `event.start_time - t` |
| `duration` | `event.end_time - event.start_time` |
| `severity_score` | 由 duration、throughput_loss、affected_nodes 综合得到 |
| `root_cause_reason` | 环境扰动或 agent 决策导致的原因 |

### 9.6 root cause 判断

root cause 可以分为两类：

```text
environment_root_cause
agent_decision_or_mask_root_cause
```

环境型 root cause：

| root cause | 来源 |
|---|---|
| `machine_failure` | 后续扰动模块 + `disturbance_log.csv`；当前可先用 machine `STOP/invalid` 近似 |
| `material_shortage` | `material_inventory_log.csv` |
| `route_blockage` | route delay + disturbance |
| `human_unavailable` | human 状态日志 |
| `buffer_capacity_limit` | buffer FULL / NEAR_FULL 状态 |

agent 决策型 root cause：

| root cause | 判定依据 |
|---|---|
| `product_release_pressure` | Agent A/B 使 `producing` 长期接近并行生产上限 |
| `task_selection_imbalance` | Agent C 选择的 task 长期集中在某一类机器或物流任务 |
| `robot_allocation_delay` | 物流任务请求到 robot/gantry 实际开始时间过长 |
| `gantry_wait_delay` | `delay_reason=finding_free_gantry` 或 gantry 长期忙碌 |
| `human_allocation_delay` | 关键 task 长时间无法获得 human，或 human self mask 长期全 0 |
| `mask_constraint_blocking` | 某类 task 长期因 material/machine/human/robot mask 不满足而不可选 |

判断逻辑：

```text
if disturbance overlaps with bottleneck event:
    root_cause = disturbance_type
else if agent action / mask / task_record pattern indicates abnormal scheduling:
    root_cause = agent_decision_or_mask_root_cause
else:
    root_cause = metric_based_rule_result
```

---

## 10. 图结构设计

### 10.1 节点定义

节点包括：

```text
machine node
human node
robot / transport_robot node
buffer node
material node
route node
```

第一版可以先用：

```text
machine
human
robot / AGV
buffer
material
```

route 可以作为 edge feature，后续再升级为 route node。

### 10.2 边定义

| 边类型 | 含义 | 示例 |
|---|---|---|
| `process_flow_edge` | 工艺流程关系 | S1 → B1 → S2 |
| `transport_route_edge` | robot / gantry / 人工搬运路径 | Storage_01 → route → machine_ws |
| `material_supply_edge` | 物料供应关系 | material_A → S3 |
| `human_assignment_edge` | 人员到工位的动态分配 | H1 → S4 |
| `agent_decision_edge` | agent 动作形成的动态关系 | Agent C selects `pipe_cutting`; Agent D assigns `human_0` and `robot_1` |

第一版建议使用前三类：

```text
process_flow_edge
transport_route_edge
material_supply_edge
```

第二版加入：

```text
human_assignment_edge
agent_decision_edge
```

### 10.3 多源动态图邻接矩阵

最终图结构不应只使用一个固定邻接矩阵。建议构建：

```text
A_final(t) =
    α A_process
  + β A_transport
  + γ A_material
  + δ A_agent(t)
  + η A_learned(t)
```

含义：

| 图 | 含义 |
|---|---|
| `A_process` | 工艺流程图 |
| `A_transport` | human / robot / gantry 路径和物流关系图 |
| `A_material` | 物料供应关系 |
| `A_agent(t)` | 当前窗口 agent 动作形成的动态关系 |
| `A_learned(t)` | 模型从数据中学习出的隐式依赖 |

这样做的原因是：

```text
瓶颈传播不只沿着物理工艺链传播；
agent 调度动作会改变资源之间的实际依赖；
某些长距离依赖无法由人工拓扑提前定义。
```

---

## 11. 模型设计：PDFormer-inspired Dynamic Graph Transformer

### 11.1 模型总体结构

```text
Input graph sequence G_{t-T:t}
        ↓
Node / Edge / Global Feature Encoder
        ↓
Prior-guided Dynamic Graph Construction
        ↓
Delay-aware Dynamic Spatio-Temporal Transformer Encoder
        ↓
STGNPP-style Multi-task Event Head
        ↓
Future bottleneck event prediction
```

### 11.2 输入编码层

#### 节点特征

每个节点输入：

```text
x_i^t = [
  queue_length,
  avg_waiting_time,
  active_pct,
  current_active_duration,
  blocked_ratio,
  starved_ratio,
  transport_waiting_time,
  material_shortage_flag,
  occupancy_ratio,
  output_rate,
  human_motion_speed,
  human_idle_duration,
  agv_stop_ratio,
  agent_action_count,
  resource_type_embedding
]
```

不同节点不适用的字段填 0，并使用 `resource_type_embedding` 表明节点类型。

#### 边特征

```text
edge_type
distance
expected_transport_time
actual_transport_time
route_delay
edge_occupancy
flow_count
blocked_flag
```

#### 全局特征

```text
total_WIP
throughput_rolling
product_release_pressure
product_mix
product_selection_rate
global_agent_reward
disturbance_flag
```

注意：

```text
disturbance_flag 只能使用当前或历史已发生扰动，不能使用未来扰动信息，避免数据泄漏。
```

### 11.3 动态图构建模块

动态图构建模块用于融合先验图和学习图：

```text
A_static = combine(A_process, A_transport, A_material)
A_dynamic(t) = build_from_agent_actions_or_task_records(agent_action_log_or_job_trace, t)
A_learned(t) = learn_from_node_embeddings(X_t)
A_final(t) = αA_static + βA_dynamic(t) + γA_learned(t)
```

其中：

- `A_static` 描述固定制造拓扑；
- `A_dynamic(t)` 描述 agent 当前调度、task_record 和资源分配造成的动态资源关系；
- `A_learned(t)` 描述模型自动发现的隐式依赖。

### 11.4 延迟感知时空 Transformer 编码器

借鉴 PDFormer，模型需要捕捉三类依赖。

#### 11.4.1 Short-range physical dependency

捕捉相邻资源之间的局部传播：

```text
S1 → B1 → S2
```

适合使用 `A_process` 和 short-range graph mask。

#### 11.4.2 Long-range bottleneck dependency

捕捉远距离依赖：

```text
产品批次并行推进过快 → 多个下游工位拥堵
物流/gantry/robot 延迟 → 多个 station starved
物料短缺 → 多个 machine 停等
```

适合使用 attention 或 long-range graph mask。

#### 11.4.3 Delay-aware propagation

显式建模资源影响的时间延迟：

```text
h_i(t) affects h_j(t + Δt)
```

例如：

```text
S2 active_pct 异常
    ↓ 30s 后
B2 occupancy 上升
    ↓ 60s 后
S3 starved
```

制造场景下，delay 可以来自：

```text
工序加工时间
buffer 等待时间
robot / gantry 运输时间
人工操作时间
物料补给延迟
```

### 11.5 输出表示

Transformer 编码器输出：

```text
H_t = [h_1^t, h_2^t, ..., h_N^t]
```

其中 `h_i^t` 表示节点 i 在当前时刻的时空表示。

再通过 pooling 得到 graph-level 表示：

```text
h_graph^t = Pool(H_t, global_features_t)
```

---

## 12. STGNPP-style Event Head

第一版采用简化多任务事件预测头。

### 12.1 输出任务

| Head | 输入 | 输出 | 任务类型 |
|---|---|---|---|
| `Occurrence Head` | `h_graph^t` | `will_bottleneck` | 二分类 |
| `Location Head` | `H_t` | `P(node_i is bottleneck)` | 节点分类 |
| `Type Head` | `h_graph^t + h_node` | `bottleneck_type` | 多分类 |
| `Time Head` | `h_graph^t` | `time_to_start` | 回归 |
| `Duration Head` | `h_graph^t` | `duration` | 回归 |
| `Severity Head` | `h_graph^t` | `severity_score` | 回归 |

### 12.2 损失函数

```text
L =
  λ1 * BCE(will_bottleneck)
+ λ2 * CE(bottleneck_node)
+ λ3 * CE(bottleneck_type)
+ λ4 * MAE(time_to_start)
+ λ5 * MAE(duration)
+ λ6 * MSE(severity_score)
```

对于没有未来瓶颈的窗口：

```text
只计算 will_bottleneck loss；
location / type / time / duration / severity 可以 mask 掉。
```

### 12.3 后续升级方向

后续可以从简化 event head 升级为 STGNPP 风格的神经点过程：

```text
history representation
    ↓
event intensity function λ(t)
    ↓
next bottleneck event time distribution
    ↓
duration distribution
```

适合研究更精细的连续时间瓶颈事件预测。

---

## 13. 训练与测试划分

### 13.1 避免随机窗口划分

不建议随机切分窗口，因为同一 episode 中相邻窗口高度相关，随机划分会造成数据泄漏。

### 13.2 按 episode 划分

```text
Train: Episode 1–70
Validation: Episode 71–85
Test: Episode 86–100
```

### 13.3 按 scenario 划分

```text
Train:
普通订单流 + 轻微扰动 + 常规 agent policy

Validation:
中等订单流 + 组合扰动

Test:
高峰订单流 + 未见过的扰动组合 + 新 agent policy
```

### 13.4 按 agent policy 划分

可设计：

```text
rule_based_policy
random_policy
heuristic_policy
RL_policy_v1
RL_policy_v2
```

训练集使用部分策略，测试集使用未见过策略，以验证模型是否能够跨调度策略泛化。

---

## 14. 实验设计

### 14.1 第一阶段：使用当前 rule-based agent 跑通闭环

当前代码已经有 rule-based 四层 agent，第一阶段不需要重新定义 order/machine/transport/human agent，而是直接使用：

```text
Agent A:
ProductWaterPipe 产品排序 / 释放 next_product

Agent B:
选择 producing 中的某个产品批次或新开一个 next_product

Agent C:
选择下一道合法 task（物流 task 或加工 task）

Agent D:
为 task 分配 human，并在物流 task 中分配 robot
```

目标是跑通：

```text
Agent A/B/C/D 调度 → TaskManager 生成 task_record → 资源 manager 推进仿真
→ BottleneckDataCollector 采集 raw logs → 离线窗口聚合 → 标签生成 → GNN 训练
```

### 14.2 第二阶段：加入随机性与扰动

加入：

```text
调整 `CfgProductOrder` 数量
扩展多产品类型 / 多产品 mix
机器处理时间波动
robot / gantry / route 延迟
人员效率波动
机器故障
物料短缺
storage / buffer 容量变化
```

目标是生成更丰富的瓶颈场景。

### 14.3 第三阶段：使用当前 MARL 入口或扩展策略

当流程稳定后，可使用当前 `marl` 入口中的 masked DQN 四层 agent，或扩展更多启发式/随机策略，比较不同策略下瓶颈模式：

```text
rule_based 数据
marl masked-DQN 数据
随机 mask 内选择策略数据
新的启发式策略数据
```

目标是验证模型是否能够学习由不同调度策略导致的瓶颈规律。

---

## 15. Baseline 设计

### 15.1 非图模型 baseline

| 模型 | 用途 |
|---|---|
| Logistic Regression / XGBoost | 检查手工特征是否有预测力 |
| LSTM / GRU | 时间序列 baseline |
| TCN | 长时间依赖 baseline |

### 15.2 时空图模型 baseline

| 模型 | 用途 |
|---|---|
| STGCN | 基础时空图 baseline |
| DCRNN | 有向传播 baseline |
| Graph WaveNet | adaptive graph baseline |
| MTGNN | multivariate graph learning baseline |
| BSTAN-style GAT-GRU | 制造瓶颈预测 baseline |
| STAEformer / PDFormer | Transformer-based STG baseline |

### 15.3 本方案主模型

```text
Prior-guided Delay-aware Dynamic Spatio-Temporal Graph Transformer
+ STGNPP-style Event Head
```

---

## 16. 消融实验设计

| 消融版本 | 目的 |
|---|---|
| 去掉 `A_agent(t)` | 验证 agent action 是否有用 |
| 去掉 `A_learned(t)` | 验证隐式动态图是否有用 |
| 只用 physical graph | 验证固定工艺图是否不够 |
| 去掉 delay-aware module | 验证瓶颈传播延迟建模是否有用 |
| 去掉 vision-derived features | 验证多模态视觉状态是否有用 |
| 去掉 agent action features | 验证多智能体调度动作是否帮助预测 |
| 只预测 binary bottleneck | 验证多任务 event prediction 是否更有价值 |
| 不使用 root cause 相关特征 | 验证模型是否依赖显式异常标记 |
| 不同 window_size / horizon | 验证时间尺度敏感性 |

---

## 17. 评估指标

### 17.1 是否发生瓶颈

```text
Accuracy
Precision
Recall
F1-score
ROC-AUC
PR-AUC
```

由于瓶颈事件可能稀疏，PR-AUC 比 ROC-AUC 更重要。

### 17.2 瓶颈位置

```text
Top-1 Accuracy
Top-k Accuracy
Mean Reciprocal Rank (MRR)
```

### 17.3 瓶颈类型

```text
Macro-F1
Weighted-F1
Confusion Matrix
```

### 17.4 时间预测

```text
MAE(time_to_start)
RMSE(time_to_start)
MAE(duration)
RMSE(duration)
SMAPE(duration)
```

### 17.5 事件级评估

```text
Event IoU
Event-level Precision
Event-level Recall
Event-level F1
```

其中 Event IoU 可定义为预测事件时间段与真实事件时间段的重叠比例：

```text
IoU = intersection(pred_event, true_event) / union(pred_event, true_event)
```

### 17.6 系统影响指标

```text
throughput_loss prediction error
severity ranking accuracy
affected_nodes prediction accuracy
```

---

## 18. 预期贡献点

### Contribution 1：多智能体仿真驱动的制造瓶颈数据生成机制

本方案基于当前代码中 A 产品排序、B 产品选择、C 工序任务规划、D 人机/机器人分配的四层 agent，使瓶颈从多 agent 决策、mask 约束、task_record 推进、资源竞争和环境扰动中自然产生，而不是完全依赖人工设定单一故障场景。

### Contribution 2：融合调度动作的多源动态制造图

本方案构建融合工艺流程、运输路径、物料供应关系和 agent 动作关系的动态制造图，能够同时表达物理拓扑、物流关系和调度行为引起的隐式依赖。

### Contribution 3：延迟感知动态图 Transformer 瓶颈预测模型

本方案提出 PDFormer-inspired delay-aware dynamic spatio-temporal graph Transformer，用于捕捉制造系统中瓶颈从局部异常到全局影响的时间延迟传播过程。

### Contribution 4：事件级瓶颈预测任务建模

本方案借鉴 STGNPP 的事件预测思想，将瓶颈预测建模为多任务事件预测问题，同时预测瓶颈是否发生、位置、类型、开始时间、持续时间和严重程度。

### Contribution 5：多模态视觉状态增强瓶颈预测

本方案将当前 perception 模块可采集或推断的 human subtask、subtask_done、area_id 和相机观测作为 human 节点增强特征；后续再扩展人体动作、robot 运动状态和 buffer 视觉占用等更通用的多模态观测。

---

## 19. 第一版最小可实现方案

为了避免第一版过于复杂，建议最小可实现版本如下。

### 19.1 数据生成

```text
当前 HC Factory Isaac Sim 环境（HRTPaHC-v1）
当前 4 层 rule-based agent
ProductWaterPipe 默认订单或调整后的固定订单
可选扰动：机器/gantry/robot 延迟、人员变慢、物料短缺、storage 容量变化
```

### 19.2 数据采集

必须采集：

```text
episode_config.csv
resource_event_log.jsonl
job_trace.csv
buffer_event_log.csv
route_transport_task.csv
material_inventory_log.csv
disturbance_log.csv
run_manifest.json
```

离线生成：

```text
agent_action_derived.csv 或后续 agent_action_log.csv
window_feature_table.csv
bottleneck_label.csv
bottleneck_event.csv
```

可选采集：

```text
perception_dataset/meta.jsonl
perception_dataset/cameras/
```

### 19.3 标签生成

第一版使用：

```text
bottleneck_score + threshold + min_duration
```

生成：

```text
will_bottleneck
future_bottleneck_object_id
future_bottleneck_type
time_to_start
duration
```

### 19.4 模型

第一版模型路线：

```text
STGCN / BSTAN-style GAT-GRU baseline
        ↓
Graph WaveNet / MTGNN baseline
        ↓
PDFormer-inspired Dynamic Graph Transformer
        ↓
Multi-task Event Head
```

### 19.5 实验

至少做：

```text
1. 和 LSTM / TCN / STGCN / BSTAN / MTGNN 对比
2. 消融 agent action features
3. 消融 learned graph
4. 消融 delay-aware module
5. 消融 vision-derived features
```

---

## 20. 后续扩展方向

### 20.1 从 rule-based agent 升级到 RL / MARL agent

后续可以将四个 rule-based agent 替换为强化学习 agent，使系统产生更复杂、更真实的调度行为。

### 20.2 从窗口预测升级为连续时间事件预测

后续可以完整引入 STGNPP / neural point process，直接建模瓶颈事件的连续时间发生强度。

### 20.3 从固定窗口聚合升级为事件驱动动态图

后续可以使用 TGN、t-PatchGNN 等方法，直接处理异步事件日志，而不是完全依赖窗口聚合。

### 20.4 从瓶颈预测升级为瓶颈缓解

预测瓶颈后，可以将预测结果反馈给调度 agent，实现：

```text
预测未来瓶颈
    ↓
提前调整产品释放、task 选择、human/robot 分配和物流节奏
    ↓
减少瓶颈持续时间和吞吐损失
```

---

## 21. 代码对齐后的后续补充清单

以下内容在研究方向上合理，但当前代码尚未完整支持。它们不建议作为第一版最小可实现方案的硬依赖，可以放入后续开发计划。

| 待补充项 | 当前状态 | 建议处理 |
|---|---|---|
| 独立订单到达流 | 当前默认使用 `CfgProductOrder` 固定订单，Agent A/B 控制产品进入生产 | 第一版用固定订单和并行生产压力；后续再加入随机到达、多产品 mix、due date |
| 独立 machine / transport / human agent | 当前没有这三个平行 agent，机器/吊车/路线由 `TaskManager` 和 manager 规则决定 | 第一版沿用 A/B/C/D；后续若要更细调度，可拆分 Agent C/D 或新增资源级 agent |
| 原始 `agent_action_log.csv` | 当前 collector 未直接记录每步 one-hot action | 第一版从 `job_trace.csv`、`resource_event_log.jsonl`、`route_transport_task.csv` 派生；后续新增 action logger |
| 窗口聚合和瓶颈标签脚本 | 当前未看到已实现脚本 | 第一版需要新增离线数据处理模块，生成 `window_feature_table.csv`、`bottleneck_label.csv`、`bottleneck_event.csv` |
| 通用视觉状态日志 | 当前 perception 聚焦 human subtask / done，不是 bbox/pose/AGV/buffer 通用视觉识别 | 第一版把 perception 作为 human 节点增强；后续再扩展通用视觉 pipeline |
| 故障、维修、充电、换型状态 | 当前 resource 状态未原生输出 `DOWN/REPAIR/CHARGING/SETUP` | 第一版用已有 `IDLE/PROCESSING/WAITING/BLOCKED/STARVED/STOP`；后续新增扰动与维修状态机 |
| 路径拥堵显式建模 | 当前 route manager 生成路线和位姿，但没有专门 route occupancy log | 第一版用 `route_transport_task.csv` 的运输时长和 delay 近似；后续记录 route occupancy |
| 瓶颈缓解闭环 | 当前模型预测和反馈调度尚未实现 | 第一版只做预测；后续把预测结果反馈给 Agent C/D 或 mask/rule |

---

## 22. 推荐论文引用清单

### 制造瓶颈方向

1. **Predicting future production system bottlenecks with a graph neural network approach**  
   Journal of Manufacturing Systems, 2023.  
   用途：制造瓶颈预测直接相关论文，支持 BSTAN baseline、station-buffer-material flow graph、blockage/starvation 指标。

2. **Digital Twin-based bottleneck prediction for improved production control**  
   Computers & Industrial Engineering, 2024.  
   用途：支持数字孪生 / 仿真环境用于未来瓶颈预测和生产控制。

3. **A dual spatial temporal neural network for bottleneck prediction in manufacturing systems**  
   Applied Soft Computing, 2025.  
   用途：支持近年制造瓶颈预测中的 dual spatial-temporal modeling，以及 starvation / blockage 时间预测。

### 时空图主模型方向

4. **PDFormer: Propagation Delay-Aware Dynamic Long-Range Transformer for Traffic Flow Prediction**  
   AAAI, 2023.  
   用途：主模型核心支撑，支持 dynamic dependency、long-range dependency、propagation delay-aware modeling。

5. **Spatio-Temporal Graph Neural Point Process for Traffic Congestion Event Prediction**  
   AAAI, 2023.  
   用途：事件预测头支撑，支持 occurrence time、duration、event-level prediction。

6. **Graph WaveNet for Deep Spatial-Temporal Graph Modeling**  
   IJCAI, 2019.  
   用途：adaptive graph learning baseline。

7. **Connecting the Dots: Multivariate Time Series Forecasting with Graph Neural Networks / MTGNN**  
   KDD, 2020.  
   用途：multivariate graph learning baseline。

8. **Diffusion Convolutional Recurrent Neural Network: Data-Driven Traffic Forecasting / DCRNN**  
   ICLR, 2018.  
   用途：有向传播 / diffusion convolution baseline。

9. **Spatio-Temporal Graph Convolutional Networks: A Deep Learning Framework for Traffic Forecasting / STGCN**  
   IJCAI, 2018.  
   用途：经典 STGNN baseline。

### 后续扩展方向

10. **Temporal Graph Networks for Deep Learning on Dynamic Graphs**  
    用途：未来直接建模 agent action event 的动态图方法。

11. **t-PatchGNN: Time Patch Graph Neural Network for Irregular Multivariate Time Series**  
    ICML, 2024.  
    用途：未来处理异步原始日志，而不是窗口聚合。

12. **STG-Mamba: Spatial-Temporal Graph Learning via Selective State Space Model**  
    2024.  
    用途：未来长序列、高效时空图建模 advanced baseline。

---

## 23. 最终总结

本方案将动态制造瓶颈预测建模为一个由多智能体调度仿真驱动的时空图事件预测问题。首先，Isaac Sim 中的 HC Factory 生产系统由 A 产品排序、B 产品选择、C 工序任务规划、D 人机/机器人分配四层 agent 串行调度运行，瓶颈在产品并行推进、物流/加工任务切换、human/robot/gantry/machine 资源竞争、mask 约束和环境扰动中自然产生。其次，系统已经能够记录资源状态、工件流转、buffer/storage 占用、运输任务和物料库存；agent 动作表、窗口特征表和瓶颈标签表作为第一版离线派生模块补齐。然后，基于 bottleneck_score 和连续窗口规则生成 bottleneck_event 标签。最后，模型借鉴 PDFormer 的传播延迟感知动态图 Transformer 思想，捕捉瓶颈在制造资源之间的动态传播；同时借鉴 STGNPP 的事件预测思想，输出未来瓶颈事件的发生、位置、类型、开始时间、持续时间和严重程度。

核心路线：

```text
Multi-agent Scheduling Simulation
        ↓
Dynamic Bottleneck Data Generation
        ↓
Multi-source Manufacturing Graph Construction
        ↓
Delay-aware Dynamic Spatio-Temporal Graph Transformer
        ↓
Bottleneck Event Prediction
```

这个方案的优势在于：

```text
1. 数据生成机制更自然：瓶颈来自多 agent 调度和资源竞争；
2. 图建模更完整：同时包含工艺流、物流、物料流和调度动作；
3. 模型机制更贴合瓶颈传播：显式考虑动态依赖和传播延迟；
4. 预测目标更完整：从 binary prediction 升级为 event-level prediction；
5. 可扩展性强：后续可以接入 RL 调度优化、连续时间事件预测和多模态视觉增强。
```
