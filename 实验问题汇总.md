# 实验问题汇总

> 日期：2026-09-07  
> 数据：`raw_data/dense_i1`，episode 划分 seed=42  
> 正式模型：`dense_i1_a1_prefix8`（Test **P=0.852 / R=0.856 / F1=0.854**，`up_r=0.455`，`on_r=0.911`）  
> 主指标：现行 A.1 `report_*`（工位对且 `|Δstart|≤3`）  
> 标签：尚未发生 ≥8 分钟且 `start≤2`；进行中剩余 ≥1 分钟  

本文汇总近期实验相关问答：消融为何打平、有效对照怎么设计、prefix 是什么、进行中/即将发生怎么同时预测、「≥8 且 start≤2」是不是 10 分钟、剩余时间何时估、两类原因为何空白、文献在评什么。数字以同一现行标签为准。

---

## 0. 目录

1. [为什么聚类 / 监督 / STGNPP 几乎一样？](#1-为什么聚类--监督--stgnpp-几乎一样)
2. [怎样对照才能显出 prefix8 好？](#2-怎样对照才能显出-prefix8-好同一标签消融)
3. [这些设置为什么能抬分？](#3-这些设置为什么能抬分)
4. [prefix 到底是什么？](#4-prefix-到底是什么)
5. [「关 prefix」和「旧 f180 复评」差在哪？](#5-关-prefix和旧-f180-复评差在哪)
6. [进行中和即将发生是同时预测吗？](#6-进行中和即将发生是同时预测吗)
7. [「≥8 分钟且 start≤2」是不是 10 分钟？](#7-8-分钟且-start2-是不是-10-分钟)
8. [订单剩余时间：何时估、还能不能再降？](#8-订单剩余时间何时估还能不能再降)
9. [瓶颈原因为何只保留四类？](#9-瓶颈原因为何只保留四类)
10. [文献里的预测时域 / 输出 / RMSE 在评什么？](#10-文献里的预测时域--输出--rmse-在评什么)
11. [论文 / 汇报怎么写](#11-论文--汇报怎么写)
12. [更远 upcoming / 启发式×聚类（过夜实验）](#12-更远-upcoming--启发式聚类过夜实验)
  - [12.1.1 开聚类 vs 无聚类指标对照](#1211-开聚类-vs-无聚类指标对照start2--8同设定)  
    - [12.1.2 upcoming 导向聚类 cluster_up](#1212-重设计upcoming-导向聚类-cluster_up)
13. [产物路径](#13-产物路径)
14. [论文可写的网络/任务创新点（相对文献与 PDFormer）](#14-论文可写的网络任务创新点相对文献与-pdformer)

---



## 1. 为什么聚类 / 监督 / STGNPP 几乎一样？

对照要拉开差距，必须：**同一套** `report_`* **标签**，且对照臂缺了正式模型真正靠的东西。上一轮三组都不满足第二条。


| 对照        | 实际动了什么                                                    | 为何打平                                                                                                           |
| --------- | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| 聚类 vs 不聚类 | 关规则簇嵌入 + 对比学习；离线 k-means 本就没进损失/解码                        | 短训 8ep：0.854 vs 0.852；同设定 100ep 见 [§12.1.1](#1211-开聚类-vs-无聚类指标对照start2--8同设定)（F1 0.861 vs 0.871，开簇略抬 upcoming） |
| 监督 vs 无监督 | `dense_i1` 上占用 y Jaccard=1.0；监督从 prefix8 微调，事件头/prefix 全留 | 只是辅损失从「重构未来 X」换成拟合很差的 `bottleneck_score` → F1 差 0.004                                                          |
| 加 STGNPP  | 小权重辅头叠在 prefix8 上，主头未换                                    | `up_r` 仍 0.455，等于白加 → F1 0.849                                                                                 |


结论：比的是 **prefix8 ± 没用的插件**。插件无效，分数就该一样。这能证明「这些模块可写进附录、不进贡献」，不能证明「正式模型比别的方法好」。

prefix8 真正得分靠的是：现行标签（进行中剩余 ≥1）、prefix 解码、抬升跟着 τ、P≥0.80 再取 F1、事件过采样——不是聚类/监督分数头/STGNPP。

---



## 2. 怎样对照才能显出 prefix8 好？（同一标签消融）

主表只动「真正得分的设置」，全部在 **现行标签** 上评；不要拿旧口径的 0.743 直接比 0.854。

### 2.1 Test 总表（同一分母）

`n_true=940`（upcoming 112 + ongoing 828）。除注明外：prefix8 权重、只改解码；旧档 = 旧权重 + 现行标签 + 关未训过的 prefix。


| 档               | 改了什么                        | P         | R         | F1        | `up_r`    | `on_r`    | 能不能当 A.1      |
| --------------- | --------------------------- | --------- | --------- | --------- | --------- | --------- | ------------- |
| **prefix8（正式）** | 事件头 + prefix + 占用并集         | 0.852     | **0.856** | **0.854** | **0.455** | 0.911     | **是**         |
| 关 prefix        | `event_decode_prefix=false` | 0.827     | 0.852     | 0.839     | 0.464     | 0.905     | 是，但更差         |
| 关占用并集           | `event_union_*=false`       | **0.874** | 0.852     | 0.863     | 0.429     | 0.909     | 是；upcoming 更差 |
| 关强制进行中          | `force_ongoing_will=false`  | 0.852     | 0.856     | 0.854     | 0.455     | 0.911     | 与正式相同         |
| Persistence     | 只报上一分钟已热                    | 0.912     | 0.818     | 0.863     | **0.000** | **0.929** | **否**         |
| 旧 `f180`        | 现行标签复评，无 prefix             | 0.837     | 0.562     | 0.672     | 0.393     | 0.585     | 是，明显弱         |
| 旧 `near5`       | 同上                          | 0.825     | 0.579     | 0.680     | 0.402     | 0.603     | 是，明显弱         |
| 旧 `v3ft`        | 同上                          | 0.819     | 0.386     | 0.525     | 0.348     | 0.391     | 是，明显弱         |


附录（加插件，**不进主表**）：nocluster F1=0.852，监督=0.850，STGNPP=0.849。

---



## 3. 这些设置为什么能抬分？



### 3.1 现行进行中标签（剩余 ≥1）

旧档把「窗内剩余也要 ≥5 / ≥8」当正例门槛。已堵 10 分钟、预报窗还剩 3 分钟：截图要求 `start=0`、报剩余 3；旧做法丢掉。`last_hot` 像闪烁，模型不敢报进行中。

现行：上一分钟已热且预报窗还接着热 → 剩余 1 分钟也算（val 进行中 396→624）。同一 `report_*` 下，旧 `f180`/`near5` 的 `on_r` 约 0.58–0.60，prefix8 是 **0.911**。标签对齐截图后，prefix 才有东西可学。

### 3.2 Prefix 解码

同一 ckpt 关 prefix：F1 **0.854→0.839**，P **0.852→0.827**。不能「上一分钟热就全报」：有的下一分钟就冷。只有估计还能连续热 ≥1.5 分钟才抬升。`force_ongoing_will` 关掉后与正式相同——prefix 已覆盖进行中抬升。

### 3.3 占用格并集

关掉：F1 0.863、P 更高，但 `up_r` **0.455→0.429**。正式宁可少约 1 个点总 F1，也要保住尚未发生。

### 3.4 Persistence 为何 F1 更高却不算赢

只报当前热：P=0.912，`on_r=0.929`，F1=0.863，`up_r=0`（112 条即将发生全漏）。进行中占 828/940，只报当前热就能抬高 F1。A.1 要求两类都报；表里标成非法基线。

### 3.5 旧档在现行标签上为何明显差

旧档在「窗内剩余也卡 5 分钟」、无 prefix 的目标上训。换到现行分母后，短剩余进行中没学过，`on_r` 掉到 0.39–0.60。prefix8 从 f180 微调 + 现行标签 + prefix：F1 **0.672→0.854**。这才是「正式更好」的对照。

---



## 4. prefix 到底是什么？

这里的 **prefix 不是 NLP 前缀，也不是 k-means**。

含义：**从「下一分钟」起，这台工位还能连续热几分钟。**

- 真值：预报窗 `y_hot` 从第 0 格往右连续为热的长度。例如热、热、热、冷…… → prefix=3；下一分钟就冷 → 0。  
- 头：`prefix_mlp` 回归这个数。  
- 解码：须同时满足  
  1. 上一分钟已热（`last_hot`）
  2. 预测还能接着热 **≥ 1.5 分钟**

成立则抬升 `will`、`start=0`、`duration` 改成该 prefix（报**剩余**）。下一分钟就冷的不报。

运行名 **prefix8** = 用了这套 prefix 解码 + 占用/尚未发生按 **8 分钟** 卡。

---



## 5. 「关 prefix」和「旧 f180 复评」差在哪？

两组都关了 prefix 解码，差的不是「有没有这一下」，而是 **权重是否在现行标签上训过**。


|                  | 关 prefix                | 旧 f180    |
| ---------------- | ----------------------- | --------- |
| 权重               | prefix8（从 f180 用现行标签微调） | f180 原档   |
| 评测时 prefix       | 关                       | 关         |
| 训练时见过「剩余 1–7 分钟」 | **见过**                  | **没见过**   |
| Test F1          | 0.839                   | 0.672     |
| `on_r`           | **0.905**               | **0.585** |


- 关 prefix：只少解码，进行中召回仍高；F1 只掉约 0.015。  
- 旧 f180：短剩余正例从未学过，进行中召回少约 32 个点。

P 上 f180 略高（0.837 vs 0.827）是因为报得更少、更保守。真正拉开的是召回。  
**prefix 解码**解释「prefix8 → 关 prefix」；**标签对齐 + 微调**解释「关 prefix → f180」。

---



## 6. 进行中和即将发生是同时预测吗？

**是同时做的。** 每个监督工位（机台 / 工作台 / 龙门 / AGV）在一次前向里只输出 **一条** 未来 15 分钟事件：`(will_block, start_min, duration_min)`。不是两套模型轮流跑。

用「上一分钟是否已热」分支规则：


|      | 进行中（ongoing）                 | 即将发生（upcoming）                |
| ---- | ---------------------------- | ----------------------------- |
| 状态   | 上一分钟已经在堵                     | 上一分钟还没堵                       |
| 正例   | 预报窗从第 0 格接着热；剩余 ≥1 分钟也算      | 新堵段 ≥8 分钟，且 `start ≤ 2`       |
| 输出   | `start=0`，`duration` = 还要堵多久 | `start>0`，`duration` = 整段会堵多久 |
| 解码重点 | prefix：还能连续热几分钟；≥1.5 才报      | 近地平线；`start>2` 丢掉             |


网络：`will` 拆成「继续堵 / 即将开始」两头；`start`/`duration`/`prefix` 共用。已热工位解码时强制 `start=0`。评测统一 `report_*`，再拆 `on_r` / `up_r`。test 约 828 进行中 + 112 即将发生。

---



## 7. 「≥8 分钟且 start≤2」是不是 10 分钟？

**不是。8 和 2 不能相加。**

两条独立条件（AND）：

1. **堵段够长**：这段堵至少持续 ≥ **8** 分钟
2. **开始够近**：从现在起，堵在 ≤ **2** 分钟内开始

预报地平线仍是 **15 分钟**。upcoming 正例只收「近处开始、且够长」的一类。

例子：第 1 分钟开始、连堵 10 分钟 → 合格；第 5 分钟才开始、哪怕连堵 20 分钟 → 不计正例（太远）。

---



## 8. 订单剩余时间：何时估、还能不能再降？



### 8.1 什么时候做？

**不是整局一开始估一次。** 和 A.1 一样，在滑动窗上 **每个时刻** 估：看过去 30 分钟 + 当前 `jobs_remaining/jobs_total`，输出「从现在起还要多少分钟整局做完」（`remain_len`，单位 60s 窗）。单做完（`jobs≤0`）不再当有效样本。评测 `remain_len_mae` 是这些中间窗上的平均误差。

### 8.2 还能不能再降？


| 档                                               | Test `remain_len_mae` | Test report F1 |
| ----------------------------------------------- | --------------------- | -------------- |
| prefix8                                         | 10.5                  | **0.854**      |
| 整网微调 remain（`w_remain_len=2`）                   | 9.9                   | 0.843（A.1 掉了）  |
| **只训 remain 头**（`w_remain_len=4` + `rate×jobs`） | **9.8**               | **0.854**      |


可以再降一点，但空间不大。冻住事件头后从 10.5→9.8；再加权重或解冻编码器会抢 A.1。要明显更准，应改成按工位/在制品估，不要继续把 `w_remain_len` 加到事件头上。

ckpt：`dense_i1_a1_prefix8_remain_frozen/`。正式 A.1 仍用 prefix8。

---



## 9. 瓶颈原因为何只保留四类？

监督的是 **整窗一个** `root_cause_reason`，只在热窗、只看当时瓶颈工位，规则优先级固定：

1. 缺料 → 2. 延误 → 3. **排队** → 4. 下游堵 → 5. 饥饿 → 6. 高负荷 → 否则 `score_threshold`（不进 loss）

`dense_i1` 全包窗口标签：


| 类                    | 窗口数    | 占比        |
| -------------------- | ------ | --------- |
| 未标注（非热窗）             | 13629  | 37.2%     |
| 到货延误                 | 7964   | 21.7%     |
| 排队                   | 7877   | 21.5%     |
| 上游饿                  | 5492   | 15.0%     |
| `score_threshold`    | 914    | 2.5%      |
| 缺料                   | 687    | 1.9%      |
| `blocked_downstream` | **68** | **0.19%** |
| `high_utilization`   | **0**  | **0**     |


训练滑窗后 `blocked_downstream` 几乎为空且 **test 为 0**，`high_utilization` 完全无样本，因此二者不满足可训练、可评估的最低支持度。

不是头不会预测，是 **标签几乎不产生这两类**：

- **下游堵**：门槛高；真堵工位常先被判成排队。  
- **高负荷**：排在最后；热窗瓶颈几乎总会先撞上延误/排队/饥饿；忙碌但前面全不中的写成 `score_threshold`。按工位分钟的规则簇里高负荷约 8.9%，但原因头不吃那个信号。

当前实验将这两类加入 loss ignore 集合，并限制推理输出只在以下四类中归一化和取 argmax：

1. `transport_delay`（到货/运输延误）
2. `material_shortage`（缺料）
3. `starved_upstream`（上游饥饿）
4. `queue_buildup`（排队积压）

旧 NPZ 和 checkpoint 仍保留原始类别编号以兼容权重，但训练损失、macro recall、逐类报告及在线推理均只使用上述四类。

---



## 10. 文献里的预测时域 / 输出 / RMSE 在评什么？

依据 `田宇潇_文献调研.xlsx`（瓶颈预测相关 1–10；后半多模态与 A.1 不对齐，略）。

### 10.1 和你是不是一样报「未来 15 分钟事件」？

**不是。** 文献很少用固定 15 分钟 + 每工位 `(will, start, duration)`。


| #    | 工作        | 时间级（大致）            | 预测内容             | 输出形式       |
| ---- | --------- | ------------------ | ---------------- | ---------- |
| 1    | BTGCN     | 12 h / 24 h        | 任务谁成瓶颈           | 任务吞吐序列回归   |
| 2    | CDT+TFT   | 采样 500 ms；预报长度自定   | 未来瓶颈 + 解释        | 序列 + XAI   |
| 3    | DSTNN     | 未写死到分钟             | 加工单元级前瞻          | 图节点分类/状态   |
| 4    | 时序网络      | 滑动窗                | 漂移型瓶颈/异常         | 异常分（检测）    |
| 5    | DT+DES    | **未来 2–4 小时** 瓶颈位置 | 谁是瓶颈             | 仿真快进 + 规则  |
| 6    | K-STGNN   | 未强调 15 min         | 瓶颈概率 + 产出损失      | 多任务概率      |
| 7/10 | **BSTAN** | 班次/日志聚合            | 阻塞/饥饿时长 → TPM 定谁 | 时长回归 + 规则  |
| 8    | LSTM-DAM  | 滑动窗 + 漂移           | 动态瓶颈             | 分类/回归 + 更新 |
| 9    | TUSB-LSTM | 仿真「时段」             | 利用率意义下的瓶颈        | 系统视角身份/趋势  |


最接近基线是 **BSTAN**（工位图 + 前瞻阻塞/饥饿），但仍不是你的事件三元组。要对齐必须改到同一套 15 分钟标签和 `report_f1` 上比。

### 10.2 他们的 RMSE / MAE 在评什么？

评的是 **连续量估偏了多少**（回归），不是「事件报对没报对」。


| 工作     | 典型误差对象            |
| ------ | ----------------- |
| BTGCN  | 未来时段任务执行频率/吞吐曲线   |
| BSTAN  | 各工位未来 **阻塞、饥饿时长** |
| LSTM 类 | 利用率、队列、综合能力等连续指标  |


对照：

- **RMSE/MAE** → 这个**数**差多少  
- 你的 `report_f1` → 这条**事件**（谁、何时开始）匹配对没有

不能和 `report_f1=0.854` 直接横比。

---



## 11. 论文 / 汇报怎么写

- 主对照：prefix8 vs 关 prefix vs 旧档现行标签复评；正式 F1 最高且 `up_r≠0`。  
- Persistence 单独一行，标非法基线。  
- 聚类 / 监督 / STGNPP 放附录。  
- 不要写「比旧口径 f180 的 0.743 高 11 点」；要写「同一现行标签下，f180 复评 0.672，prefix8 是 0.854」。  
- 文献对比强调：时域多为小时～班次，输出多为身份/时长回归；你的贡献是固定 15 分钟工位事件报告 + `report_*`。

---



## 12. 更远 upcoming / 启发式×聚类（过夜实验）

正式口径仍是 `start ≤ 2`、时长 ≥ 8。过夜串行两档（各 **100 epoch**，`patience=100`，从 prefix8 微调，wandb offline 若未 login）：

### 12.1 启发式标签 × 原因对齐聚类（`heur_cluster`）

**可以结合，且不要用聚类替换占用/事件真值。** 正确接法：


| 层         | 仍用启发式                           | 聚类怎么接                                             |
| --------- | ------------------------------- | ------------------------------------------------- |
| 占用 / 事件 y | `ops_hot_mask` → will/start/dur | **不**用簇 id 当堵/不堵                                  |
| 工况表征      | —                               | `seed_cluster_ids`（与原因规则同门槛）→ `hist_cluster` 嵌入融合 |
| 辅损失       | cause CE                        | `w_cluster` 预报未来原因对齐簇；`w_contrast` 同簇拉近           |


配置：`FactoryBN_dense_prefix8_heur_cluster.json`（`w_cluster=0.35`，`w_contrast=0.20`，`fuse_hist_cluster=true`）  
目录：`dense_i1_a1_prefix8_heur_cluster_ep100/`

**结果（100 ep，与正式 prefix8 同标签分母，可横比）：**


|                    | Val best                     | Test (best ckpt)          | 正式 prefix8 Test       |
| ------------------ | ---------------------------- | ------------------------- | --------------------- |
| A.1 P / R / F1     | ep52 ≈ 0.899 / — / **0.902** | **0.871 / 0.850 / 0.861** | 0.852 / 0.856 / 0.854 |
| up_r / on_r        | —                            | 0.500 / 0.897             | 0.455 / 0.911         |
| cluster_acc (test) | —                            | 0.573                     | —                     |


相对正式：Test F1 **+0.007**（P↑、R↓）；upcoming recall 略升、ongoing 略降。不替换正式模型，仅作辅损/表征消融结果。

#### 12.1.1 开聚类 vs 无聚类指标对照（start≤2、≥8，同设定）

两档均从正式 `prefix8` 热启，**100 epoch / patience=100**，同一现行标签分母；唯一差别是是否开簇辅损与簇嵌入融合。


| 项         | **开聚类** `heur_cluster`                                                                             | **无聚类** `nocluster_ep100`                                                                          |
| --------- | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 配置        | `FactoryBN_dense_prefix8_heur_cluster.json`                                                        | `FactoryBN_dense_prefix8_nocluster_ep100.json`                                                     |
| 目录        | `dense_i1_a1_prefix8_heur_cluster_ep100/`                                                          | `dense_i1_a1_prefix8_nocluster_ep100/`                                                             |
| 聚类开关      | `w_cluster=0.35`，`w_contrast=0.20`，`fuse_hist_cluster=true`                                        | 三者全关（0 / 0 / false）                                                                                |
| best ckpt | ep52（Val F1≈0.902）                                                                                 | ep19（Val F1≈0.899）                                                                                 |
| wandb     | [rl8q04vu](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/rl8q04vu) | [456jis8e](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/456jis8e) |


**Test（best ckpt）主表：**


| 指标             | 开聚类       | 无聚类       | Δ（开 − 无） | 正式 prefix8（参考） |
| -------------- | --------- | --------- | -------- | -------------- |
| report P       | 0.871     | **0.904** | −0.033   | 0.852          |
| report R       | **0.850** | 0.840     | +0.010   | 0.856          |
| report F1      | 0.861     | **0.871** | −0.010   | 0.854          |
| up_r（upcoming） | **0.500** | 0.438     | +0.062   | 0.455          |
| on_r（ongoing）  | **0.897** | 0.895     | +0.002   | 0.911          |
| cause_acc      | **0.981** | 0.971     | +0.010   | 0.966          |


**结论（同标签、同训程）：**

- 主指标 F1：无聚类略高（**0.871 vs 0.861**），主要靠更高精度（P 0.904 vs 0.871）。
- 开聚类更偏召回侧：R / `up_r` 更好（尤其 upcoming +0.062），ongoing 几乎打平。
- 相对正式 prefix8：两档 Test F1 都略高；开聚类换来的是 upcoming 更稳，不是整体 F1 碾压。
- 不替换正式模型；附录可写「簇辅损对 A.1 F1 无增益，对远 upcoming 略有帮助」。

（另：早期短训 `dense_i1_a1_prefix8_nocluster/` 仅 8 ep，Test F1=0.852，与正式几乎打平；上表以同 100ep 对照为准。）

#### 12.1.2 重设计：upcoming 导向聚类 `cluster_up`

旧 heur_cluster 的簇 CE 学的是整段未来工况，**不对准冷工位 onset**；`recall_lift_threshold=0` 让簇→upcoming 解码抬升成空操作；`w_contrast` 实际是占用 SupCon，不是同簇。因此簇对 `up_r` 只有弱增益。

**提升 upcoming 的通用杠杆（与簇无关也可单用）：** 提高 `event_will_upcoming/precursor/near_start` 权重、`oversample_upcoming_windows`、略降选模 P 门控、`event_f1_beta` 偏召回。

**本档把簇真正接到 upcoming（相对 heur 的改动）：**


| 改动                                         | 作用                                                 |
| ------------------------------------------ | -------------------------------------------------- |
| `cluster_loss_cold_only` + `early_steps=5` | 簇 CE 只在冷工位、预报前 5 分钟                                |
| `cluster_loss_abnormal_boost=2.5`          | 异常工况簇（缺料/延误/排队等）加权重                                |
| `w_cluster_contrast=0.20`                  | 真正按 `hist_cluster` 做 SupCon（补文档与代码不一致）             |
| `recall_lift_threshold=0.55`               | 打开簇感知 near-miss 抬升（此前为 0）                          |
| upcoming 权重 / oversample                   | upcoming×10、precursor×10、近 onset×8；upcoming 窗过采样×6 |
| 选模                                         | P≥0.78、R≥0.75，避免锁死高 P 低 `up_r`                     |


对照臂 `cluster_up_ctrl`：**同样 upcoming 杠杆，关簇**（隔离「簇是否真有用」）。  
配置：`FactoryBN_dense_prefix8_cluster_up.json` / `_ctrl.json`  
脚本：`python -u -m factory_bn.run_overnight_sleep`（100 ep，patience=40，从 prefix8 热启）  
目录：`dense_i1_a1_prefix8_cluster_up/`、`…_ctrl/`

**结果（100 ep，同标签 start≤2、≥8）：**


|                 | **cluster_up**            | **ctrl（关簇）**          | Δ（开−关）          | 正式 prefix8            |
| --------------- | ------------------------- | --------------------- | --------------- | --------------------- |
| best            | ep53                      | ep68                  | —               | ep8                   |
| Test P / R / F1 | **0.826 / 0.866 / 0.846** | 0.811 / 0.863 / 0.836 | F1 **+0.010**   | 0.852 / 0.856 / 0.854 |
| up_r / on_r     | **0.518 / 0.913**         | 0.500 / 0.912         | up_r **+0.018** | 0.455 / 0.911         |


相对同杠杆关簇：开簇 **F1 与 up_r 都略升**（成功标准弱达成）；相对正式 prefix8：F1 略低（−0.008）、upcoming 明显更好（+0.063）。相对旧 heur_cluster（up_r=0.500）再抬一点。不替换正式模型。

#### 12.1.3 过夜队列（2026-09-09）— 已跑完

脚本：`python -u -m factory_bn.run_overnight_sleep`（四档均 exit=0）。


| #   | 档                    | 标签          | epoch | Test P/R/F1               | up_r / on_r       |
| --- | -------------------- | ----------- | ----- | ------------------------- | ----------------- |
| 1   | `cluster_up`         | start≤2、≥8  | 100   | **0.826 / 0.866 / 0.846** | **0.518 / 0.913** |
| 2   | `cluster_up_ctrl`    | 同上          | 100   | 0.811 / 0.863 / 0.836     | 0.500 / 0.912     |
| 3   | `start5_cluster40`   | start≤5、≥10 | 40    | 0.714 / 0.758 / 0.735     | 0.559 / 0.816     |
| 4   | `start5_nocluster40` | 同上          | 40    | **0.716 / 0.771 / 0.743** | 0.559 / **0.833** |


**start5 40ep 结论：** 两档 `up_r` 打平（0.559）；无聚类 F1/on_r 略好。相对此前 start5_v2（F1=0.775）本轮 P 掉到 ~0.71（召回/簇抬升过猛 + 选模 P 门控 0.76），**未超过 v2**。远口径上聚类未显示优势；正式仍用 start≤2。

#### 12.1.4 `heur_cluster` 上的 `will_15 + Min5` 分层起点（2026-09-12）

本档以 §12.1 `heur_cluster` 为母配置，保留原因对齐聚类（`w_cluster=0.35`、`w_contrast=0.20`、`fuse_hist_cluster=true`）和 prefix8 热启，重新定义输出：

- 主指标仅为逐工位二分类：未来 15 分钟内是否发生满足 Min5 的瓶颈（`will_15`）；不再要求起点命中才算主指标 TP。
- 起点条件分层为 `ongoing / 1–5 / 6–10 / 11–15`，先预测桶，再预测桶内分钟。
- 起点单独报告 `start MAE`、桶准确率、精确分钟准确率和联合 F1@1/@2/@3。
- 配置：`FactoryBN_dense_heur_will15_min5_hier.json`；入口：`python -u -m factory_bn.run_heur_will15_min5 --phase screen`。
- 数据沿用 §12.1 的 `dense_i1` 历史口径：204 条目录项，episode 划分为 140/28/36。该集合含已知重复 episode 和跨 split 重复，因此本档只作与 §12.1 的结构复现/诊断，不能作为论文无泄漏主结果。
- W&B：[3vxuu66q](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/3vxuu66q)；20 epoch，precision gate（P≥0.80、R≥0.50）未打开，最终按 ungated validation best（ep12）评测。

**Test 结果：** `will_15 P/R/F1 = 0.548 / 0.542 / 0.545`；`start MAE = 2.08 min`；起点桶准确率 `0.709`；精确分钟准确率 `0.538`（仅 upcoming 为 `0.151`）；F1@1/@2/@3 = `0.344 / 0.386 / 0.418`。按起点距离分层，upcoming start MAE 分别为：1–5 分钟 `1.75`、6–10 分钟 `3.89`、11–15 分钟 `7.98`；对应严格 report recall 为 `0.426 / 0.185 / 0.028`。

**结论：** 分层头能学到粗粒度桶（bucket accuracy 0.709），但越远起点误差明显增大，且 `will_15` P/F1 均未达到 0.80；不启动 100 epoch 长训，也不作为正式结果。当前证据表明缺口主要来自远 upcoming 的可预测信号不足，不是单纯把 MAE 容差调大可以解决。

**MAE 口径校准：** 当前 `start_mae` 不是全局所有样本平均，而是只在 who-TP（真实有事件且模型也报出该工位）上计算；FN/FP 不进入 MAE，所以不能单独代表端到端能力。训练和汇报不降低“序列开始”样本的权重：模型输入本身已有 30 分钟历史窗口，随意降权会进一步削弱 11–15 分钟预警。更合理的容错方式是同时保留无容差的 `will_15 F1`、条件 `start MAE`、分桶 MAE/桶准确率，以及联合 F1@1/@2/@3。

### 12.2 `start < 6` 且时长 ≥ 10（`start5_min10`）


| 项                                       | 正式 prefix8                    | 本档                                                                      |
| --------------------------------------- | ----------------------------- | ----------------------------------------------------------------------- |
| `event_max_start_windows`               | 2                             | **5**（start ≤ 5，即 start < 6）                                            |
| `event_min_windows` / `hot_min_windows` | 8                             | **10**                                                                  |
| 配置 / 目录                                 | `FactoryBN_dense_f1_p80.json` | `FactoryBN_dense_start5_min10.json` → `dense_i1_a1_start5_min10_ep100/` |


此前 `start4_min10`（start<5、≥10、16 epoch）Test **F1=0.780**，P 未过 0.80；换分母，不与 0.854 横比。

**结果（100 ep，换标签分母，勿与正式 0.854 横比；可与 start4_min10 粗比）：**


|                | Val best                         | Test (best ckpt)          | start4_min10 Test（参考） |
| -------------- | -------------------------------- | ------------------------- | --------------------- |
| A.1 P / R / F1 | ep57 ≈ 0.850 / 0.840 / **0.845** | **0.805 / 0.731 / 0.766** | 0.771 / 0.790 / 0.780 |
| up_r / on_r    | —                                | 0.564 / 0.779             | 0.494 / 0.859         |


相对 start4：Test P↑、R↓，F1 略低（0.766 vs 0.780）；upcoming recall 更高、ongoing 更低。更远 start 窗更难，不替换正式口径。

#### 12.2.1 召回向短微调 `start5_min10_v2`

从 ep100 best 热启；提高 ongoing/upcoming 正样本权重与 `event_f1_beta`；`patience=20`、`lr=2e-5`、`max_epoch=40`；选模门控 P≥0.78 / R≥0.75。  
配置：`FactoryBN_dense_start5_min10_v2.json` → `dense_i1_a1_start5_min10_v2/`（early stop ep33，best=ep13）


|                 | start5 ep100              | **v2**                    | start4（参考）            |
| --------------- | ------------------------- | ------------------------- | --------------------- |
| Test P / R / F1 | 0.805 / 0.731 / **0.766** | **0.800 / 0.752 / 0.775** | 0.771 / 0.790 / 0.780 |
| up_r / on_r     | 0.564 / 0.779             | 0.559 / **0.808**         | 0.494 / 0.859         |
| Val best F1     | 0.845 (ep57)              | 0.848 (ep13)              | —                     |


相对 ep100：Test F1 **+0.009**（主靠 R↑、尤其 on_r）；仍略低于 start4 的 0.780。正式口径不变。

#### 12.2.2 再冲分 `start5_min10_v3`

相对 v2 / 过夜 40ep 的诊断：主缺口仍是 **on_r**（0.808 vs start4 的 0.859）；远 start 正例难；过猛 upcoming/簇抬升会把 P 砸到 0.71。

**v3 改动：** 从 v2 best 热启；加强 ongoing（pos×8、prefix 阈 1.0、`prefix_ongoing_boost=3.5`、ongoing F1 mix）；新增 **far-start** will 权重与 start 损失加权；upcoming 过采样降到 3.5；略增正则；关簇/关 recall_lift；60 ep / patience=25。  
配置：`FactoryBN_dense_start5_min10_v3.json` → `dense_i1_a1_start5_min10_v3/`（early stop ep34，best=ep9）


|                 | start5 ep100          | v2                        | **v3**                | start4（参考）            |
| --------------- | --------------------- | ------------------------- | --------------------- | --------------------- |
| Test P / R / F1 | 0.805 / 0.731 / 0.766 | **0.800 / 0.752 / 0.775** | 0.779 / 0.763 / 0.771 | 0.771 / 0.790 / 0.780 |
| up_r / on_r     | 0.564 / 0.779         | 0.559 / 0.808             | 0.535 / **0.830**     | 0.494 / 0.859         |


相对 v2：`on_r` **+0.022**（方向对），但 `up_r`↓、P↓，Test F1 **−0.004**，仍低于 start4。ongoing 加压有效，远 upcoming 被挤掉；未达 ≥0.79。当前 start5 最优仍是 **v2（0.775）**。

脚本：`python -u -m factory_bn.run_overnight_ep100`（两档均已跑完）；v2/v3 单独训。

### 12.3 固定 Min8 的 start≤5/10/15 对照（2026-09-13）

三档均沿用 §12.2 base 配置与同一 prefix8 热启，使用 `dense_i1`、seed42、20 分钟标签观测范围，各训练 100 epoch；除 `event_max_start_windows=5/10/15` 外配置完全一致。运行脚本：`python -u -m factory_bn.run_start_horizon_min8_ep100`。

- **start≤5：** Test report P/R/F1=`0.831/0.807/0.819`，who F1=`0.820`，up_r/on_r=`0.542/0.923`，start MAE=`0.13 min`，F1@1/@2/@3=`0.801/0.814/0.819`；剩余订单时间 MAE=`10.64 min`，瓶颈持续时间 MAE=`1.93 min`，原因 accuracy/macro recall=`0.965/0.969`。[W&B](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/oat8bqew)
- **start≤10：** Test report P/R/F1=`0.745/0.758/0.751`，who F1=`0.757`，up_r/on_r=`0.550/0.922`，start MAE=`0.24 min`，F1@1/@2/@3=`0.730/0.749/0.751`；剩余订单时间 MAE=`9.61 min`，瓶颈持续时间 MAE=`1.77 min`，原因 accuracy/macro recall=`0.965/0.970`。[W&B](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/25pfzgut)
- **start≤15：** Test report P/R/F1=`0.770/0.714/0.741`，who F1=`0.743`，up_r/on_r=`0.489/0.921`，start MAE=`0.28 min`，F1@1/@2/@3=`0.706/0.735/0.741`；剩余订单时间 MAE=`10.20 min`，瓶颈持续时间 MAE=`1.88 min`，原因 accuracy/macro recall=`0.969/0.974`。[W&B](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/f8nlpnws)

**结论：** 在完全匹配设置下，起点范围扩大使正例分母包含更多远 upcoming，Test F1 从 `0.819→0.751→0.741` 下降；ongoing recall 基本不变，主要损失来自 upcoming。start≤5 同时达到 P/F1>0.80。

### 12.4 start10/15 precision-first 与进度加权剩余时间（2026-09-13）

改进档采用 start5→start10→start15 课程式热启，提高 hard-FP/负样本代价、关闭会引入额外 FP 的 occupancy union，并对远 upcoming 加权。配置为 `FactoryBN_dense_12_3_start10_min8_precision.json`、`FactoryBN_dense_12_3_start15_min8_precision.json`；20 epoch 筛选结果：

- **start≤10：** Test P/R/F1=`0.836/0.657/0.736`。P 已超过 0.80，但召回下降使 F1 低于原始 `0.751`。[W&B](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/m7xaoc3n)
- **start≤15：** Test P/R/F1=`0.803/0.645/0.715`。P 刚超过 0.80，但 F1 低于原始 `0.741`。[W&B](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/iiw5tgq2)

**结论：** precision-first 成功压低 FP，但只是沿 PR 曲线换取精度，没有改善远 upcoming 的可分性，因此不启动40 epoch长训。要让 P 和 F1 同时超过0.80，需要在 P≥0.80 时把 recall 提升到约0.80，下一步应增加可观测的未来先导特征或多扰动训练数据，而不能只继续提高阈值/FP权重。

剩余订单时间训练改为按生产进度加权：前1/3权重由 `0.25` 起步，随后平滑升至后期 `1.0`；原始全局 MAE仍保留，另报进度加权及分段 MAE。start10 Test 为全局/加权=`9.9/8.0 min`，early/middle/late=`14.9/7.5/3.9 min`；start15 为`9.7/7.8 min`，分段=`14.6/7.3/4.0 min`。这证实订单越接近完成越容易预测；加权指标只用于训练侧重点与辅助汇报，不能替换无权全局 MAE。

---



## 13. 产物路径


| 路径                                            | 含义                                            |
| --------------------------------------------- | --------------------------------------------- |
| `dense_i1_a1_prefix8/`                        | 正式 A.1                                        |
| `dense_i1_a1_prefix8_heur_cluster_ep100/`     | 启发式事件 + 原因对齐簇辅损（100 ep，Test F1=0.861）         |
| `dense_i1_a1_prefix8_nocluster_ep100/`        | 同上设定关聚类（100 ep，Test F1=0.871）                 |
| `dense_i1_a1_prefix8_cluster_up/`             | upcoming 导向聚类（过夜 100ep）                       |
| `dense_i1_a1_prefix8_cluster_up_ctrl/`        | 同 upcoming 杠杆、关聚类对照                           |
| `dense_i1_a1_start5_min10_cluster40/`         | start≤5、≥10 + 聚类（40ep）                        |
| `dense_i1_a1_start5_min10_nocluster40/`       | 同上关聚类（40ep）                                   |
| `dense_i1_a1_start5_min10_ep100/`             | 试：start<6、≥10 分钟（100 ep，Test F1=0.766）        |
| `dense_i1_a1_start5_min10_v2/`                | 同上标签，召回向短微调（Test F1=0.775）                    |
| `dense_i1_a1_start5_min10_v3/`                | start5 ongoing+far-start（Test F1=0.771，on_r↑） |
| `dense_i1_a1_start4_min10/`                   | 试：start<5、≥10（16 ep，F1=0.780）                 |
| `dense_i1_12_2_start{5,10,15}_min8_ep100/`    | 固定 Min8 的三档起点范围对照（Test F1=0.819/0.751/0.741）  |
| `dense_i1_a1_ablate_noprefix/`                | 同权重，关 prefix                                  |
| `dense_i1_a1_ablate_persist/`                 | 只报当前热工位                                       |
| `dense_i1_a1_ablate_nounion/`                 | 关占用并集                                         |
| `dense_i1_a1_ablate_noforce/`                 | 关强制进行中（与正式相同）                                 |
| `dense_i1_a1_ablate_{f180,near5,v3ft}/`       | 旧权重 × 现行标签                                    |
| `dense_i1_a1_prefix8_remain_frozen/`          | 只训 remain，MAE 9.8，A.1 不变                      |
| `dense_i1_a1_prefix8_{nocluster,sup,stgnpp}/` | 附录：插件消融                                       |
| `factory_bn/run_prefix8_real_ablations.py`    | 有效设置复现脚本                                      |
| `田宇潇_文献调研.xlsx`                               | 文献表（§10）                                      |


Test（新标签：start<5、≥10 分钟）： P=0.771 / R=0.790 / F1=0.780，up_r=0.494，on_r=0.859

---



## 14. 论文可写的网络/任务创新点（相对文献与 PDFormer）

> 写作原则：**能写进 Contribution 的，必须是「任务/结构/评价协议」相对文献的缺口**，且最好有消融支撑。单纯「把 PDFormer 换到工厂」不够；插件式聚类/STGNPP 若主表打平，应降为 appendix或方法细节，勿吹成核心创新。



### 14.1 相对文献在评什么（缺口 → 你的任务形式）


| 文献常见做法（§10）                  | 缺口                  | 你可主张的贡献                                                                            |
| ---------------------------- | ------------------- | ---------------------------------------------------------------------------------- |
| BSTAN / BTGCN：阻塞–饥饿时长或任务吞吐回归 | 不是固定短时域内的**工位事件报告** | **固定 H=15 min、每工位 (will, start, duration)** + `report_`*（工位对且 |\Delta start|\le 3） |
| DT+DES / TUSB：小时～班次「谁是瓶颈」    | 时域太粗，难做分钟级预警        | **分钟级、可行动的短时域预报**（与仿真 \Delta=60s 对齐）                                               |
| 异常检测 / 漂移 LSTM               | 报异常分，不报「谁、何时开始、多久」  | 事件三元组 + 精度优先选模（P≥0.80 再取 F1）                                                       |
| 交通 PDFormer                  | 稠密流量场；同质路段；强日历周期    | 见下节 **工厂化改写**                                                                      |
| STGNPP                       | 稀疏「下一次」事件           | 本任务要覆盖时域内**全部相关工位** → 事件头主路径，STGNPP 仅附录                                            |


一句话定位：不是又一个图回归器，而是 **制造图上的短时域工位瓶颈事件预报 + 可复现 report 协议**。

### 14.2 网络结构：PDFormer 挪用后「真改了什么」（C2）

相对原 PDFormer（交通流量），你实现里站得住的结构改写：


| 改写           | 代码/设计落点                                          | 为何算创新而不算套壳                                 |
| ------------ | ------------------------------------------------ | ------------------------------------------ |
| **去日历嵌入**    | 关掉 day/week；动力学由在制/订单进度驱动                        | 工厂无真实日历周期；直接复用交通周期归纳会错配                    |
| **异构资源图**    | 工艺链 + buffer + 物流服务边；类型 one-hot / grouped embed  | 交通节点同质；工厂机台/人/龙门/AGV 量纲不同                  |
| **延迟感知地理注意** | Geo + delay pattern（短 episode 用 k-means pattern） | 对齐「上游积压 → 数窗后下游饥饿」；BSTAN 类局部序列弱于此          |
| **语义注意**     | 行为相似、图上可非邻接                                      | 捕捉同扰动下的非局部耦合（如多站同时缺料）                      |
| **事件头取代流量头** | will / start / dur + prefix 解码                   | 输出从稠密场 → **可报告事件**；prefix 消融显示对 ongoing 关键 |


建议主表消融支撑 C2：**prefix 开/关、占用并集、force ongoing**（已有 §2）；图/延迟 pattern 若要写硬创新，最好再补「无 Geo 延迟」或「无语义注意」一臂。

### 14.3 多层次输出（C3）——可写，但要分主次

同一编码器上：

1. **A.1 工位事件**（主贡献、主指标）
2. **订单剩余时间** `remain_len`（交期视角；MAE 仍偏大，写「辅助层」更稳）
3. **过程原因** A.3（规则过程标签，不抄扰动注入名；两类空白原因要诚实写限制）

相对文献「只报身份 / 只报时长回归」：联合三层是合理贡献；**不要**把 STGNPP 或监督 score 头写成第三创新（实验已打平）。

### 14.4 聚类（C1）——怎么写才不翻车


| 可写                                                                                       | 不宜夸大                                 |
| ---------------------------------------------------------------------------------------- | ------------------------------------ |
| 两层工况表征（工位簇 / 整线簇）作**先验与可解释性**，不替代占用真值                                                    | 「聚类显著抬升 A.1 F1」——100ep 对照无聚类反而 F1 略高 |
| 原因对齐 `seed_cluster_ids` + 可选簇 CE / 簇感知 precursor                                         | 把 k-means id 当瓶颈标签                   |
| upcoming 导向簇损（cold/early/abnormal + recall lift）：相对同杠杆 ctrl，Test F1 +0.010、`up_r` +0.018 | 在主贡献里与 prefix 解码并列（增益仍偏小，写子贡献/附录更稳）  |


**投稿建议：** C1 写成 *representation / inductive bias*；主 claim 仍放在 **事件任务形式 + 工厂化 PDFormer + report 协议**。若 `cluster_up` 相对 ctrl 的 `up_r` 拉开 ≥5pt 且 F1 不掉，再把「簇感知 upcoming」升到子贡献。

### 14.5 建议的 Contribution 三条（可直接改写进论文）

1. **任务与协议**：提出面向人机协同产线的短时域工位瓶颈事件预报，定义 (will,start,duration) 与 `report_`* 评价，区分 upcoming / ongoing，并给出精度优先选模。
2. **结构**：将 PDFormer 的时间 / 地理（含延迟） / 语义注意迁移到异构过程–物流图，去除日历归纳，以事件头与热前缀解码对齐制造预警。
3. **多层次预报**：共享时空表示上联合工位事件、订单剩余与过程原因；工况聚类作为可解释先验（辅），不以簇替代事件真值。



### 14.6 写作红线（结合你已有实验）

- 勿写「相对旧 f180 口径涨 11 点」；写「同一现行标签下 prefix8 vs 复评」。  
- 聚类 / 监督 / STGNPP 打平结果 → 附录证明「插件非主因」。  
- Persistence / 只报当前热工位 → 非法或弱基线，单独标注。  
- 与 BSTAN 比必须同一 `report_*` 标签；他们的 RMSE 不能和 F1 横比（§10）。

过夜训练启动后的 wandb（第 1 档）：  
[https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/ul5lchzp](https://wandb.ai/u3660958-the-university-of-hong-kong/FactoryBN_PDFormer/runs/ul5lchzp) （`cluster_up` 100ep）

---


