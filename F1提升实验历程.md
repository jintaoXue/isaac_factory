# A.1 `report_f1` 提升历程

> 日期：2026-09-05  
> 数据：`raw_data/dense_i1`（N=38，F=27，episode 划分 seed=42，窗 20826 / 4331 / 5354，局 140 / 28 / 36）  
> 主指标：[`模型评估指标.md`](./模型评估指标.md) 的 `report_*`（工位对且 `|Δstart|≤3` 分钟）  
> **正式保留：`dense_i1_a1_prefix8`**（Test P=0.852 / R=0.856 / **F1=0.854**）

本文只记「在截图 / [`modelnote.md`](./modelnote.md) 第 3 节输出不变的前提下，F1 是怎么从 ~0.54 拉到 0.85 的」。训练一直是无监督（`train_mode=unsupervised`，占用 y 用 `ops_hot_mask`）。监督 `bottleneck_score` 对照见 [`无监督vs监督bottleneck_score对照.md`](./无监督vs监督bottleneck_score对照.md)，对 A.1 没有帮助。

---

## 0. 始终没改的输出契约

编码器 PDFormer。每个监督工位（机台 / 工作台 / 龙门 / AGV）预报未来 **15 分钟一条事件**：

| 输出 | 含义 |
| --- | --- |
| `will_block` | 未来 15 分钟会不会出现一段 **≥8 分钟** 的堵 |
| `start_min` | 从现在起第几分钟开始。上一分钟已经在堵 → 强制 0，只报还要堵多久 |
| `duration_min` | 持续几分钟（进行中训的是剩余时长） |

尚未发生（`start>0`）和已经在堵（`start=0`）都要报。人 / buffer 不当目标。辅助头（收工剩余、六类过程原因）不抢主分数。

**禁止当成「提高 F1」的手段：** 只评进行中、最短 2 分钟、把上一分钟已热的工位全报一遍。那是 persistence，不是预警。`dense_i1_f1_p80_v7` 的 Test P≈0.83 / F1≈0.87 **作废**，就是踩了这条。

主分数要 P≥0.80 再取最大 F1。P=0.80 时 F1≥0.80，召回也得大约 0.80。

---

## 1. 总表（同一包，从旧到新）

除注明外，预报 15 分钟、含 upcoming、`start≤2`、占用格整局平滑 8 分钟。  
**「旧标签」** = 预报窗剩余也卡 5 分钟（`event_min=5`）。  
**「现行标签」** = 尚未发生仍要 ≥8 分钟；进行中按剩余计（截图：已在堵则报剩余）。

| 档 | 标签 | Val P / R / F1 | Test P / R / F1 | Test `up_r` / `on_r` | 结论 |
| --- | --- | ---: | ---: | ---: | --- |
| 旧包 unsup_best | 旧口径参考 | — | 0.817 / 0.447 / 0.578 | — | 历史下限 |
| `dense_i1_a1_15min` | 旧 | ≈0.44 | — | — | 从零训，停掉 |
| `dense_i1_a1_v3ft` | 旧 | 0.641 / 0.544 / 0.589 | 0.617 / 0.477 / 0.538 | 0.300 / 0.677 | 早期微调 |
| `dense_i1_f1_p80_v7` | **作废** | — | ≈0.83 / ≈0.87 | 只评进行中 | persistence |
| `dense_i1_a1_near5` | 旧 | 0.808 / 0.605 / 0.692 | **0.801 / 0.651 / 0.718** | 0.312 / 0.719 | 第一次过 P=0.80 |
| `dense_i1_a1_near5_sup` | 旧 | 0.803 / 0.601 / 0.688 | 0.817 / 0.634 / 0.714 | 0.286 / 0.705 | 加分数回归，没帮忙 |
| `dense_i1_a1_f180` | 旧 | **0.815 / 0.715 / 0.762** | 0.785 / 0.705 / **0.743** | 0.375 / 0.772 | 召回上来，test P 掉到门下 |
| f180 + `start≤1` | 旧 | 0.774 / 0.733 / 0.753 | 0.765 / 0.739 / 0.752 | 0.411 / 0.772 | 收地平线不够 |
| **`dense_i1_a1_prefix8`** | **现行** | **0.883 / 0.881 / 0.882** | **0.852 / 0.856 / 0.854** | 0.455 / 0.911 | **正式保留** |
| `dense_i1_a1_uphist` | 现行 | 0.812 / 0.911 / 0.859 | 0.800 / 0.856 / 0.827 | **0.518** / 0.902 | 换 F1 换 `up_r`，不采用 |

数字不能跨标签直接比。`prefix8` 的 0.854 相对 `f180` 的 0.743，有一部分来自「进行中剩余 1–4 分钟按截图算正例」，不是同一套分母上硬抬了 11 个点。

---

## 2. 阶段一：先过 P=0.80（`near5`，旧标签 F1=0.718）

起点是 `v3ft`（test F1=0.538）。问题不是模型完全不会报，而是：

- 远地平线（第 5–15 分钟才开始）几乎没有前兆，乱报会打穿精度。
- 只报「上一分钟已热」能把 F1 做很高，但那不是 A.1。

做法：

- 正例仍要持续占用，但报告最短先放到 **5 分钟**（占用格仍按整局 8 分钟平滑）。
- 尚未发生只计 **`start≤2` 分钟**。
- val 上扫 τ，**P≥0.80 才存盘**。
- 从 `v3ft` 微调，解冻最后 2 个 encoder block。

结果：`dense_i1_a1_near5` Test **P=0.801 / R=0.651 / F1=0.718**。第一次同时满足「含 upcoming + P 过门」。缺口在召回：val 最好也只有约 0.60。

同期用同一初始化跑了监督 `bottleneck_score`（`dense_i1_a1_near5_sup`）。`dense_i1` 上两种占用标签 8 分钟平滑后 Jaccard=1.0，监督只是多了一个拟合很差的分数回归。Test F1 0.714，略差。主路径继续无监督。

---

## 3. 阶段二：修解码、抬召回（`f180`，旧标签 F1=0.743）

`near5` 扫 τ=0.85–0.98 时，强制进行中只抬到 0.80，这些真值会被更高的 τ 扫掉。`last_hot` 相对「剩余≥5 分钟事件」的精度只有约 0.55，不能全报。

改了三处，任务没改成 persistence：

1. **抬升跟着 τ 走**：`force_to = max(配置, 当前 τ)`，进行中不会被扫描抹掉。
2. 加重「上一分钟已热、后面不成事件」的假阳性（`event_will_last_hot_fp_weight=4.5`）。
3. 多解冻 4 个 encoder block，加强占用格（`w_hot/dice/iou` 上调）。

试过、否掉的：

- **`hot_min_windows=5`**：占用 y 放宽，val 尚未发生从 98 涨到 284，任务变难、和 near5 对不齐。跑到一半杀掉。
- **`event_max_start_windows=1`**：test F1 只到 0.752，不够。

结果：`dense_i1_a1_f180` Val F1=0.762，Test **P=0.785 / R=0.705 / F1=0.743**。召回比 near5 高约 5 个点，但 test 精度掉到门下。单靠再训、再收 `start` 上限，旧标签上到不了 0.80。

---

## 4. 阶段三：对齐截图 + 剩余前缀（`prefix8`，现行 F1=0.854）

卡点不在「模型再聪明一点」，而在标签和截图不一致。

占用格已经在**整局**上按 ≥8 分钟平滑。一段已经堵了 10 分钟、预报窗里还剩 3 分钟的工位：

- 截图：上一分钟已热 → `start=0`，报剩余 3 分钟。
- 旧做法：`event_min=5` 卡的是**窗内剩余**，这条被丢掉。  
  于是 `last_hot` 看起来像「闪烁」（精度约 0.55），其实很多是「真堵、只是快结束了」。

现行标签（`node_event_targets`）：

- **尚未发生**：新段 ≥8 分钟，且 `start≤2`（val 仍是 98 条）。
- **进行中**：上一分钟已热、预报窗还接着热 → 剩余 ≥1 分钟也算（val 396 → **624**）。

解码加 **`prefix_mlp`**：预测「从下一分钟起还能连续热几分钟」。只有 `last_hot` **且** prefix≥1.5 才抬升，不是把当前热工位全报一遍。真正下一分钟就冷的不报。

训练：从 `f180` 微调，解冻全部 5 个 encoder block，事件窗 4× 过采样，16 epoch。Best = epoch 8。

| | Val | Test |
| --- | ---: | ---: |
| **P / R / F1** | **0.883 / 0.881 / 0.882** | **0.852 / 0.856 / 0.854** |
| 尚未发生 / 进行中召回 | 0.633 / 0.920 | 0.455 / 0.911 |
| 正例条数 | 98 + 624 | 112 + 828 |

这是正式结果。ckpt：

`libcity/cache/model_cache/dense_i1_a1_prefix8/BNPDFormer_best.pt`

复现：

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN_dense_f1_p80.json \
  --data_dir raw_data/dense_i1 \
  --save_dir libcity/cache/model_cache/dense_i1_a1_prefix8 \
  --device cuda --no_wandb
```

---

## 5. 阶段四：冲 `up_r`（`uphist`，不采用）

`prefix8` 的短板是尚未发生：test 112 条只召回 45%。试过前兆特征，而不是把编码器 Tin 改成 45/60（那样接不上 prefix8 权重）。

- `precursor_mlp`：近 5 分钟队列 / 堵饿 / 缺料 / 到货 + 编码器窗再往前 30 分钟的池化 + 邻接工位队列。
- 尚未发生窗 8× 过采样，进行中只 2×。
- `will≥0.75` 的冷工位抬过扫到的 τ，避免再被 0.98 抹掉。

Test **P=0.800 / R=0.856 / F1=0.827**，`up_r` **0.455 → 0.518**（大约多 7 条）。精度贴着 0.80 门，总 F1 下降。正式配方不换成这一档。ckpt 留在 `dense_i1_a1_uphist/`，仅作对照。

---

## 6. 什么有效、什么无效

**有效**

1. 近地平线（`start≤2`）+ P≥0.80 再取 F1，而不是全 15 分钟乱报。
2. 强制进行中的抬升必须 ≥ 正在扫的 τ，否则高 τ 把真值扫掉。
3. 进行中标签按截图用**剩余时长**，不要用「窗内剩余也 ≥8 / ≥5」把快结束的真堵丢掉。
4. 用「还能接着热多久」区分继续堵和下一分钟就冷，不要 persistence。
5. 无监督占用已经够用；这份包上再回归 `bottleneck_score` 没有新正例。

**无效或作废**

1. 只评进行中 / 最短 2 分钟 / 全报 `last_hot`（v7）。
2. 从零训 15 分钟配方（val F1≈0.44）。
3. 监督分数头（test F1 0.714 vs 无监督 0.718）。
4. 占用最短改成 5 分钟（标签爆炸）。
5. 只把 `start` 收到 1 分钟（F1 0.752）。
6. 前兆头 + 加长历史池（`up_r` 有限，F1 下降）。

---

## 7. 正式口径（保留 prefix8）

| 项目 | 取值 |
| --- | --- |
| 配置 | `factory_bn/configs/FactoryBN_dense_f1_p80.json` |
| ckpt | `libcity/cache/model_cache/dense_i1_a1_prefix8/BNPDFormer_best.pt` |
| 训练模式 | 无监督 |
| 输入 / 预报 | 30 min → 15 min |
| 占用格 | 整局平滑 ≥8 分钟 |
| 尚未发生 | ≥8 分钟，`start≤2` |
| 进行中 | 已热且还接着热，剩余 ≥1 分钟 |
| 解码 | 事件头 + `last_hot` 且 prefix≥1.5 |
| Test | **P=0.852 / R=0.856 / F1=0.854** |

流程总述仍看 [`modelnote.md`](./modelnote.md)。指标键名看 [`模型评估指标.md`](./模型评估指标.md)。
