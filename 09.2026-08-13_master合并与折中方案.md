# 2026-08-13：`origin/master` 合入 `dev_tyx` 与折中方案记录

> 日期：2026-08-13  
> 分支：`dev_tyx`（合并后相对 `origin/dev_tyx` 超前）  
> 合并提交：`c2a81a6` *Merge origin/master into `dev_tyx`*  
> 父母提交：`3d17c13`（当时的 `dev_tyx`）+ `e49a86c`（当时的 `origin/master`）  
> 目的：把 master 上的分层调度、仓储 API、真实路径/龙门速度接进来，同时保住本分支的瓶颈采集与 18 件订单设定；随后用折中参数把并行拉回 5，并对齐聚合/训练的资源 id。

本文只记**今天实际发生的代码与数据变更**，不替代 `00.运行方式.md` / `02` / `06` 的使用说明。

---

## 1. 合并策略

**不是 checkout master**，而是把 `origin/master` merge 进当前 `dev_tyx`。

冲突处理原则（写在 merge commit message 里）：

| 保留 `dev_tyx` | 接入 master |
|----------------|--------------|
| 瓶颈数据采集（`BottleneckDataCollector`、Stage-C、PDFormer） | 分层 TPA：`source/algo/hierarchical/` |
| 18 件水管订单（`CfgProductOrder` / `CfgRegistrationInfos` = 18） | 仓储相对位姿 / storage API |
| 已有扰动 CLI 与 bottleneck 日志 | 真实路径导航 + 龙门 `move_speed` |

有冲突的文件（merge 时手工解过）：

- `env_asset_cfg/cfg_hc_env.py`
- `env_asset_cfg/cfg_material_product.py`
- `hc_single_env_base.py` / `hc_vector_env_base.py`
- `src/machine.py` / `src/material.py` / `src/task_progress_manager.py` / `src/utils.py`
- `train.py`

调度代码从 `source/algo/multiagent/hc_factory/rule_based.py` 迁到 `source/algo/hierarchical/hc_factory/`。旧 MARL 目录对应文件删除；配置从 `marl.yaml` 改名为 `flat.yaml`，并新增 `hier.yaml`。

---

## 2. Master 合入后的行为变化（相对合并前的 `dev_tyx`）

这些是 master 带来的物理/调度语义，**折中方案里刻意保留**。

### 2.1 行走按真实路径，不再用固定步数

`CfgSubtaskPredefinedTimeGallery`：

```text
go_to_material / go_to_goal_area / go_to_processing_machine = None
```

`None` 表示该子任务一直等到导航走完，而不是旧数据里的固定 `10` 步。搬运/装夹等仍是固定步数（如 `material_on_gantry=10`）。

### 2.2 龙门用速度，不再用 `animation_time`

`cfg_machine.py` → `num07_gantry_group.registration_infos`：

| 字段 | 当前值 | 含义 |
|------|--------|------|
| `move_speed` | 0.5 | 每逻辑步位移（世界/关节 xy） |
| `move_dt` | 1.0 | 每步时间尺度 |
| `loaded_speed_scale` | 0.5 | 负载时减速 |
| `move_speed_noise_std` | 0.0 | 速度噪声（扰动模块会改） |

旧采集里龙门走的是 `animation_time`；合并后扰动若再写 `animation_time` 会直接报错（见 §3.1）。

### 2.3 分层调度与并行派发

- `rule_based` / `hier` / `flat` 都走 `hierarchical_dispatch` 的 A→B→C→D。
- 新参数 `max_parallel_cd_dispatch`：每步最多派发多少个 C/D。Master 默认是 **1**（一拍只开一个产品），这是合并后第一轮采集 WIP≈2、开工间隔变长的主因，不是单件加工变慢。
- CLI：`rule_based` **不会**只看 `--max_episodes` 停仿真，还要 `--max_sim_episodes`。采集必须两个一起写。

### 2.4 订单 / 仓储 / 物料 prim

- 订单规模仍是 **18**（`cfg_material_product.py`）。
- USD 里水管物料模板只有 `00–04`；超过的 idx 运行时 clone（`ensure_product_water_pipe_prims`）。
- 仓储改为相对位姿 API；黄库/地面库容量相对旧 18 件采集有收缩（合并时跟 master）。

### 2.5 环境其它默认（合并当时）

合并刚完成时（折中方案之前）：

| 项 | 旧 `dev_tyx` 采集 | 合并当时 |
|----|-------------------|----------|
| `parallel_producing_limit` | 5 | 10 |
| `max_parallel_cd_dispatch` | 无（旧调度） | 1 |
| `go_to_*` | 10 | **None（保留）** |
| 龙门 | `animation_time` | **`move_speed=0.5`（保留）** |
| 产品数 | 18 | **18（保留）** |

---

## 3. 合并后立刻修的运行时问题

合入后用 `rule_based` + `machine` 扰动跑采集时踩到的，已改在工作区（未单独再开 commit）。

### 3.1 物流扰动还在写龙门 `animation_time`

文件：`env_asset_cfg/cfg_disturbance.py`

- 快照/恢复改为 `move_speed` / `move_speed_noise_std`。
- 物流变慢：`move_speed = base_speed / gantry_time_scale`。
- 旧「动画步噪声」按相对比例折到速度噪声。

不改的话 `--disturbance_dim logistics` 会 KeyError。

### 3.2 clone 物料缺 `cube_raw`

文件：`src/material.py` → `_PRODUCT_PRIM_SPECS`

USD 克隆列表漏了 `("cubes", "cube_raw")`，18 件订单走到 `cube_raw_07` 会崩。已补上。

### 3.3 `episode_config.csv` 仍读已删除字段

文件：`src/bottleneck_data.py` → `_write_episode_config`

- `CfgProductProcessGallery` 不再带 `process_time`：改为从对应机台 `animation_time` 填。
- `gantry_config` 改为记录 `move_speed` / `move_speed_noise_std`，不再写 `animation_time`。

---

## 4. 折中方案（合并后按讨论落地）

目标：**物理跟真实产线（路径 + 龙门速度），并行跟旧瓶颈数据（WIP=5），训练能吃到 L2 事件。**

### 4.1 并行回到 5

| 位置 | 改前 | 改后 |
|------|------|------|
| `cfg_hc_env.py` `single_env_parallel_producing_limit` | 10 | **5** |
| `algo_cfg/rule_based.yaml` / `hier.yaml` `parallel_producing_limit` | 10 | **5** |
| `algo_cfg/flat.yaml` `parallel_producing_limit` | 10 | **5** |
| `max_parallel_cd_dispatch`（yaml + dispatch 默认） | 1 | **5** |
| `data_preprocess_for_buffer.py` `max_ongoing` | 10 | **5** |
| `hierarchical_tpa.py` / `flat_tpa.py` / `rule_based.py` 默认 | 10 / K=1 | 5 / K=5 |

只改 WIP 上限、不改 `max_parallel_cd_dispatch` 时，每步仍只能开 1 单，WIP 会卡在 ~2。K=5 才能真正用满 5 个并行槽。

**未改：** `go_to_*=None`、龙门 `move_speed=0.5`、产品数 18。

### 4.2 扰动 / 特征表 / PDFormer 的资源 id 对齐

特征表和 PDFormer 节点用**工位 id**：

```text
num02_rollerbedCNCPipeIntersectionCuttingMachine_ws0
gantry_0
human_0
```

合并后 L2 扰动日志写的是**机台组名**（没有 `_ws0`）。Stage-C 事件对不上节点 → `export_dataset` 丢事件 → 训练 `event+train=0`，STGNPP 等于没开。

修改：

1. **采集**（`src/disturbance.py`）  
   激活后按实际工位写 `target_resource_id`：  
   `{machine}_ws{i}` / `gantry_{idx}` / `human_{idx}`。
2. **聚合**（`tools/build_bottleneck_features.py`）  
   `resolve_feature_resource_id`：组名 → `{group}_ws0`（兼容旧 `disturbance_log.csv`）。
3. **导出**（`PDFormer/factory_bn/graph.py` `resolve_to_node_id` + `export_dataset.py`）  
   打包事件和 `future_bottleneck_object_id` 时同样映射，避免再丢点。

---

## 5. 数据与训练对照

### 5.1 合并后、折中前的 3 集（已采完）

目录：`output/bottleneck_dataset/2026-08-13_15-39-28_seed42`  
设定：`machine` / intensity 2.0 / 18 件 / **当时并行 10、K=1** / 真实路径 + `move_speed`。

| | 旧基线 `18_machine2.0`（约 50 ep） | 这 3 集 |
|--|--|--|
| 单件周期 | ~1917 s | ~1845 s（接近） |
| Makespan | ~10297 s | ~16502 s |
| 开工间隔中位数 | ~508 s | ~868 s |
| 平均 WIP | ~3.4 | ~2.0 |

Makespan 变长主要是**开工变稀**，不是单件变慢。

折中代码落地后，对**同一 run** 重新聚合 + 导出 + 训练（`bn_pdformer`）：

| | 对齐前 | 对齐后 |
|--|--|--|
| `bottleneck_event.resource_id` | `...CuttingMachine`（对不上节点） | `...CuttingMachine_ws0` |
| 导出 events | 被 skip | 3 集各 1 条 |
| `event+train` | 0 | 6 |
| event loss | ~0 | 有下降（约 2.8 → 2.0） |
| test `score_mae` | ~0.199 | ~0.184 |
| test `will_acc` | ~0.942 | ~0.992 |

`will_acc` 偏高是因为每集 horizon 内正样本极少（大约 1 个窗），不能当产能指标。权重目录：

`PDFormer/libcity/cache/model_cache/FactoryBN_2026-08-13_machine2.0/`

配置：`PDFormer/factory_bn/configs/FactoryBN_20260813_machine2.json`

### 5.2 折中后的新采集

并行 5 + K=5 只对**新仿真**生效。上面那 3 集不会因此变短。新采集示例：

```bash
conda activate env_isaaclab
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim machine --disturbance_intensity 2.0 \
  --max_episodes 3 --max_sim_episodes 3 --seed 42
```

两个 max 都要；漏 `--max_sim_episodes` 时 `rule_based` 不会按集数停。

聚合 / 导出 / 训练仍用 `00.运行方式.md` 里的三段命令；环境分别是系统 `python3`（聚合）和 `bn_pdformer`（导出、训练）。

---

## 6. 文件清单（按阶段）

### 6.1 合并引入（`c2a81a6`，相对当时 `dev_tyx`）

主要新增/迁移：`source/algo/hierarchical/hc_factory/*`（含 `hierarchical_dispatch.py`、`hierarchical_tpa.py`、`flat_tpa.py`、`tpa_info_pool.py`、`rule_based.py` 等），`algo_cfg/hier.yaml`，`src/data_preprocess_for_buffer.py`，以及 `cfg_process_subtask_gallery.py` / `cfg_machine.py` / `cfg_storage.py` / `task_progress_manager.py` / `utils.py` 的路径与龙门速度。

### 6.2 合并后修复 + 折中（工作区，相对 `c2a81a6`）

| 文件 | 内容 |
|------|------|
| `cfg_disturbance.py` | 龙门扰动改 `move_speed` |
| `src/material.py` | 补 `cube_raw` clone |
| `src/bottleneck_data.py` | `episode_config` 适配新工艺/龙门字段 |
| `src/disturbance.py` | L2 日志写工位 id |
| `cfg_hc_env.py` + 三个 `algo_cfg/*.yaml` | 并行 5、K=5 |
| `data_preprocess_for_buffer.py` | `max_ongoing=5` |
| `hierarchical_dispatch.py` / `hierarchical_tpa.py` / `flat_tpa.py` / `rule_based.py` | 默认并行/K 对齐 |
| `tools/build_bottleneck_features.py` | 组名 → `_ws0` |
| `PDFormer/factory_bn/graph.py` | `resolve_to_node_id` |
| `PDFormer/factory_bn/export_dataset.py` | 事件/未来节点映射 |

### 6.3 同日文档（非合并代码，但是当天写的）

| 文件 | 内容 |
|------|------|
| `codenote.md` | statedic / `norm_state` → 18 维特征怎么进 PDFormer |
| `08.期望输出.md` | 模型各头期望输出 |
| `06.瓶颈预测模型_PDFormer与点过程适配.md` | 按更直白的语言重写 |
| 本文 `09...` | 合并 + 折中变更记录 |

`02.离线瓶颈特征与标签流水线.md` 当天也有改动，与 Stage-C 说明对齐。

---

## 7. 之后采集/训练时容易踩的点

1. **停仿真**：`--max_episodes` 和 `--max_sim_episodes` 一起设。  
2. **并行**：WIP=5 和 `max_parallel_cd_dispatch=5` 必须一起看；只改其中一个仍会串行开工。  
3. **事件 id**：新采集应直接看到 `*_ws0`；旧 log 靠聚合映射，不必重采也能训 STGNPP。  
4. **环境**：仿真 `env_isaaclab`，PDFormer `bn_pdformer`。  
5. **RL 观测槽位数**：`max_ongoing` 已改为 5，和并行上限一致；旧的 10 槽 checkpoint 不能直接加载。
