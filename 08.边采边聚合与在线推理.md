# 边采边聚合与在线推理

> 日期：2026-08-16（`evt` 已是剩余占用头）  
> 代码：`tools/bn_agg/`（`--follow` / `--closed-windows`）、`PDFormer/factory_bn/infer.py`  
> 离线 Stage-C 全量聚合：[`03.离线特征聚合&打标签.md`](./03.离线特征聚合&打标签.md)  
> 采集 / 训练命令：[`00.运行方式.md`](./00.运行方式.md)  
> 模型头与指标：[`06.PDFormer+STGNPP实现.md`](./06.PDFormer+STGNPP实现.md)  
> tmux：[`07.tmux运行方式.md`](./07.tmux运行方式.md)

Isaac 采集还在写 raw 7 表时，另开 CPU 进程把**已经关完**的 30s/60s 窗刷进 `derived/`，再用训练好的 `BNPDFormer_best.pt` 预报 **从当下到剩余工单收工** 的工位占用。仿真循环里**没有**接聚合；当前实现是旁路 sidecar。

---

## 1. 为什么可以边采边聚，训练却还要等整局

| 产物 | 因果性 | 边采能不能用 |
|------|--------|----------------|
| `window_feature_table`（60s 窗特征 + `bottleneck_score_s`） | 窗一关就能算 | 可以，只发满长窗 |
| `disturbance_active_s` | 进行中的 L2 可截到「当前日志末端」 | 特征可用；**事件 onset 要等 L2 结束** |
| `will_bottleneck` / mark（未来 H=180s） | 要看到 `t+180` | 未到期的窗 `label_horizon_ready=0`，**不能当训练标签** |
| `bottleneck_event.csv` 里未完成的 L2 | 还没有 `end_time_step` | 不写事件行 |

所以：

- **推理**：凑齐 12 个已关 60s 窗 + `job_kpi` 剩余工单，`evt` 权重预报到收工的 (窗×工位) 占用（A.1）。没有 `remain_to_jobs_done` 的旧 ckpt 才仍是下一窗 + 180s `will`。现训 hot P=0.10，事件列表能打出来但误报多。
- **训练 / 导出**：采集全部结束后，**不带** `--follow` / `--closed-windows` 再全量聚合一次，再走 `batch_bn_export_train.sh`（npz 必须带 `jobs_remaining`）。

窗口特征在 60s 关窗时就是因果的；训练 `will` 还要再等 180s。这就是「Isaac 只写 raw、CPU 另开聚合」的分工，而不是把 Stage-C 塞进仿真步。

---

## 2. 三条进程

不要和 Isaac 抢同一张 GPU。聚合不占 GPU；推理默认会用 `cuda`（若可用），采集还在跑时请加 `--device cpu`。

```text
tmux bn_collect   env_isaaclab     train.py → bottleneck_dataset/<run_id>/episode_*/env_00/*.csv
tmux bn_follow    env_isaaclab     python -m bn_agg --run_dir ... --follow  →  <run_id>/derived/
tmux / 普通终端    bn_pdformer      python -m factory_bn.infer --run_dir ... --episode N
```

`--follow` 轮询 raw 日志体积变化，整份 run 里所有 episode 都会重扫一遍。已收尾局的数字几乎不变，正在写的那一局 `episode_end` / `jobs=k/n` 会往前走。

---

## 3. 实现：闭窗聚合

入口：`tools/bn_agg/pipeline.py`。`cd tools && python3 -m bn_agg`。

### 3.1 `--closed-windows`（单次）

`closed_windows_only=True` 时：

1. `episode_end` = 当前日志里最大时间戳 `as_of_s`。
2. `compute_window_features` 丢掉长度不足 `window_size` 的最后一截（例如 200s、60s 窗只发 0/1/2，不发 20s 残窗）。离线全量模式会保留残窗。
3. `parse_disturbance_l2_intervals(..., open_end_s=as_of_s)`：已结束的 L2 照常；**还没写 end 的区间截到 `as_of_s`**，只给特征列 `disturbance_active_s`。打事件表时 `if interval.get("open"): continue`，未完成 L2 **不进** `bottleneck_event.csv`。
4. `will`：`(t + horizon) <= as_of_s` 才 `label_horizon_ready=1`；否则 will/mark 按 censored 写，不能当监督。

`--follow` **隐含** `--closed-windows`。

### 3.2 `--follow`（循环）

```text
每 poll 秒（默认 5）:
  发现 episode_*/env_* 
  用 (路径, resource_event_log.jsonl 字节数) 当签名
  签名变了 → 对整份 run 再跑一遍闭窗聚合
  写 derived/ 与 all_env_summary.json
Ctrl-C 停止
```

日志里会循环打印 `episode_00` … 当前局，并带 `[closed windows]`。半局示例：`jobs=7/10`、`episode_end=8179s`。

签名只看 `resource_event_log.jsonl` 大小。该文件在涨才会触发；若只有别的表在写、这个 jsonl 暂时没变，会等到下次体积变化。

### 3.3 切勿用闭窗结果当训练 derived

`--closed-windows` / `--follow` **覆盖** 同一路径下的 `derived/`。曾在已完成的 `old_machine2.0` 上试过一次闭窗，会把全量 30+60 表改成「只有满窗」。训练用 run 必须再跑**无这两个开关**的聚合。

测过闭窗逻辑：`tools/bn_agg/test_streaming.py`（残窗丢弃、未结束 L2 截断）。

---

## 4. 实现：推理

入口：`PDFormer/factory_bn/infer.py`。环境 **`bn_pdformer`**。

训练脚本只做离线 test。在线 / 回放走这条 CLI，或代码里 `BNPredictor.predict_x`。

### 4.1 输入

必须是 Stage-C 的 `window_feature_table.csv`（`window_size_s=60`），**不能**直接丢 `job_trace.csv`。节点顺序对齐 checkpoint 里的 `data_meta['resource_ids']`（当前 34 个）。历史长度 `input_window=12`（12×60s）。

三种数据源（互斥）：

| 参数 | 读什么 |
|------|--------|
| `--run_dir` + `--episode N` | `<run>/derived/episode_NN/env_00/` |
| `--episode_dir` | 任意 `derived/.../env_00` 目录 |
| `--data_dir` | 已导出的 `raw_data/<标签>/episodes.npz` |

`load_derived_episode` 把 CSV 转成 `(T, N, F)`，并读 `job_kpi.csv` 得到 `jobs_remaining`。`build_infer_sample` 取 `X = features[t-12:t]`。在线时观测窗是 `0..T-1`，传 `t=T`。`remain_to_jobs_done` 权重预报 **直到剩余工单清零** 的占用图（最长 `max_remain_windows=512` 窗 ≈ 8.5h）。STGNPP 在剩余时间上右删失。现盘 `evt/` 已是该头。

未接进 Isaac 步进循环。仿真里若要调，组 `(12, N, F)`（checkpoint 节点序）+ 剩余工单数，再调 `BNPredictor.predict_x`。

### 4.2 时间切法 `--at`

| `--at` | 行为 |
|--------|------|
| `last`（默认） | 最后 12 个已关窗 + 当前剩余工单 → 预报到收工。新头打印 `[until remaining jobs done]` |
| `all` | 每个滑窗回放一次剩余占用 |
| 整数 `t` | 指定第一步未来下标 |

`--closed`：把 episode 当已经结束（`phase` 用最后一窗结束时刻）。**正在采的局不要加**；已收尾回放可以加。

### 4.3 终端字段

```text
[infer] ... remain_jobs=True Kmax=512
t=136  t_s=8100  will_prob=...  jobs=4/10  R_hat=128w (~7680s)  [until remaining jobs done]
  A.1 events until jobs done (3):
    8160-8520s  dur=360s  num01_weldingRobot_ws0
  top-5 mean score_pred over remaining windows:
     ...
```

| 字段 | 含义 |
|------|------|
| `jobs` | 当前未完工 / 总工单（来自 `job_kpi`） |
| `R_hat` | 预测还要多少个 60s 窗才收工 |
| `A.1 events` | 占用图上的连通 1 段 = 开始时间 / 持续时间 / 工位 |
| `will_prob` | 近 180s 辅头（不是 A.1 主输出） |
| `score_pred` | 剩余各窗分数的均值（按工位） |
| `hot_win` | 该工位在剩余地平线里被判热的窗数 |

权重：`PDFormer/libcity/cache/model_cache/<标签>/BNPDFormer_best.pt`。**现有 `evt/` 已是剩余占用头**（`lchf07o9`，ckpt=`hot_f1`）。格式能出 A.1，现训 precision 不够，见 [`04.期望输出.md`](./04.期望输出.md)。扰动维度和训练子集不一致时只当通路测试。

---

## 5. 运行方法

### 5.1 采集还在跑：旁路聚合

Isaac 已写出 `bottleneck_dataset/<run_id>/` 之后另开终端（建议 tmux）：

```bash
conda activate env_isaaclab
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools

python3 -m bn_agg \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --follow
# --poll 5          # 秒，默认 5
# Ctrl-C 停止
```

单次只刷满窗、不循环：

```bash
python3 -m bn_agg --run_dir ../output/bottleneck_dataset/<run_id> --closed-windows
```

`--follow` 不要停掉再在同一目录开第二个；会抢写 `derived/`。

### 5.2 对最新闭窗做推理

另开窗口，**不要**停 `--follow`：

```bash
conda activate bn_pdformer
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.infer \
  --ckpt libcity/cache/model_cache/evt/BNPDFormer_best.pt \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --episode 9 \
  --device cpu
```

`--episode` 与正在写的 `episode_09` 对应（十进制，补零由脚本完成）。`--follow` 刚刷过 derived，这就是当前最后一扇已关 60s 窗。过几分钟同一条命令再跑即可跟上。

缺 derived 或满 60s 窗不足 12 个会报错，等 `--follow` 再刷一轮。

### 5.3 已收尾的局 / 整局回放

```bash
python -m factory_bn.infer \
  --ckpt libcity/cache/model_cache/evt/BNPDFormer_best.pt \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --episode 8 \
  --closed \
  --device cpu

# 每个滑窗 + CSV
python -m factory_bn.infer \
  --ckpt libcity/cache/model_cache/evt/BNPDFormer_best.pt \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --episode 8 \
  --closed \
  --at all \
  --out_csv /tmp/bn_infer.csv \
  --out_json /tmp/bn_infer.json
```

也可用 `--episode_dir ../output/bottleneck_dataset/<run_id>/derived/episode_08/env_00`，或 `--data_dir raw_data/evt`（已导出 npz）。

### 5.4 采集全部结束后（训练前必做）

1. `--follow` 里 Ctrl-C。
2. **无** `--follow` / `--closed-windows` 再聚合（或 `./batch_bn_agg.sh` 指向该文件夹）。
3. 确认 `derived/*/pipeline_summary.json` 里 `closed_windows_only` 为 false。
4. 再 `batch_bn_export_train.sh`。`norm` 对照只聚合、不进训练。

---

## 6. 代码地图

| 文件 | 作用 |
|------|------|
| `tools/bn_agg/pipeline.py` | CLI、`--follow` 轮询、`closed_windows_only` |
| `tools/bn_agg/features.py` | 满窗才发特征行 |
| `tools/bn_agg/labels.py` | 未结束 L2 截断、事件不发 open、`label_horizon_ready` |
| `tools/bn_agg/test_streaming.py` | 残窗 / 开区间 L2 |
| `PDFormer/factory_bn/remain.py` | 剩余工单、占用格子、A.1 连通块 |
| `PDFormer/factory_bn/infer.py` | `BNPredictor`、CLI、`predict_x` / `predict_episode` |
| `PDFormer/factory_bn/test_remain.py` | 剩余工单 / 占用 packing |
| `PDFormer/factory_bn/dataset.py` | `build_infer_sample` |
| `PDFormer/factory_bn/export_dataset.py` | `load_derived_episode` |
| `PDFormer/factory_bn/test_infer.py` | 组样（可无 GPU） |

自测：

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools
python3 -m bn_agg.test_streaming

cd ../PDFormer
conda activate bn_pdformer
python -m factory_bn.test_infer
```
