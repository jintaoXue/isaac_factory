# 模型期望输出

> 日期：2026-08-21  
> 现有模型：BNPDFormer（固定 H=15 min 占用头 + 订单剩余时间 + STGNPP 事件点过程 + A3 原因分类头）  
> 现训权重：`PDFormer/libcity/cache/model_cache/evt/BNPDFormer_best.pt`（WandB `lchf07o9` / `evt_ep50`，epoch 47，`ckpt_metric=hot_f1`）——**这是旧占用定义**（几乎只靠 `score≥0.55` / TPM）。2026-08-21 后占用 y 含排队 / 缺料饥饿 / 到货延误 / 上游堵，需重聚再训。  
> 数据：异质资源日志 → 窗口特征 / 瓶颈事件 / `job_kpi`

---

## 1. 期望输出（实验维度）

### A. 工位角度


|     | 输出                           | 形式                          | 用途        |
| --- | ---------------------------- | --------------------------- | --------- |
| A.1 | 瓶颈是否发生（开始时间，持续时间，发生在哪个工位/工序） | 0/1（窗口 / 工位）                | 预警        |
| A.2 | 不同环境 setting 对各工位影响          | 热力图（setting × 工位）           | 对比扰动 / 配置 |
| A.3 | 瓶颈原因                         | 多分类（扰动类型 / 阻塞 / 缺料 / 高利用率等） | 指导环境与资源配置 |


**当前问题：当前仿真未覆盖不同 task planning / allocation，所以我们希望从扰动与资源配置敏感性**（机器故障、人力离岗、物流延迟等）的角度指导实际生产。

### B. 产品角度


|     | 输出                 | 形式  | 用途     |
| --- | ------------------ | --- | ------ |
| B.1 | 当前订单能否按时/按期完工      | 0/1 | 指导实际生产 |
| B.2 | 单件产品完工时间           | 分布  |        |
| B.3 | 各工序 / subtask 完工时间 | 分布  | 定位瓶颈工序 |


---



## 2. 当前代码和训练能否实现 A


| 目标                           | 代码                                                                                                                         | 现训权重（evt / lchf07o9）                                                                          | 判定             |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------- |
| **A.1 瓶颈 0/1（开始 / 持续 / 工位）** | **已接。** 占用图画未来 **H=15 分钟**；`remain_len` 另报还要多久收工。`occupancy_to_events` 把连通 1 段收成开始时间 / 持续时间 / 工位。占用格子 = `node_hot_mask`（拐点 / 排队 / 缺料饥饿 / 到货延误 / 上游堵 / `score≥0.55`；**不含** L2）。`will` 只是近 180s 辅头。 | 旧 `evt` 是「画到收工」+ 旧 y，**作废**。短单须重采 `preview_n10_*` 再训。 | **接口已改成固定 H；旧权重不够**   |
| **A.2 setting×工位热力图**        | **没有这个头。** 多 setting 跑仿真或对 A.1 占用按工位聚合即可画。                                                                                 | I=1.0 旧包：machine 关 ws1 → 环焊/工作台热；material 点亮两侧工作台。画图不依赖网络。 | **实验可做，非模型输出** |
| **A.3 瓶颈原因**                 | **已接。** `cause_head` 10 类 CE；标签 `root_cause_reason`（过程启发式，**不抄** L2 类型名）。                                                                       | 旧包多数塌成 `starved_upstream`；物料热窗可打出 `material_shortage`。新门槛（到货 20s / 排队 1.0 / 堵 40%）后应更分维。旧 test **cause_acc=1.0** 虚高。 | **头在，旧标签不可信**     |
| **B.1 / B.2 / B.3**          | 未接产品/工序头。`job_kpi` 只用来算 A.1 的剩余工单数，不预测单件完工。                                                                                | —                                                                                             | **暂不可直接出**     |


**总评**：A 的**形状**（占用图 + 连通事件 + 原因头）代码已经齐；**现盘 `evt` 权重实现不了可用的 A**。A.1 会乱报工位，A.3 看不出真会分类，A.2 本来就不是网络输出。产品侧 B 仍需新任务。2026-08-21 改了占用正例和 L2 时长之后，应先用新 derived 重训，再评 A.1 / A.3。

---

## 3. 建议推进顺序（汇报用）

1. **按短单重采 + 重聚 + 重训 A.1**（ckpt 仍看 `hot_f1`，同时盯 precision）。预览用 `collect_preview_n10_1ep.sh`，正式包 `collect_n10_i1_20.sh`（I=1.0 四维），不要混 I=2.0，也不要混 18 根旧包。
2. **A.2**：用已有整单（norm、material I=1.0、machine/logistics 等）按工位聚合瓶颈率画热力图，不必改网络。
3. **A.3**：先把 `cause_acc` 对上 `cause_maj` 和混淆矩阵；确认 `material_shortage` / `transport_delay` / `queue_buildup` 能分维。标签不够就先当分析字段。
4. **再开 B**：从 `job_kpi` / `job_trace` 建产品与工序监督。

