# 模型期望输出

> 日期：2026-08-10  
> 现有模型：BNPDFormer（PDFormer 分数预报 + STGNPP 事件点过程）  
> 数据：异质资源日志 → 窗口特征 / 瓶颈事件 / `job_kpi`

---

## 1. 期望输出（实验维度）

### A. 工位角度


|     | 输出                           | 形式                          | 用途        |
| --- | ---------------------------- | --------------------------- | --------- |
| A.1 | 瓶颈是否发生（开始时间，持续时间，发生在哪个工位/工序） | 0/1（窗口 / 工位）                | 预警        |
| A.2 | 不同环境 setting 对各工位影响          | 热力图（setting × 工位）           | 对比扰动 / 配置 |
| A.3 | 瓶颈原因                         | 多分类（扰动类型 / 阻塞 / 缺料 / 高利用率等） | 指导环境与资源配置 |


**当前问题：当前仿真未覆盖不同 task planning / allocation，所以我们希望从扰动与资源配置敏感性**（机器故障、人力离岗、物流延迟等）的角度指导实际生产。

### B. 产品角度


|     | 输出                 | 形式  | 用途     |
| --- | ------------------ | --- | ------ |
| B.1 | 当前订单能否按时/按期完工      | 0/1 | 指导实际生产 |
| B.2 | 单件产品完工时间           | 分布  |        |
| B.3 | 各工序 / subtask 完工时间 | 分布  | 定位瓶颈工序 |


---



## 2. 当前模型能否实现


| 目标                   | 判定            | 依据                                                     |
| -------------------- | ------------- | ------------------------------------------------------ |
| **A1 瓶颈 0/1**        | **可实现（已接近）**  | 标签 `will_bottleneck` / 事件 NLL 已有；补阈值或分类头即可出 0/1        |
| **A2 setting×工位热力图** | **实验可做，非模型头** | 多 setting 跑仿真 → 按工位聚合瓶颈率/时长即可画图；模型可复用 A1 做跨 setting 推断 |
| **A3 瓶颈原因**          | **标签有、模型无**   | 流水线已有 `root_cause_reason`（扰动类型 + 规则启发式）；需加分类头并评估规则标签噪声 |
| **B1 完工 0/1**        | **暂不可直接出**    | 现网输入是资源窗口特征，无「产品/订单状态」节点；有 `job_kpi`，需新产品级标签与输入        |
| **B2 产品完工时间分布**      | **暂不可直接出**    | 同上；可先做点估计（回归），分布需分位数/混合密度等头 + 足够订单样本                   |
| **B3 小 task 时间分布**   | **暂不可直接出**    | `job_trace` 可派生工序时长标签，但未进 BNPDFormer；需工序级监督与输出头        |


**总评**：工位侧（A）与现模型主线一致，**短期可出汇报结果**；产品侧（B）需新任务定义与标签接入，**不是改 loss 就能立刻有的输出**。

---



## 3. 建议推进顺序（汇报用）

1. **先做 A1 + A2**：多环境 setting 下瓶颈 0/1 / 时长 → 热力图（故事完整、数据已通）。
2. **并行试 A3**：用现成 `root_cause_reason` 训分类头，报告准确率与规则基线对比。
3. **再开 B**：从 `job_kpi` / `job_trace` 建产品与工序监督，单独或加分支预测完工风险与时长。

---



## 4. 一句话结论

> 当前模型擅长 **工位级瓶颈是否发生与何时发生**；**原因分类可加头落地**；**产品/工序完工预测与分布尚未接入，需新数据管线与任务设计。**

