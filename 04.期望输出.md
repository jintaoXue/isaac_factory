# 模型期望输出

> 日期：2026-09-04  
> 现有模型：BNPDFormer（过去 30×60s → 未来 **15 min** 工位事件头 + 占用辅头 + 订单剩余时间 + A.3 原因）  
> STGNPP / 近 180s `will/mark/tts`：**已关**（权重 0，`use_stgnpp=false`）  
> 指标与 baseline 对照口径：[`模型评估指标.md`](./模型评估指标.md)  
> 数据：异质资源日志 → `bn_agg` → `export_dataset` → `episodes.npz`

---

## 1. 期望输出（实验维度）

### A. 工位角度

|     | 输出 | 形式 | 用途 |
| --- | --- | --- | --- |
| A.1 | 瓶颈是否发生（开始时间，持续时间，发生在哪个工位/工序） | 每个监督工位一条事件：`will_block` / `start_min` / `duration_min` | 预警 |
| A.2 | 不同环境 setting 对各工位影响 | 热力图（setting × 工位） | 对比扰动 / 配置 |
| A.3 | 瓶颈原因 | 6 类过程原因（缺料 / 到货延误 / 排队 / 下游堵 / 上游饿 / 高负荷） | 指导环境与资源配置 |

监督工位 = 机台、工作台、龙门、AGV。人与 buffer 只当图输入，不当 A.1 目标。

**当前问题：** 仿真尚未覆盖不同 task planning / allocation，因此从扰动与资源配置敏感性（机器故障、人力离岗、物流延迟等）指导实际生产。

### B. 产品角度

|     | 输出 | 形式 | 用途 |
| --- | --- | --- | --- |
| B.1 | 当前订单能否按时/按期完工 | 0/1 | 指导实际生产 |
| B.2 | 单件产品完工时间 | 分布 | |
| B.3 | 各工序 / subtask 完工时间 | 分布 | 定位瓶颈工序 |

---

## 2. 当前代码和训练能否实现 A

| 目标 | 代码 | 现行状态 | 判定 |
| --- | --- | --- | --- |
| **A.1 瓶颈（开始 / 持续 / 工位）** | **已接。** 主输出是工位事件头。占用图仍训但只辅助对齐连通段。`occupancy_to_events` 可把格子收成段，正式评分用 `station_report_metrics`（工位对且 start 误差 ≤ 3 分钟）。占用 y = `ops_hot_mask`（无监督）或 `node_hot_mask`（有监督）。最短正例 8 分钟。 | 唯一配方 `FactoryBN_dense_f1_p80.json`（`report_f1`，P≥0.80）。旧 `evt` / 画到收工权重作废。 | **接口可用；对照必须走事件口径** |
| **A.2 setting×工位热力图** | **没有这个头。** 多 setting 跑仿真或对 A.1 按工位聚合即可画。 | 画图不依赖网络。 | **实验可做，非模型输出** |
| **A.3 瓶颈原因** | **已接。** `cause_head` 10 类 CE；loss / 报告只监督 6 个过程类。标签 `root_cause_reason`，**不抄** L2 注入名。 | 看 `cause_macro_recall` 与 per-class recall，对照 train 多数类 `cause_majority_acc`。不要把 `cause_acc` 当成果。 | **头在；按 6 类报告** |
| **B.1 / B.2 / B.3** | 未接产品/工序头。`job_kpi` 只用来算订单剩余窗数 `remain_len`，不预测单件完工。 | — | **暂不可直接出** |

**总评：** A.1 的现场交付是「每个工位一条事件」，不是 15×工位热力图。A.2 不是网络输出。A.3 只报过程原因。产品侧 B 仍需新任务。完整指标表见 [`模型评估指标.md`](./模型评估指标.md)。

---

## 3. 建议推进顺序（汇报用）

1. **按短单重采 + 重聚 + 重训 A.1**（ckpt 看 `report_f1`，门限 P≥0.80）。采集用根目录 `train.py`（`HRTPaHC-v1`、`rule_based`），两个停局参数一起写。不要用已删除的 `collect_preview_n10_1ep.sh` / `collect_n10_i1_20.sh` 去覆盖生产链接。
2. **A.2**：用已有整单按工位聚合瓶颈率画热力图，不必改网络。
3. **A.3**：对上 `cause_macro_recall`、per-class recall 和 train 多数类基线。
4. **再开 B**：从 `job_kpi` / `job_trace` 建产品与工序监督。
