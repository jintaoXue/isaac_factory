# 模型评估指标（baseline 对齐协议）

> 日期：2026-09-04  
> 代码：`factory_bn/remain.py`、`factory_bn/train.py::_epoch_loop`、`factory_bn/eval_ckpt.py`、`factory_bn/causes.py`  
> 现行唯一配方：`factory_bn/configs/FactoryBN_dense_f1_p80.json`（`ckpt_metric=report_f1`）  
> 本文是 **PDFormer / BSTAN / LSTM / XGBoost / GCN-GRU** 对照实验的指标与颗粒度唯一口径。模型结构可以不同，**标签、窗、监督节点、匹配规则、指标公式必须相同**。

---

## 0. 对齐原则

1. **比的是同一份报告，不是同一张热力图。** 主任务是：未来 15 分钟内，每个监督工位最多一条事件（会不会堵、堵在哪、几点开始、持续几分钟）。
2. **阈值在 validation 上选，test 冻结。** 我方 `report_threshold_sweep` 只改解码阈值，不改标签。
3. **划分按整局（episode），不按滑窗随机切。** 同一 episode 的相邻窗口不得同时进 train 和 test。
4. **统计只在有效格子上做：** `remain_mask`（未来窗还没出 episode 边界）× `occ_node_mask`（该节点允许当 A.1 目标）。
5. **不要用占用格 F1 / IoU 事件 F1 / 原因 overall acc 当主结论。** 它们每个 epoch 都会打，只作附录。

实现入口：训练结束写 `libcity/cache/model_cache/<标签>/last_metrics.json` 的 `test` 字段；离线复评用 `python -m factory_bn.eval_ckpt`。

---

## 1. 必须对齐的任务契约

| 项目 | 现行值 | 代码 |
| --- | --- | --- |
| 聚合窗 | 60 s = 1 格 = 1 分钟 | `window_size_s=60` |
| 历史窗 | 过去 **30** 格 = 30 分钟 | `input_window=30` |
| 主预报窗 H | 未来 **15** 格 = 15 分钟 | `occupancy_horizon_windows=15` |
| 时间锚点 | 最后一个输入窗的**结束边界** `t` | `dataset._build_samples`：输入 `[t-30, t)`，占用 y `[t, t+H)` |
| 监督节点 | 机台 / 工作台 / 龙门 / AGV | `remain.occupancy_node_mask`；人、buffer 为 0 |
| 工作台拆分 | `resource_id` 含 `workbench` 的机台列单独计 `*_workbench` | `model.occupancy_type_node_masks` |
| 最短正例 | **8** 格；中间 ≤1 格冷洞先填再滤短段 | `hot_min_windows=8`，`hot_gap_windows=1` |
| 占用 y（无监督现行） | 过程启发式 `ops_hot_mask`，**不用** `bottleneck_score` / L2 类型名 | `remain.ops_occupancy_raw` |
| 事件真值 | 预报窗内该工位**最长**一段；已在堵 → `start=0`，`dur`=剩余时长 | `remain.node_event_targets` |
| 进行中判定 | 上一分钟该工位已热 `hist_last_hot=1` | 预测强制 `start=0` |
| 划分 | 按 episode、按 run 前缀分层；`train/val/test ≈ 0.7/0.15/0.15`，`seed=42` | `dataset.split_episodes_by_name` |
| scaler / 多数类 | **只在 train 拟合** | `cause_majority` 来自 train |

`FactoryBN_dense_f1_p80.json` **没有** `event_max_start_windows`，15 分钟窗内全部合格段都计入 who/report。对照时不要自行加 start 上限。

---

## 2. 模型实际会打出的全部指标

`_epoch_loop(..., train=False)` 一次评完会写入 `dict`。下面按 **对照必须报 / 附录 / 已关闭头** 分组。键名与 `last_metrics.json["test"]`、wandb `test_*`、终端缩写一致。

### 2.1 主任务：工位事件报告（必须对齐）

实现：`remain.station_report_metrics`。

每个样本、每个监督工位最多一对 `(will, start_idx, duration_windows)`。

```text
pred_pos = (event_will_prob ≥ τ) ∧ occ_node_mask
true_pos = (窗内最长热段 ≥ 8 格) ∧ occ_node_mask ∧ (start ≤ event_max_start_windows，若启用)
who_tp   = pred_pos ∧ true_pos
report_hit = who_tp ∧ (|start_pred − start_true| ≤ 3)
```

| 键 | 终端缩写 | 公式 / 含义 | 对照要求 |
| --- | --- | --- | --- |
| `who_precision` | `who_p` | `who_tp / n_pred` | 报出来的工位有多少真会堵 |
| `who_recall` | `who_r` | `who_tp / n_true` | 真会堵的工位报出了多少 |
| `who_f1` | — | `2PR/(P+R)` | 只比工位，不管开始时间 |
| `report_precision` | `rep_p` | `report_hit / n_pred` | **工位对且 start 误差 ≤ 3 分钟** |
| `report_recall` | `rep_r` | `report_hit / n_true` | 同上，对真值 |
| `report_f1` | `rep_f1` | `2PR/(P+R)` | **正式主分数** |
| `start_mae` | `st_mae` | `mean(\|start_pred−start_true\|)`，**只在 who_tp 上** | 单位：分钟 |
| `dur_mae` | `dur_mae` | `mean(\|dur_pred−dur_true\|)`，**只在 who_tp 上** | 单位：分钟；进行中是剩余时长 |
| `who_recall_ongoing` | — | 真值 `start=0` 子集的 who 召回 | 已经在堵 |
| `report_recall_ongoing` | `on_r` | 同上，加 start 容差 | 进行中几乎总是 start=0 |
| `start_mae_ongoing` / `dur_mae_ongoing` | — | 进行中 who_tp 上的 MAE | |
| `n_true_ongoing` | — | 进行中真值条数 | |
| `who_recall_upcoming` | — | 真值 `start>0` 子集的 who 召回 | 尚未发生 |
| `report_recall_upcoming` | `up_r` | 同上，加 start 容差 | **当前主缺口** |
| `start_mae_upcoming` / `dur_mae_upcoming` | — | 尚未发生 who_tp 上的 MAE | |
| `n_true_upcoming` | — | 尚未发生真值条数 | |
| `n_pred_who` / `n_true_who` | — | 预报 / 真值事件条数 | 分母 |
| `report_threshold_used` | — | 本轮实际用的 τ | val sweep 后可能 ≠ 配置默认 |

**失败定义（写进论文/组会时用这一句）：** 工位报错，或工位对但开始时间差超过 **3 分钟**，整条报告算错。宁可不报（伤 recall），不要乱报（伤 precision）。

**现行存盘门（`FactoryBN_dense_f1_p80.json`）：**

```text
ckpt_metric = report_f1
ckpt_min_report_precision = 0.80
ckpt_min_report_recall    = 0.35
event_report_threshold    = 0.68
```

`report_threshold_sweep = [0.55, 0.60, …, 0.85]`：在 validation 上找 **P≥0.80 且 F1 最大** 的 τ，test 用同一个 τ。分类型默认 τ（仅解码，不改标签）：

| 类型 | `event_report_threshold_by_type` |
| --- | --- |
| gantry | 0.80 |
| agv | 0.72 |

baseline 可以有自己的 τ，但 **start 容差、最短 8 分钟、who/report 公式不能改**。

---

### 2.2 附录：占用格（15×工位 0/1）

实现：`train._will_metrics`，阈值 `hot_eval_threshold=0.55`。只在 `remain_mask ∧ occ_node_mask` 的格子上算（micro）。

| 键 | 终端缩写 | 含义 |
| --- | --- | --- |
| `hot_precision` / `hot_recall` / `hot_f1` | `hot_p` / `hot_r` / `hot_f1` | 格子 P/R/F1 |
| `hot_ap` | — | 非插值 AP（sklearn 风格） |
| `hot_pos_rate` / `hot_pred_pos_rate` | `true` / `pred` | 真值 / 预报正例率 |
| `hot_precision_<type>` 等 | `m_p` `g_p` `a_p` `w_p` | machine / gantry / agv / workbench |
| `hot_f1_<type>` / `hot_recall_<type>` / `hot_pos_rate_<type>` / `hot_pred_pos_rate_<type>` | — | 分类型 |
| `hot_type_hmean` | `type_h` | 有正例的类型 F1 的调和平均 |

`<type> ∈ {machine, gantry, agv, workbench}`。格子指标**不决定**现行存盘，也不作为模型对照主表。

---

### 2.3 附录：连通段 IoU 事件

实现：`remain.occupancy_event_metrics`。把占用格连成段，同 `resource_id` 且时间 IoU ≥ **0.5** 一对一贪心匹配。

| 键 | 终端缩写 | 含义 |
| --- | --- | --- |
| `event_precision` / `event_recall` / `event_f1` | `ev_p` / `ev_r` / `ev_f1` | IoU≥0.5 的事件 P/R/F1 |
| `event_n_pred` / `event_n_true` | — | 预报 / 真值段数 |

对 8 分钟真事件偏严（开始偏 2 分钟，IoU 就掉到 0.5）。**不要**用它替代 `report_*`。有事件头时，匹配用的是 `event_occ`（will/start/dur 栅格化），阈值 0.5；没有事件头时退回 `hot_prob` + `hot_eval_threshold`。

---

### 2.4 次要任务：订单剩余时间

| 键 | 终端缩写 | 含义 |
| --- | --- | --- |
| `remain_len_mae` | `remain_mae` | `mean(\|remain_len_pred − remain_len\|)`，单位 **分钟（窗）** |

`remain_len` 是「还要多少个 60s 窗才 `jobs_remaining=0`」，**不是**占用图长度，也不截断到 15。不抢存盘。

---

### 2.5 次要任务：过程原因（A.3）

实现：热窗且标签 ≥0，并丢掉 `CAUSE_IGNORE_IN_LOSS`。多数类 **只看 train**。

监督 / 报告的 6 类（`CAUSE_REPORT_CLASSES`）：

| 顺序 | 标签 | 白话 |
| --- | --- | --- |
| 1 | `material_shortage` | 缺料传播 ≥ 0.25 |
| 2 | `transport_delay` | 入站或路径延误 ≥ 20 s |
| 3 | `queue_buildup` | 队列 ≥ 1 或等待 ≥ 20 s 或缓冲占用 ≥ 70% |
| 4 | `blocked_downstream` | blocked 为主且 ≥ 窗长 40% |
| 5 | `starved_upstream` | `starved_time > 0`（前面几类已先匹配） |
| 6 | `high_utilization` | 忙碌比例 ≥ 80% |

不进 loss、不进 macro 的占位类：`score_threshold`、`machine_failure`、`human_unavailable`、`unavailable`。

| 键 | 含义 | 对照要求 |
| --- | --- | --- |
| `cause_recall_material_shortage` 等 6 个 | 该类 TP / support | **要报** |
| `cause_macro_recall` | 6 类中 **support>0** 的 recall 平均 | **要报** |
| `cause_acc` | 上述有效样本上的 overall acc | 仅背景；易被多数类抬高 |
| `cause_majority_acc` | 永远猜 train 多数类的 acc | 必须同时报 |
| `cause_n` | 有效监督样本数 | |

某一类在 test episode 上 support 不足时，只作探索，不下「该类可预测」的结论。

---

### 2.6 已关闭头（日志仍可能出现，对照不要用）

`use_stgnpp=false`，`w_will=w_mark=w_tts=w_event=w_score=0`。无监督现行下这些键常为 0 或无意义。

| 键 | 旧含义 | 处理 |
| --- | --- | --- |
| `will_acc` / `will_precision` / `will_recall` / `will_f1` / `will_ap` / `will_pos_rate` / `will_pred_pos_rate` | 近 **180 s** 图级「会不会有事件」 | **不是** 15 分钟工位 `will_block` |
| `score_mae` | 有监督 `bottleneck_score` 回归 | 无监督不评 |
| `nll` / `nll_arrival` / `nll_surv` | STGNPP 点过程 | 已关 |
| `cluster_acc` | 无监督未来窗 cluster 分类 | 现行 `w_cluster=0`，不当成果 |

终端里的 `will_f1` 指的是这个 **180 s 辅头**，不要和主任务 `event_will_prob` / `who_*` 混读。

---

### 2.7 训练 loss（每个 epoch 的 train 侧，不是对照指标）

`calculate_loss` 还会平均出：`loss`、`loss_hot`、`loss_dice`、`loss_iou`、`loss_cause`、`loss_remain_len`、`loss_event_will`、`loss_event_start`、`loss_event_dur`、`loss_contrast`、`loss_recon`、`loss_cluster` 等。baseline **不必复现 loss**，只需复现 §2.1–2.5 的评测。

---

## 3. 现场推理输出（不是指标，但是同一颗粒度）

`factory_bn.infer` / `BNPredictor.predict_x` 每个监督工位一条：

| 字段 | 对应指标 |
| --- | --- |
| `will_block` | who / report 的预报概率 |
| `start_min` | `start_mae` 的预报（窗下标，分钟） |
| `duration_min` | `dur_mae` 的预报 |
| `ongoing` | 是否由 `hist_last_hot` 判定为进行中 |
| `remain_len_pred` | `remain_len_mae` |
| `cause_name` | A.3 |
| `a1_events` | 占用连通段（附录 IoU 口径） |

---

## 4. baseline 最小对照表（请按此填）

正式横向只填这一张。阈值写明 val 上怎么选。P/R/F1 保留三位小数，MAE 保留两位。

| 模型 | `rep_p` | `rep_r` | `rep_f1` | `who_p` | `who_r` | `on_r` | `up_r` | `st_mae` | `dur_mae` | `remain_mae` | `cause_macro` | `cause_maj` | τ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 我方 PDFormer |  |  |  |  |  |  |  |  |  |  |  |  |  |
| B2 XGBoost |  |  |  |  |  |  |  |  |  |  |  |  |  |
| B3 LSTM |  |  |  |  |  |  |  |  |  |  |  |  |  |
| B4 GCN-GRU |  |  |  |  |  |  |  |  |  |  |  |  |  |
| B5 BSTAN GAT-GRU |  |  |  |  |  |  |  |  |  |  |  |  |  |

可选附录列：`hot_p` / `hot_f1` / `ev_p` / `ev_f1` / 分类型 `m_p,g_p,a_p,w_p`。B0/B1（No-event / Persistence）不实现为模型；若需要背景，只报 prevalence 与 always-negative 的 `rep_p/r`。

---

## 5. 组员实现检查清单

对照实验交数前请自检：

- [ ] 同一 `episodes.npz`（或同一 raw → 同一 `bn_agg` + `export_dataset`）
- [ ] 同一 episode split（读我方 `last_metrics.json` 的 `train_episodes_by_run` / `val_…` / `test_…`，或同一 `seed=42` + `train_rate=0.7`）
- [ ] 输入 30×60s，预报未来 15×60s，锚点在输入结束边界
- [ ] 只监督机台 / 工作台 / 龙门 / AGV；人与 buffer 不进分母
- [ ] 正例最短 8 分钟；进行中 `start=0`、时长用剩余
- [ ] who = 工位对；report = 工位对 **且** start 误差 ≤ 3 分钟
- [ ] `start_mae` / `dur_mae` 只在工位对的样本上平均
- [ ] 拆 ongoing / upcoming
- [ ] 原因只报 6 个过程类的 per-class recall + macro；majority 来自 **train**
- [ ] 不把格子 F1、IoU F1、180s `will_f1`、`cause_acc` 写进主表
评测可直接调我方函数，避免各写各的匹配：

```python
from factory_bn.remain import station_report_metrics, occupancy_event_metrics, node_event_targets
```

`y_hot` 形状 `(B, 15, N)`，`will_prob/start_idx/dur` 形状 `(B, N)`，再传入 `remain_mask`、`occ_node_mask`。

---

## 6. 现行配方与历史配方（避免拿错表）

| 配方 | 配置 | 主指标 | 备注 |
| --- | --- | --- | --- |
| **现行（唯一保留）** | `FactoryBN_dense_f1_p80.json` | `report_f1`，P≥0.80，R≥0.35 | τ=0.68，gantry/AGV 分类型阈值；无 start 上限 |

历史数字（只作背景，不是本次对照目标）：unsup_best Test `rep_p=0.817 / rep_r=0.447 / rep_f1=0.578`。

---

## 7. 相关文档

- 任务与期望输出：[`04.期望输出.md`](./04.期望输出.md)
- 模型实现：[`06.PDFormer+STGNPP实现.md`](./06.PDFormer+STGNPP实现.md)
- 采集 / 训练命令：[`00.运行方式.md`](./00.运行方式.md)
- 实验设计：[`BN实验设计note.md`](./BN实验设计note.md)
- 与 BSTAN 共享实验：[`PDFormer-BSTAN共享实验七点设计评审.md`](./PDFormer-BSTAN共享实验七点设计评审.md)
