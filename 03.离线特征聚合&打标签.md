# 离线瓶颈特征与标签流水线（阶段 C）

> 日期：2026-08-13（相对同日上午：耦合特征局部化、TPM/活跃期检测、L2 与 score 事件不再合并 onset）  
> 对应规范：`01.瓶颈数据采集规范.md` §7–§8 阶段 C  
> 脚本：`tools/bn_agg/`（`cd tools && python3 -m bn_agg`）  
> 相关：`03.实验设计_瓶颈维度与扰动.md`、`00.运行方式.md`（Stage-C → 训练导出）  
> 模型：BNPDFormer = PDFormer 稠密分数 + STGNPP 逐节点事件；标签与特征按这两路需求写。

---

## 1. 做什么

在仿真侧原始表采集（阶段 A/B）已落盘后，**离线**只读 CSV/JSONL，做阶段 C：

1. 按逻辑时间窗聚合每资源运营特征，计算 `bottleneck_score`
2. 生成窗口级标签（当前瓶颈节点、未来是否出事件等）
3. 生成瓶颈事件表：热窗按节点合并 **∪** 已完成 L2 区间（**两条 onset 不合并**，供 STGNPP）
4. 从 `job_trace` 汇总订单级 KPI（makespan / cycle / 吞吐）

**不在仿真内写派生表。** 无 Isaac 依赖，可单独 `python3` 跑。

事件双源的原因：仅靠分数时，龙门宕机等 L2 可能不够尖。L2 仍写入事件表，但**不与 score 事件合并 onset**，以免 STGNPP 的起始时刻被抹平。L2 同时写入节点特征 `disturbance_active_s` 给 PDFormer。

---

## 2. 输入 / 输出

### 2.1 输入目录（两种布局）

脚本会自动发现：

| 布局 | 路径示例 |
|------|----------|
| 嵌套（当前常用） | `<run_dir>/episode_00/env_00/` … |
| 旧扁平 | `<run_dir>/env_00/` |

每个 `env_*` 目录读取：

| 文件 | 用途 |
|------|------|
| `resource_event_log.jsonl` | 资源状态时间轴 |
| `job_trace.csv` | 排队 / 工单起止 |
| `buffer_event_log.csv` | Buffer 占用 |
| `route_transport_task.csv` | 物流延时 |
| `material_inventory_log.csv` | 缺料 |
| `episode_config.csv` | `run_id` / `env_id` / `episode_id` |
| `disturbance_log.csv` | L2 扰动区间（可缺省；缺则仅 score 事件） |

### 2.2 输出

默认写到 `<run_dir>/derived/`，并**镜像** episode 嵌套：

```text
<run_dir>/derived/
├── all_env_summary.json
└── episode_00/env_00/          # 或扁平时 env_00/
    ├── window_feature_table.csv
    ├── bottleneck_label.csv
    ├── bottleneck_event.csv
    ├── job_kpi.csv
    └── pipeline_summary.json
```

---

## 3. 如何运行

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools

python3 -m bn_agg \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --window_sizes 30,60 \
  --horizon 180 \
  --score_threshold 0.55 \
  --min_event_windows 2
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `--run_dir` | 必填 | 含 `env_*` 或 `episode_*/env_*` 的 run 目录 |
| `--env_id` | 全部 | 只处理 `env_{id:02d}` |
| `--window_sizes` | `30,60` | 逻辑秒窗口（`logic_dt=1` 时等同步数）。训练导出默认只用 **60** |
| `--horizon` | `180` | 预测标签未来视野 H（逻辑秒）；旧文档/旧命令里的 60 已废弃 |
| `--score_threshold` | `0.55` | 热窗分数阈值；还需满足绝对活动门槛（见 §4.5） |
| `--min_event_windows` | `2` | score 合并成事件所需的最少连续高分窗数 |
| `--out_dir` | `<run_dir>/derived` | 输出根目录 |

终端会打印每个 env 的 `features / labels / events` 与订单 KPI；多 env 时另写 `all_env_summary.json`。

---

## 4. 算法说明

### 4.1 资源状态时间轴

从 `resource_event_log.jsonl` 按 `resource_id` 排序，离散事件 → 连续区间 `[t_i, t_{i+1}) → to_state`。窗口内求交得到：

- `active_pct_s`：`PROCESSING` 占比  
- `blocked_time_s`：`BLOCKED` 时长  
- `starved_time_s`：`STARVED` / `WAITING` 时长  
- `stop_time_s` / `unavailable_pct_s`：`STOP` 时长与占比（L2 宕机、L0 关掉的 ws1）  
- `current_active_duration_s`：窗末向前连续 PROCESSING 时长  

### 4.2 队列与等待

- `job_trace` 的 `queue_enter` / `queue_leave` 配对  
- **挂到下游工位**：若 `output_buffer_id` 以 `num` 开头（下一台机器），等待记在 `{machine}_ws0`，而不是正在完工的 `station_id`  
- 例：切管结束等坡口时，队列记在坡口机，不记在切管机  
- `avg_waiting_time_s`、`queue_length_s`（buffer 仍用占用均值）；`departure` 仍挂在当前 `station_id`

### 4.3 Buffer / 物流 / 缺料（节点局部，供 PDFormer 图注意力）

- Buffer：自身 `occupancy_ratio_s`、`queue_growth_rate_s`；按 `BUFFER_MACHINE_AFFINITY` 把占用写到相关机台的 `affiliated_buffer_occ_s`（BSTAN 边权思想）
- `route_delay_s`：**只写到执行该运次的** `carrier_id`（龙门/人/车），不再整窗广播
- `inbound_wait_s`：运次终点工位（`to_node` 映射到 `_ws0`）上的到货等待
- `material_shortage_propagation_s`：只写消耗点——管坯→切管 `ws0`，法兰/弯头→工作台 `ws0`

### 4.4 上下游耦合（工艺邻接，不再用全局占比）

- `upstream_blocked_ratio_s`：`PROCESS_NEIGHBORS` 中**直接上游**本窗 BLOCKED 占比（多上游取均值）
- `downstream_starved_ratio_s`：直接下游 STARVED/WAITING 占比
- 工作台 / 环焊双工位是并联 sibling（Lai TPM），共用同一对上下游，不当串行
- 链首无上游：用本站饥饿占比；链尾无下游：用本站阻塞占比
- 人、龙门、buffer：这两项为 0（不套工艺链）
- `tb_minus_ts_s`：`(blocked − starved) / window`，拐点法输入

### 4.5 瓶颈分数（PDFormer 稠密回归目标）

同一窗口内跨资源 min-max 归一化（`unavailable_pct_s` / `active_pct_s` 已是 0–1，不再 min-max）：

```text
bottleneck_score_s =
    0.20 * norm(queue_length_s)
  + 0.15 * norm(avg_waiting_time_s)
  + 0.20 * active_pct_s
  + 0.10 * norm(current_active_duration_s)
  + 0.10 * upstream_blocked_ratio_s
  + 0.10 * downstream_starved_ratio_s
  + 0.15 * unavailable_pct_s
```

权重未改；上下游项已是邻接量，分数对节点可分，才能给 PDFormer 的 geo/semantic 头用。

**当前窗位置** `bottleneck_node_t`（不是 score argmax）：

1. 机台/龙门中 `is_momentary_bn`（当前连续 PROCESSING 最长，Roser / IJPR DT Active Period）
2. 否则 `is_turning_point`（上游 TB−TS>0 且本站 <0，且本站有阻塞或饥饿）
3. 否则 score argmax

**热窗**（`is_bottleneck_window` / STGNPP 的 score 事件）任一节点满足：

- `is_turning_point`，或
- 本窗分数峰值（`is_window_peak`，峰值 ≥ 0.20）且有绝对活动，或
- `bottleneck_score_s ≥ 0.55` **且** 绝对活动

`is_momentary_bn` 每个窗都会有一个最长活跃资源，**不单独当事件**（否则 STGNPP 每窗都有 onset）。它只用于 `bottleneck_node_t` 和 FEATURE_COLS。

L0 关掉且无人排队的 ws1：`unavailable=1` 但不是 momentary/TP，分数约 0.15，**不会**整局都标成事件。

`disturbance_active_s`：本窗与该节点已完成 L2 区间重叠则为 1，作为 PDFormer 输入（扰动是特征，不是唯一标签）。

### 4.6 瓶颈事件（STGNPP：双源、不合并 onset）★

```text
bottleneck_event
  = 按节点合并的热窗事件（score / TP / 活跃期）
  ∪ disturbance L2 事件（对齐到时间窗后）
```

同一节点上 score 与 L2 **时间重叠也保留两条**，不合成 `event_source=both`。STGNPP 按节点吃 `(onset, duration)` 序列，合并会抹掉起始时刻。

**（1）热窗合并**

- 按**节点**连续 ≥ `min_event_windows` 个热窗（§4.5）
- `event_source = "score"`（含 TP / momentary 触发的热窗）

**（2）Disturbance L2**

从 `disturbance_log.csv` 只取**已完成**区间，类型仍为 `machine_failure` / `human_unavailable` / `transport_delay` / `material_shortage`。  
**不并入**：`quality_hold`。  
`target_resource_id` 映射到特征表节点（组名→`_ws0`，SKU→消耗工位）。  
`event_source = "disturbance_log"`。

**（3）编号**

- 两源拼接后按 `(window_size, start_s, resource_id)` 排序，重编号 `event_id`

### 4.7 窗口预测标签（PDFormer will / mark 辅头）

对每个窗口起点 `t`、视野 `H`（用**未合并 onset** 的 events）：

| 字段 | 含义 |
|------|------|
| `will_bottleneck` | `(t, t+H]` 内是否有事件起点（图级，对 will_head） |
| `future_bottleneck_object_id` / `type` | 该视野内**最早**事件的资源（对 mark_logits） |
| `time_to_start` / `duration` | 距该事件起点、事件时长 |

STGNPP 训练不读这两列，直接用 `bottleneck_event.csv` 按节点构造历史/下一次事件。

### 4.8 `root_cause_reason`

1. 若本窗与某条 `event_source=disturbance_log` 的事件时间重叠 → 填其 `disturbance_type`
2. 否则且 `is_bottleneck_window`：规则启发式  
   `blocked_downstream` / `starved_upstream` / `unavailable` / `high_utilization` / `queue_buildup` / `score_threshold`

### 4.9 订单 KPI（`job_kpi.csv`）

| 定义 | 来源 |
|------|------|
| 开工 | 首个 `job_selected` |
| 完工 | `stage_complete`；缺则回退涂装 `process_end` |
| `cycle_time_s` | 完工 − 开工 |
| `order_makespan_s` 等 | 写入 `pipeline_summary.json` 的 `order_kpi` |

---

## 5. `pipeline_summary.json` 要点

除行数、热点节点、`will_bottleneck_rate` 外，现含：

| 字段 | 含义 |
|------|------|
| `n_disturbance_l2` | 解析到的 completed L2 区间数 |
| `n_events_from_disturbance` | `event_source` 为 `disturbance_log` 的事件数（与 score 事件分开计数） |
| `order_kpi` | makespan、mean/median cycle、吞吐等 |

无扰动 run：`n_disturbance_l2 = 0`，行为退化为「仅 score 事件」（与初版一致）。

---

## 6. 字段速查

### `window_feature_table.csv`

`run_id, env_id, window_index, window_start_s, window_end_s, window_size_s, resource_id, resource_type, queue_length_s, avg_waiting_time_s, occupancy_ratio_s, queue_growth_rate_s, active_pct_s, current_active_duration_s, blocked_time_s, starved_time_s, stop_time_s, unavailable_pct_s, inter_departure_var_s, upstream_blocked_ratio_s, downstream_starved_ratio_s, route_delay_s, inbound_wait_s, material_shortage_propagation_s, affiliated_buffer_occ_s, tb_minus_ts_s, disturbance_active_s, is_turning_point, is_momentary_bn, bottleneck_score_s, norm_*`

### `bottleneck_label.csv`

`window_*, bottleneck_node_t, bottleneck_type_t, bottleneck_score_t, is_bottleneck_window, horizon_s, will_bottleneck, future_bottleneck_object_id, future_bottleneck_type, time_to_start, duration, root_cause_reason`

### `bottleneck_event.csv`

`event_id, run_id, env_id, window_size_s, resource_id, resource_type, start_window_index, end_window_index, start_s, end_s, duration_s, max_score, n_windows, event_source, disturbance_id, disturbance_type`

`event_source` 为 `score` 或 `disturbance_log`（不再写 `both`）。

### `job_kpi.csv`

`run_id, env_id, episode_id, job_id, product_type, start_s, complete_s, cycle_time_s, completed, complete_source`

---

## 7. 与实验 / 训练的关系

```text
仿真采集（含可选 --disturbance_dim）
  → bottleneck_dataset/<run_id>/episode_*/env_*/
       原始表 + disturbance_log.csv
  → 本脚本 Stage-C
       derived/.../window_feature_table + label + event + job_kpi
  → factory_bn.export_dataset → 训练张量
  → BNPDFormer / 基线训练
```

- **无扰动 / G0**：事件仍可能偏稀，属负载问题，不是管道失败。  
- **有 L2 扰动**（human / logistics / machine / material）：即使分数分散，事件表也应出现对应 L2（`quality_hold` 除外）；可用 `n_events_from_disturbance` 自检。  
- 下游训练导出见 `00.运行方式.md`「离线聚合 → 导出 → 训练」。

---

## 8. 已知局限

1. PDFormer 稠密头仍回归 `bottleneck_score_s`，尚未改成直接回归 blocked/starved（BSTAN 原设定）
2. `quality_hold` 只在原始 `disturbance_log`，不进 `will_bottleneck`
3. G0 弱负载下正样本仍可能稀少，训练需 G1 负载或扰动 run
4. `PROCESS_NEIGHBORS` 与 `graph.py` 的 `PROCESS_CHAIN` 需手工同步

## 9. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-13 晚 | 耦合改为工艺邻接；物流/缺料不再广播；TPM + 最长活跃期；L2 作 `disturbance_active_s` 且事件不与 score 合并 onset；导出 FEATURE_COLS 增列。对齐 PDFormer 图注意力与 STGNPP 逐节点事件 |
| 2026-08-13 | STOP/`unavailable_pct_s` 进分数；热窗加绝对活动门槛；队列挂下游 `output_buffer_id`；`quality_hold` 不进事件并集；默认 horizon **180** |
| 2026-08-10 | 并入 `disturbance_log` L2 → 事件双源；`event_source` / 扰动优先 root cause；支持 `episode_*/env_*`；增加 `job_kpi` 与 summary 中扰动统计；文档按当时代码重写 |
| 2026-07-18 | 初版：窗口特征 + score 事件 + 标签；在 `2026-07-18_17-46-35_seed42` 跑通 |
