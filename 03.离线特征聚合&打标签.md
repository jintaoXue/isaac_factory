# 离线瓶颈特征与标签流水线（阶段 C）

> 日期：2026-08-21（扰动=环境上下文，瓶颈=过程热窗；占用 y 与 STGNPP 对齐；L2 更少更长）  
> 对应规范：`01.瓶颈数据采集规范.md` §7–§8 阶段 C  
> 脚本：`tools/bn_agg/`（`cd tools && python3 -m bn_agg`）  
> 相关：`02.瓶颈维度&环境扰动.md`、`00.运行方式.md`（Stage-C → 训练导出）、[`06.PDFormer+STGNPP实现.md`](./06.PDFormer+STGNPP实现.md)、[`08.边采边聚合与在线推理.md`](./08.边采边聚合与在线推理.md)（`--follow` 闭窗 sidecar）  
> 模型：BNPDFormer = 共享编码器 + **固定 H=15 min 占用头** + **订单剩余时间** + **STGNPP 稀疏事件** + will/mark/cause 辅头。

---

## 1. 做什么

在仿真侧原始表采集（阶段 A/B）已落盘后，**离线**只读 CSV/JSONL，做阶段 C：

1. 按逻辑时间窗聚合每资源运营特征，计算 `bottleneck_score`
2. 生成窗口级标签（当前瓶颈节点、未来是否出事件等）
3. 生成瓶颈事件表：工艺链 **TPM 拐点**、工位 WIP 堆积、组焊台**缺料饥饿**、**到货延误饥饿**、**上游堵死**，按节点合并（供 STGNPP）
4. 从 `job_trace` 汇总订单级 KPI（makespan / cycle / 吞吐）

**不在仿真内写派生表。** 无 Isaac 依赖，可单独 `python3` 跑。

**扰动 ≠ 瓶颈。** `disturbance_log` 里的 L2（宕机、人离岗、物流停、藏料）是**环境配置 / 注入条件**，写入节点特征 `disturbance_active_s` 以及 `episode_config` 的 dim/intensity，给模型当输入。瓶颈是生产过程里自然出现的过程性现象（队列堆积、上游阻塞、下游饥饿、工艺链拐点）。STGNPP / will / mark / cause **只监督过程现象**，不把 L2 区间本身当成一次瓶颈事件。模型要学的是：**不同环境扰动下会冒出怎样的瓶颈现象**。

### 1.1 哪些列给哪个模型

共享编码器读的是同一套窗口特征 **X**。两条主路径和辅头的**监督标签**不同：

| 模型 / 头 | 学什么 | 监督来自 | 不学什么 |
|---|---|---|---|
| **PDFormer 占用**（稠密，A.1） | 未来 **H=15 分钟**（15 个 60s 格）每窗每工位堵不堵 | 未来 H 内各窗 `bottleneck_score_s` + `node_hot_mask` 0/1（拐点 / 排队 / 缺料饥饿 / 到货延误 / 上游堵 / `score≥0.55`）；解码器宽度 `occupancy_horizon_windows=15` | 不把 L2 `disturbance_active_s` 当占用 y；**不**画到收工 |
| **订单剩余时间** | 还要多少个 60s 格收工 | `remain_len`（`job_kpi` → `jobs_remaining`） | 不是占用图 |
| **STGNPP**（稀疏） | 下一次过程热窗何时开始、持续多久、在哪个节点 | `bottleneck_event.csv` 按节点的 `(onset, duration)`（`row_is_hot`，**不含**单纯高分） | 日常加工、库存、窗内峰值、注入的 L2 本身 |
| **will 辅头** | 未来 H=180s 内会不会出现上述事件 | `bottleneck_label.will_bottleneck` | 不是 A.1 主输出 |
| **mark 辅头** | 若会发生，最早落在谁 | `future_bottleneck_object_id` | |
| **cause 辅头（A.3）** | 该窗原因 10 类 | `root_cause_reason`（空字符串不监督） | |
| **job_kpi** | 只提供剩余工单数（A.1 条件） | `jobs_remaining` 序列 | 不预测单件/订单完工（B 未接） |

输入特征（§4.1–4.4 的运营列、`is_momentary_bn`、`is_window_peak`、buffer 占用等）**两条路径都看见**，但只有标了「监督」的列进对应 loss。

---

## 2. 输入 / 输出

### 2.1 输入目录（两种布局）

脚本会自动发现：

| 布局 | 路径示例 |
|------|----------|
| 嵌套（当前常用） | `<run_dir>/episode_00/env_00/` … |
| 旧扁平 | `<run_dir>/env_00/` |

每个 `env_*` 目录读取：

| 文件 | 用途 |
|------|------|
| `resource_event_log.jsonl` | 资源状态时间轴 |
| `job_trace.csv` | 排队 / 工单起止 |
| `buffer_event_log.csv` | Buffer 占用 |
| `route_transport_task.csv` | 物流延时 |
| `material_inventory_log.csv` | 缺料 |
| `episode_config.csv` | `run_id` / `env_id` / `episode_id` |
| `disturbance_log.csv` | L2 扰动区间 → 仅填 `disturbance_active_s`（可缺省） |

### 2.2 输出

默认写到 `<run_dir>/derived/`，并**镜像** episode 嵌套：

```text
<run_dir>/derived/
├── all_env_summary.json
└── episode_00/env_00/
    ├── window_feature_table.csv   # 输入 X + PDFormer 的 score 标签
    ├── bottleneck_label.csv       # will / mark / cause 辅头；热窗标记
    ├── bottleneck_event.csv       # STGNPP 事件序列
    ├── job_kpi.csv                # 导出 jobs_remaining（A.1 地平线）；不学产品侧 B
    └── pipeline_summary.json      # 质检，不进模型
```

---

## 3. 如何运行

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools

python3 -m bn_agg \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --window_sizes 30,60 \
  --horizon 180 \
  --score_threshold 0.55 \
  --min_event_windows 1
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `--run_dir` | 必填 | 含 `env_*` 或 `episode_*/env_*` 的 run 目录 |
| `--env_id` | 全部 | 只处理 `env_{id:02d}` |
| `--window_sizes` | `30,60` | 逻辑秒窗口（`logic_dt=1` 时等同步数）。训练导出默认只用 **60** |
| `--horizon` | `180` | 预测标签未来视野 H（逻辑秒）；旧文档/旧命令里的 60 已废弃 |
| `--score_threshold` | `0.55` | 保留 CLI；**不再**用高分单独打 STGNPP 事件 |
| `--min_event_windows` | `1` | 连续拐点窗合并成 score 事件的最少窗数（孤立拐点也算一次 onset） |
| `--out_dir` | `<run_dir>/derived` | 输出根目录 |

终端会打印每个 env 的 `features / labels / events` 与订单 KPI；多 env 时另写 `all_env_summary.json`。

---

## 4. 算法说明

### 4.1 资源状态时间轴（**两路输入特征**）

从 `resource_event_log.jsonl` 按 `resource_id` 排序，离散事件 → 连续区间 `[t_i, t_{i+1}) → to_state`。窗口内求交得到：

- `active_pct_s`：`PROCESSING` 占比  
- `blocked_time_s`：`BLOCKED` 时长  
- `starved_time_s`：`STARVED` / `WAITING` 时长  
- `stop_time_s` / `unavailable_pct_s`：`STOP` 时长与占比（L2 宕机、L0 关掉的 ws1）  
- `current_active_duration_s`：窗末向前连续 PROCESSING 时长  

### 4.2 队列与等待（**两路输入特征**）

- `job_trace` 的 `queue_enter` / `queue_leave` 配对  
- **挂到下游工位**：若 `output_buffer_id` 以 `num` 开头（下一台机器），等待记在 `{machine}_ws0`，而不是正在完工的 `station_id`  
- 例：切管结束等坡口时，队列记在坡口机，不记在切管机  
- `avg_waiting_time_s`、`queue_length_s`（**仅工位 WIP**；buffer 库存只写 `occupancy_ratio_s`，不进 `queue_length_s`）；`departure` 仍挂在当前 `station_id`

### 4.3 Buffer / 物流 / 缺料（**两路输入特征**，给 PDFormer 图注意力）

- Buffer：自身 `occupancy_ratio_s`、`queue_growth_rate_s`；按 `BUFFER_MACHINE_AFFINITY` 把占用写到相关机台的 `affiliated_buffer_occ_s`（BSTAN 边权思想）
- `route_delay_s`：**承运方** `carrier_id`（龙门/人/车）在本窗的等待（含未完成运次），不再整窗广播
- `inbound_wait_s`：运次终点（`to_node`）上的窗内等待；若终点是库区，再摊到该库区亲和工位里**本窗 STARVED/WAITING** 的机台。这样冻车时饥饿工位才能打出 `transport_delay`，而不是一律 `starved_upstream`
- `material_shortage_propagation_s`：只写消耗点——管坯→切管 `ws0`，法兰/弯头→工作台 `ws0` 和 `ws1`。套件 SKU 只统计已到坡口/组焊阶段的行（`finished_task` ∈ grooving / logistic_for_batch_spot_welding / batch_spot_welding），仓库快照不进均值；多 SKU 取 max

### 4.4 上下游耦合（**两路输入特征**；拐点检测也用）

- `upstream_blocked_ratio_s`：`PROCESS_NEIGHBORS` 中**直接上游**本窗 BLOCKED 占比（多上游取均值）
- `downstream_starved_ratio_s`：直接下游 STARVED/WAITING 占比
- 工作台 / 环焊双工位是并联 sibling（Lai TPM），共用同一对上下游，不当串行
- 链首无上游：用本站饥饿占比；链尾无下游：用本站阻塞占比
- 人、龙门、buffer：这两项为 0（不套工艺链）
- `tb_minus_ts_s`：`(blocked − starved) / window`，拐点法输入

### 4.5 瓶颈分数（**仅 PDFormer 稠密回归目标**）

同一窗口内 queue / wait / 活跃时长做跨资源 min-max；`active_pct`、`unavailable_pct`、stall 已是 0–1：

```text
stall_pct = (blocked_time_s + starved_time_s) / window

bottleneck_score_s =
    0.15 * norm(queue_length_s)          # 工位 WIP，不含仓库库存
  + 0.10 * norm(avg_waiting_time_s)
  + 0.20 * stall_pct                     # 阻塞+饥饿（过程现象）
  + 0.10 * active_pct_s
  + 0.05 * norm(current_active_duration_s)
  + 0.10 * upstream_blocked_ratio_s
  + 0.10 * downstream_starved_ratio_s
  + 0.00 * unavailable_pct_s             # STOP 是扰动/配置，不进 y
```

每个节点每个窗都有分数 → **只作为 PDFormer 的 y**。STGNPP **不**回归这个分数。分数表达的是相对拥堵（队列、阻塞、饥饿、上下游耦合），不是「谁在加工」，也不是「谁被注入了 L2」。`unavailable_pct_s` / `stop_time_s` / `disturbance_active_s` 仍在 **X** 里，模型可以看见环境。

**当前窗位置** `bottleneck_node_t`（分析/summary 用，**不是** PDFormer 或 STGNPP 的训练目标）：

1. 本窗 STGNPP 热节点（拐点）里分数最高
2. 否则 `is_turning_point`
3. 否则 `is_momentary_bn`（最长 PROCESSING）
4. 否则 score argmax

**不再**把 `disturbance_active_s=1` 的节点当成瓶颈位置。

**热窗** `is_bottleneck_window` / STGNPP `row_is_hot`（`features.py`；**不是** PDFormer 分数是否存在）：

- 任一节点 `is_turning_point`
- 或工位 `queue_length_s≥2` 且 stalled（`blocked+starved>0`）
- 或组焊台 `material_shortage_propagation_s≥0.25` 且本窗饥饿 ≥ 一半窗口
- 或工位 `inbound_wait_s≥20` 或 `route_delay_s≥20`，且饥饿 ≥ 30% 窗口
- 或工位 `blocked_time_s≥40%` 窗口且 `queue_length_s≥1`
- 或机台耦合 stall：`blocked+starved≥40%` 且（上下游耦合 / 到货延误 / 缺料 / 队列≥1）
- 或机台过程停机：`unavailable_pct≥0.5` **且** `disturbance_active≥0.5`（L2 已注入且工位实际 STOP；L0 关掉的 ws1 只有 unavailable，不当事件）

人 / buffer 不当热窗。龙门 / AGV 只在 **到货或路径延迟 ≥20s 且 stall ≥30% 窗口** 时打正（冻住无延迟仍是 X）。连续热窗合并时允许隔 **1** 个冷窗。

**占用 y** `node_hot_mask`（`factory_bn/remain.py`）= 机台过程规则 **或** `bottleneck_score_s≥0.55`，再加上延迟+停顿的龙门 / AGV。`occ_node_mask` 监督 machine+gantry+AGV。仅有列 18 `disturbance_active_s`、没有 STOP，**不**进占用 y。

**不当热窗、只当两路输入特征**：`is_momentary_bn`、`is_window_peak`、日常 stall、仓库库存、L2 区间。`score≥0.55` 单独打占用格子，**不再**单独打 STGNPP 事件。

门槛常量集中在 `tools/bn_agg/constants.py`（`HOT_*`），须与 `remain.py` 手写常数同步。

L0 闲置 ws1：`unavailable_pct_s` 仍在 X 里，但不再抬高该节点分数，也没有拐点 → 不是事件。

### 4.6 瓶颈事件（**仅 STGNPP 主监督**；过程热窗，不含 L2）★

```text
bottleneck_event
  = 按节点合并的热窗（TPM 拐点 / WIP 堆积 / 缺料饥饿 / 到货延误饥饿 / 上游堵死；event_source=score）
```

注入的 L2 **不写进这张表**。龙门 / AGV **冻住无延迟**、人离岗本身不是瓶颈；延迟且停顿的承运才打过程事件。若 L2 只在工艺链上挤出阻塞/饥饿，拐点事件会自然出现。**STGNPP** 按节点吃 `(onset, duration)`。PDFormer 不读这张表当 y。

**热窗合并**

- 工艺链 TPM：上游 TB−TS>0 且本站 <0，且本站有阻塞或饥饿
- 以及 `row_is_hot` 的排队 / 缺料 / 到货延误 / 上游堵死（buffer 永不进 score 事件）
- 按节点连续 ≥ `min_event_windows`（默认 **1**，孤立热窗也是一次 onset）

**编号**

- 按 `(window_size, start_s, resource_id)` 排序，重编号 `event_id`
- 列里仍留 `event_source`（恒为 `score`）、`disturbance_id` / `disturbance_type`（空），以免旧导出脚本断列

### 4.7 窗口预测标签（**will / mark 辅头**；标签来自 STGNPP 事件，不是 PDFormer 分数）

对每个窗口起点 `t`、视野 `H`（过程拐点事件）：

| 字段 | 含义 | 谁学 |
|------|------|------|
| `will_bottleneck` | `(t, t+H]` 内是否有事件起点 | **will 辅头** |
| `future_bottleneck_object_id` / `type` | 该视野内**最早**事件的资源 | **mark 辅头** |
| `time_to_start` / `duration` | 距该事件起点、事件时长 | 导出保留；STGNPP 主路径用事件表自己算 τ / d |

**STGNPP 主训练不读** will/mark 这两列，直接用 `bottleneck_event.csv` 按节点构造历史/下一次事件。will/mark 只是共享编码器上的辅助分类。

### 4.8 `root_cause_reason`（**仅 cause 辅头 / A3**）

仅当 `is_bottleneck_window` 时填过程启发式（优先序，门槛见 `constants.py`）：

1. `material_shortage`：`shortage_propagation≥0.25`
2. `transport_delay`：`inbound_wait≥20` 或 `route_delay≥20`
3. `queue_buildup`：`queue≥1.0` 或 `avg_waiting≥20` 或 buffer 占用 ≥0.70
4. `blocked_downstream`：本窗 blocked ≥ starved 且 blocked > 40% 窗口
5. `starved_upstream`：`starved_time>0`
6. `high_utilization`：`active_pct≥0.8`
7. `score_threshold`：兜底

**不再**用 `disturbance_type`（`machine_failure` 等）当原因。扰动是 X，不是 y。TPM 拐点按定义常带饥饿，所以上面缺料 / 到货 / 排队必须排在 `starved_upstream` 之前，否则原因会塌成一类。

BNPDFormer **A3** 头用这一列做分类（`factory_bn.causes.ROOT_CAUSE_CLASSES` 仍保留旧 L2 类名以免旧 npz 错位；新标签不会写出那些类）。空字符串不监督。导出进 `episodes.npz` 的 `{episode}_cause`。

### 4.9 订单 KPI（`job_kpi.csv`，**当前两个模型都不学**）

| 定义 | 来源 |
|------|------|
| 开工 | 首个 `job_selected` |
| 完工 | `stage_complete`；缺则回退涂装 `process_end` |
| `cycle_time_s` | 完工 − 开工 |
| `order_makespan_s` 等 | 写入 `pipeline_summary.json` 的 `order_kpi` |

---

## 5. `pipeline_summary.json` 要点

除行数、热点节点、`will_bottleneck_rate` 外，现含：

| 字段 | 含义 |
|------|------|
| `n_disturbance_l2` | 解析到的 L2 区间数（**仅特征上下文**，含 follow 时截断的开区间） |
| `n_events_from_disturbance` | 应为 **0**（L2 不再进事件表；留下作自检） |
| `order_kpi` | makespan、mean/median cycle、吞吐等 |

无扰动 run：`n_disturbance_l2 = 0`，事件只来自过程拐点。有扰动 run：L2 进 `disturbance_active_s`，事件仍只来自拐点；若扰动没有在工艺链上打出拐点，事件可以是 0，这不是管道失败。

---

## 6. 字段速查（附：谁用）

### `window_feature_table.csv`

一行 = 一个资源 × 一个时间窗。除 `bottleneck_score_s` 外，下列运营列都是 **PDFormer 与 STGNPP 的共享输入 X**（导出进 `episodes.npz` 的 features；**不含** score，防泄漏）。

| 字段 | 谁用 |
|------|------|
| `queue_length_s` … `tb_minus_ts_s`、`disturbance_active_s` | **两路输入** |
| `is_turning_point` | **两路输入**；为 1 时还触发 STGNPP 拐点事件 |
| `is_momentary_bn` | **仅输入特征**（Roser 最长活跃期），不当事件 |
| `is_window_peak`（若写出） | **仅输入特征**，不当事件 |
| `bottleneck_score_s`、`norm_*` | **PDFormer 占用头的 y**（剩余视界上各窗分数 / 热窗） |

列顺序：`run_id, env_id, window_index, window_start_s, window_end_s, window_size_s, resource_id, resource_type, queue_length_s, avg_waiting_time_s, occupancy_ratio_s, queue_growth_rate_s, active_pct_s, current_active_duration_s, blocked_time_s, starved_time_s, stop_time_s, unavailable_pct_s, inter_departure_var_s, upstream_blocked_ratio_s, downstream_starved_ratio_s, route_delay_s, inbound_wait_s, material_shortage_propagation_s, affiliated_buffer_occ_s, tb_minus_ts_s, disturbance_active_s, is_turning_point, is_momentary_bn, bottleneck_score_s, norm_*`

### `bottleneck_label.csv`

一行 = 一个时间窗（图级）。**PDFormer 主头不读这张表。**

| 字段 | 谁用 |
|------|------|
| `bottleneck_node_t` / `type_t` / `score_t` | 分析 / summary，不进 loss |
| `is_bottleneck_window`、`n_hot_nodes` | 质检；will/cause 是否处于「有瓶颈」窗 |
| `will_bottleneck` | **will 辅头** |
| `future_bottleneck_object_id` / `type` | **mark 辅头** |
| `time_to_start`、`duration` | 导出保留 |
| `root_cause_reason` | **cause 辅头（A3）** |

列：`window_*, bottleneck_node_t, bottleneck_type_t, bottleneck_score_t, is_bottleneck_window, horizon_s, will_bottleneck, future_bottleneck_object_id, future_bottleneck_type, time_to_start, duration, root_cause_reason`

### `bottleneck_event.csv`

**仅 STGNPP**（按节点历史事件 + 下一次 τ / duration）。`event_source` 恒为 `score`（过程热窗）。L2 不进此表。

列：`event_id, run_id, env_id, window_size_s, resource_id, resource_type, start_window_index, end_window_index, start_s, end_s, duration_s, max_score, n_windows, event_source, disturbance_id, disturbance_type`

### `job_kpi.csv`

导出时变成 `jobs_remaining` 序列，决定 A.1 占用图画多长。**不学**单件 cycle / 订单能否按时（产品侧 B 尚未接入）。

列：`run_id, env_id, episode_id, job_id, product_type, start_s, complete_s, cycle_time_s, completed, complete_source`

---

## 7. 与实验 / 训练的关系

```text
仿真采集（含可选 --disturbance_dim）
  → bottleneck_dataset/<run_id>/episode_*/env_*/
       原始表 + disturbance_log.csv
  → 本脚本 Stage-C
       derived/.../window_feature_table + label + event + job_kpi
  → factory_bn.export_dataset → 训练张量
       X = 特征（不含 score）
       y_PDFormer = 剩余视界上的 bottleneck_score / 占用 0/1
       jobs_remaining = job_kpi（地平线，不是 B 头）
       y_STGNPP = 事件序列
       y_will / y_mark / y_cause = label 表
  → BNPDFormer 训练
```

- **无扰动 / G0**：事件仍可能偏稀，属负载问题，不是管道失败。  
- **有 L2 扰动**（human / logistics / machine / material）：L2 只出现在 `disturbance_active_s` / `episode_config`；事件表只应出现过程热窗。`n_events_from_disturbance` 必须为 0。改过 `row_is_hot` / `node_hot_mask` 后必须重跑 `bn_agg` 再 `export_dataset`。旧 `evt` 权重是旧占用定义训的，不能直接当新 y 的评测。  
- 下游训练导出见 `00.运行方式.md`「离线聚合 → 导出 → 训练」。改规则后必须重跑 `bn_agg` 再 `export_dataset`。

---

## 8. 已知局限

1. 占用头的分数仍来自 `bottleneck_score_s`（剩余视界上回归 + 热窗 0/1），尚未改成直接回归 blocked/starved（BSTAN 原设定）。占用 0/1 已与 STGNPP 过程规则对齐，但 `score≥0.55` 仍只进占用、不进事件。
2. `quality_hold` 只在原始 `disturbance_log`，不进特征 L2 集合，也不进事件 / will
3. G0 弱负载下正样本仍可能稀少，训练需 G1 负载或扰动 run
4. `PROCESS_NEIGHBORS` 与 `graph.py` 的 `PROCESS_CHAIN` 需手工同步

## 9. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-08-23 | 龙门 / AGV 占用正例：`route_delay` 或 `inbound_wait` ≥20s **且** stall ≥30% 窗口。人 / buffer 仍不监督。L2 冻住无延迟仍不当事件。 |
| 2026-08-22 | **短单**：订单 10 根、WIP 5。A.1 占用固定 **H=15 窗 / 15 min**；`remain_len` 仍到工单清零。占用 / 事件只打 **machine**。新增耦合 stall 和「L2 已注入且工位实际 STOP」。事件合并允许隔 1 窗。龙门到货等待不再当占用 y。 |
| 2026-08-21 | **占用 y 对齐过程热窗**：`node_hot_mask` = 拐点 / 排队 / 缺料饥饿 / 到货延误 / 上游堵 / `score≥0.55`。STGNPP 增加 inbound-starve 与 blocked-backlog。`inbound_wait` 含未完成运次并摊到饥饿工位。cause 门槛下调（到货 20s、排队 1.0、堵死 40%）。仿真 L2 改为 2–3 条、10–30 min（见 02）。 |
| 2026-08-19 | **扰动与瓶颈拆开**：L2 只作 `disturbance_active_s`（X）；STGNPP / will / mark / cause 只监督过程拐点。分数去掉 `unavailable_pct`（STOP 不进 y）。cause 不再写 `machine_failure` 等扰动类型。 |
| 2026-08-14 | 双路径拆开：PDFormer 稠密分数改为含 stall/宕机、buffer 库存退出 queue；STGNPP 事件只保留拐点∪L2，默认 `min_event_windows=1` |
| 2026-08-13 晚 | 耦合改为工艺邻接；物流/缺料不再广播；TPM + 最长活跃期；L2 作 `disturbance_active_s` 且事件不与 score 合并 onset；导出 FEATURE_COLS 增列。对齐 PDFormer 图注意力与 STGNPP 逐节点事件 |
| 2026-08-13 | STOP/`unavailable_pct_s` 进分数；热窗加绝对活动门槛；队列挂下游 `output_buffer_id`；`quality_hold` 不进事件并集；默认 horizon **180** |
| 2026-08-10 | 并入 `disturbance_log` L2 → 事件双源；`event_source` / 扰动优先 root cause；支持 `episode_*/env_*`；增加 `job_kpi` 与 summary 中扰动统计；文档按当时代码重写 |
| 2026-07-18 | 初版：窗口特征 + score 事件 + 标签；在 `2026-07-18_17-46-35_seed42` 跑通 |
