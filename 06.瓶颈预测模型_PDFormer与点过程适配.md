# 瓶颈预测模型：当前实现

> 日期：2026-08-01（通俗重写 2026-08-10）  
> 代码：`source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer/factory_bn/`  
> 数据示例：`output/bottleneck_dataset/18_materials/derived/`  
> 借鉴：  
>
> 1. [PDFormer](https://ojs.aaai.org/index.php/AAAI/article/view/25556) — 稠密「分数/流量」预报
> 2. [STGNPP](https://ojs.aaai.org/index.php/AAAI/article/view/26669) — 稀疏「下一次事件何时、多久」

---

## 0. 总结

两件不同的事：


| 问题                    | 类比             | 模型路径         | 输出                          |
| --------------------- | -------------- | ------------ | --------------------------- |
| 接下来每个工位「有多堵」？         | 天气预报：未来一小时气温曲线 | **PDFormer** | 每个资源下一窗的 `bottleneck_score` |
| 下一次**明显瓶颈**几点开始、持续多久？ | 预报下一场暴雨        | **STGNPP**   | 下一事件的间隔时间 τ、时长 d            |


两件事不能混成一个头：前者要**每个窗口都有数**（稠密），后者事件很少（稀疏）。所以做成 **BNPDFormer = 共享时空编码器 + 两条输出路径**。

```text
过去 12 分钟各资源状态 X
        │
        ▼
   时空编码器（PDFormer 骨干）
        │
        ├──────────────► 路径① 分数预报：下一窗每个节点堵不堵（连续分数）
        │
        └──────────────► 路径② 事件预报：下一次瓶颈何时来、堵多久
```

**当前最大短板不是结构，是事件太少**（有的 episode 只有 0～2 个事件）→ 路径②能跑通，但还谈不上泛化。

---



## 1. 工厂问题 ↔ 交通论文


| 交通             | 工厂                                        |
| -------------- | ----------------------------------------- |
| 路段节点           | `resource_id`（机台 / 龙门 / 人 / AGV / buffer） |
| 流量、速度          | 队列、等待、利用率、BLOCKED/STARVED、物流延迟…           |
| 未来几步流量         | 未来窗口 `bottleneck_score_s`                 |
| 下一次拥堵 (时刻, 时长) | 下一次 `bottleneck_event` 的开始与持续             |


离线标签（见 `02.离线瓶颈特征与标签流水线.md`）：

- `window_feature_table.csv`：资源 × 时间窗特征 + score  
- `bottleneck_label.csv`：会不会出事件、落在谁身上、多久开始…  
- `bottleneck_event.csv`：合并后的稀疏事件序列

---



## 2. 数据怎么变成模型输入（先于网络）

```text
仿真 7 张异质原始表
    → python3 -m bn_agg（Stage-C）
    → derived/.../*.csv
    → factory_bn.export_dataset
    → raw_data/FactoryBN/episodes.npz
```

要点（细节见 `codenote.md` 状态编码）：

1. 各资源 raw state 先映射成统一词表（IDLE / PROCESSING / BLOCKED…）
2. 再按 60s 窗口聚成 **13 维连续运营特征**（不是 state Embedding）
3. 加上 **5 维资源类型 one-hot** → 每个节点每个窗共 **F=18**
4. **不把** `bottleneck_score` **当输入**（防标签泄漏）；score 只当监督

默认时间设定：窗长 60s；看过去 **12** 窗（约 12 分钟）→ 预报下一窗分数。

图怎么建（`graph.py`）：工艺链上下游 + buffer 亲和 + 物流资源连机台 + 同类弱连。  
这张图提供：地理邻域（Geo）、Laplacian 位置编码；语义邻域用行为相关度近似（短 episode 没用 DTW）。

---



## 3. 模型内部：共享编码 + 双路径



### 3.1 整体

```text
X (B, T_in=12, N≈34, F=18)
        │
        ▼
DataEmbedding
  Linear(18→64) + 时间位置编码 + 图 Laplacian PE
        │
        ▼
STEncoderBlock × L（PDFormer）
  · Temporal：每个节点看自己的历史
  · Geographic：工艺/物流上邻近的节点 + 「延迟形态」pattern
  · Semantic：行为相似的资源（即使图上不相邻）
        │
        ├─► score_pred (B, T_out, N, 1)     # 路径①
        │
        └─► 历史事件 → Inquirer → Continuous GRU
              → 周期门控强度 λ → 下一事件 τ / duration   # 路径②
              （辅助：will / mark / tts，事件极少时兜底）
```

形状直觉（18 物料、60s 窗）：


| 符号           | 含义        | 典型     |
| ------------ | --------- | ------ |
| N            | 资源数       | ~34    |
| T_in / T_out | 历史窗 / 预报窗 | 12 / 1 |
| F            | 输入维       | 18     |
| D            | 隐向量维      | 64     |




### 3.2 路径① 在干什么（PDFormer）

用注意力在「时间 × 资源图」上读历史，预报**下一窗每个资源的拥堵分数**。

三个注意力头各管一件事：

- **时间**：这台机器自己过去堵过吗？  
- **地理**：上下游/物流邻居在堵不堵？（hop 太远的 mask 掉）  
- **语义**：别的行为很像的资源在堵不堵？

**延迟（DFT）**：上游「积压/高利用率」的短时波形，过一会儿才传到下游。实现上用 k-means 得到一批短时 pattern，匹配后**加到地理注意力的 Key**里——让模型算「该看谁」时带上邻居刚发生过的形态。

（相对原文：语义距离用相关代替 DTW；pattern 用 k-means 代替 k-Shape——仿真 episode 短，够用。）

### 3.3 路径② 在干什么（STGNPP）

分数是每分钟都有的；**真正的大堵车很少**。路径②专门预测「下一次」：

1. **Inquirer**：在编码器隐状态里，按历史事件发生的时刻×节点把上下文抠出来
2. **Continuous GRU**：事件之间状态慢慢变；事件发生时猛跳一下
3. **周期门控强度 λ**：用点过程似然（NLL）学「再过多久会发生」；另有时长 MAE

工厂没有真实「星期几」，周期用 episode 内相位近似（订单推进到哪一阶段）。

窗口级 `will_bottleneck`（未来 H 内有没有事件）是**粗标签辅助**；事件多了应以 NLL 为主。

### 3.4 实现 vs 原文（对照表）


| 模块      | 原文                       | 当前代码                                        |
| ------- | ------------------------ | ------------------------------------------- |
| 稠密骨干    | PDFormer STAttn + DFT    | 已接（相关 / k-means 简化）                         |
| 事件上下文   | Spatio-temporal Inquirer | `stgnpp.SpatioTemporalInquirer`             |
| 事件动力学   | Continuous GRU           | `stgnpp.ContinuousGRU`                      |
| 强度 + 训练 | 周期门控 λ + NLL             | `PeriodicGatedIntensity` + NLL；保留 will/mark |
| 推理下一时刻  | 对密度二分取中位数                | 仍多直接读 τ；二分可后补                               |
| 数据量     | 交通数十万事件                  | 当前每 episode 常 0～2 个 → 管线通、泛化弱               |


---



## 4. 怎么跑



### 导出

```bash
cd source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer
python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/18_materials \
  --window_size 60
```

得到 `raw_data/FactoryBN/episodes.npz`（features / scores / will / mark / 事件序列 / adj 等）。

默认只用 **60s** 窗：当前数据里合并后的事件主要落在 60s；30s 上 `will` 常为 0。

### 训练

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --max_epoch 50
```


| 参数                | 默认   | 含义              |
| ----------------- | ---- | --------------- |
| `input_window`    | 12   | 12 分钟历史         |
| `output_window`   | 1    | 下一窗 score       |
| `use_stgnpp`      | true | 打开事件路径          |
| `will_pos_weight` | 20   | will 正样本稀疏，加大权重 |


权重：`libcity/cache/model_cache/FactoryBN/BNPDFormer_best.pt`。

推理主要看：`score_pred`、`will_prob`、`mark_prob`、事件时长 / 累积强度。

---



## 5. 代码地图


| 文件                       | 干什么                          |
| ------------------------ | ---------------------------- |
| `backbone.py`            | Embedding + ST 注意力块          |
| `stgnpp.py`              | Inquirer / ContGRU / λ + NLL |
| `graph.py`               | 工厂邻接、hop、语义距离                |
| `export_dataset.py`      | CSV → npz                    |
| `dataset.py`             | 滑窗样本 + 事件打包                  |
| `model.py`               | BNPDFormer 双路径拼装             |
| `train.py`               | 训练                           |
| `configs/FactoryBN.json` | 超参                           |
| `BORROWING.md`           | 两篇论文各借了什么                    |


入口：`python -m factory_bn.train`（不要走原版交通 `run_model.py`）。

---



## 6. 取舍与下一步

**已做的简化（合理）**

- 相关距离代替 DTW；k-means 代替 k-Shape  
- 周期用 episode 相位，无日历日/周  
- 事件少时保留 will/mark 辅助损失

**数据现实**

- 事件头只能证明「管线通了」；要效果必须多采扰动/高负载 episode

**建议优先级**

1. 把每个资源上的事件序列采够
2. 推理补二分求下一时刻（对齐 STGNPP）
3. 评估：score MAE + NLL / MAE-t / MAE-d；加规则基线对比
4. 数据够长后再考虑换回 DTW / k-Shape

---



## 7. 一句话

> **同一套时空编码**：PDFormer 头报「每个工位下一窗有多堵」；STGNPP 头报「下一次明显瓶颈何时、多久」。  
> 结构已接通；**先把事件数据采够，再谈事件预测准不准。**

