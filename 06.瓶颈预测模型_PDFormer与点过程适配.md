# 瓶颈预测模型：PDFormer × 时空图神经点过程适配解读

> 日期：2026-08-01（格式修订 2026-08-02）  
> 代码目录：`source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer/factory_bn/`  
> 数据示例：`output/bottleneck_dataset/18_materials/derived/`  
> 原文：  
> 1. [PDFormer](https://ojs.aaai.org/index.php/AAAI/article/view/25556)（Jiang et al., AAAI 2023）— 稠密**流量**预测  
> 2. [STGNPP](https://ojs.aaai.org/index.php/AAAI/article/view/26669)（Jin et al., AAAI 2023）— 稀疏**拥堵事件**预测（发生时刻 + 持续时长）

---

## 0. 两篇原文在说什么（精读摘要）

两篇论文解决的是**不同任务**，不应混成「同一个交通 Transformer」。

| 对比项 | PDFormer | STGNPP |
|--------|----------|--------|
| 任务 | 给定历史 T 步流量，预测未来 T' 步流量（稠密、固定网格时间） | 给定历史状态 + 历史拥堵事件，预测**下一次**拥堵的发生时刻与持续时长（稀疏、连续时间） |
| 输出 | 流量张量 `X̂`，形状 `R^{T' × N × C}` | 下一事件 `(t_{i+1}, d_{i+1})` |
| 核心难点 | 动态空间依赖、长程语义依赖、**传播时延** | 稠密状态 vs 稀疏事件；连续演化 + 瞬时跳变；峰时周期 |
| 主损失 | MAE / Huber 等回归 | **点过程 NLL**（对间隔时间）+ 时长 MAE |

### 0.1 PDFormer 方法结构（与开源代码一致）

整体流程：

```text
Embedding → L 层 ST Encoder → skip + 1×1 conv → 多步输出
```

1. **Embedding**  
   流量线性映射 + 图 Laplacian PE + 星期/日内周期嵌入 + 时间位置编码。

2. **Spatial Self-Attention（动态）**  
   每个时刻对节点做注意力，再乘两类 mask：  
   - **GeoSSA**：hop 距离 ≤ λ（短程地理邻域）  
   - **SemSSA**：历史序列 DTW 相似度 top-K（长程语义邻域）

3. **Delay-aware Feature Transformation（DFT，论文核心）**  
   - 用长度 S 的滑窗切历史流量，k-Shape 聚类得到短时模式集 P  
   - 当前节点最近 S 步与 P 做相似度匹配，得到融合表示 R_t  
   - **只加到 GeoSSA 的 Key**：`K̃ = K + R_t`  
   - 含义：算空间注意力时，query 能「看见」邻居过去几步的形态 → 显式建模**传播延迟**

4. **Temporal Self-Attention**  
   每个节点在时间维做全局注意力。

5. **异构多头融合**  
   geo / sem / temporal heads 拼接后投影；每层 skip；最后直接多步预测（非自回归）。

工厂对应：节点 = 资源；流量通道 ≈ 运营特征 / `bottleneck_score`；地理图 = 工艺+物流图；语义邻域 = 行为相似机台；DFT = 匹配「上游积压/高利用率」短时形态再传到下游。

### 0.2 STGNPP 方法结构

整体（论文 Fig.2）：

```text
历史交通状态 X + 路网 A
        ↓
Spatio-Temporal Graph Learning
  · Link-wise Transformer（时间自注意力 + 因果 mask）
  · Adaptive GCN（A + 可学习邻接，mix-hop）
  · Spatio-temporal Inquirer：按「事件发生的路段×时段」抽取上下文 H_c
        ↓
事件表示 H_e = FC([H_c, 历史 duration])
        ↓
Continuous GRU
  · 事件之间：GRU-flow（neural flow，连续演化）
  · 事件时刻：离散 GRU（瞬时跳变）
        ↓
Intensity Network（周期门控）
  λ(τ|h) = f_l⁺([h,τ]) ⊙ σ(f_p([P_day, P_week]))
        ↓
训练：NLL(间隔时间) + α · MAE(下一事件 duration)
推理：对 p(t|history) 用二分法取中位数作为下一发生时刻
```

要点：

- 预测对象是事件序列上的「下一次」，不是每个固定窗口的 `will_bottleneck` 二分类。  
- 强度函数是对间隔时间 τ 的条件强度，并用累积强度写似然。  
- **周期门控**：峰时/平峰改变 λ（工厂可对应班次/订单推进阶段）。  
- Continuous GRU 同时建模：无拥堵时状态缓变 + 拥堵发生时状态突变。

数据设定（原文）：北京/成都路段，用过去 6 小时状态与事件预测下一次拥堵；北京约 25 万事件量级——远高于当前 `18_materials` 的个位数事件。

### 0.3 当前 BNPDFormer 实现 vs 原文

| 模块 | 原文 | 当前 `factory_bn` |
|------|------|-------------------|
| 稠密预报骨干 | PDFormer STAttn + DFT | 已沿用（语义距离用相关代替 DTW；pattern 用 k-means 代替 k-Shape） |
| 事件编码 | ST 状态 → Inquirer 按事件索引抽上下文 | 已实现：`SpatioTemporalInquirer` |
| 事件动力学 | Continuous GRU（flow + discrete） | 已实现：`ContinuousGRU` |
| 强度 / 训练 | 周期门控 λ + NLL | 已实现：`PeriodicGatedIntensity` + NLL；另保留 will/mark 辅助损失 |
| 推理下一时刻 | 对密度函数二分取中位数 | 仍用回归 / 直接读 next_tau；二分推理可后续补 |
| 适用数据量 | 数十万事件 | 当前 0–2 个事件/episode → NLL 可跑通，但谈不上泛化 |

结论：**PDFormer 侧与 STGNPP 主体模块均已接到代码里**；瓶颈在数据量（事件太少），不是结构缺失。

---

## 1. 我们要解决什么问题

| 交通问题 | 工厂瓶颈问题 |
|----------|--------------|
| 传感器 / 路段节点 | `resource_id`（machine / gantry / human / AGV / buffer） |
| 流量、速度 | 队列、等待、利用率、BLOCKED/STARVED、物流延迟等 |
| PDFormer：未来若干步流量 | 未来窗口的 `bottleneck_score_s`（稠密） |
| STGNPP：下一次拥堵 `(t, d)` | 下一次 `bottleneck_event` 的 `start_s` 与 `duration_s`（稀疏） |

项目已有离线标签（见 `02.离线瓶颈特征与标签流水线.md`）：

- `window_feature_table.csv`：资源 × 窗口特征 + score  
- `bottleneck_label.csv`：窗口级 `will_bottleneck`、未来节点、TTS、duration（窗口近似）  
- `bottleneck_event.csv`：合并后的事件序列（更接近 STGNPP 的事件集）

当前 **BNPDFormer** 双路径：

1. **PDFormer 编码器** → 节点分数预报  
2. **STGNPP 事件路径** → Inquirer + Continuous GRU + 周期门控 NLL；窗口级 `will` 仅作稀疏数据辅助

---

## 2. 适配后的模型架构（当前代码）

```text
原始仿真日志 (7 表)
        │
        ▼
build_bottleneck_features.py          ← Stage-C（无需为模型再改）
        │
        ▼
derived/episode_*/env_00/*.csv
        │
        ▼
factory_bn.export_dataset
        │
        ▼
raw_data/FactoryBN/episodes.npz
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│ BNPDFormer                                               │
│                                                          │
│  X: (B, T_in, N, F)                                      │
│       F = 13 运营特征 + 5 类型 one-hot                   │
│                                                          │
│  ┌─ DataEmbedding ─────────────────────────────────────┐ │
│  │  Token(Linear) + 位置编码 + Laplacian PE            │ │
│  └─────────────────────────────────────────────────────┘ │
│                          │                               │
│  ┌─ STEncoderBlock × L（PDFormer）─────────────────────┐ │
│  │  · Temporal heads — 节点自身历史                     │ │
│  │  · Geographic heads — 工艺图 hop + pattern-key 延迟  │ │
│  │  · Semantic heads — 行为相似资源                     │ │
│  └─────────────────────────────────────────────────────┘ │
│                          │                               │
│          enc ──skip/conv──► score_pred  (B,T_out,N,1)    │
│           │                                              │
│           ▼                                              │
│  ┌─ STGNPP 事件路径 ───────────────────────────────────┐ │
│  │  Inquirer(历史事件) → Continuous GRU → h_event       │ │
│  │  PeriodicGatedIntensity → NLL(τ) + duration MAE      │ │
│  │  辅助：will / mark / tts（事件极少时兜底）            │ │
│  └─────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 2.1 形状约定（18 物料、60s 窗）

| 符号 | 含义 | 典型值 |
|------|------|--------|
| N | 资源节点数 | 34 |
| T_in | 历史窗口数 | 12（= 12 分钟） |
| T_out | 分数预测步数 | 1 |
| F | 输入特征维 | 13 + 5 = 18 |
| H | 事件视野 | 60s（= 1 个窗口） |
| D | 嵌入维 embed_dim | 64 |

**输入不包含 `bottleneck_score_s`**，避免标签泄漏；score 只作为回归监督信号。

---

## 3. 论文概念 → 工厂 / 当前代码对照

### 3.1 PDFormer — 基本可落地

| 论文模块 | 工厂实现（当前） |
|----------|------------------|
| 路网邻接 / Geo mask | `graph.py` 工艺链 + buffer + 物流；`sh_mx` hop；`far_mask_delta` |
| Sem mask + DTW | `sem_mx` 相关距离代替 DTW（短 episode 无「多天」） |
| DFT + k-Shape patterns | `make_pattern_keys`：k-means 短窗；写入 Geo 注意力 Key |
| Laplacian PE | 由 `adj_mx` 计算 |
| 流量回归 | `score_pred` → `bottleneck_score_s` |

### 3.2 STGNPP — 结构已接，数据仍稀

| 论文概念 | 工厂标签 | 当前代码 |
|----------|----------|----------|
| 事件 `(t, d)` | `bottleneck_event.start_s, duration_s` | 已导出并进入训练 |
| 历史状态 X | `window_feature_table` | 进 PDFormer 编码器 |
| Spatio-temporal Inquirer | 按事件时刻取节点隐状态 | `stgnpp.SpatioTemporalInquirer` |
| Continuous GRU | 事件间连续 + 事件瞬时 | `stgnpp.ContinuousGRU` |
| 周期门控 λ + NLL | — | `PeriodicGatedIntensity` + NLL |
| 同时预测 t, d | next_tau / next_dur | 已有；辅助保留窗口 will/tts |

窗口级 `will_bottleneck` 是粗粒度代理；事件变多后应以 NLL 为主。

---

## 4. 工厂图怎么建

`factory_bn/graph.py` 构造无向加权邻接（含自环，稳定 LapPE）：

1. **工艺链**（ProductWaterPipe）：cutting → grooving → workbench → weldingRobot → rotaryPipe  
2. **同机多工位**（`*_ws0` / `*_ws1`）软连接  
3. **Buffer 亲和**：Black / Yellow / Ground storage 连到相关机台  
4. **物流代理**：gantry / human / robot ↔ 全部 machine  
5. **同类代理**之间弱连接  

---

## 5. 数据流水线

### 5.1 导出

```bash
cd source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer
python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/18_materials \
  --window_size 60
```

输出：

```text
raw_data/FactoryBN/
├── episodes.npz          # 训练主输入
├── meta.json
├── node_map.json
├── FactoryBN.geo         # 可选 LibCity 原子文件
├── FactoryBN.rel
├── FactoryBN.dyna
└── config.json
```

`episodes.npz` 含：`features`、`scores`、`will` / `mark` / `tts` / `duration`、以及 `event_node` / `event_start_s` / `event_duration_s` / `event_start_ti`，外加 `adj_mx` / `sh_mx` / `sem_mx`。

### 5.2 滑窗采样

对每个 episode，时间下标 t（历史终点的下一拍）：

- `X = features[t - T_in : t]`  
- `y_score = scores[t : t + T_out]`  
- 历史事件：落在输入窗内的 `bottleneck_event`  
- 下一事件：`start_ti >= t` 的第一条 → `next_tau` / `next_dur`

多 episode 混合后约 70/15/15 划分；标准化只在训练集上拟合。

### 5.3 为何默认只用 60s 窗

当前 `18_materials` 中合并后的 `bottleneck_event` 只出现在 `window_size_s=60`；30s 上 `will_bottleneck_rate=0`。

---

## 6. 训练与推理

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --max_epoch 50
```

配置要点（`factory_bn/configs/FactoryBN.json`）：

| 参数 | 默认 | 说明 |
|------|------|------|
| `input_window` | 12 | 12×60s = 12 min 历史 |
| `output_window` | 1 | 下一窗 score |
| `enc_depth` | 4 | 工厂图小，比原版 PeMS 略浅 |
| `geo/sem/t_num_heads` | 2/2/4 | 总和 8，整除 `embed_dim=64` |
| `far_mask_delta` | 4 | 工厂直径小，勿过大 |
| `use_stgnpp` | true | 打开事件 NLL 路径 |
| `w_event` | 1.0 | STGNPP 损失权重 |
| `will_pos_weight` | 20 | 稀疏 will 辅助加权 |

权重保存：`libcity/cache/model_cache/FactoryBN/BNPDFormer_best.pt`。

推理 `model.predict(batch)` 主要返回：

- `score_pred`：未来节点瓶颈分数  
- `will_prob`：未来视野内发生事件的概率（辅助）  
- `mark_prob`：更可能落在哪个资源  
- `dur_event` / `Lam`：STGNPP 时长与累积强度  

---

## 7. 代码文件地图

| 路径 | 职责 |
|------|------|
| `factory_bn/backbone.py` | PDFormer 编码器构件 |
| `factory_bn/stgnpp.py` | Inquirer / ContGRU / 周期门控强度 + NLL |
| `factory_bn/graph.py` | 工厂邻接 / hop / 语义距离 |
| `factory_bn/export_dataset.py` | derived CSV → npz |
| `factory_bn/dataset.py` | 滑窗 Dataset + 事件序列 |
| `factory_bn/model.py` | BNPDFormer 双路径 |
| `factory_bn/train.py` | 训练循环 |
| `factory_bn/configs/FactoryBN.json` | 超参 |
| `factory_bn/BORROWING.md` | 两篇论文分别借鉴什么 |

工厂训练入口：`python -m factory_bn.train`（不要走原版交通 `run_model.py`）。

---

## 8. 设计取舍与后续建议

**已做的务实简化**

1. 用相关距离代替 DTW（仿真 episode 远短于「多天交通」）。  
2. 用 k-means 代替 k-Shape。  
3. 周期特征用 episode 相位代理（无日历日/周）。  
4. 事件极少时保留 will/mark 辅助损失。

**数据现实**

- `18_materials` ep0 约 2 个事件、ep1 为 0 → 事件头只能做管线打通，不能宣称泛化。  
- 需要更多 run（扰动、更高负载、更多 episode）后再看 NLL / MAE-t / MAE-d。

**建议下一阶段**

1. 继续采数据，拉长每个资源上的事件序列。  
2. 推理侧补上 STGNPP 原文的二分求下一时刻。  
3. 数据变长后可把 pattern 换回 k-Shape、语义邻域换回 DTW。  
4. 评估对齐 Jin et al.：NLL / MAE-t / MAE-d，外加 score MAE。  
5. 规则基线：`argmax score` 持久化、buffer 占用阈值告警。

---

## 9. 一句话结论

- **PDFormer**：稠密时空预报 + 显式传播延迟 → 工厂里预报 `bottleneck_score`。  
- **STGNPP**：稀疏事件的「下一次何时开始、持续多久」→ 工厂里用 Inquirer + Continuous GRU + 周期门控 NLL。  
- 代码双路径已接通；**先把事件数据采够**，再谈事件预测效果。
