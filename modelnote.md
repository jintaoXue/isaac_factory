# 当前实验流程（modelnote）

实验整体流程：**先在仿真厂里构造瓶颈数据，再按分钟把现场切成格子，用规则标出「谁堵了多久」，最后让模型看过去半小时、报未来 15 分钟会不会堵。**

组员跑 baseline 时，指标与颗粒度以 [`模型评估指标.md`](./模型评估指标.md) 为准。主分数 `report_f1`：工位对，且开始时间误差 ≤ **3** 分钟。

---

## 1. 数据采集

仿真里一局订 **10 根水管**，同时最多 5 根在制。没扰动时大约 2 小时 13 分做完。

时间规定：调度 1 步 = 1 秒厂时。后面说的「1 分钟」就是 60 步。

---

## 2. 数据聚类处理

**60 秒一window** 把每个资源（机台、工作台、龙门、AGV、人、buffer）聚成一行数：队列多长、等了多久、堵没堵、饿不饿、到货晚不晚、缺料传没传、这分钟有没有扰动。人 / buffer 只当上下文，**不算要预报的工位**。

然后做两层 **k-means（k=8）**：

- **工位的簇** `cluster_id`：这一分钟、这一台机器「长什么样」（闲着 / 排队 / 缺料 / 冻着……）。
- **整条流水线的样子** `window_cluster`：把这一分钟所有工位平均一下，再聚一次，表示「整条线现在是哪种工况」。

切训练样本时：

- **输入**：过去 **30 格 = 30 分钟**
- **要预报的**：未来 **15 格 = 15 分钟**
- 一局按 episode 切 train/val/test，**相邻窗口不会一边训练一边测试**

---

## 3. 模型必须输出（A.1，不可改成 persistence）

编码器是 PDFormer。主输出是每个监督工位（机台 / 工作台 / 龙门 / AGV）**一条未来 15 分钟事件**，不是「把上一分钟已热的工位再报一遍」。

| 输出 | 含义 |
| --- | --- |
| `will_block` | 未来 15 分钟这台会不会出现一段 ≥8 分钟的堵（0/1） |
| `start_min` | 从现在起第几分钟开始。上一分钟已经在堵 → 强制 0，只报还要堵多久 |
| `duration_min` | 持续几分钟（进行中训的是剩余时长） |

尚未发生（`start>0`）和已经在堵（`start=0`）都要报。人 / buffer 不当 A.1 目标。

另外还有两个训练头（不抢主分数）：

- **还要多少分钟收工**（回归）
- **过程原因：六大类**（缺料 / 到货延误 / 排队 / 下游堵 / 上游饿 / 高负荷）

评测主分数是 `report_*`（工位对且 start 误差 ≤ 3 分钟），必须拆 `on_r` / `up_r`。完整键名见 [`模型评估指标.md`](./模型评估指标.md)。

---

## 4. 对照实验口径（现行唯一配方）

配置：`factory_bn/configs/FactoryBN_dense_f1_p80.json`  
数据：`raw_data/dense_i1`，episode split，`seed=42`

| 项目 | 取值 | 为什么 |
| --- | --- | --- |
| 输入 / 预报 | 30 min → **15 min** | 笔记第 2、3 节 |
| 占用格 | 整局平滑 **≥ 8 分钟**（`hot_min_windows=8`） | 截图：堵段本身 ≥8 分钟 |
| 尚未发生正例 | 窗内新段 **≥ 8 分钟**，且 `start ≤ 2` | 截图 `will_block`；远地平线没有前兆 |
| 进行中正例 | 上一分钟已热且预报窗还接着热 → **剩余 ≥ 1 分钟也算** | 截图：已在堵则 `start=0`，报剩余时长 |
| 解码 | 事件头 + **剩余热前缀**（`last_hot` 且 prefix≥1.5 才抬升） | 不是把当前热工位全报一遍 |
| 扫 τ | val 上要求 **P≥0.80** 再取最大 F1 | 宁可不报，不要乱报 |
| **禁止** | 只评进行中、最短 2 分钟、报出全部当前热工位 | 那是 persistence，不是预警 |

存盘：`ckpt_metric=report_f1`，`ckpt_min_report_precision=0.80`，`ckpt_min_report_recall=0.70`。

训练：从 `dense_i1_a1_f180` 微调；解冻全部 5 个 encoder block；事件窗 4× 过采样；`prefix_mlp` 只在「上一分钟已热且还能接着热」时抬升。F1 是怎么上来的，见 [`F1提升实验历程.md`](./F1提升实验历程.md)。

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN_dense_f1_p80.json \
  --data_dir raw_data/dense_i1 \
  --save_dir libcity/cache/model_cache/dense_i1_a1_prefix8 \
  --device cuda --no_wandb
```

---

## 当前 bestmodel

正式保留：`dense_i1_a1_prefix8`（epoch 8）  
Val **P=0.883 / R=0.881 / F1=0.882**（`up_r=0.633`，`on_r=0.920`，`n_up=98`，`n_on=624`）  
Test **P=0.852 / R=0.856 / F1=0.854**（`up_r=0.455`，`on_r=0.911`，`n_up=112`，`n_on=828`）

`uphist` 把 test `up_r` 抬到 0.518，总 F1 掉到 0.827，**不采用**。各档数字和否掉的实验见 [`F1提升实验历程.md`](./F1提升实验历程.md)。

上一档（旧标签，剩余也卡 5 分钟）：`dense_i1_a1_f180` Test **P=0.785 / R=0.705 / F1=0.743**。  
`dense_i1_f1_p80_v7` 的 0.83 / 0.87 **仍作废**（只评进行中 + 最短 2 分钟）。
