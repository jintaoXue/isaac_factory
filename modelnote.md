# 当前实验流程（modelnote）

实验整体流程：**先在仿真厂里构造瓶颈数据，再按分钟把现场切成格子，用规则标出「谁堵了多久」，最后让模型看过去半小时、报未来 15 分钟会不会堵。**

组员跑 baseline 时，指标与颗粒度以 [`模型评估指标.md`](./模型评估指标.md) 为准（主分数 `report_f1`：工位对且 start 误差 ≤ 3 分钟）。

---

## 1. 数据采集

仿真里一局订 **10 根水管**，同时最多 5 根在制。没扰动时大约 2 小时 13 分做完。正式训练只开 **I=1.0**，而且 **一次只开一维**：机台 / 人 / 物流 / 物料。混维只做分布外评测，不并进训练集。

时间规定：调度 1 步 = 1 秒厂时。后面说的「1 分钟」就是 60 步。

---

## 2. 数据聚类处理

**60 秒一window** 把每个资源（机台、工作台、龙门、AGV、人、buffer）聚成一行数：队列多长、等了多久、堵没堵、饿不饿、到货晚不晚、缺料传没传、这分钟有没有扰动。人 / buffer 只当上下文，**不算要预报的工位**。

然后做两层 **k-means（k=8）**：

- **工位的簇** `cluster_id`：这一分钟、这一台机器「长什么样」（闲着 / 排队 / 缺料 / 冻着……）。
- **整条流水线的样子** `window_cluster`：把这一分钟所有工位平均一下，再聚一次，表示「整条线现在是哪种工况」。

切训练样本时：

- **输入**：过去 **30 格 = 30 分钟**
- **要预报的**：未来 **15 格 = 15 分钟**
- 一局按 episode 切 train/val/test，**相邻窗口不会一边训练一边测试**

---



## 3. 模型当前输出

编码器是 PDFormer。主要输出：每个监督工位（机台 / 工作台 / 龙门 / AGV）一条事件：


| 输出             |                                    |
| -------------- | ---------------------------------- |
| `will_block`   | 未来 15 分钟这台会不会堵（0/1）                |
| `start_min`    | 从现在起第几分钟开始。上一分钟已经在堵 → 强制 0，只报还要堵多久 |
| `duration_min` | 持续几分钟（进行中训的是剩余时长）                  |


另外还有两个训练头：

- **还要多少分钟收工**（回归）
- **过程原因：六大类**（缺料 / 到货延误 / 排队 / 下游堵 / 上游饿 / 高负荷）



评测主分数不是格子 F1，而是 `report_*`（工位对且开始误差 ≤ 3 分钟），并拆 ongoing / upcoming。完整键名见 [`模型评估指标.md`](./模型评估指标.md)。

## 当前bestmodel

unsup_best Test： who/report **P=0.817 / R=0.447 / F1=0.578**（工位对且 start ≤ 3 分钟）。现行唯一配方是 `FactoryBN_dense_f1_p80.json`（`report_f1`，P≥0.80，R≥0.35）。