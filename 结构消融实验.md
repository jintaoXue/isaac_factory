# 工厂化 PDFormer 结构消融实验

> 日期：2026-09-14  
> 目的：给论文里「图神经网络 / 工厂化 PDFormer」这一条贡献补**同一标签、只动结构**的消融。  
> 数据：`raw_data/dense_i1`（不用 mix 包，避免和结构因素缠在一起）  
> 任务口径：Min8，`start≤5/10/15` 课程式串行，四类原因，`will_15` 选模（P≥0.80），剩余时间主指标 `middle_weighted`  
> 入口：`python -u -m factory_bn.run_struct_ablation_min8 --phase main --seed 42`

编码器里的 T / Geo / Sem 注意力公式来自交通 PDFormer（Jiang et al., AAAI 2023），**不是新层**。本表只拆你接到工厂之后改掉的模块。prefix / 占用并集 / force ongoing 是解码协议，见 [`实验问题汇总.md`](./实验问题汇总.md) §2，不进本表。聚类、监督 score、STGNPP 已打平，继续放附录。

---

## 1. 主张（写进论文时用这一句）

把 PDFormer 的时空注意接到**异构过程–物流图**上，用**分组类型嵌入**替换同质路段嵌入，去掉日历周期，用短局相关语义邻接和 hop 受限地理注意（含延迟 pattern）捕捉「上游积压 → 数分钟后下游饥饿」。

不要写「提出了一种新的图注意力层」。若关图或关分组嵌入之后主指标几乎不动，把结构贡献降成方法细节，主贡献仍放事件任务与 `report_*` 协议。

---

## 2. 五臂（Full 已有，只新跑四条关臂）

| 臂 | 配置前缀 | 只改什么 | 证明什么 |
| --- | --- | --- | --- |
| **full** | `FactoryBN_dense_12_3_start{5,10,15}_min8_opt.json` | 异构图 + grouped embed + delay pattern + Sem 头 | 完整工厂化 PDFormer |
| **nogroup** | `FactoryBN_dense_struct_nogroup_*` | `use_grouped_embed=false` | 类型/量纲分组是否必要 |
| **nograph** | `FactoryBN_dense_struct_nograph_*` | `graph_mode=identity`（只留自环） | 工艺链+物流边是否必要 |
| **nopattern** | `FactoryBN_dense_struct_nopattern_*` | `use_delay_pattern=false` | Geo 头里的延迟 pattern 是否必要 |
| **nosem** | `FactoryBN_dense_struct_nosem_*` | `sem_num_heads=0`，把头补给 T（2+0+6），`embed_dim` 仍整除 | 非邻接语义耦合是否必要 |

实现开关：

- 图：`factory_bn/graph.py` 的 `graph_mode`（`factory` / `identity` / `chain` / `same_type`），训练时按 `resource_ids` 重建 `adj_mx` / `sh_mx`，不必重导出 npz。
- 延迟 pattern：`model.encode` 在 `use_delay_pattern=false` 时不再往 Geo Key 加 pattern。
- 分组嵌入：已有 `use_grouped_embed`。

`full` 的 start≤5/10/15 已有正式结果，直接作为基准，不默认重跑。每条关臂内部仍是 **start≤5 → 10 → 15**，后一档热启前一档 `BNPDFormer_best.pt`。按预期证据价值排序，默认顺序为：`nograph → nogroup → nosem → nopattern`。

---

## 3. 固定不变量

- 数据包、seed=42、事件头、prefix 解码、占用并集、P≥0.80 再取 `will_15` F1。
- `nosem` 保持总头数=8，避免「更小模型」被当成结构差。
- 主报告：`report` / `will_15` 的 P/R/F1，`up_r` / `on_r`。结构是否立功，优先看 **upcoming**。
- 剩余时间仍报全局 MAE 和 `middle_weighted`，但不作为结构消融主结论。

---

## 4. 服务器怎么跑

在 `PDFormer` 目录、已激活训练环境：

```bash
# 正式：跳过已有 full，四条关臂 × 三档起点
# 默认顺序 nograph → nogroup → nosem → nopattern
python -u -m factory_bn.run_struct_ablation_min8 --phase main --seed 42

# 先短筛 15 epoch
python -u -m factory_bn.run_struct_ablation_min8 --phase screen --seed 42

# 只跑其中几臂或显式复跑 full（可重复 --arm）
python -u -m factory_bn.run_struct_ablation_min8 --phase main --seed 42 --arm full --arm nograph
```

W&B 项目 `FactoryBN_PDFormer`，run 名形如：

`dense_i1_struct_{arm}_start{5|10|15}_min8_{screen|main}_seed42`

ckpt 目录：

`libcity/cache/model_cache/dense_i1_struct_{arm}_start{5|10|15}_min8_{phase}_seed42/`

服务器需同步：最新 `factory_bn/`（含 `graph.py` / `model.py` / `train.py` / 上述 json / 本脚本），以及 `raw_data/dense_i1`。不必同步 `dense_i1_mix`。

建议 `tmux` 里跑，例如：

```bash
tmux new -s bn_struct
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer
python -u -m factory_bn.run_struct_ablation_min8 --phase main --seed 42
```

---

## 5. 结果怎么填（跑完后）

对每个 `start∈{5,10,15}` 一张表：已有 `full` 加四条新关臂，共五行；列为 P / R / F1 / `up_r` / `on_r` / remain middle-weighted MAE。

解读：

- `nograph` 的 `up_r` 掉最多：异构过程–物流图成立。
- `nogroup` 掉：分组嵌入成立。
- `nopattern` 主要伤远 upcoming：延迟感知成立。
- `nosem` 掉：非局部语义邻接成立。
- 若四条关臂都几乎打平：不要把结构写成核心创新，主文改回任务与评价协议。

不要和 §12.3 旧 Min8 数字直接横比：本档用四原因 + `will_15` 选模 + 进度加权 remain，分母和选模都不同。
