# 运行方式

## 需要流媒体传输

1. 服务器终端1：

```
conda activate env_isaaclab
python train.py \
  --task HRTPaHC-v1 \
  --algo rule_based \
  --num_envs 1 \
  --active_livestream \
  --livestream_public_ip 10.68.14.234 \
  --livestream_port 49100 \
  --device cuda:0 \
  --headless
```

2. livestream client捕捉实时仿真画面

或  

服务器终端2：

```
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output
python3 -m http.server 8888 --bind 0.0.0.0
```

3. MES前端：
```
pnpm dev
```

## 只做训练

服务器终端

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --max_episodes 1
需要采集视频(mp4)的话可加：--video --video_length 10000 --video_interval 10000
```

默认不加 `--max_episodes` 时 episode 会无限循环；数据采集建议加 `--max_episodes 1`。

## 注入瓶颈扰动（四选一维度）

一次 run **只能选一个** `--disturbance_dim`（互斥）。强度用 `--disturbance_intensity`。

### `--disturbance_intensity` 是什么

浮点数，**≥ 0**，默认 `1.0`。作为该维度扰动的总开关/倍率：

| 取值 | 含义 |
|------|------|
| `0` 或 `dim=none` | 不注入扰动（等同基线） |
| `1.0` | 标准实验强度（推荐采集用） |
| `>1.0` | 加重：噪声更大、事件更长、减人/减车更狠、缺料比例更高 |

各维度在 `intensity=1.0` 时大致效果：

| 维度 | L1（持续） | L2（时段事件） |
|------|------------|----------------|
| `machine` | 加工时间噪声 std≈5；成功率≈0.92 | 约 step 800 起，目标机停机 ~120 步 |
| `human` | 子任务噪声更大；耗时 ×1.35；人数约 5→3 | 约 step 600 起，`human_0` 离岗 ~150 步 |
| `logistics` | 龙门动画更慢/更噪；AGV 约 2→1；intensity≥0.75 时启用 gantry 数→1 | 约 step 700 起，`gantry_0` 失效 ~100 步 |
| `material` | 原料缺料比例≈0.25；机台成功率≈0.90 | 无时段事件 |

可选覆盖人数/车数（不设则由 intensity 推算）：

- `--disturbance_human_count N`（仅 `human`）
- `--disturbance_agv_count N` / `--disturbance_gantry_count N`（仅 `logistics`）

更细的实验设计见 [`03.实验设计_瓶颈维度与扰动.md`](./03.实验设计_瓶颈维度与扰动.md)。

### 四维采集命令（各跑若干 episode）

**设备 / machine**

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim machine --disturbance_intensity 1.0 \
  --max_episodes 3
```

**人员 / human**

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim human --disturbance_intensity 1.0 \
  --max_episodes 3
```

固定人数示例（覆盖 intensity 推算）：

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim human --disturbance_intensity 1.0 \
  --disturbance_human_count 3 \
  --max_episodes 3
```

**物流 / logistics**

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim logistics --disturbance_intensity 1.0 \
  --max_episodes 3
```

固定 AGV / 龙门数量示例：

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim logistics --disturbance_intensity 1.0 \
  --disturbance_agv_count 1 --disturbance_gantry_count 1 \
  --max_episodes 3
```

**物料 / material**

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim material --disturbance_intensity 1.0 \
  --max_episodes 3
```

**无扰动基线**

```
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim none \
  --max_episodes 3
```

数据写在 `source/isaaclab_tasks/.../hc_factory/output/bottleneck_dataset/<run_id>/`；  
`episode_config.csv` 含 `disturbance_dim` / `disturbance_intensity`；事件见 `disturbance_log.csv`。

---

## 瓶颈预测模型训练（BNPDFormer）

仿真采集完成后，离线做三步：**聚合 → 导出 → 训练**。  
代码在 `source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer/`。  
说明见 [`06.瓶颈预测模型_PDFormer与点过程适配.md`](./06.瓶颈预测模型_PDFormer与点过程适配.md)。

### 环境注意（CUDA）

- **不要用** `env_isaaclab` 里自带的旧 Torch 在 RTX 5090 上跑 `--device cuda`（会报 Error 804 / forward compatibility）。
- 训练请用独立环境 **`bn_pdformer`**（见下），或先 `--device cpu`。
- 仿真采集仍用 `env_isaaclab`；**模型训练与仿真环境分开**。

### 新建训练环境 `bn_pdformer`

与 Isaac Lab 隔离，只装 BNPDFormer 需要的库：

```bash
# 创建环境（Python 3.11）
conda create -n bn_pdformer python=3.11 -y
conda activate bn_pdformer

# PyTorch（RTX 5090 / 新驱动建议 cu128；若官方索引更新可改 cu129）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# 训练脚本依赖
pip install numpy scipy

# 可选：看进度条
pip install tqdm

# 自检
python -c "import torch, numpy, scipy; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'cpu')"
```

若 `torch.cuda.is_available()` 为 `False`：

1. 先看驱动是否正常：`nvidia-smi`  
   - 若出现 **`Driver/library version mismatch`**：内核模块与用户态驱动版本不一致（例如刚升级了驱动包但未重启）。**重启机器**后再测。  
   - 重启前可先用 CPU 训：`--device cpu`。
2. 确认当前 shell 没有 Isaac 残留路径：`echo $LD_LIBRARY_PATH` 应为空或不含 `_isaac_sim`。
3. 重启后复测：

```bash
conda activate bn_pdformer
nvidia-smi
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
```

之后训练前先：

```bash
conda activate bn_pdformer
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer
```

### 目录约定

| 角色 | 路径 |
|------|------|
| 原始采集 | `.../hc_factory/output/bottleneck_dataset/<run_id>/` |
| Stage-C 聚合 | `<run_id>/derived/episode_*/env_00/` |
| 训练输入 | `PDFormer/raw_data/FactoryBN_<标签>/` |
| 权重 / 指标 | `PDFormer/libcity/cache/model_cache/FactoryBN_<标签>/` |

下文以 **`15_normal`** 为例；换成 `18_materials` 等只需改 `run_id` / 导出目录名。

### ① 离线聚合（Stage-C）

若该 run 下已有完整 `derived/`，可跳过。需要重跑时：

```bash
cd ~/work/isaac_factory

python3 source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools/build_bottleneck_features.py \
  --run_dir source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output/bottleneck_dataset/15_normal \
  --window_sizes 30,60 \
  --horizon 60 \
  --score_threshold 0.55 \
  --min_event_windows 2
```

输出：`.../15_normal/derived/episode_*/env_00/`  
（`window_feature_table.csv`、`bottleneck_label.csv`、`bottleneck_event.csv`、`job_kpi.csv`、`pipeline_summary.json`）

### ② 导出训练张量

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/15_normal \
  --out_dir raw_data/FactoryBN_15_normal \
  --window_size 60
```

输出：`raw_data/FactoryBN_15_normal/episodes.npz`（训练主输入），以及 `meta.json`、`node_map.json` 等。

其他 run 示例：

```bash
# 18 物料
python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/18_materials \
  --out_dir raw_data/FactoryBN_18_materials \
  --window_size 60
```

多 run 合并（可重复传 `--run_dir`；episode 键为 `{run_id}__episode_XX`）：

```bash
ROOT=../output/bottleneck_dataset
python -m factory_bn.export_dataset \
  --run_dir $ROOT/15_normal \
  --run_dir $ROOT/18_human3 \
  --run_dir $ROOT/18_logistics \
  --run_dir $ROOT/18_machine \
  --run_dir $ROOT/18_materials \
  --run_dir $ROOT/18_normal \
  --out_dir raw_data/FactoryBN_all6 \
  --window_size 60
```

### ③ 训练

先改配置里的数据目录与保存目录（避免不同 run 互相覆盖）。编辑：

`factory_bn/configs/FactoryBN.json`

```json
"data_dir": "raw_data/FactoryBN_all6",
"save_dir": "libcity/cache/model_cache/FactoryBN_all6"
```

然后训练（在 **支持 CUDA 的 Python** 下）。六 run 合并示例：

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/FactoryBN_all6 \
  --max_epoch 50 \
  --device cuda
```

单 run 把 `--data_dir` 换成对应目录即可（如 `raw_data/FactoryBN_15_normal`）。

CPU 回退：

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/FactoryBN_all6 \
  --max_epoch 50 \
  --device cpu
```

可选参数：`--batch_size 16`（覆盖配置）。

### 训练产物

| 文件 | 说明 |
|------|------|
| `libcity/cache/model_cache/FactoryBN_15_normal/BNPDFormer_best.pt` | 验证集最优权重 + 配置 + 图/scaler |
| `libcity/cache/model_cache/FactoryBN_15_normal/last_metrics.json` | 最终 test 指标 |

终端每个 epoch 会打印 `train_loss`、`score`、`will`、`event`、`val_loss`、`score_mae` 等。

### 一条龙（15_normal，含重聚合）

```bash
cd ~/work/isaac_factory

python3 source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools/build_bottleneck_features.py \
  --run_dir source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output/bottleneck_dataset/15_normal \
  --window_sizes 30,60 --horizon 60

cd source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/15_normal \
  --out_dir raw_data/FactoryBN_15_normal \
  --window_size 60

# 确认 FactoryBN.json 中 data_dir / save_dir 已指向 *_15_normal
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/FactoryBN_15_normal \
  --max_epoch 50 \
  --device cuda
```
