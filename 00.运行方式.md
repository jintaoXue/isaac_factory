# 运行方式

> 仿真采集：`conda activate env_isaaclab`，仓库根目录跑 `train.py`。  
> 模型训练：`conda activate bn_pdformer`（不要用 Isaac 自带的旧 Torch）。  
> 默认负载（G1）：水管 **18** 件、并行上限 **5**、行走 `go_to_*=None`（等导航走完）、龙门 `move_speed=0.5`。  
> 相关：[`01.瓶颈数据采集规范.md`](./01.瓶颈数据采集规范.md)、[`02.离线瓶颈特征与标签流水线.md`](./02.离线瓶颈特征与标签流水线.md)、[`03.实验设计_瓶颈维度与扰动.md`](./03.实验设计_瓶颈维度与扰动.md)、[`05.龙门架死锁修复.md`](./05.龙门架死锁修复.md)、[`06.瓶颈预测模型_PDFormer与点过程适配.md`](./06.瓶颈预测模型_PDFormer与点过程适配.md)。

采集用 `--algo rule_based`（`hier` / `flat` 同样走分层调度）。瓶颈 7 表默认就会写到 `output/bottleneck_dataset/`，无需另开开关。

**停采集必须两个参数一起写**（`rule_based` / `hier` / `flat` 主循环只认 `--max_sim_episodes`；`--max_episodes` 只让环境在第 N 局后标 `run_done`）：

```bash
--max_episodes 3 --max_sim_episodes 3
```

只写 `--max_episodes` 时仿真不会按局数停。

---

## 需要流媒体传输

1. 服务器终端 1：

```bash
conda activate env_isaaclab
python train.py \
  --task HRTPaHC-v1 \
  --algo rule_based \
  --num_envs 1 \
  --active_livestream \
  --livestream_public_ip 10.68.14.234 \
  --livestream_port 49100 \
  --device cuda:0 \
  --headless
```

2. livestream client 捕捉实时仿真画面

或服务器终端 2：

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output
python3 -m http.server 8888 --bind 0.0.0.0
```

3. MES 前端：

```bash
pnpm dev
```

不加 `--max_episodes` / `--max_sim_episodes` 时 episode 会一直 reset 循环（适合看画面）。

---

## 仿真采集（无画面）

仓库根目录、`env_isaaclab`：

```bash
conda activate env_isaaclab
cd ~/work/isaac_factory

python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --max_episodes 1 --max_sim_episodes 1
```

需要 mp4 时加上（`train.py` 里 `--video_length` 默认只有 200 步，采集请显式加长）：

```bash
  --video --video_length 10000 --video_interval 10000
```

输出：

```text
source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output/bottleneck_dataset/<run_id>/
  run_manifest.json
  episode_00/env_00/          # 原始 7 表
    episode_config.csv
    disturbance_log.csv
    resource_event_log.jsonl
    job_trace.csv
    buffer_event_log.csv
    route_transport_task.csv
    material_inventory_log.csv
```

`<run_id>` 形如 `2026-08-13_21-15-23_seed42`。

---

## 注入瓶颈扰动（四选一维度）

一次 run **只能选一个** `--disturbance_dim`（互斥）。强度用 `--disturbance_intensity`。  
L2 时段事件**每个 episode 重新采样**（`event_schedule_mode=resample_per_episode`）。

### `--disturbance_intensity`

浮点数，**≥ 0**，默认 `1.0`。同时放大 L0 数量、L1 噪声、L2 条数与时长（1.0 / 2.0 / 3.0 三档，中间值按就近档）：

| 取值 | 含义 |
|------|------|
| `0` 或 `dim=none` | 不注入扰动（等同基线） |
| `1.0` | 标准实验强度（推荐采集用） |
| `2.0` / `3.0` | 加重：噪声更大、事件更多更长、减人/减车更狠；物料开局藏得更多更久 |

各维度在 **`intensity=1.0`** 时大致效果（实现：`env_asset_cfg/cfg_disturbance.py`）：

| 维度 | L0 / L1（持续） | L2（时段事件，约 2–4 条） |
|------|-----------------|---------------------------|
| `machine` | 关工作台 ws1；加工噪声 std=5；成功率≈0.88；刀具磨损 15/千步 | 机台故障 + 1–2 个 `quality_hold` |
| `human` | 人数 5→4；全员 ×1.25，技能梯 0.8–1.4（有效约 1.00–1.75）；弱背景加工噪声 | 工人离岗 |
| `logistics` | AGV 2→1；龙门仍 2 台但 `move_speed` 变慢 | 龙门失效 |
| `material` | 开局限时缺管坯/法兰/弯头（件数、时长按强度在区间内抽，到期放回）；成功率≈0.90 | 再限时藏库 |

`logistics` 在 **I≥1.75** 时会把 active 龙门减到 1 台；`machine` 在 **I≥1.5** 时额外关掉旋转焊机 ws1。

可选覆盖人数/车数（不设则由 intensity 推算）：

- `--disturbance_human_count N`（仅 `human`）
- `--disturbance_agv_count N` / `--disturbance_gantry_count N`（仅 `logistics`）

更细的实验设计见 [`03.实验设计_瓶颈维度与扰动.md`](./03.实验设计_瓶颈维度与扰动.md)。

### 四维采集命令

下面都在仓库根目录、`env_isaaclab` 下跑。`--max_episodes` 与 `--max_sim_episodes` 必须成对。

**设备 / machine**

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim machine --disturbance_intensity 1.0 \
  --max_episodes 3 --max_sim_episodes 3
```

**人员 / human**

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim human --disturbance_intensity 1.0 \
  --max_episodes 3 --max_sim_episodes 3
```

固定人数示例：

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim human --disturbance_intensity 1.0 \
  --disturbance_human_count 3 \
  --max_episodes 3 --max_sim_episodes 3
```

**物流 / logistics**

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim logistics --disturbance_intensity 1.0 \
  --max_episodes 3 --max_sim_episodes 3
```

固定 AGV / 龙门数量示例：

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim logistics --disturbance_intensity 1.0 \
  --disturbance_agv_count 1 --disturbance_gantry_count 1 \
  --max_episodes 3 --max_sim_episodes 3
```

**物料 / material**

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim material --disturbance_intensity 1.0 \
  --max_episodes 3 --max_sim_episodes 3
```

**无扰动基线**

```bash
python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 \
  --seed 42 --device cuda:0 --headless \
  --disturbance_dim none \
  --max_episodes 3 --max_sim_episodes 3
```

`episode_config.csv` 含 `disturbance_dim` / `disturbance_intensity` / `disturbance_applied`；事件见 `disturbance_log.csv`。

---

## 瓶颈预测模型训练（BNPDFormer）

仿真采集完成后，离线三步：**聚合 → 导出 → 训练**。  
代码在 `source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer/`。  
聚合入口：`tools/` 下 `python -m bn_agg`。  
说明见 [`06.瓶颈预测模型_PDFormer与点过程适配.md`](./06.瓶颈预测模型_PDFormer与点过程适配.md)。

改过 Stage-C 特征列之后，**旧的 `episodes.npz` 不能直接用**，需要重跑聚合再导出。

### 环境注意（CUDA）

- **不要用** `env_isaaclab` 里自带的旧 Torch 在 RTX 5090 上跑 `--device cuda`（会报 Error 804 / forward compatibility）。
- 训练请用独立环境 **`bn_pdformer`**（见下），或先 `--device cpu`。
- 仿真采集仍用 `env_isaaclab`；**模型训练与仿真环境分开**。

### 新建训练环境 `bn_pdformer`

与 Isaac Lab 隔离，只装 BNPDFormer 需要的库：

```bash
conda create -n bn_pdformer python=3.11 -y
conda activate bn_pdformer

# PyTorch（RTX 5090 / 新驱动建议 cu128；若官方索引更新可改 cu129）
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

pip install numpy scipy
pip install tqdm   # 可选：进度条
pip install wandb  # 可选：训练曲线上传到 Weights & Biases

python -c "import torch, numpy, scipy; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'cpu')"
```

若 `torch.cuda.is_available()` 为 `False`：

1. 先看驱动：`nvidia-smi`  
   - 若出现 **`Driver/library version mismatch`**：内核模块与用户态驱动不一致（例如刚升级驱动未重启）。**重启机器**后再测。  
   - 重启前可先用 CPU：`--device cpu`。
2. 确认当前 shell 没有 Isaac 残留路径：`echo $LD_LIBRARY_PATH` 应为空或不含 `_isaac_sim`。
3. 重启后复测：

```bash
conda activate bn_pdformer
nvidia-smi
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
```

之后训练前先：

```bash
conda activate bn_pdformer
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer
```

### 目录约定

| 角色 | 路径 |
|------|------|
| 原始采集 | `.../hc_factory/output/bottleneck_dataset/<run_id>/` |
| Stage-C 聚合 | `<run_id>/derived/episode_*/env_00/` |
| 训练输入 | `PDFormer/raw_data/<标签>/` |
| 权重 / 指标 | `PDFormer/libcity/cache/model_cache/<标签>/` |

`FactoryBN.json` 的 `data_dir` / `save_dir` 只写父目录（`raw_data`、`libcity/cache/model_cache`）。**每次训练必须在命令里指定子文件夹**，否则不同 run 会互相覆盖：

| CLI | 指向 | 里面的文件 |
|-----|------|------------|
| `--data_dir raw_data/<标签>` | 导出包 | **`episodes.npz`**（训练主输入）、`meta.json`、`node_map.json` |
| `--save_dir libcity/cache/model_cache/<标签>` | 权重目录 | 训完后的 `BNPDFormer_best.pt`、`last_metrics.json` |

约定标签（不再带 `FactoryBN` 前缀）。命令里选中的组合并成**一份**张量再训一次：

| 命令 | 标签 | 源文件夹 |
|------|------|----------|
| `OLD` | `old2.0` | `old_{machine,human,logistics,material}2.0` |
| `NORM` | `norm` | `new_norm10` + `new_norm20` |
| `I1` | `new1.0` | `new_{dim}1.0` |
| `I2` | `new2.0` | `new_{dim}2.0` |
| `I3` | `new3.0` | `new_{dim}3.0` |
| `NORM I1` | `norm_new1.0` | 上面两组并在一起 |
| `ALL` | `all` | 五组能找到的 run 全部合并 |

### ① 离线聚合（Stage-C）

若该 run 下已有**现行特征列**的完整 `derived/`，可跳过。需要重跑时单条：

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools

python3 -m bn_agg \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --window_sizes 30,60 \
  --horizon 180 \
  --score_threshold 0.55 \
  --min_event_windows 1
```

多 run 批量（仓库根目录，`env_isaaclab`；缺目录会跳过，新组没采完也能先聚旧组）：

```bash
./batch_bn_agg.sh OLD    # old_{machine,human,logistics,material}2.0
./batch_bn_agg.sh NORM   # new_norm10 + new_norm20
./batch_bn_agg.sh I1     # new_{dim}1.0
./batch_bn_agg.sh I2     # new_{dim}2.0
./batch_bn_agg.sh ALL    # OLD → NORM → I1 → I2 → I3
# 已有 derived/ 想跳过: SKIP_EXISTING=1 ./batch_bn_agg.sh OLD
```

默认窗长 `30,60`、预报地平线 `180`。输出在 `<run_id>/derived/episode_*/env_00/`：

- `window_feature_table.csv`
- `bottleneck_label.csv`
- `bottleneck_event.csv`（score 热窗与 L2 完成区间是**两条 onset**，不合并成 `both`）
- `job_kpi.csv`
- `pipeline_summary.json`

### ② 导出训练张量

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --out_dir raw_data/<标签> \
  --window_size 60
```

输出：`raw_data/<标签>/episodes.npz`（训练主输入），以及 `meta.json`、`node_map.json` 等。  
A3 原因头需要 npz 里的 `{episode}_cause`。改过 `export_dataset` 之后必须重导出，旧包也能训但原因头没有标签。

多 run 合并（可重复传 `--run_dir`；episode 键为 `{run_id}__episode_XX`）。推荐直接走仓库根目录的 `./batch_bn_export_train.sh`（导出完立刻训练，见下面「批量导出 + 训练」）。

### ③ 训练

不要改 JSON 里的父目录。在 **`bn_pdformer`** 下把 `<标签>` 换成这次导出的文件夹名（与 `raw_data/<标签>/episodes.npz` 一致）：

```bash
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/PDFormer

python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/<标签> \
  --save_dir libcity/cache/model_cache/<标签> \
  --max_epoch 50 \
  --device cuda
```

例如旧四维 I=2.0 合并包：

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/old2.0 \
  --save_dir libcity/cache/model_cache/old2.0 \
  --max_epoch 50 \
  --device cuda
```

CPU 回退：`--device cpu`。可选：`--batch_size 16`（覆盖配置）。

实时监控（Weights & Biases，默认关闭）加 `--wandb_activate`。实现与用法见 [`06.瓶颈预测模型_PDFormer与点过程适配.md`](./06.瓶颈预测模型_PDFormer与点过程适配.md) 第 7 节。首次使用：

```bash
pip install wandb
wandb login
```

然后：

```bash
python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/<标签> \
  --save_dir libcity/cache/model_cache/<标签> \
  --max_epoch 50 \
  --device cuda \
  --wandb_activate \
  --wandb_project FactoryBN_PDFormer \
  --wandb_name "<标签>_ep50"
```

终端会打印 `[wandb] ... url=https://wandb.ai/...`，浏览器打开即可看每个 epoch 的 Train/Val 曲线。不加 `--wandb_activate` 时不依赖 `wandb` 包。

### 训练产物

| 文件 | 说明 |
|------|------|
| `libcity/cache/model_cache/<标签>/BNPDFormer_best.pt` | 验证集最优权重 + 配置 + 图/scaler |
| `libcity/cache/model_cache/<标签>/last_metrics.json` | 最终 test 指标 |

终端每个 epoch 会打印 `train_loss`、`score`、`will`、`event`、`val_loss`、`score_mae` 等。

### 批量导出 + 训练

仓库根目录 `./batch_bn_export_train.sh`：命令里列出的组合并成**一份** `raw_data/<标签>/episodes.npz`，再训一次到 `libcity/cache/model_cache/<标签>/`。不是每组各自导出再各自训练。文件夹名不再带 `FactoryBN`。缺目录或没有 `derived/` 的 run 跳过。

建议 `tmux new -s bn_train`。聚合不需要 Isaac；导出/训练用 **`bn_pdformer`**。**不要和 Isaac 采集抢同一张 GPU。**

```bash
conda activate bn_pdformer
cd ~/work/isaac_factory

./batch_bn_export_train.sh OLD          # old_*2.0 → old2.0
./batch_bn_export_train.sh NORM I1      # norm + new1.0 → norm_new1.0（一份张量、一次训练）
./batch_bn_export_train.sh ALL          # 五组合并 → all
# 已有 npz 只训练: SKIP_EXISTING=1 ./batch_bn_export_train.sh OLD
# 只导出不训练:     SKIP_TRAIN=1    ./batch_bn_export_train.sh OLD
# 打开 wandb:       WANDB=1         ./batch_bn_export_train.sh OLD
```

产物（一次命令一份）：

| 命令 | 张量 | 权重 |
|------|------|------|
| `OLD` | `raw_data/old2.0/episodes.npz` | `model_cache/old2.0/BNPDFormer_best.pt` |
| `NORM I1` | `raw_data/norm_new1.0/episodes.npz` | `model_cache/norm_new1.0/BNPDFormer_best.pt` |
| `ALL` | `raw_data/all/episodes.npz` | `model_cache/all/BNPDFormer_best.pt` |

`new_norm20` 还在采时，含 `NORM` 的命令会先并上已有的 `new_norm10`；采完并聚合后再重跑同一条命令即可把 20 局并进去。

### 一条龙（单个新 run）

把 `<run_id>` / `<标签>` 换成实际目录名。

```bash
conda activate env_isaaclab   # 仅仿真；聚合脚本本身不需要 Isaac
cd ~/work/isaac_factory/source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools

python3 -m bn_agg \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --window_sizes 30,60 --horizon 180

conda activate bn_pdformer
cd ../PDFormer

python -m factory_bn.export_dataset \
  --run_dir ../output/bottleneck_dataset/<run_id> \
  --out_dir raw_data/<标签> \
  --window_size 60

python -m factory_bn.train \
  --config factory_bn/configs/FactoryBN.json \
  --data_dir raw_data/<标签> \
  --save_dir libcity/cache/model_cache/<标签> \
  --max_epoch 50 \
  --device cuda \
  --wandb_activate \
  --wandb_name "<标签>_ep50"
```
