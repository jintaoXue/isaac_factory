# 博士申请汇报：PPT 文案与讲稿（修订稿）

> 体例对齐中期报告 *BNPDFormer——Dynamic bottleneck prediction in digital manufacturing*（ARIN7600 Interim Report, 2026-09-06）。  
> 页数：6；时长：10–12 分钟。  
> 主结果以现行协议 `dense_i1_a1_prefix8` 为准（中期报告 Table 4.2 的 0.578 为旧包，**不可混引**）。  
> 用语：吞吐量瓶颈、工位级事件、精度优先、过程一致性标签、episode 级泄漏控制。避免口语（「堵」「报一遍」「冲分」等）。

**时间分配**

| 页 | 对应中期报告 | 时长 |
| --- | --- | ---: |
| 1 | Ch.1 背景、目标与研究问题 | 1.5 min |
| 2 | Ch.2 相关工作与定位 | 2 min |
| 3 | Ch.3.1 数据引擎；Ch.3.2.1 问题形式化 | 2.5 min |
| 4 | Ch.3.2 模型 | 2 min |
| 5 | Ch.4 进展（更新后的主实验） | 2 min |
| 6 | Ch.5–6 结论与后续研究 | 1.5 min |

---

# 第 0 页｜封面（可选）

**BNPDFormer：面向人机协同数字制造的动态瓶颈事件预测**  
*Dynamic Bottleneck Event Prediction in Digital Manufacturing*

Isaac Sim 数字孪生导向仿真 · 异构工厂图 · 工位级事件预报

姓名 · 申请博士阶段汇报 · 2026 年 9 月

**封面讲稿（约 20 秒）。** 本次汇报基于硕士课题 BNPDFormer。研究目标是：在人机协同产线的异构资源图上，依据近期运行历史，预报固定短时域内的工位级吞吐量瓶颈事件。共六页，依次说明问题、定位、数据与形式化、方法、结果与后续工作。

---

# 第 1 页｜研究背景、目标与贡献

## 页标题

**动态吞吐量瓶颈需要事件级、精度优先的预报，而非事后识别**

## 页面文案

**背景（中期报告 §1.1）**

模块化建筑将现场作业转入受控工厂。产线吞吐取决于机台、工人、龙门、AGV、缓冲与物料供给的耦合，而非单一串行约束。吞吐量瓶颈是对系统产出约束最强的资源，**不必**是队列最长或利用率最高的节点；约束可随订单推进与扰动传播而转移。

既有方法主要依据队列状态、过程状态或二者的组合，识别**当前或历史**瓶颈。事后检测往往无法支持预防性干预。可行动的预报应同时给出：**是否发生、作用于哪一可报告资源、起始时刻、持续时间**。误报会触发不必要的重调度，因此预报必须**精度优先**。

真实工厂难以在可控条件下重复故障与缺料，完整运行标签亦不易获得。本研究以 NVIDIA Isaac Sim / Isaac Lab 重建模块化建筑水管配件产线，在受控扰动下生成质量门控轨迹。

**研究目标**

在异构人机协同工厂图上，由近期运行历史预报工位级瓶颈事件；标签须由可观测工厂行为导出，而非由注入扰动的类型名直接指定。

**研究问题（对齐 §1.3）**

- **RQ1** 近期运行历史能否预报即将发生的工位级瓶颈，并定位受影响的可报告资源？
- **RQ2** 过程–物流拓扑及传播时延建模，是否优于表格与序列基线、并相对图基线提供增量？
- **RQ3** 起始时刻与持续时间估计是否达到可支持预警的精度？

**阶段贡献**

1. 可控的人机协同工厂**数据引擎**（四维单轴扰动、质量门控轨迹）
2. **过程一致性**事件标注框架（扰动为上下文，不自动成为正例）
3. **事件导向**的图时空预报模型 BNPDFormer
4. episode 级、防泄漏的**共享评测协议**（定位 + 计时联合判定）

**建议图。** 左：Isaac 产线总览（中期报告 Fig. 4.1）。右：问题对照——事后识别 / 节点分类 / 占用网格 vs. 完整事件 \((w, s, d)\)。

---

## 第 1 页讲稿（约 90 秒）

本研究关注人机协同制造中的动态吞吐量瓶颈。模块化建筑把大量作业转入工厂，但产线并不是简单的串行系统：吞吐由机台、人工、物流与物料供给的相互作用决定。文献中的瓶颈，指对系统产出约束最强的资源，并不等价于队列最长或利用率最高的节点。扰动沿上下游传播后，约束资源本身会转移。

现有工作以队列状态、过程状态或二者的组合为主，长于识别当前或历史瓶颈。对调度而言，更有行动价值的是预报：未来短时域内事件是否发生、落在哪一可报告资源、何时开始、持续多久。四元组必须联合评价。同时，误报成本高，因此协议采用精度优先，而不是单纯追求召回。

可控、带标签的轨迹在真实工厂中很难获得。本研究用 Isaac Sim 构建数字孪生导向的仿真数据引擎，再在异构工厂图上做工位级事件预报。中期阶段提出三个问题：资源定位是否可行，拓扑与时延建模有无增量，起始与持续时间是否达到可用精度。后面各页按这三条展开。

---

# 第 2 页｜相关工作与研究定位

## 页标题

**完整「发生–资源–起始–持续」事件表述在制造瓶颈文献中仍不常见**

## 页面文案

**瓶颈识别与预测（§2.1）**

| 路线 | 代表工作 | 主要输出 |
| --- | --- | --- |
| 队列状态 | 队长 / 等待时间 | 堆积位置 |
| 过程状态 | Roser et al., 活跃期；shifting bottleneck | 瓶颈身份、转移 |
| 组合状态 | Li et al. turning point；Su et al. | 阻塞 / 饥饿 + 缓冲证据 |
| 前瞻预测 | ARIMA 活跃期；时空网络 | 未来身份或机台状态 |

缺口：输出多为**瓶颈身份**或**未来机台状态**；较少将预测表述为同时包含发生、可报告资源、起始与持续时间的完整事件。

**图上的时空预报（§2.2）**

- **BSTAN**（GAT + GRU）：预报未来阻塞 / 饥饿，再识别瓶颈位置——与本任务最接近的制造图基线。
- **BTGCN**：任务网上的图卷积 + GRU；需适配到工位级事件。
- **PDFormer**：地理 / 语义注意 + 传播时延，源自交通流；工厂中扰动可经延迟的工艺与物流影响非邻接节点。
- **STGNPP**：稀疏拥堵事件的发生时刻与持续时间；启发将起始与持续视为事件属性，但本任务要求固定时域内报告**全部**相关工位事件，而非下一次稀疏大事件。

交通节点量纲相近、周期性强；工厂图含异质机台、工作台、龙门、AGV、工人与缓冲，动力学由在制进度与资源竞争驱动。架构须适配，不能假定直接迁移。

**数字孪生导向仿真（§2.3）**

完整数字孪生要求信息模型、数据处理与双向通信。本环境更准确的定位是**数字孪生导向的仿真与数据引擎**，而非已与物理产线同步的孪生。仿真不消除 sim-to-real 间隙。

**定位（§2.4）**

本研究处于三线交叉：**运行瓶颈分析 × 图时空预报 × 仿真接地的人机协同制造**。不假设交通模型可直接用于制造；用受控轨迹检验事件级协议在异构工厂图上是否成立。

**建议图。** 一张定位图：横轴「检测 → 身份预测 → 完整事件」，纵轴「表格 / 序列 → 局部图 → 时延感知」。本工作落在右上。

---

## 第 2 页讲稿（约 2 分钟）

相关工作可以收成三条线。

第一条是制造瓶颈本身。系统综述把方法分为队列状态、过程状态与组合状态。Roser 等人用最长平均活跃期定义瓶颈，并进一步区分瞬时、独占与转移瓶颈。Li 等人的转折点方法把阻塞、饥饿与缓冲记录合在一起。后续工作把预测推进到未来活跃期或未来瓶颈身份。这些研究说明未来瓶颈具有可学习的时序与结构模式，但输出通常停在「是哪一台」或「未来处于何种状态」，而不是一条带起始与持续时间的完整事件。

第二条是图时空模型。BSTAN 与 BTGCN 是最接近的制造图基线，本项目将二者适配到同一工位级事件任务上，作为对照。PDFormer 提供长程关系与传播时延机制，这与工厂里「上游阻塞、数窗之后下游饥饿」的延迟传播一致。STGNPP 则提醒我们：起始与持续是事件属性，不应只预报稠密状态序列。但本任务是固定 15 分钟时域内的工位报告，不是预报下一次稀疏大事件，因此点过程路径在现行配方中关闭。

第三条是数据基础。真实产线难以在受控条件下重复扰动。Isaac 环境提供可重复轨迹，同时必须承认它还不是与物理系统双向同步的数字孪生。

综合起来，任务形式化、模型适配与仿真数据是三个需要同时处理的缺口。本研究不把其中任何一项当作工程细节略过。

---

# 第 3 页｜数据引擎与问题形式化

## 页标题

**过程一致性标签 + 泄漏控制划分 + 精度优先的报告级判定**

## 页面文案

**数据引擎（§3.1）**

Isaac Sim 重建水管配件产线：仓储、龙门 / AGV、切管 / 坡口 / 焊接 / 涂装、工作台。规则控制器执行工艺与物流。每局订单 10 件，在制上限 5。逻辑时间与物理时间分离；采集器只读、事件驱动，写七张原始表。

正式训练：强度 \(I=1.0\)，每 run **单轴**扰动（machine / human / logistics / material）。配置层（减员、关工位、减少 AGV）与情节层冲击（故障、离岗、冻车、限时缺套件）叠加。**注入扰动是环境上下文，不是瓶颈标签**；L2 仅以 `disturbance_active_s` 进入特征。

异质原始状态按 \(\Delta=60\,\mathrm{s}\) 聚合。占用由过程一致性证据导出：队列堆积、阻塞、饥饿、到货延误、缺料传播等。监督分数不进入输入 \(X\)，以避免标签泄漏。划分在 **episode 级**分层进行，禁止按滑窗随机切分。

现行包 `dense_i1`：\(N=38\)，\(F=27\)；episode 划分 140 / 28 / 36（seed 42）。可报告监督节点：机台、工作台、龙门、AGV；工人与缓冲保留为图上下文。

**形式化（§3.2.1）**

资源集合 \(R=\{r_1,\ldots,r_N\}\)，过程–物流图 \(G=(R,E)\)。窗口 \(t\) 上节点特征 \(x_{t,n}\in\mathbb{R}^F\)。输入历史 \(X_t\in\mathbb{R}^{T_{\mathrm{in}}\times N\times F}\)，\(T_{\mathrm{in}}=30\)（30 min）。对每一可报告工位预报
\[
(w_{t,n},\; s_{t,n},\; d_{t,n})
\]
即 will-block、起始偏移（分钟）、持续时间；时域 \(H=15\) min。

**报告正确当且仅当：** 工位匹配，且 \(|\hat s-s|\le 3\) min。阈值在 validation 上选取，约束报告精度 \(\ge 0.80\)，再最大化 report F1；测试集冻结同一阈值。

**正例契约（与交付格式一致，防止持续性基线冒充预警）**

- 占用段：整局连续热窗 \(\ge 8\) min。
- 尚未发生（upcoming）：新段 \(\ge 8\) min，且 \(s\le 2\) min（更远起始缺乏稳定前兆）。
- 进行中（ongoing）：上一窗已热且预报窗内延续，按**剩余时长**计（\(\ge 1\) min）。
- 主指标拆分 upcoming / ongoing 召回。占用网格 F1、IoU 事件 F1 **不作为**存盘依据。

**明确排除的退化协议。** 仅评价进行中事件、将最短段放宽至 2 min、或将上一窗已热工位全部报出，等价于持续性基线，其表面 F1 不可与本协议比较。

**建议图。** 上：采集–聚合–导出流水线（中期报告 Fig. 3.1 左半）。下：一条工位上的 \((w,s,d)\) 与 report 判定示意。

---

## 第 3 页讲稿（约 2.5 分钟）

方法部分先把数据和任务说清楚，再谈网络。这与中期报告的结构一致：没有过程忠实的轨迹和可比较的标签，模型对照没有意义。

仿真侧，我们重建了一条水管配件人机协同产线，用规则控制器执行工艺与物流。每局 10 件、在制上限 5，使后续工件排队进线，从而在订单中段产生持续约束，而不是由仿真死锁制造虚假拥堵。采集是事件驱动的：仅在状态、工序、缓冲、运输或库存发生变化时写记录，以保留事件边界。扰动按单轴、强度 1.0 注入。关键约定是：扰动解释环境，不自动生成正例。标签来自可观测的队列、阻塞、饥饿、延误与缺料传播。

特征按 60 秒窗聚合，输入过去 30 个窗，预报未来 15 分钟。划分必须在 episode 级完成。制造时序窗高度重叠，按窗随机切分会使测试集包含与训练集近乎重复的序列，从而高估泛化。

任务上，每个可报告工位输出三元组：是否发生、起始偏移、持续时间。一条报告被计为正确，当且仅当资源定位正确，且起始误差不超过 3 分钟。模型选择在验证集上进行，并施加 0.80 的精度下限，因为误报会诱发不必要干预。

有两类事件必须同时评价。尚未发生的新段要求持续至少 8 分钟，且起始落在未来 2 分钟以内——更远的起始目前没有稳定前兆，放开监督会破坏精度约束。进行中事件按剩余时长标注，避免把「整段已满足 8 分钟、但本窗仅余数分钟」的真事件丢弃。若只评价进行中、或把当前已热工位全部输出，得到的是持续性基线，不是预警模型；该类结果不得纳入主表。

---

# 第 4 页｜BNPDFormer

## 页标题

**延迟感知的时空编码 + 工位事件头；占用、剩余时间与原因为辅助任务**

## 页面文案

**编码器（§3.2.2）**

将异质原始状态规范为统一运营特征，并拼接资源类型 one-hot，得到 \(X\in\mathbb{R}^{30\times 38\times 27}\)。堆叠时空块：

- **时间注意**：逐节点建模历史；
- **地理注意**：受过程–物流邻接 / 类型化邻域约束，并可编码传播时延；
- **语义注意**：连接行为相似、但空间上未必邻接的节点。

去除交通 PDFormer 的日历嵌入；位置编码由工厂邻接的拉普拉斯特征给出。不另建 HeteroGNN：类型差异由特征与掩码承担。

**预报头（现行主路径）**

| 头 | 形式 | 角色 |
| --- | --- | --- |
| will / start / duration | 每工位、时域 \(H\) | **主任务**（A.1） |
| prefix | 下一窗起的连续热前缀 | 区分「将延续」与「下一窗即结束」 |
| 占用网格 | \(H\times N\) | 连通段对齐，不决定存盘 |
| 订单剩余时间 | 标量（窗） | 订单层上下文 |
| 过程原因 | 6 类 | A.3，次要 |

STGNPP 稀疏计时路径：`use_stgnpp=false`。本任务是固定时域内的稠密工位报告，与「下一次稀疏热点」的监督目标不同。

**解码。** 事件头概率超过阈值才报告。对上一窗已热的工位，仅当预测前缀 \(\ge 1.5\) 窗时抬升，从而抑制闪烁，同时保留仍将持续的进行中事件。

**目标函数。** 无监督占用（`ops_hot_mask`）+ 事件头 + 轻量重构。同包对照：附加 `bottleneck_score` 回归后，8 分钟平滑占用与过程占用 Jaccard \(=1.0\)，test report F1 由 0.854 降至 0.850。现行主路径保持无监督，并非未尝试监督分数。

**建议图。** 横向：\(X\) → 时空块（时间 / 地理+时延 / 语义）→ 事件头加粗，其余头标为 auxiliary。可复用中期报告 Fig. 3.1 右半。

---

## 第 4 页讲稿（约 2 分钟）

BNPDFormer 把工厂表示为过程–物流图。节点是异构资源，边编码工艺先后、缓冲附着与物流服务关系。编码器采用 PDFormer 式分解：时间注意刻画单节点历史，地理注意限制在工艺与物流邻域并允许延迟模式，语义注意连接行为相似的非邻接节点。日历嵌入被去掉，因为工厂动力学由在制进度驱动，而不是由星期或小时周期驱动。

异质状态不进入独立的异构图网络，而是先映射到同一套运营特征，再加类型编码。这样做的原因是：在本任务上，标签语义与评测协议对指标的影响，大于再增加一层类型化消息传递。

主输出是每工位一条事件，而不是 \(15\times N\) 占用图。占用头仍然训练，用于对齐连通段，但不参与模型选择。额外的 prefix 头估计从下一窗起仍将连续热的时长：仅当资源当前已热且前缀显示其将延续时，才抬升报告概率。这与「输出全部当前热工位」的持续性规则不同。

训练占用来自过程启发式。我们在同一划分、同一初始化下对照了分数回归：平滑后两类占用完全重合，分数热窗是过程热窗的子集，额外回归项不提供新的 8 分钟正例，主指标略降。因此正式模型保持无监督配方。点过程头关闭，因为现行监督目标是时域内全部相关工位事件，而不是下一次稀疏事件的等待时间。

---

# 第 5 页｜实验结果

## 页标题

**精度约束下 report F1 = 0.854；尚未发生事件的召回仍是开放问题**

## 页面文案

**协议。** `dense_i1`；\(T_{\mathrm{in}}=30\)，\(H=15\)；episode 划分 seed 42；`ckpt_metric=report_f1`，精度下限 0.80。主指标：工位匹配且 \(|\hat s-s|\le 3\) min。中期报告 Table 4.2（旧包 unsup_best，F1 \(=0.578\)）仅作历史参照，**不得与下行混列。**

| 设定 | 协议要点 | Test P / R / F1 | upcoming / ongoing 召回 |
| --- | --- | ---: | ---: |
| 中期旧包 | 历史下限 | 0.817 / 0.447 / 0.578 | 0.320 / 0.579 |
| near5 | 精度首次过门 | 0.801 / 0.651 / 0.718 | 0.312 / 0.719 |
| f180 | 旧正例定义 | 0.785 / 0.705 / 0.743 | 0.375 / 0.772 |
| **prefix8（正式）** | 剩余时长 + prefix 门控 | **0.852 / 0.856 / 0.854** | **0.455 / 0.911** |
| 前兆增强 | upcoming 对照，不采用 | 0.800 / 0.856 / 0.827 | 0.518 / 0.902 |
| + score 回归 | 同契约监督对照 | 0.855 / 0.846 / 0.850 | 0.473 / 0.896 |

Validation（prefix8）：P / R / F1 \(= 0.883 / 0.881 / 0.882\)。Test 正例：upcoming 112，ongoing 828。

**解读（对应 RQ）**

- **RQ1（定位）。** 在精度 \(\ge 0.80\) 约束下，报告级 F1 达到 0.854，表明近期运行历史足以支持可报告资源上的事件预报。
- **RQ3（计时）。** 工位匹配样本上起始误差普遍落在 3 分钟容差内（who 与 report 接近）；进行中召回 0.911。**尚未发生召回 0.455**，与中期报告指出的主失败模式一致，仍是预警意义上的缺口。
- **RQ2。** XGBoost / LSTM / BTGCN / BSTAN 已纳入同一协议；横向表尚在按共享 split 填齐，本日不以未完成对照下结论。

**有效因素。** 近起始监督（\(s\le 2\)）、精度门控的阈值选择、进行中按剩余时长标注、prefix 门控。  
**无效或不可比。** 持续性退化协议；从零训练且标签未对齐的 15 min 配方；在本包上回归 `bottleneck_score`；将占用最短段放宽至 5 min（正例膨胀）。  
**不可跨标签直接比较。** 0.743 → 0.854 的部分增益来自进行中剩余正例的重新计入，不是同一分母上的纯模型增益。

**辅助（不作主结论）。** 过程原因 macro recall \(\approx 0.97\)（规则标签可分，不等于根因推断）；订单剩余时间 MAE \(\approx 10.5\) min。

**建议图。** 主表只加粗 prefix8 一行。旁注柱状图：ongoing 0.91 vs upcoming 0.46。不展示占用格 F1。

---

## 第 5 页讲稿（约 2 分钟）

现行正式结果是 prefix8。验证集 report F1 为 0.882，测试集精度 0.852、召回 0.856、F1 0.854。进行中召回 0.911，尚未发生召回 0.455。相对于中期报告旧包的 0.578，主指标上升，但开放问题没有改变：模型对已在持续的约束恢复较好，对尚未起始的事件仍然偏弱。

需要强调可比性。中期数字与现行数字来自不同标签与解码协议，不能写成「从 0.58 提升到 0.85」而不加限定。在现行契约内部，精度门控之后 F1 由约 0.72 升至 0.85，主要来自两项修正：进行中剩余时长与交付格式对齐，以及用前缀门控代替「当前热即报告」。

两组对照支持方法选择。第一，附加瓶颈分数回归不增加新的占用正例，测试 F1 为 0.850。第二，前兆头将尚未发生召回提高到 0.518，但总 F1 降至 0.827，精度贴着 0.80 下限，故不替换正式模型。这表明：在精度优先协议下，upcoming 召回与报告精度之间存在需要单独处理的权衡，而不是简单增加特征即可消除。

基线对照（表格、序列、BSTAN / BTGCN 适配）使用同一 pack、同一 split 与同一 report 指标。横向结果尚未全部冻结，因此 RQ2 本日只陈述协议，不下性能结论。

---

# 第 6 页｜结论、局限与博士阶段研究

## 页标题

**事件级协议已经可检验；博士工作应转向提前量、可干预性与迁移**

## 页面文案

**结论（对齐中期 Ch.5，按现行数字更新）**

1. 吞吐量瓶颈可以形式化为工位级短时域事件，并用**定位与计时联合**的报告指标评价；占用网格或持续性规则不能替代该指标。
2. 在受控 Isaac 轨迹上，过程一致性占用足以监督 A.1；本包中分数回归不提供新的事件正例。
3. 精度优先约束下，test report F1 \(=0.854\)，进行中事件已较稳定；**尚未发生事件的召回（0.455）仍是核心科学缺口**，与中期报告一致。

**局限**

- 单一产线、单一产品与单一布局；未见拓扑未评估。
- 主训练为单轴扰动；混维仅宜作分布外检验，尚非主结果。
- \(s>2\) min 的起始缺少稳定前兆，现行任务不予报告。
- 产品层输出（能否按期、单件 / 工序完工分布）尚未接入。
- 环境是数字孪生**导向**仿真，sim-to-real 未经物理产线校准。

**博士阶段拟研究的问题（由中期 Track I/II 收紧为研究问题）**

| | 问题 | 与现有工作的关系 |
| --- | --- | --- |
| 1 | 在报告精度 \(\ge 0.80\) 下提高 upcoming-onset 召回 | 中期与现行实验共同指出的主失败模式；预警的定义所在 |
| 2 | 由过程原因走向可干预预报：资源增减是否消除该事件 | 现有 cause 头是规则可分的分类，不能当作因果真值 |
| 3 | 跨布局 / 跨产品，或映射到 MES–PLC 信号后的迁移 | 回应数字孪生文献中的 sim-to-real 约束 |

**收束。** 本阶段完成了可重复的数据引擎与可比较的事件级评测。博士阶段的目标不是在同一协议上继续微调 F1，而是处理**提前量、干预效应与跨环境有效性**——这三者决定该问题能否从仿真研究进入制造决策。

**建议图。** 三列：已建立（引擎 / 协议 / 进行中事件）｜开放问题（upcoming）｜博士问题（提前量 / 干预 / 迁移）。

---

## 第 6 页讲稿（约 90 秒）

小结。本工作把动态吞吐量瓶颈写成可检验的工位级事件预报问题，并给出从 Isaac 采集到报告级指标的完整链路。在精度优先协议下，测试集 F1 为 0.854；进行中事件恢复稳定。尚未发生事件的召回仍低，这与中期报告的判断一致，说明主缺口不在工程实现，而在短时域、弱前兆条件下的事件起始估计。

局限需要说清楚：单一布局与产品；混维扰动未进入主训练；产品层预报未做；仿真与真实产线之间的映射尚未建立。

若继续攻读博士，我不打算把同一 F1 再提高若干个百分点当作题目。更值得做的是三个问题。第一，在不放松精度约束的前提下提高尚未发生事件的召回，这才对应预警而非状态跟踪。第二，在原因分类之后提出可干预问题：增加运输或人工容量是否消除该事件，这需要反事实结构，而不是再增加一个规则可分的分类头。第三，检验该任务在变布局、变产品或真实运行日志上是否仍然成立。现有数据引擎和评测协议可以保留，作为后续研究的共同底版。

汇报到此。指标定义、标签对照与失败案例分析可以补充。

---

# 问答（学术表述）

**Q1. 这是否只是把当前拥堵工位再次输出？**

不是。仅评价进行中事件、或将上一窗已热工位全部报告，属于持续性基线；其一版表面 F1 约 0.87，因任务被替换而不予采用。正式协议同时评价 upcoming 与 ongoing；解码要求预测热前缀显示事件将延续。测试集 ongoing 召回 0.911、upcoming 召回 0.455，说明模型并非仅复制当前状态。

**Q2. 标签是否泄漏未来信息？**

占用由过程启发式在整局上平滑，但 train / val / test 按 episode 划分，相邻窗不得跨集合。预报锚点为输入结束边界 \(t\)，目标区间为 \([t,t+H)\)。注入类型不写入占用正例。阈值仅在 validation 选择。在线推理只使用已闭合窗口。

**Q3. 为何不以 `bottleneck_score` 为监督？**

同一协议下对照过两次。`dense_i1` 经 8 分钟平滑后，过程占用与分数占用 Jaccard \(=1.0\)；分数热窗是过程热窗的子集，转折点多出的短段被平滑消除。附加回归项后 test F1 为 0.850，低于 0.854。结论是：在本包上分数不构成新的事件监督信号，而不是未实施监督。

**Q4. 0.85 是否过拟合？基线在哪里？**

划分与标准化只在训练集拟合。同协议下的消融已记录有效与无效因素。表格、序列与图基线共用 report 指标与 split；横向数字尚未全部冻结，故不以未完成对照声称优于 BSTAN / BTGCN。即便缺少横向表，标签口径、分数头与前兆头的对照已足以支持主路径选择。

**Q5. 为何起始只监督 2 分钟？15 分钟时域是否冗余？**

持续时间与进行中剩余仍在 \(H=15\) 上回归。受到限制的是**新事件起始**的监督范围：更远起始缺乏稳定前兆，纳入后无法维持精度下限。这是任务设计，不是把预报时域改为 2 分钟。

**Q6. 原因分类接近 0.97，是否构成贡献？**

该类标签由过程规则生成，类间可分，macro recall 高并不等价于根因或因果推断。A.3 只作辅助分析，主结论不依赖 `cause_acc`。若后续研究干预效应，原因监督必须重做，不能沿用现行规则作为因果真值。

**Q7. 与交通拥堵预报的本质差异是什么？**

工厂图异质、正例稀疏、动力学由在制与资源竞争驱动，而非日历周期。评测上必须排除持续性基线：交通中「当前拥堵在下一时刻仍拥堵」往往可接受；在工厂预警中，若模型只能复述当前热工位，则不具备干预提前期。此外，本任务要求固定时域内的全部相关工位事件，而不是下一次稀疏拥堵的点过程。

**Q8. 与申请方向如何衔接？**

按导师方向替换括号内对象：

> 现有工作已将问题收束为可评测的工位级事件预报，并提供可重复的仿真引擎与泄漏控制协议。贵组若关注〔具身智能 / sim-to-real / 因果决策 / 时空基础模型 / 工业人工智能〕，我所缺的是〔物理产线数据 / 干预识别 / 理论分析〕；数据引擎与 report 协议可以继续作为共同实验床。

---

# 汇报提示（不写入幻灯片）

1. **第 2–3 页决定学术观感。** 先立文献缺口与形式化，再报 0.854。
2. **中期旧包与现行主结果必须分开说。** 混引会被认为不理解自己的协议。
3. **第 5 页只强调正式一行与 upcoming 缺口。** 不要通读实验档名。
4. **第 6 页给研究问题，不给论文题目承诺。**
5. 若只有 6 分钟：保留 1、3、5、6；第 2、4 页各压缩至 30 秒（只留定位句与框图）。
6. 工业背景听众：强调误报成本与报告级判定。方法背景听众：强调 episode 划分、阈值冻结、标签重合与 upcoming / ongoing 拆分。
)
<|eos|>