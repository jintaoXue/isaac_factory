# tmux 运行方式

面向本仓库的长任务场景：仿真采集（`train.py --max_episodes N`）、特征聚合、BNPDFormer 训练。SSH / Cursor 终端断开后，**会话里的进程仍继续跑**。

---

## 1. 是什么、为什么用

`tmux`（terminal multiplexer）在一台机器上维护独立的「会话」：

- 会话里可以有多个**窗口**（类似多个终端标签）
- 每个窗口可以再拆成多个**窗格**（分屏）
- 你用 `detach` 离开后，会话在后台继续；之后再 `attach` 回来看输出

适合：

| 场景 | 说明 |
|------|------|
| 长时间仿真 | `max_episodes 50` 一类任务 |
| 远端 SSH | 网络抖动、笔记本合盖不断掉任务 |
| 并行盯几个任务 | 一边采数据，一边看 GPU / 日志 |

---

## 2. 安装（若尚未安装）

```bash
# Ubuntu / Debian
sudo apt update && sudo apt install -y tmux

# 确认
tmux -V
```

---

## 3. 三个核心概念

```
会话 (session)     ← 可以 detach / attach
 └── 窗口 (window) ← 类似标签页
      └── 窗格 (pane) ← 同一窗口内的分屏
```

日常只记：**会话名 + 前缀键 `Ctrl-b`**。

几乎所有快捷键都是：**先按 `Ctrl-b`，松开后再按下一个键**。

---

## 4. 会话：创建 / 离开 / 回来

### 常用命令

```bash
# 新建命名会话（推荐始终起名字）
tmux new -s bn_collect

# 列出所有会话
tmux ls

# 重新进入某个会话
tmux attach -t bn_collect
# 简写
tmux a -t bn_collect

# 结束某个会话（会杀掉里面的进程）
tmux kill-session -t bn_collect

# 结束全部会话（慎用）
tmux kill-server
```

### 在会话里「脱离」但继续跑

1. 确认任务已在跑  
2. 按 **`Ctrl-b`**，再按 **`d`**（detach）  
3. 终端回到普通 shell；用 `tmux ls` 应还能看到该会话  

**不要**在跑任务的窗格里直接 `exit` 或关光所有窗格——那样会话会结束，进程也会停。

### 本仓库采集示例

```bash
tmux new -s bn_machine

conda activate env_isaaclab
cd ~/work/isaac_factory

python train.py --task HRTPaHC-v1 --algo rule_based --num_envs 1 --device cuda:0 --headless \
  --disturbance_dim machine --disturbance_intensity 1.0 \
  --max_episodes 20 --max_sim_episodes 20

# 离开：Ctrl-b 然后 d
# 之后回来：
#   tmux attach -t bn_machine
```

导出 + 训练示例（不要和 Isaac 采集抢同一张 GPU）：

```bash
tmux new -s bn_train

conda activate bn_pdformer
cd ~/work/isaac_factory
./batch_bn_export_train.sh OLD          # 一份 old2.0
# ./batch_bn_export_train.sh ALL        # 一份 all（不含 norm 对照）
# WANDB=0 ./batch_bn_export_train.sh OLD   # 默认开 wandb，关掉时加这个

# Ctrl-b d 脱离
# 权重：PDFormer/libcity/cache/model_cache/<标签>/BNPDFormer_best.pt
# 现行主任务是 15 min 工位事件头，存盘看 val report_f1（不是格子 hot_f1）
# wandb：终端 [wandb] url=...（见 06 第 7 节）
```

---

## 5. 窗口（标签页）

在会话内操作（先 `Ctrl-b`）：

| 按键 | 作用 |
|------|------|
| `c` | 新建窗口 |
| `n` | 下一个窗口 |
| `p` | 上一个窗口 |
| `w` | 列出窗口并选择 |
| `0`–`9` | 跳到编号窗口 |
| `,` | 重命名当前窗口 |
| `&` | 关闭当前窗口（需确认） |

示例布局：

- 窗口 0：跑 `train.py`
- 窗口 1：`watch -n 2 nvidia-smi`
- 窗口 2：看输出目录 / 日志

---

## 6. 窗格（分屏）

仍在会话内，先 `Ctrl-b`：

| 按键 | 作用 |
|------|------|
| `%` | 左右分屏 |
| `"` | 上下分屏 |
| `←` `→` `↑` `↓` | 在窗格间移动焦点 |
| `x` | 关闭当前窗格（需确认） |
| `z` | 当前窗格最大化 / 还原 |
| `空格` | 切换预置分屏布局 |
| `{` / `}` | 与相邻窗格交换位置 |

拖动边界改大小：`Ctrl-b` 后按住 `Ctrl`，再用方向键微调（不同终端略有差异；也可用鼠标，见下节）。

---

## 7. 复制模式（翻历史输出）

仿真日志很长时，不必依赖终端滚动条：

1. `Ctrl-b` 然后 `[` 进入复制模式  
2. 用方向键 / `PageUp` `PageDown` 翻页  
3. 需要退出：按 `q`

若启用了 vi 模式（见配置一节），可用 `Ctrl-b [` 后按 `?` / `/` 搜索。

---

## 8. 鼠标（可选）

临时开启：

```bash
# 在 tmux 内执行
tmux set -g mouse on
```

开启后可以：

- 点击切换窗格 / 窗口  
- 拖动分隔条  
- 滚轮翻历史  

选中文本复制时，按住 `Shift` 再拖选（部分终端需要），避免被 tmux 抢走鼠标事件。

---

## 9. 实用小技巧

### 会话里发送命令（不 attach）

适合脚本化：

```bash
tmux new -d -s bn_collect   # -d：创建后不进入
tmux send-keys -t bn_collect 'conda activate env_isaaclab' Enter
tmux send-keys -t bn_collect 'cd ~/work/isaac_factory' Enter
tmux send-keys -t bn_collect 'python train.py --task HRTPaHC-v1 ...' Enter
```

### 看某个会话在跑什么

```bash
tmux ls
tmux capture-pane -t bn_collect -p | tail -40
```

### 命名约定（建议）

| 会话名 | 用途 |
|--------|------|
| `bn_machine` / `bn_human` / … | 各扰动维度采集（`env_isaaclab`，`--device cuda:0`） |
| `bn_preview` | 根目录 `train.py --max_episodes 1 --max_sim_episodes 1`（1 局×四维，不覆盖 `new_*`） |
| `bn_agg` | Stage-C 全量聚合（采集结束后） |
| `bn_follow` | 边采边聚：`python -m bn_agg --run_dir ... --follow` |
| `bn_train` | PDFormer / BNPDFormer 训练（`bn_pdformer`；不要和 Isaac 抢同一张 GPU） |

---

## 10. 可选：`~/.tmux.conf` 最小配置

第一次用可跳过。若想更舒服，可写入：

```bash
# ~/.tmux.conf
set -g mouse on
set -g history-limit 50000
set -g base-index 1
setw -g pane-base-index 1

# 状态栏显示会话名
set -g status-left '[#S] '
set -g status-right '%Y-%m-%d %H:%M '

# 更好的颜色（若终端支持）
set -g default-terminal "screen-256color"
```

改完后生效：

```bash
tmux source-file ~/.tmux.conf
```

---

## 11. 速查表

| 目标 | 怎么做 |
|------|--------|
| 开新会话 | `tmux new -s 名字` |
| 离开但继续跑 | `Ctrl-b` `d` |
| 列会话 | `tmux ls` |
| 回来 | `tmux a -t 名字` |
| 杀会话 | `tmux kill-session -t 名字` |
| 新窗口 | `Ctrl-b` `c` |
| 左右分屏 | `Ctrl-b` `%` |
| 上下分屏 | `Ctrl-b` `"` |
| 翻历史 | `Ctrl-b` `[` … `q` 退出 |
| 窗格最大化 | `Ctrl-b` `z` |

---

## 12. 常见问题

**Q: `tmux attach` 提示 `no sessions`？**  
会话已结束（任务退出且最后一个窗格 `exit` 了）。用 `tmux new -s ...` 重新开。

**Q: detach 后进程没了？**  
可能误在窗格里 `exit`，或用了 `kill-session`。detach 只能用 `Ctrl-b d`。

**Q: 和 Cursor / IDE 终端一起用？**  
可以。在 IDE 终端里 `tmux a -t ...` 一样能看；IDE 关掉只影响本地显示，**服务器上的 tmux 会话仍在**（前提是任务跑在远程机上的 tmux 里，而不是只在本地）。

**Q: 已经在普通终端开跑了，怎么「挪进」tmux？**  
进程不能无损迁移。应：停掉当前任务 → `tmux new -s ...` → 再启动。新任务请直接在 tmux 里开。

**Q: GPU 被占着 attach 不上？**  
`attach` 只是连上终端显示，不额外占 GPU。用 `tmux ls` / `nvidia-smi` 区分「会话是否在」和「训练是否在用卡」。

---

## 13. 推荐工作流（本仓库）

```text
终端 A（本地或 SSH）
  └─ tmux new -s bn_machine
       └─ env_isaaclab + train.py 采集

需要看进度时：
  tmux a -t bn_machine
  （看完再 Ctrl-b d）

采集还在跑、要边关窗边聚 / 推理：
  tmux new -s bn_follow  → python -m bn_agg --run_dir ... --follow
  另开终端（bn_pdformer）→ python -m factory_bn.infer ...

采集结束后另开：
  tmux new -s bn_preview → ./collect_preview_l2long_1ep.sh（1 局×四维，不改 new_*）
  tmux new -s bn_I1      → ./batch_bn_collect.sh I1（或 NORM / FIVE / material；会改写 new_* 链接）
  tmux new -s bn_agg     → ./batch_bn_agg.sh OLD（或 NORM / I1 / …）
  tmux new -s bn_train   → ./batch_bn_export_train.sh OLD（wandb 默认开；WANDB=0 关掉）
```

不要 `kill` 别人的 Isaac `kit` 进程。采集 / 训练都绑 `--device cuda:0` 时串行，不要两台 Isaac 抢一张卡。  
边采边聚与 infer：[`08.边采边聚合与在线推理.md`](./08.边采边聚合与在线推理.md)。  
更完整的仿真 / 训练命令见根目录 [`00.运行方式.md`](./00.运行方式.md)。
