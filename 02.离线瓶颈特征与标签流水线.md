# 离线瓶颈特征与标签流水线（阶段 C）

> 日期：2026-08-10（相对 2026-07-18 初版：并入 `disturbance_log` L2、嵌套 episode 布局、`job_kpi`）  
> 对应规范：`01.瓶颈数据采集规范.md` §7–§8 阶段 C  
> 脚本：`source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools/build_bottleneck_features.py`  
> 相关：`03.实验设计_瓶颈维度与扰动.md`、`00.运行方式.md`（Stage-C → 训练导出）

---

## 1. 做什么

在仿真侧原始表采集（阶段 A/B）已落盘后，**离线**只读 CSV/JSONL，做阶段 C：

1. 按逻辑时间窗聚合每资源运营特征，计算 `bottleneck_score`
2. 生成窗口级标签（当前瓶颈节点、未来是否出事件等）
3. 生成瓶颈事件表：高分窗口合并 **∪** `disturbance_log` 中已完成的 L2 扰动区间
4. 从 `job_trace` 汇总订单级 KPI（makespan / cycle / 吞吐）

**不在仿真内写派生表。** 无 Isaac 依赖，可单独 `python3` 跑。

事件双源的原因：仅靠分数合并时，物流龙门架宕机等 L2 扰动可能「分数不尖、窗口分散」而被漏标；把 completed L2 并入后，`bottleneck_event` / `will_bottleneck` 能覆盖注入真值。

---

## 2. 输入 / 输出

### 2.1 输入目录（两种布局）

脚本会自动发现：

| 布局 | 路径示例 |
|------|----------|
| 嵌套（当前常用） | `<run_dir>/episode_00/env_00/` … |
| 旧扁平 | `<run_dir>/env_00/` |

每个 `env_*` 目录读取：

| 文件 | 用途 |
|------|------|
| `resource_event_log.jsonl` | 资源状态时间轴 |
| `job_trace.csv` | 排队 / 工单起止 |
| `buffer_event_log.csv` | Buffer 占用 |
| `route_transport_task.csv` | 物流延时 |
| `material_inventory_log.csv` | 缺料 |
| `episode_config.csv` | `run_id` / `env_id` / `episode_id` |
| `disturbance_log.csv` | L2 扰动区间（可缺省；缺则仅 score 事件） |

### 2.2 输出

默认写到 `<run_dir>/derived/`，并**镜像** episode 嵌套：

```text
<run_dir>/derived/
├── all_env_summary.json
└── episode_00/env_00/          # 或扁平时 env_00/
    ├── window_feature_table.csv
    ├── bottleneck_label.csv
    ├── bottleneck_event.csv
    ├── job_kpi.csv
    └── pipeline_summary.json
```

---

## 3. 如何运行

```bash
cd ~/work/isaac_factory

python3 source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/tools/build_bottleneck_features.py \
  --run_dir source/isaaclab_tasks/isaaclab_tasks/direct/hc_factory/output/bottleneck_dataset/15_normal \
  --window_sizes 30,60 \
  --horizon 60 \
  --score_threshold 0.55 \
  --min_event_windows 2
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `--run_dir` | 必填 | 含 `env_*` 或 `episode_*/env_*` 的 run 目录 |
| `--env_id` | 全部 | 只处理 `env_{id:02d}` |
| `--window_sizes` | `30,60` | 逻辑秒窗口（`logic_dt=1` 时等同步数） |
| `--horizon` | `60` | 预测标签未来视野 H（逻辑秒） |
| `--score_threshold` | `0.55` | 当前窗是否「瓶颈窗」的分数阈值 |
| `--min_event_windows` | `2` | score 合并成事件所需的最少连续高分窗数 |
| `--out_dir` | `<run_dir>/derived` | 输出根目录 |

终端会打印每个 env 的 `features / labels / events` 与订单 KPI；多 env 时另写 `all_env_summary.json`。

---

## 4. 算法说明

### 4.1 资源状态时间轴

从 `resource_event_log.jsonl` 按 `resource_id` 排序，离散事件 → 连续区间 `[t_i, t_{i+1}) → to_state`。窗口内求交得到：

- `active_pct_s`：`PROCESSING` 占比  
- `blocked_time_s`：`BLOCKED` 时长  
- `starved_time_s`：`STARVED` / `WAITING` 时长  
- `current_active_duration_s`：窗末向前连续 PROCESSING 时长  

### 4.2 队列与等待

- `job_trace` 的 `queue_enter` / `queue_leave` 配对，按 `station_id` 挂资源  
- `avg_waiting_time_s`、`queue_length_s`（buffer 用占用均值）

### 4.3 Buffer / 物流 / 缺料

- Buffer：`occupancy_ratio_s`、`queue_growth_rate_s`  
- `route_delay_s`：窗内完成物流任务 `(end - request)` 均值  
- `material_shortage_propagation_s`：窗内 `shortage_flag=1` 占比  

### 4.4 上下游耦合（启发式）

- `upstream_blocked_ratio_s` / `downstream_starved_ratio_s`：全局 BLOCKED/STARVED 占比，并对 machine/gantry 叠加自身占比（上限 1）  
- 未建显式工艺邻接图；够用跑通管道  

### 4.5 瓶颈分数（规范 §7.2）

同一窗口内跨资源 min-max 归一化后：

```text
bottleneck_score_s =
    0.25 * norm(queue_length_s)
  + 0.20 * norm(avg_waiting_time_s)
  + 0.25 * active_pct_s
  + 0.10 * norm(current_active_duration_s)
  + 0.10 * upstream_blocked_ratio_s
  + 0.10 * downstream_starved_ratio_s
```

`bottleneck_node_t = argmax_s score_s(t)`。`is_bottleneck_window = 1` 当 `score_t ≥ score_threshold`。

### 4.6 瓶颈事件（双源并集）★ 相对初版主要变更

```text
bottleneck_event
  = score 事件（连续高分窗合并）
  ∪ disturbance L2 事件（对齐到时间窗后）
```

**（1）Score 合并**

- 连续 ≥ `min_event_windows` 个窗口：`score_max ≥ threshold` **且** `bottleneck_node` 相同  
- `event_source = "score"`

**（2）Disturbance L2**

从 `disturbance_log.csv` 只取**已完成**区间（有 `end_time_step` / `end_logic_time_s`），且类型属于：

| `disturbance_type` | 含义 |
|--------------------|------|
| `machine_failure` | 机台工位失效 |
| `human_unavailable` | 工人离岗 |
| `transport_delay` | 物流（如龙门架宕机） |

忽略配置级行（`machine_config` / `logistics_config` / …）。  
`target_resource_id` 映射到特征表的 `resource_type`（如 `gantry_*` → `gantry`）。  
区间与各 `window_size` 的窗做重叠对齐；无重叠时回退到最近窗。  
`event_source = "disturbance_log"`，并带 `disturbance_id` / `disturbance_type`。

**（3）合并规则**

- 同 `window_size_s`、同 `resource_id`，时间区间重叠 → 扩成一条，`event_source = "both"`  
- 不重叠 → 两条都保留  
- 重排后重编号 `event_id`

### 4.7 窗口预测标签

对每个窗口起点 `t`、视野 `H`（用**合并后**的 events）：

| 字段 | 含义 |
|------|------|
| `will_bottleneck` | `(t, t+H]` 内是否有事件起点 |
| `future_bottleneck_object_id` / `type` | 该视野内最早事件的资源 |
| `time_to_start` / `duration` | 距该事件起点、事件时长 |

### 4.8 `root_cause_reason`

1. 若本窗与某条 `event_source ∈ {disturbance_log, both}` 的事件时间重叠 → 优先填其 `disturbance_type`  
2. 否则且 `is_bottleneck_window`：规则启发式  
   `blocked_downstream` / `starved_upstream` / `high_utilization` / `queue_buildup` / `score_threshold`

### 4.9 订单 KPI（`job_kpi.csv`）

| 定义 | 来源 |
|------|------|
| 开工 | 首个 `job_selected` |
| 完工 | `stage_complete`；缺则回退涂装 `process_end` |
| `cycle_time_s` | 完工 − 开工 |
| `order_makespan_s` 等 | 写入 `pipeline_summary.json` 的 `order_kpi` |

---

## 5. `pipeline_summary.json` 要点

除行数、热点节点、`will_bottleneck_rate` 外，现含：

| 字段 | 含义 |
|------|------|
| `n_disturbance_l2` | 解析到的 completed L2 区间数 |
| `n_events_from_disturbance` | `event_source` 为 `disturbance_log` 或 `both` 的事件数 |
| `order_kpi` | makespan、mean/median cycle、吞吐等 |

无扰动 run：`n_disturbance_l2 = 0`，行为退化为「仅 score 事件」（与初版一致）。

---

## 6. 字段速查

### `window_feature_table.csv`

`run_id, env_id, window_index, window_start_s, window_end_s, window_size_s, resource_id, resource_type, queue_length_s, avg_waiting_time_s, occupancy_ratio_s, queue_growth_rate_s, active_pct_s, current_active_duration_s, blocked_time_s, starved_time_s, inter_departure_var_s, upstream_blocked_ratio_s, downstream_starved_ratio_s, route_delay_s, material_shortage_propagation_s, bottleneck_score_s, norm_*`

### `bottleneck_label.csv`

`window_*, bottleneck_node_t, bottleneck_type_t, bottleneck_score_t, is_bottleneck_window, horizon_s, will_bottleneck, future_bottleneck_object_id, future_bottleneck_type, time_to_start, duration, root_cause_reason`

### `bottleneck_event.csv`

`event_id, run_id, env_id, window_size_s, resource_id, resource_type, start_window_index, end_window_index, start_s, end_s, duration_s, max_score, n_windows, event_source, disturbance_id, disturbance_type`

### `job_kpi.csv`

`run_id, env_id, episode_id, job_id, product_type, start_s, complete_s, cycle_time_s, completed, complete_source`

---

## 7. 与实验 / 训练的关系

```text
仿真采集（含可选 --disturbance_dim）
  → bottleneck_dataset/<run_id>/episode_*/env_*/
       原始表 + disturbance_log.csv
  → 本脚本 Stage-C
       derived/.../window_feature_table + label + event + job_kpi
  → factory_bn.export_dataset → 训练张量
  → BNPDFormer / 基线训练
```

- **无扰动 / G0**：事件仍可能偏稀，属负载问题，不是管道失败。  
- **有 L2 扰动**（human / logistics / machine）：即使分数分散，事件表也应出现对应 L2；可用 `n_events_from_disturbance` 自检。  
- 下游训练导出见 `00.运行方式.md`「离线聚合 → 导出 → 训练」。

---

## 8. 已知局限

1. 上下游比例仍是全局启发式，未建工艺邻接图  
2. `queue_enter` 挂在当前 task 的 `station_id`，与下游 waiting 工位不完全同一节点  
3. L2 仅三类运行时事件；物料短缺等无时段 L2 仍主要靠特征 / score  
4. L2 与 score 重叠时合并为一条，不保留「双峰」细分  
5. G0 弱负载下正样本仍可能稀少，训练需 G1 负载或扰动 run  

---

## 9. 变更记录

| 日期 | 说明 |
|------|------|
| 2026-07-18 | 初版：窗口特征 + score 事件 + 标签；在 `2026-07-18_17-46-35_seed42` 跑通 |
| 2026-08-10 | 并入 `disturbance_log` L2 → 事件双源；`event_source` / 扰动优先 root cause；支持 `episode_*/env_*`；增加 `job_kpi` 与 summary 中扰动统计；文档按当前代码重写 |
